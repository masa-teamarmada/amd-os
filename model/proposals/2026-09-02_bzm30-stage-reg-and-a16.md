状態: **proposal**（未承認）。まさ 2026-09-02「BZM 自体も修正すべきポイントがあれば正本を修正しておいて」を受けて、
p07 LST の用途 C（核融合向けリチウム6濃縮）を測るあいだに見つかった3点を提案として書く。
**いずれも式・係数・値の変更ではなく、限界の明記と運用規律の追加**である。

# 段階と規制属性の相互作用・近似 A16 の実装不整合・規制属性の粒度

## 提案1: 証拠水準の段階が、規制属性を変えると意味を変える（§6.I-1-4 へ限界を追記）

### 何が起きたか

用途 C の規制属性を REG-0 から REG-1（輸出認証が要る区分）へ上げて計算したところ、
**スコアが1円も変わらなかった**。

| 前提 | 次に越えるゲート | $v$ | 中央値 | M4 到達 | 到達月数 |
|---|---|---:|---:|---:|---:|
| REG-0・段階4 | M3 | 3.225 | 401億 | 33.5% | 21.4か月 |
| REG-1・段階4 | M3 | 3.225 | 401億 | 33.5% | 21.4か月 |

### 原因

段階は「ゲート列の何番目まで通過したか」で表される（§6.I-1-4）。
`stageOf` は M2 と T4規格試験の両方に**同じ段階4** を割り当てている。

- REG-0 の列: T1→T2→T3→**M2**→M3→M4。段階4 は「M2 を通過」、次は M3
- REG-1 の列: T1→T2→T3→**M2**→**T4規格試験**→M3→M4。段階4 は「M2 と T4規格試験の両方を通過」と読まれ、次は M3

つまり **規制を重くしたはずが「規格試験は済んでいる」という有利な扱いに化ける**。
段階3 で測れば差は正しく出る。

| 前提 | 次に越えるゲート | $v$ | 中央値 | M4 到達 | 到達月数 |
|---|---|---:|---:|---:|---:|
| REG-0・段階3 | M2 | 1.167 | 145億 | 8.5% | 31.8か月 |
| REG-1・段階3 | M2 | 0.805 | **100億** | 3.1% | 43.0か月 |

規格試験1段の値段は、**中央値 −31%、M4 到達が3分の1、到達が 11.2か月遅れる**。

### 提案

§6.I-1-4 の段階定義の直後へ、この限界と**運用規律**を追記する。

> **運用の規律**: 規制属性を変えるときは、証拠水準を同じ実態へ置き直してから計算する。
> 構造として直すなら段階を「通過したゲートの集合」で持つ形になるが、版の更新を伴うので、当面はこの注意で運用する。

**式・値は変えない。** 構造の変更（段階を集合で持つ）は、必要になった時点で別の提案として出す。

## 提案2: 参照実装が近似 A16 に違反していた（**実施済みのバグ修正**。正本へは実例として追記）

### 何が起きたか

近似 A16 は「**用途を1本に畳む**。複数の用途を持つ案件は、用途ごとに計算して足すと同じゲート列を二重に数える。
当面は主たる用途1本で計算し、他の用途は参考として別に併記する」と定めている。

ところが `bzm30_score_seeds.cjs` の `ceilingTotal()` は **`is_primary` を見ずに全行を合計**していた。
実害が1件あった。

**p01 OPTMASS** — 窓の熱線遮蔽フィルム 399億円/年（`is_primary=true`）と
建材一体型太陽光（透明型）199.5億円/年（`is_primary=false`）を足した **598.5億円/年** で計算されていた。

### 対応（2026-09-02 実施済み）

`ceilingTotal()` から `is_primary === false` の行を除いた。A16 は承認済みの近似なので、
**実装をそれに合わせるバグ修正**であり、モデルの式・係数は変えていない。

p01 を再計算した結果:

| | 天井 | 下限 | 中央 | 上限 |
|---|---:|---:|---:|---:|
| 修正前 | 598.5億円/年 | 21.8億 | **31.7億** | 45.5億 |
| 修正後 | 399.0億円/年 | 12億 | **17億** | 24億 |

天井を持つ42案件すべてに `is_primary=true` の行があることを確認済みで、
**p01 以外のスコアは動かない**。

### 提案

近似表の A16 の行へ、この不整合と p07 の実例を追記する。

> **2026-09-02 の実例**: p07 LST は用途を3つ持つ（国内のリチウム化合物供給 171.2億円/年・
> 海外のかん水事業者向け装置 1,050億円/年・核融合向けリチウム6濃縮 124.4億円/年）が、
> スコアは主たる用途1本だけで出ている。**用途を複数持つこと自体が価値の分散だが、現行のスコアはそれを表現しない。**

## 提案3: 規制属性は案件に1つしか持てない（§6.A-3 へ限界を追記）

p07 LST は、主たる用途 A（国内のリチウム化合物供給）が **REG-0**、
用途 C（核融合向けリチウム6濃縮）が **REG-1**（濃縮リチウム6は外為法・原子力供給国グループの輸出管理の対象）で、
案件として一つを選ぶと片方が実態と合わない。

近似 A16 で用途を1本に畳んでいるあいだは**主たる用途の規制を採る**（p07 は REG-0 のまま）。
A16 を外して複数用途を扱うなら、規制属性も用途ごとに持つ必要がある。

### 提案

§6.A-3 の規制属性の表の直後へ、この粒度の限界を追記する。

## 決めてほしいこと

1. **提案1〜3 の追記を正本へ入れてよいか。** いずれも限界の明記と運用規律で、式・係数・値は変えない
2. 提案2 のバグ修正（実施済み・p01 が 31.7億 → 17億）を追認してよいか。差し戻すなら A16 の側を変えることになる
3. 承認するなら `model/APPROVALS.md` へ記録して `node pwa/scripts/model_lock.cjs relock --approval <id>` を実行する
