# 次セッションへの引っ越しプロンプト

cwd: `/Users/masa/projects/AMD/amd-os`

> 2026-08-29 追記: この日のセッションはマニュアルのつくよみ Q&A の不具合（口語の質問で全42章が0件）を直して本番反映まで完了し、
> 残作業はない。詳細は `HANDOFF.md` の C 節。**次の着手先は、下のとおり BZM 3.0 のモデル本体のまま。**

---

AMD OS の作業を続けて。**BZM 3.0 のモデル本体に、いま入っていない2つを入れる**のが今回のタスク。

まさ 2026-08-28:
> 組織の評価ができていないことと「本来は『これ以上ゲートを越えない』を到達確率の側で表すべきで、その入力が無い。」の両方をモデルに入れるところに着手しよう。

## 読む順（この順で全部読む）

1. `/Users/masa/projects/AGENTS.common.md` — えいみ共通ルールの正本
2. `/Users/masa/.claude/projects/-Users-masa-projects-AMD/memory/MEMORY.md` — AMD 横断の記憶
3. `/Users/masa/.claude/projects/-Users-masa-projects-AMD-amd-os/memory/MEMORY.md` — このPJの記憶
4. `HANDOFF.md` — 現在地・止まっているところ・まさの判断待ち
5. **`model/proposals/2026-08-28_bzm30-organization-input.md`** — 今回のタスクの提案（**未承認**）。提案7件と優先順位
6. `model/MODEL_VERSION_LEDGER.md`（= OS のモデルページ `/model` の正本）の **§3 要件表 / §5.3 案件パラメータ /
   §6.B-1 八機能 / §6.I-3 空席の遅延 / §6.I-9-1 案件パラメータの既定**
7. `model/README.md` — 変更の運用規約。**(b) 提案中の値を承認前に使わない / (c) 提案→承認→反映の順**
8. `model/cases/SCORES.md` — いまのスコア一覧と、モデルのどこに何が入るかの整理
9. `BUGS.md` — 今回のセッションで出た事故4件。**同じことを繰り返さない**

## 状態

- git: `main` 一本、origin と同期済み。HEAD は「提案に変換能力 c を足す」
- 本番: `https://amd-os-pwa.vercel.app`。push すれば Vercel が自動デプロイ。`/api/build-info` の `git_sha` で確認
- 画面: シーズ一覧・シーズ詳細に**金額が出ている**（`BZM30_SCORES_PUBLISHED = true`）。
  21件すべてに金額があり、入力の充足の表には**パラメータ1件ずつの根拠**が出る
- DB: `seed_bzm30_inputs`（21件・全12項目に値と根拠）/ `seed_value_ceilings`（22行・全件 SAM）/
  `seed_bzm30_scores`（append-only、画面は最新1件を読む）
- **`pwa/src` を触った commit は `check_pwa_critical_ui.cjs` が pre-commit で強制的に走る**（迂回不可）
- 共有 checkout に5〜10セッションが並行している。未コミットは他セッションのもの。触らない

## タスク — 提案の優先順位に沿って着手する

### 1. 変換能力 $c$ を案件ごとに受け取る（提案7。**これが最優先**）

**正本の要件1に挙げた量が、実装に入っていない。** §3 の要件表は
「変換能力 = 技術と資本金を戦略余力に変える能力」と定義し、根拠に
まさ 2026-08-22「それらをいかに効率よく戦略余力に変換していくか、その変換能力が高ければSPSが高くなるべき」を引いている。
§5.3 は測り方まで書いている——「履歴から推定する: これまでに投入した資金と月数に対して越えたゲートの数の比を、分野の基準値と比べる」。

ところが `model/tools/bzm30_forward.cjs` は `cfgL.cNodes = [[0.55,0.25],[1.00,0.50],[1.82,0.25]]` を
全案件共通で回すだけで、`init` に $c$ の口が無い。

**$c$ が測るもの**: 「投入資源あたりに前進を生む速さの乗数」（§5.3）。つまり
**案件が持っている戦略余力（設立後の資金と、資金以外の余力）を、どれだけ前進へ変換できるか**。
まさ 2026-08-28「設立後の資金や、資金以外も戦略余力があると思うけど、それをどのくらい変換できるかの指標」。

**これで2つ直る。**

- **「これ以上ゲートを越えない」。** p03 ティエムファクトリのように技術も製品もあるのに伸びない案件は、
  投入した資源が前進へ変わらない状態＝$c$ が極小。
  **いまは天井を絞って代用している**（66.5億→13.3億）が、本来は $c$ を下げるのが正しい
- **設立前に獲得した資金の評価（まさの拡張）。** まさ 2026-08-28「ただしそれを設立前の資金にも拡張できると考えた。
  設立前の獲得資金については、設立後の残高と同等に考えたらダメ。その資金がどの程度設立後SUの資産に変換されたかを
  評価する必要がある。ダメなPJだと、SUと無関係な、自分の研究のために使われることもある。一方でいいPJだと、
  よくその費用でそこまで知財を創出したね、みたいなのもある」。
  設立前の公的資金は額をそのまま戦略余力に数えず、**知財・試作/実証・データ・人へどれだけ変わったか**で $c$ を推し量る

実装は `cNodes` を $c$ を中心とした3点へ差し替えるだけ（幅は GSD 1.65 のまま。点推定にすると不確かさが消える）。

### 1-2. 戦略余力の、資金以外の部分（提案8）

**要件2（§3）の「戦略余力 = 次の行動を起こすための資金。金だけではない」が実装に入っていない。**
参照実装の戦略余力は資金の残高 $s$（万円）だけで、大学の設備を使える権利・共同研究先が負担する工数・
引き当てられる人・応募できる公的支援の枠が入らない。**設立前の案件で効く。**
資金へ換算して足す案なら実装が要らず、換算の規約を `model/cases/README.md` で決めるだけ。

### 2. 案件ごとのバーンレート（提案6）

`seed_bzm30_inputs.burn_rate_yen_month` に値は入っているが、参照実装が受け取らない。
実績は型別の既定値の**2〜37倍**ずれている（p22 OptQC が月1.5億円に対し既定400万円）。
`init.burnMan`（万円／月）を受け取って $\mu$ を差し替える。

### 3. 八機能の充足（提案1〜3）

**機能2（技術の核）が「空席にならない」と規約で固定されている**（§6.I-3-1）ので、
p16 ORLIB の「誰も電池を作れない」を表せない。担い手 $e$ を下端0.20まで下げても $v$ は 0.507→0.281 にしか落ちず、
**証拠水準を段階3→1に下げて代用している**。これは本来おかしい。

`init.funcs = { f1..f7 }` を受け取り、`vacancyProbs` の64通りを128通り（機能2を含む）へ広げる。
計算量が2倍（1件2〜3分が5〜6分）。

### 4〜5

権利残件に資本の種類（SHA未締結・支配権の集中）、機能の補完性（噛み合わせ係数）。詳細は提案を読む。

## 進め方（`model/README.md` (c) の手順を守る）

1. 提案の内容をまさへ**本文（チャット）で提示する。** 要約や骨格ではなく、実際の変更文面を見せる
2. まさが承認したら、その発言をそのまま `model/APPROVALS.md` へ記録する（**要約・言い換えをしない**）
3. `model/MODEL_VERSION_LEDGER.md`（モデルページ本体）を変更する。**別ファイルへ切り出さない**
4. `node pwa/scripts/model_lock.cjs relock --approval <id>` を実行する
5. 同じ commit で `bzm/9-5-appendix-changelog.md` に1行追記する
6. 参照実装を直し、21件を再計算して `model/cases/SCORES.md` を貼り替える

**`model/LOCK.json` を先に書き換えて後から承認を取る、という順序の逆転はしない。**

## まさの判断待ち（先に聞いたほうがいいもの）

1. **休眠の承認** — `model/proposals/2026-08-27_bzm30-dormancy.md`（未承認）。
   「会社化前の資金切れでは死なない」という改訂。**承認されると会社設立前の6件の残高の感度が消える**ので、
   $c$ の実装より先に決まると手戻りが減る。**まさから「休眠の承認ってなに？」と聞かれているので、
   まず何の話かを説明するところから**
2. 置き換え分の割合（p21 7割・p02 8割・p05 5割はいずれも暫定。純増が2〜4倍動く）
3. 旧SPS の DB データ（`seed_screening_bands` 998行）を物理削除するか

## このPJで守るルール

- **正本はモデルページ本体ただ一つ。** 定義・式・値は `model/MODEL_VERSION_LEDGER.md` に書く。
  別ファイルへ切り出して本文からリンクしない。`model/proposals/` は提案中のものだけ
- **未承認の値を画面に出さない**（`model/README.md` (b)）。承認済みの版で計算し、
  どの版で計算したかを `seed_bzm30_scores.inputs.impl` に残す。
  承認済みの版: `git show 40aebfe9:model/tools/bzm30_forward.cjs > /tmp/bzm30_forward_approved.cjs`
- **天井は TAM ではなく SAM。** 担う工程 / 方式 / 用途 / 代替手段があること で絞り、
  **シェアは天井に入れない**（モデル側の取り分 $\phi_u$ が担うので二重計上）。規約は `model/cases/README.md`
- **空欄を作らない**（まさ 2026-08-28）。精度が低くても数値を置き、導出・確度・感度を書く。
  保留は慎重ではなく判断を止めること
- **調べた事実はその場でOSの該当テーブルへ入れる。** ネット調査で得た数値を入力欄にだけ置くと、
  後から見た自分に「根拠なし」と消される（OptQC 70億の事故）
- **「OSの全データを読んだ」は「その案件を分かった」ではない。** 読み終わったら
  「この案件が死ぬとしたら何が原因か」をOSのデータだけで言えるか自問し、言えなければまさに聞く
- **画面から要素を消したら、その存在を要求している guard も同時に外す**（pre-commit で強制されるが、落ちてから直さない）
- **検証**: `npx tsc --noEmit` / `npx eslint <触ったファイル>` / `npm run build` /
  `npm run test:critical-ui` / `npm run test:reference-data-cache`。commit・push・deploy まで進めて本番で確認する
- **`next dev` は `pwa/AGENTS.md` を勝手に書き換える。** dev サーバを起動したら commit 前に
  `git diff pwa/AGENTS.md` を見て戻す
- **長い計算はバックグラウンドで投げたら turn を終える。** 投げた直後に確認しても実時間は進まない
- **`xargs` に長い `--impl` パスを直接渡さない。** `command line cannot be assembled, too long` で落ちる。
  `bash -c` で関数を `export -f` して渡す（例は `/tmp/recall_all.sh` と `design_log/sessions_2026-08.md`）

## まさへの報告の書き方

ファイル名・テーブル名・関数名・commit番号を本文に並べない。**まさが開く画面で何がどう変わるか**、
**それでまさが何をできるようになったか**で書く。判断してほしいことは選択肢を先に短く出す。
