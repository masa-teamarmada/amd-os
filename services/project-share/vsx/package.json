{
  "name": "amd-os-project-share-vsx",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check build.mjs && node --check api/index.mjs && for f in server/lib/*.mjs server/routes/*.mjs; do node --check \"$f\"; done",
    "test": "node --test"
  },
  "dependencies": {
    "@fontsource-variable/noto-sans-jp": "5.3.0",
    "@sparticuz/chromium": "143.0.0",
    "@vercel/blob": "^2.6.1",
    "puppeteer-core": "25.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
