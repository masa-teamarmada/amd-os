{
  "name": "amd-os-project-share-cx",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check build.mjs && node --check api/index.mjs && for f in server/lib/*.mjs server/routes/*.mjs; do node --check \"$f\"; done",
    "test": "node --test"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
