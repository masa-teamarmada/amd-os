# 次セッション引き継ぎプロンプト — P1論文（1報目）2026-08-30

（下の本文をそのまま新セッションへ貼る。cwd = `/Users/masa/projects/AMD/amd-os/bzm`）

---

P1論文（1報目）の続き。cwd は `/Users/masa/projects/AMD/amd-os/bzm`。

## 読む順（この順で）

1. `/Users/masa/projects/AGENTS.common.md`（えいみ共通ルールの正本）
2. `/Users/masa/.claude/projects/-Users-masa-projects-AMD/memory/MEMORY.md`（AMD横断memory）
3. `bzm/AGENTS.md`（BZM構築セッションの研究規律。反論可能性の探索・二重批判監査）
4. `bzm/HANDOFF_P1_2026-08-30.md`（現在地・残作業・正本の在り処）
5. `bzm/S5_REVISION_PLAN_V2.md`（模擬査読5本の統合と改稿計画 R-A〜R-Q。**どの指摘をなぜ採ったか/採らなかったか**）
6. `bzm/PAPER_P1_DRAFT_V2.md`（稿の正本 v4.1。英語・本文11,026語・図5点・文献77件）
7. `/Users/masa/projects/AMD/amd-os/BUGS.md` の末尾1件（今回の教訓）

## 状態

- 稿 = `PAPER_P1_DRAFT_V2.md` **v4.1**。読む用HTML = `PAPER_P1_PREVIEW_V2.html`（pandocで生成。`pandoc PAPER_P1_DRAFT_V2.md -o PAPER_P1_PREVIEW_V2.html --standalone --mathml --css=/tmp/paper_v2_style.css --embed-resources --resource-path=. --metadata lang=en`。CSSが消えていたら再作成が要る）
- git は push 済み・未push 0件（`73fd5bae` まで）。Vercel のデプロイ枠は残り少ないので **docs以外のpushは束ねる**
- モデルのスコアの凍結版 = `a149fc30`。**モデル正本（`model/` 配下）は触らない**（別セッションの担当・ロックあり）
- §7 の計算は凍結した分類（`CLASSIFICATION_FREEZE.md`、sha256記録）で再実行済み。結果は `paper_p1_dual_scoring.md`

## 残る作業（この順）

1. **語数の刈り込み** 11,026 → 10,000。編集長の指摘どおり、**中身を削るのではなく置き場所を変える**。§4.2〜§4.5 の記法（状態ベクトルの成分列挙・月次遷移の順序）と §6 の運用一式の細部を補足資料（SM）へ移す。§2 と §7 は削らない（査読で最も評価された部分）
2. **まさの判断待ち2件**を片付ける（下記）

（**図2の差し替えは完了** — commit `73fd5bae`。あわせて `bzm/tools/dual_scoring.cjs` の欠陥2件を修正した。詳細は `HANDOFF_P1_2026-08-30.md`）

## まさの判断待ち

- **匿名化**: 3人の査読者が「案件が特定可能」と判定（方式・年数・人的事情の記述から）。開示内容に資金ショート時期・解雇見込み・議決権支配など非公開かつ相手の不利になる事実を含む。(i) 関係者から書面同意／(ii) 個別記述を落として型×規制と統計だけに。**まさは 2026-08-29 に「先延ばしでOK、他の指摘のが根幹」と判断済み**なので、残作業1〜2の後に持ち出す
- **投稿先**: 編集長が desk reject 判定（着想ではなく投稿要件と§7の構造が理由。再投稿は歓迎と明記）。(i) JTT へ改稿して再投稿／(ii) 誌を変える／(iii) 二報に分ける

## このPJで確立している運用ルール（守ること）

- **モデル正本は触らない**。`model/` 配下はロック済みで、変更は BZM 3.0 モデル側セッションの担当。モデルが動いたら追報が来るので、正本で裏を取ってから論文へ反映する
- **計算は本番テーブルへ書き込まない**。`tools/dual_scoring.cjs` に書き込み経路は無い（`--mode records|elicitation` / `--only <項目>`）。1件2〜3分なので `xargs -P 6` で並列に回す
- **分類は計算の前に凍結する**。結果を見てから分類を変えない。変えるなら理由と新しい凍結記録を別に作る
- **査読・監査・エージェントの指摘は、正本か実装か領域の実態で裏を取ってからまさへ渡す**。強い主張ほど確認する。裏が取れないものは「未確認」と明示。ただし当たっていた指摘まで捨てない
- **正本に無い事実を成果物に書かない**（特に利害・関係・権限）
- **「実装済み」と読める記述を書く前に実装を見る**。設計と実装がずれていたら、ずれを書く方が価値が高い
- **報告は開発を知らない人向けに**。記号・ファイル名・関数名を本文に並べない。まさが読む画面と、まさが次にできることで書く
- 数式は `$$` の独立行。チャットにインライン `$...$` を書かない
