# HANDOFF_P1_2026-08-30.md — P1論文（1報目）

最終更新: 2026-08-30 JST / 直近のセッション: **図2を凍結版へ差し替え＋二重採点ツールの欠陥2件を修正**（その前: 模擬査読5人格＋引用照合 → 改稿 → 入力分類の凍結と再計算）

## 今回やったこと

1. **模擬査読5人格＋引用照合を実施**。判定は 編集長=desk reject（再投稿は歓迎）／経済学者=major（reject寄り）／技術移転研究者=major／方法論=major／実務家=条件つきyes。引用照合は72件中71件正しく**捏造ゼロ**（誤りはPenroseの出版社1件）。
2. **指摘を正本・実装で1件ずつ裏取りしてから改稿**。最も重かったのは「原稿が書いていることを実装がやっていない」型の食い違い6件で、全部当たっていた。数字に依存しない改稿はすべて反映済み（詳細は `S5_REVISION_PLAN_V2.md` の R-A〜R-Q）。
3. **入力分類を3条件でやり直し、計算の前に凍結**（`CLASSIFICATION_FREEZE.md`、sha256記録、commit `a4534aef`）。その後 21件×2条件42回＋単独マスク34回を実行。結論3点は保たれ、効果はやや小さく出た（前の分類が記録の持つ値まで既定へ戻して水増ししていたため）。
4. **専有可能性を外した値は再計算不要**と判明。価値の積み上げにしか入らず判定に効かないので、スコア÷取り分で厳密に出る（8桁で確認）。

現在の稿 = `PAPER_P1_DRAFT_V2.md` **v4.1**（本文11,026語・図5点・文献77件・未引用ゼロ）。読む用HTML = `PAPER_P1_PREVIEW_V2.html`。

## repo の状態

- **push 済み**（`73fd5bae` まで origin/main に載っている。未push 0件）。セッション途中で Vercel のデプロイ枠が 100/100 に達して guard が止めていたが解消済み。`AMD_OS_DEPLOY_QUOTA_OVERRIDE=1` は使っていない（使わないこと）
- 未コミットの他セッション差分あり（`.vercelignore` / `pwa/vercel.json` / `pwa/scheduled-tasks/...`）。**触らないこと**

## 図2の差し替えと二重採点ツールの修正（2026-08-30、commit `73fd5bae`）— 完了

図2を凍結版 `a149fc30` の21件へ差し替えた。転記元は丸めた `model/cases/SCORES.md` ではなく
DB `seed_bzm30_scores`（最新 computed_at 2026-08-29T14:33Z）。13位 ORLIB 1.823億 と 14位 輝翠 1.800億 が
表示上どちらも「1.8億」で順位が付かないため。21件とも幅の倍率が SCORES.md と一致することを検算した。
キャプションの幅の倍率の上限を 11.6 → **11.8**。図5点を再生成し、本文幅（736px）の1倍実描画で全図の文字が読めることを確認。

差し替えの過程で `bzm/tools/dual_scoring.cjs` の欠陥を2件見つけて直した。

1. **会社化後の計画バーン `burn_post_yen_month` を読んでいなかった**（参照実装 `model/tools/bzm30_score_seeds.cjs` にはある）。
   未会社化でこの値を持つ2件（B = CryoX 月150万・L = SolvioraX 月703万）だけ凍結版のスコアを再現できていなかった。
   修正後は21件すべて一致。**動いたのは §7 の表の水準2行だけ**で、比・順位・§1の要約・§3の単独マスクはすべて不変
2. **p09 と p04 のラベルが入れ替わっていた**（DB の `seed_projects` が正: `3c5cdae2…`=p04 輝翠 ／ `eeba1bfd…`=p09 JOYCLE）。
   数値は seed_id 単位なので結果は動かないが、この入れ替わりが原因で
   `paper_p1_input_classification.md` §6・§7 の「JOYCLE の資金調達ラウンド記録の不整合」という観察が生じていたので**取り下げた**

凍結ファイル2点（`paper_p1_input_classification.json`／`.md`）は sha256 を保つため書き換えず、
訂正は `CLASSIFICATION_FREEZE.md` の末尾と `paper_p1_dual_scoring.md` §5.5 に記録した。

## 次のセッションの最初の一手

**語数の刈り込み** 11,026 → 10,000。編集長の指摘どおり §4.2〜§4.5 の記法（状態ベクトルの成分列挙・月次遷移の順序）と
§6 の運用一式の細部を補足資料（SM）へ移す。**中身を削るのではなく置き場所を変える。**
§2 と §7 は削らない（査読で最も評価された部分）。

## 残りの作業

1. **語数の刈り込み**（上記）
2. **まさの判断待ち2件**（下記）

## まさの判断待ち（2件）

- **匿名化**: 3人の査読者が「案件が特定可能」と判定（方式・年数・人的事情の記述から）。開示内容に資金ショート時期・解雇見込み・議決権支配など非公開かつ相手の不利になる事実を含む。(i) 関係者から書面同意を取る／(ii) 個別記述を落として型×規制と統計だけにする。まさは 2026-08-29 に「先延ばしでOK、他の指摘のが根幹」と判断済み
- **投稿先**: 編集長が desk reject 判定。(i) JTT へ改稿して再投稿／(ii) 誌を変える／(iii) 二報に分ける

## 正本の在り処

| 内容 | ファイル |
|---|---|
| 稿（正本） | `PAPER_P1_DRAFT_V2.md` ／ 読む用 `PAPER_P1_PREVIEW_V2.html` |
| 進捗台帳 | `PAPER_P1_PROGRESS.md` |
| 査読の統合と改稿計画 | `S5_REVISION_PLAN_V2.md`（R-A〜R-Q） |
| 分類の凍結記録 | `CLASSIFICATION_FREEZE.md` ／ 分類本体 `paper_p1_input_classification.md` `.json` |
| §7 の計算結果 | `paper_p1_dual_scoring.md` |
| 計算ツール | `tools/dual_scoring.cjs`（`--mode records|elicitation` / `--only <項目>`。**本番テーブルへ書き込まない**） |
| 図の生成 | `paper_p1_figures_v2.py` → `figures_v2/*.png` |
| モデル正本 | OS のモデルページ（読み込み元 `../model/MODEL_VERSION_LEDGER.md`）。スコアの凍結版 = `a149fc30` |

## この作業で守っている規律

- **モデル正本（`model/` 配下）は触らない**。ロックがかかっており、変更は別セッション（BZM 3.0 モデル側）の担当
- **計算は本番テーブルへ書き込まない**（`dual_scoring.cjs` に書き込み経路が無い）
- **分類は計算の前に凍結する**。結果を見てから分類を変えない（変える場合は理由と新しい凍結記録を別に作る）
- **査読・監査の指摘は、正本または実装で裏を取ってから採否を決める**。強い主張ほど確認する
