# 引き継ぎ: PJ別 利益構造ダッシュボードを作り直す (2026-08-30)

cwd は `/Users/masa/projects/AMD/amd-os`。main 一本。新branch・worktreeを作らない。

## この引き継ぎが出た理由

前セッションが `/admin/project-profitability`（PJ別 利益構造）を実装したが、
**報酬モデルの前提を繰り返し取り違え、まさから11回の指摘を受けた**。
指摘のたびに部分修正を重ねたため設計が継ぎ接ぎになっている。白紙で作り直す。

現在の実装（本番稼働中）は**そのまま踏襲しない**。下の「禁止事項」に該当する指標が残っている。

## まさが本当に知りたいこと

> どのPJが儲かっていて、どのPJがまさの持ち出しで回っているか

ここでの「儲かっている」は、**まさ自身の稼働を織り込んだうえで**の話。
現金が出ていかないだけの状態を利益と呼ばない（まさの労働の対価を会社に付け替えているだけ）。

## まさから受けた指摘（＝禁止事項）

作り直すときに、以下を絶対に繰り返さない。すべてまさの直接の言葉が根拠。

1. **「まさが自分の配分を放棄してメンバー分を捻出している」は誤り。**
   正しくは「まさが動けばMSのポイント消化をまさが奪えるので、メンバーに払う額を減らせる」。
   まさへの現金支払0円は設計どおりであり、放棄でも持ち出しでもない。
2. **「持ち出し」「取り分ゼロ」という語を使わない。** 意味を定義せずに使い、しかも中身が逆だった。
3. **メンバー支払0円を悪いことのように書かない。** まさ曰く「理想的でしょ？」。
4. **「枠超え」と書かない。** 配った額が上限を超えることはない（cap按分で必ず原資内）。
   まさ「そもそも枠ってなに？ ちゃんと65%の予算に収まってるように見えるんだけど」。
5. **年で切らない。シーズン（plan cycle）で見る。**
   まさ「そのシーズンで払うべき額は最初から決まってるわけなので、シーズン全体で試算した方がよくない？」
6. **未払残（`stockYen`）を収益率に混ぜない。**
   まさ「現状未払かどうかって、そのPJの収益率を見るうえでは邪魔な情報だと思う」。
7. **まさの投下稼働を織り込む。** まさ「おれがどれだけ稼働しなきゃいけなかったかが折り込まれてなくない？」
8. **ポイント（MS pt / 稼働需要 `grossDueYen`）を収益率の指標にしない。**
   まさ「ポイントなんて、ここで考慮する必要すらなくない？」
   「マイルストーンをどのように設定しようが、原資を超える支出にならない設計じゃん」。
   ポイントは誰にいくら配るかの内訳を決めるだけで、会社の収支には効かない。
   → 「需要が原資のN倍」「マイルストーン設定が大きすぎる」という警報は**無意味**。作らない。
9. **OSにデータが無いことを、お金の状態として語らない。**
   前セッションは「シーズンが終わったのに275万が未配分」「原資200万が丸ごと未配分」と表示した。
   まさ「報酬を渡すべき人には渡し終わってるよ。計算ができてないだけ」。
   → 未計算は「OSに計算データが無い」と書く。金額を未配分として出さない。
10. **`tally_weekly_effort_entries` を他メンバーとの比較に使わない。**
    まさ「tallyはおれの稼働だけをカウントするアプリだよ。そもそも前提が間違ってる」。
    実データでも `member_id='ID001'`（まさ）の行しか存在しない（9PJ / 501.3時間）。
    → 「まさ依存度」のような比率は**作れない**。前セッションはこれを作ってCXで79%と表示し、
    まさから「CXはおれそんなに稼働してる？ ほとんどあきが稼働してくれてると思うんだけど」と否定された。
11. **まさの違和感は強いシグナル。** 順位や比率を出したら、実態を知るまさに見せて確かめる。

## データで確定した事実（実データで裏取り済み。再調査不要）

計算正本は `pwa/manual/7-1-reward-calc-spec.md`。**着手前に Read で全文通すこと。**

| 事実 | 根拠 |
|---|---|
| `billing_cycles.budget_yen` は**請求額の65%（配分原資）**であって売上ではない | KUTE `fee_amount 654,545 × 0.65 = 425,454 = budget_yen`、SE `100,000 × 0.65 = 65,000` で完全一致 |
| シーズン原資の正本は `value_plan_cycles.budget_yen` | SX現行シーズン 5,642,000（7-1章の「2026-06-19 に 6,812,000 → 5,642,000 へ是正」と一致） |
| シーズンは年をまたぎ、1PJが複数シーズンを持つ | SX `202601-202603` と `202604-202703`、KUTE `202605-202703` |
| 配分が進んだシーズンでは **外部支払 + 会社留保 = シーズン原資** | KUTE 4,679,994、CX 585,000 でぴったり一致 |
| 請求額のうち35%は配分原資の外（AMD運営費30% + クローザー報酬5%）で `reward_summary_json` に入らない | 7-1章 |
| 現金支払の対象外判定は `exclude_from_payout_notice`（`is_officer` ではない） | 7-1章 2026-07-29 訂正。まさ・きよ・りり・**あき**・info が true |
| `reward_summary_json` の使えるキー | `externalPayoutCapYen`（外部へ実際に出た現金）、`companyReserveYen`（支払対象外メンバーへの非現金配賦）、`members[]`（`memberId` / `totalPay` / `companyReserveYen` / `payoutExcluded`） |
| `tally_weekly_effort_entries` はまさ専用 | `member_id` は ID001 のみ。CXは まさ30.8時間の記録だけで、あきの行は0件 |
| `members` の表示名は `code_name`（`members.name` は存在しない） | schema |

## 未解決（まさに確認するか、仮置きして根拠を示す）

- **まさの時間の価値をどう置くか。** 「まさの稼働を織り込む」ために必要だが、基準額が未確定。
  前セッションは 20,000円/時 を暫定で置いたが、まさの承認は取っていない。
- **他メンバーの投下時間データが存在しない。** tallyがまさ専用なので、
  「まさ以外がどれだけ動いたか」はOS内に無い。これを推測で埋めない。
  必要なら、無いことをまさへ伝えて判断を仰ぐ。

## 現状のコードと本番

- 本番稼働中（`/admin/project-profitability`、左メニュー「契約・お金」＞「PJ別利益構造」）
- 直近の実装 commit は `862bbeaf`（シーズン単位版）。**禁止事項 8・9 に該当する表示が残っている**
  （「需要 N×」警報、「未配分 ¥X」の金額表示）
- 関連ファイル
  - `pwa/src/lib/project-profitability.ts` — 集計
  - `pwa/src/lib/project-profitability-client.ts` — 参照系キャッシュのクライアント層
  - `pwa/src/app/api/admin/project-profitability/route.ts` — API
  - `pwa/src/components/admin/AdminProjectProfitabilityClient.tsx` — 画面
  - `pwa/src/app/(app)/admin/project-profitability/page.tsx` — ページ
  - `pwa/spec/5-14-project-profitability-current-spec.md` — 仕様正本
  - `pwa/scripts/check_project_profitability.mts` — 再発防止の検査（`npm run test:project-profitability`、deploy.sh に登録済み）
- 検査は**禁止事項に合わせて書き直す**。現在の検査は途中の設計を固定してしまっている。

## 進め方

1. `pwa/manual/7-1-reward-calc-spec.md` を Read で全文読む。grep で済ませない。
2. 上の禁止事項11点を満たす指標設計を、**コードを書く前に**まさへ短く提示して合意を取る。
   抽象的なA/B/C案ではなく、実データを入れた表の形で見せる。
3. 参照系キャッシュ規範（`pwa/spec/5-10-reference-data-caching-current-spec.md`）に従う。
   `npm run test:reference-data-cache` に通す。
4. 実装後は本番相当の実データで desktop 実寸を確認する（PWAのスマホ幅は対象外）。
5. `AMD_OS_VERCEL_DEPLOY_APPROVED=1 bash pwa/scripts/deploy.sh` で反映。
   ただし Vercel は1日100デプロイ/アカウント全体で、`pwa/scripts/check_deploy_quota.mjs` が90超で止める。
   枯渇時は override せず枠が空くのを待つ。

## 補足: このセッションで直したデプロイ不具合（作り直しとは別件、対応済み）

`235b333f` が入れたパス限定ビルドスキップが原因で、2026-08-29 21:15 以降 PWA の本番反映が
1件のERRORと複数の誤スキップで止まっていた。`.vercelignore` が `.git` を消してから ignoreStep が
走るため `git diff` が失敗すること、ビルド環境の浅いクローンでは過去commitとの比較が成立しないことが原因。
`76cd42d6` で「main は必ずビルド」に戻して解消済み。**この設定を再びパス判定へ戻さない。**
