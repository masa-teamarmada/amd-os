// No-op stand-in for the "server-only" package, used only by register_server_only_stub.mjs when a
// plain node test script imports a src/lib module that starts with `import "server-only"`.
export {};
