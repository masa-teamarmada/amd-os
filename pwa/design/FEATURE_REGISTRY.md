# PWA Feature Registry

AMD OS PWA の重要機能を、画面単位で「消してはいけない契約」として列挙する。

このファイルは実装の詳細仕様ではなく、回帰防止用の登録簿。画面・API・DBのいずれかから機能を削る場合は、同じ commit でこの登録を更新し、理由を残す。未更新のまま UI を削除しない。

## 運用ルール

- 新しい業務導線を追加したら、このファイルか該当 `pwa/design/*.md` に機能契約を残す。
- `design_log/` は作業ログであり正本ではない。恒久仕様は `pwa/design/` 配下へ入れる。
- `npm run test:critical-ui` は、この登録簿と実装内の重要 anchor を検査する。
- 重要 UI を置き換える場合は、旧 anchor を消す前に新 UI の anchor と仕様を登録する。

## 全画面surface catalog

目的: 新画面を追加するたびに独立したtitle、ナビ、writer、状態を増やさず、役割別の見え方と業務正本を分ける。

必須機能:

- `src/lib/surface-catalog.ts` が主要画面のtitle、domain、lens、status、pathを一つの正本として持つ。
- statusは `canonical`、`projection`、`transitional`、`mirror`、`deprecated` の閉じた集合とし、別URLが別writerを持つ根拠にしない。
- SSRの`generateMetadata`とclientの`PageTitleSetter`は同じ`surfaceTitleForPath()`を使い、別々のroute title表を持たない。
- ホーム `/dashboard` は `canonical`として維持し、本収束作業で再設計しない。
- `/project/[projectId]/weekly-control`と`navigation`、`/notifications`と`/proactive`は移行中レンズとして登録し、共通作業・判断カーネルへ寄せるまでは独立writerを増やさない。
- HUDとdashboard実験面は`mirror`とし、正規画面から独立した業務正本を持たせない。
- admin navigationは同じcatalogから項目を取り、「組織・権限」「契約・お金」「PJ・実行」「知識・AI」「運用」の職務groupで並べる。既存routeは削除しない。

回帰防止:

- `npm run test:surface-catalog-contract`が共通title解決、ホーム凍結、移行中・mirror status、全admin route登録、職務groupを検査する。

## PJ共有ダッシュボード / role-based top

目的: AMDメンバーは全PJを横断し、SX等のPJ限定メンバーは自分の参加PJだけを同じAMD OS基盤で確認する。

必須機能:

- role-based top: `members.os_access_scope='portfolio'` は `/dashboard`、`project` は参加1件なら `/project/[projectId]/workspace`、複数なら `/my-projects`。
- SX entry: AMD内部の `/dashboard` にあるSX (`p21`) の行は `/project/p21/cockpit` を開く。PJ実行memberは現行 `/project/p21/workspace`、研究機関memberは `/workspaces` から機関別 `/workspace/[slug]/project/p21` へ入る。三画面の見た目は統一せず、workspaceのmanagement正本と承認済みpublicationをrole別に投影する。
- narrow auth: PJ限定OAuthはGoogle本人確認後に通常のSupabase sessionを破棄し、HTTP-only署名付きPJセッションへ交換する。各requestでactive member / scope / PJ membershipを再確認する。破棄は `signOut({ scope: "local" })` に限る (scope省略の既定 `global` はそのユーザーの全デバイスを巻き添えにする)。
- route isolation: PJ限定ユーザーの通常画面は `/my-projects` と許可された `/project/[projectId]/workspace` だけ。他PJ、`/dashboard`、社内cockpit、admin、financeへ遷移できない。
- safe DTO: 共有面はPJ名、表示名、役割、週次時間、5区分、MS、抽出済み活動の件数・種別・最終日だけ。raw本文、URL、email、報酬、契約、内部戦略を含めない。
- effort write: `project_weekly_effort_entries` はPJ / member / week / categoryで一意。PJ限定ユーザーは自分だけ、portfolio/adminはactive PJ memberを更新できる。
- admin onboarding: `/admin/members` でPJ限定Googleアカウントを事前登録し、既存のPJメンバー編集で対象PJへ紐付ける。作成時は支払通知対象外。

## /portfolio-preview — 研究ポートフォリオ構造プレビュー (2026-08-02 /dashboard へ統合済み・退役)

目的だった「研究機関リストとシーズリストをOSの主たる母集団に置き、PJを両者から契約成立後に生まれる実行レイヤーとして見せる新IA」の実データ検証は完了し、2026-08-02 まさ確定で `/dashboard` へ正式採用した。`/portfolio-preview` は独立シェルを撤去し、`redirect("/dashboard")` だけを持つ旧URL維持用ページになった (旧URLをリンクしていた場所を壊さないため)。仮設ナビ・「構造プレビュー」バッジ・「現行OSへ戻る」導線は撤去済み。統合後の正本仕様は下の `## /dashboard` を見る。

## 旧 /portfolio-preview 由来の恒久仕様 → /dashboard へ移設

- 母集団優先の並び順ロジック (PJ化済み → PJ化検討中 → 評価済み → その他) は [`src/lib/portfolio-pulse.ts`](../src/lib/portfolio-pulse.ts) の `buildPortfolioPulseModel` へ移設した。機関・シーズのライフサイクル判定は `/institutions` `/seeds` と同じ正本helperを再利用する。
- 未確認保護: 関係表に無いPJを名称から推定せず「由来要確認」と表示するロジックは維持 (`needsClassification`)。AMD全体 `p00` は研究ポートフォリオPJ件数から外す。
- スコア分離: ECRは研究機関環境、SPSは個別シーズとして別集計・別表示し、単一スコアへ合算しない (継続)。
- 回帰テスト: `npm run test:portfolio-preview-contract` は `npm run test:portfolio-home-contract` (`scripts/check_portfolio_home_contract.mjs`) に置き換え。redirect化、GlobalNav統合、ECR/SPS分離、実route接続、amd-home-page-skinのborders-onlyを検査する。

## 外部ワークスペースアクセス (研究機関 / PJ外部共有、build v3.53.19、migration 212/213)

目的: 研究機関やPJの外部関係者が、AMD OSの社内情報へ触れずに、許可された機関ワークスペースと許可されたPJだけを同じ基盤で読めるようにする。migration 212/213 は2026-08-01に本番適用済み。詳細設計は [institution_seed_project_model.md](institution_seed_project_model.md) §6、route契約は `pwa/spec/2-1-pwa-runtime-routes.md`。

必須機能:

- public top: `/` は認証不要で、認証状態にかかわらず自動転送せず必ずポータルを表示する。`institution_workspaces` の `status='active'` かつ `is_publicly_listed=true` の行だけを slug / ワークスペース名 / 機関の名称・種別・地域で一覧し、説明文、件数、ECR、AMD Score を公開面へ出さない。ログイン済み内部メンバーにはARMADA OSへの明示ボタン、外部アカウントには `/workspaces` への明示リンクを出す。
- external hub: `/workspaces` は所属する機関ワークスペースと、個別に許可されたPJだけを並べる。機関所属をPJ一覧の根拠にしない。PJ取得失敗は参加0件へ変換せず、参加状況は変わっていないことと再読込案内を出す。
- institution workspace: `/workspace/[slug]` は内部アプリのchromeを共有しない独立シェルで、対象機関のPJ・シーズ・ECRを読み取り専用で出す。個別に許可されたPJリンクは `/workspace/[slug]/project/[projectId]` を開き、最新の承認済みpublicationだけを研究機関レンズで表示する。
- dual surface: `/project/[projectId]/workspace` は**同じURLのまま**、アクセス解決の後に内部メンバー向け詳細バンドルか外部向け読み取り専用DTOかを選ぶ。外部面のURLを別に切らない。
- explicit grant only: 認可はアカウント登録 (`workspace_user_accounts`) + 機関ワークスペース所属 (`institution_workspace_memberships`) + PJ個別アクセス (`project_access_memberships`) の3つの明示的な付与だけ。**機関所属はPJアクセスを含意しない**。メールのドメイン一致を認可の根拠にしない。
- narrow auth: メールリンクでログインし、Supabaseセッション成立直後に `signOut({ scope: "local" })` してHTTP-only署名cookie `amd_os_workspace_session` へ交換する。cookieは毎リクエスト検証したうえで必ずDBを引き直し、アカウント停止・所属失効・ワークスペースpauseを次のリクエストで反映する。cookieの中身だけを信用しない。メール送信は登録account単位のDB atomic claimで60秒cooldown、15分5回までとする。
- route isolation: `amd_os_workspace_session` が通常セッションの代わりになるのは `/workspaces`、`/workspace/**`、`/project/[projectId]/workspace` だけ。内部メンバーrouteや `/admin/**` へは効かない。
- fail closed: 未認可・不明slug・未許可PJは redirect せず not found。機関やPJの存在自体を漏らさない。`/api/auth/email-start` は登録の有無で応答の形も状態コードも変えない。
- safe DTO: 外部PJ面はPJ名・状態、計画サイクル、マイルストーンの表題・目標年月・進捗率だけ。進捗行なしは `null` と「未登録」を返し、0%へ変換しない。内部バンドルの取得経路を外部面から呼ばず、メンバー名、工数、根拠、出典、社内管理項目 (目的 / 成果 / 論点 / 仮説 / 意思決定 / 資金 / 関係先)、連絡先を含めない。機関ワークスペースDTOも要約・説明・URL・出典・連絡先・根拠の形をした列と、SPSの `axis_evidence` / `evaluator`、ECRの note / evaluator / rubric を返さない。
- list sorting: 機関ワークスペースのシーズ一覧は `/seeds` と同じライフサイクル優先度 (PJ化済み → PJ化検討中 → PJなし・SPS算出済み → その他) で並べ、同区分内は表題の日本語順。
- ECR transpose: ECR は1機関の縦並びで総合値と8軸を上から読む。機関横断の比較表 (1機関=1行×軸を列) とは別の見せ方として維持し、**SPSとは別系列のまま合算しない** (合成スコア・相関・因果指標を作らない)。
- 愛媛スコープ: p30 は `ehime` ワークスペースへ `shared_surface='summary'` (サマリのみ共有、詳細ワークスペースは非共有)。**p21 の詳細ワークスペースはPJ個別付与でのみ到達可能**で、機関ワークスペースのPJ範囲には含めない。シーズ範囲は `inst_ehime` 紐付け全件をPJ化有無で絞らない。
- 資料室: `/workspace/[slug]/files` と `/project/[projectId]/workspace/files` は `workspace_documents` + private Storage `workspace-files` を共通正本にする。資料は機関またはPJのどちらか一方へ属し、機関所属からPJ資料権限を派生させない。外部は `workspace_shared` だけ、AMD内部は `amd_internal` も読める。fileの実URLを一覧DTOへ返さず、open APIが毎回権限を再検証して60秒の署名URLを発行する。`canUpload`はfile/link/folder追加に加えてHTML本文編集とfile/link/folderのarchiveを行え、整理（名称変更・移動・共有範囲変更）だけは`canManage`に残す。activeな同名fileの追加は、同じfolderだけを対象にFinder型の中止／両方残す／置き換える確認を出す。`資料 2.pdf`の連番はactive entryと同時追加queueを避け、置き換えは`canUpload`かつserver再認可済みの同名active fileへ明示した時だけ既存Storage objectを上書きする。link/folderは置き換えず、通常追加の競合raceも409で止めて無言上書きしない。archiveは実体を消さず、通常一覧・共有画面から外して保護領域に残す（復元UIは未実装）。AMD内部のPJコックピットは資料名・件数・最新資料一覧を本文へ展開せず、`WorkspaceDocumentLauncher` の1ボタンからURLを変えない大きなモーダルを開く。閉じた後は同じコックピット文脈へ戻り、独立routeは直接到達用に残す。モーダルは所属・現在folder・共有範囲・権限を細いscope railへ、件数・検索・絞り込み・追加操作を同じtoolbarへ集約する。desktopの一覧行と操作は高密度の36〜55px帯、drop入口は36px railとし、1440×900では10行分の一覧高を確保する。mobileは検索・追加・閉じる・パンくずを44px以上にし、一覧以外へ横overflowを出さない。資料室の全面配色は白・graphite・deep navy・AMD blue・cyanで、旧ivory/green/amberを使わない。migration 216〜219は2026-08-02に本番適用済み。
- HTML表示・編集とPDF化ダウンロード: 保存済み `text/html` と `.html` / `.htm` は短期署名URLへ直接リダイレクトしない。資料名は資料ごとの権限を再確認したsandboxプレビューを別タブへ表示し、HTML内のJavaScript・外部通信・form送信を止める（5MBまで）。`canUpload`のHTML編集は専用source APIで毎request再認可し、署名URLを返さず、HTMLソースを実行せずにtextareaへ渡す。空欄不可・UTF-8 5MBまでをserverで検証し、保存時は既存private Storage objectとサイズ/MIME/hash/更新日時を更新する。右端のPDF化ダウンロードだけが、同じく再認可したPDF APIで日本語フォントを埋め込んだA4 PDFとして返す。PDF入力HTMLは8MB、返却PDFは4MBまで。元HTMLをダウンロードとして配布しない。
- BOX移行: VSX/CX/SE/SX/ZMP/KUTEの旧Project ShareをPJ資料室へ非破壊コピー済み。既存 `project_documents` のDrive資料は `AMD内部/Drive資料` へ内部限定linkとして併記し、旧テーブルは既存機能のため残す。旧Project Shareは、外部メールアカウントとPJ個別grantを登録して到達確認するまで入口を閉じず並行稼働する。旧BOXを資料の正本として新規運用し続けない。
- capability: role名は `workspace-capabilities.ts` で明示capability束へ変換する。manager / ownerという名前から、未実装の所属管理、共同PJ更新、相手方主権データ確定を推定して付与しない。
- mutation origin: cookie認証付き資料変更はsame-origin guardを必須とし、`Origin`、`Sec-Fetch-Site`、`Referer`のいずれでも同一originを確認できないrequestを403で閉じる。
- admin management: `/admin/access` (内部admin限定) + `GET/POST/PATCH /api/admin/workspace-access` が唯一の付与・停止導線。`kind` を必須にし汎用upsert経路を作らない。停止済み (suspended / revoked) は作成では復活せず明示的な PATCH のみ。機関所属の付与がPJアクセスを自動作成しない。`auth.users` の id は select も返却もしない。GETは全対象tableをpaginationし、500/1000件で黙って切らない。
- audit: `workspace_access_audit_logs` にログイン要求・送信・成功・拒否・ログアウト・admin操作を記録する。account、grant、資料metadataのrow変更はDB triggerで同じtransactionに記録し、semantic audit insert失敗も成功扱いしない。メール本文、URL、トークン、未登録アドレス、Storage pathは残さない。
- RLS: migration 212 の新設7テーブルは anon / 一般 authenticated のポリシーを持たない (admin + service_role のみ)。migration 213 は既存15テーブルの anon read を撤去し、authenticated を `amd_os_is_member()` ゲートへ寄せる。ECR / SPS の軸値と評価行そのものへは INSERT / UPDATE / DELETE を行わない。
- internal read client: migration 213で閉じた内部テーブルをブラウザから読む場合は、匿名固定clientでなくSupabaseのログイン済みsessionを持つbrowser clientを使う。server routeはservice clientを明示注入する。PJ取得失敗を0件表示へ潰さず、dashboardに再読み込み可能なエラーを出す。

回帰防止:

- `npm run test:workspace-access-scope` (所属・失効・機関→PJの暗黙付与なし)、`test:workspace-access-session` (署名cookieの検証)、`test:workspace-email-start-contract` (登録有無を漏らさない応答とOTP claim)、`test:workspace-next-path` (遷移先の絞り込み)、`test:external-project-workspace` (外部DTOが内部バンドルへ広がらないこと)、`test:workspace-access-admin` (admin APIの権限・自動復活禁止・完全pagination・`auth.users` 非返却)、`test:workspace-rls-closure` (213の閉鎖範囲)、`test:workspace-documents-core` (path/URL/storage key・HTML本文byte・Finder連番検証)、`test:workspace-documents-contract` (権限再検証・明示置き換えだけのStorage上書き・raceでの自動上書き禁止・HTML編集の署名URL非返却・capabilityでの編集/削除・非破壊archive)、`test:workspace-fact-origin-contract`、`test:workspace-security-migration-contract`、`test:workspace-capabilities` が構造的に検査する。
- 外部面へ内部cockpit DTOや `getProjectWorkspaceBundle` を流用しない。外部が見る情報を増やす場合は外部専用の許可列を広げ、内部バンドルの再利用で代替しない。
- `/project/[projectId]/workspace` を外部用の別URLへ分割しない (二面構成が契約)。

## /manual

目的: AMD OS の使い方・仕様・運用履歴の正本を、読み手が自力で検索し、必要ならページ限定つくよみに質問できる状態にする。

必須機能:

- 左カラム検索: `pwa/manual/*.md` から生成した `ManualSearchDocument` を使い、章タイトル / summary / 見出し / 本文 / 画面パス / テーブル名を横断検索する。検索結果は章番号・タイトル・抜粋・topic/screen chip を出し、該当 `/manual/[slug]` へ遷移できる。
- 章本文と目次の維持: `/manual` はセクション別目次、`/manual/[slug]` は本文 + 左目次を表示し、検索を使っても既存の `ManualMapClient` / `MarkdownView` 導線を消さない。
- ページ限定つくよみ: `ManualTsukuyomiFloat` は `/manual` と `/manual/[slug]` だけに出す。global layout の visible mascot を復活させない。
- Gemini Q&A: `POST /api/manual/tsukuyomi/ask` は `GEMINI_API_KEY` と `gemini-2.5-flash` で該当章のマニュアル本文を根拠に回答し、「ここ見たらOK」の参照章リンクを返す。DB 書き込み、PJ 修正 tool、`tsukuyomi_chat_logs` 保存は持たせない。

回帰防止:

- global `TsukuyomiChatBridge` は従来どおり invisible event bridge のまま。`Mascot.tsx` を `(app)/layout.tsx` に戻さない。
- マニュアル章の追加・削除・構成変更は `pwa/src/app/(app)/manual/manual-chapters.ts` と `pwa/design/os_manual.md`、必要なら `pwa/manual/9-3-appendix-changelog.md` を同じ作業単位で更新する。

## /bzm/map — BZM 2.0 理論マップ (2026-07-30 追加、2026-08-01 本人作成へリセット、2026-08-02 ノード内メモ概念修正・小型閲覧インスペクタ化・座標永続化・小窓外クリック閉じ、build v3.55.3)

目的: BZM 2.0 の主張・概念・測定・決定・文献・未解決論点を9関係で結び、自分の理解からノードとエッジを育てる**論証台帳**。「真理マップ」ではなく、ノード数・接続数は真偽・確信度を表さない。ノード=理論要素、エッジ=理論要素同士の関係、メモ=選択ノードの内側へ積む記録で、この3者は別概念であり、メモを追加してもノード・エッジは増えない。

必須機能:

- source of truth: `bzm_theory_nodes` / `bzm_theory_edges` / `bzm_theory_node_memos` の3テーブルだけ。migration 208で旧seedを全削除し、0件からまさ本人が育てる。migration 214が`bzm_theory_node_memos`を空で追加。Markdown 21ノード / 34関係とmigration 203のseedは検証・復元用履歴で、ランタイムへ自動表示しない。
- write contract: member read、admin write (3テーブル共通)。API `/api/bzm/theory-map` は認証後だけ管理クライアントを使い、RLSも `is_admin()` writeを強制。ノード保存、Cmd/Ctrl二点目の即時接続、確認済み接続解除、選択ノードへのメモ追加だけを書き、通知・外部送信・自動保存はしない。
- create: マップ空白クリックで、地図を覆わない予約区画の作成panelを開く。desktopは地図右、mobileは地図下。マップ外に作成ボタンを置かない。別文献や別理論要素をグラフへ加える場合だけこの経路を使う。新規作成ではkindを問わず、基本項目の「参考リンク（任意）」からURL・DOI・書誌情報・OS内参照を`source_ref`へ保存できる。リンク値はマップ上や常設UIへ表示しない。
- memo: 選択ノードの操作帯にある「メモを追加」1ボタンから、draft nodeを作らずマップ内オーバーレイでメモ本文→役割 (`supports`/`challenges`/`refutes`/`raises`/`tests`) の2項目だけを書き、`POST { action: "create_memo" }` で `bzm_theory_node_memos` へ1行追加する。ノードもエッジも作らず、nodes/edges件数表示は不変。保存後もボタンは残り同じノードへ何件でも追加できる。
- connect/edit: 通常クリックはノードの**閲覧**小窓を開き、`編集する`を押して初めてフォームへ入る。通常ドラッグは位置変更だけで、終了時に`position_x`/`position_y`を保存する。nullの旧座標は決定的な散在fallback表示だけを行い、閲覧時にDBを更新しない。`Cmd+click`（他OSは`Ctrl+click`）で2ノードを順に選び、2点目で `1点目 → 2点目` を即時保存する。relationは1点目選択後の小さな待ち帯で選べ、初期値は `supports`。「つなぐ」ボタンや接続panelは出さない。
- ノード削除 (2026-08-02 追加、build v3.54.8): 管理者はノードを削除できるが、物理DELETEではなく `bzm_theory_nodes.archived_at` を設定するrecoverable soft-deleteで、store関数 `archiveNode` が担う。API は認証済み管理者の `DELETE /api/bzm/theory-map?nodeId=<id>` から呼び、`edgeId`/`nodeId` は片方だけを受け両方/両方なしは400。active ノードだけを対象にし `updated_by` を記録する。UIは`BzmTheoryComposerDialog`のeditモードfooter左に44px hit targetの控えめなvermilion削除ボタン (desktop=`Trash2`+「削除」、狭幅=icon主体+`aria-label`) を置き、押すと同じオーバーレイ内でノード名・接続件数・メモ件数・影響説明を示す削除確認へ切り替える (中央モーダル・三点メニューは作らない)。「編集へ戻る」でフォーム未保存値を保持し、即時削除はしない。削除成功後はnode・incident edges・node memos・positionをUI stateから除き、選択とcomposerを閉じて成功通知する。エッジ削除の入口は線クリックだけで、`DELETE /api/bzm/theory-map?edgeId=<id>` を呼ぶ (2026-08-02 の右台帳撤去で、旧「接続しているノード」一覧の行内44px削除ボタンは廃止)。
- 選択ノード台帳の撤去・全幅化 (2026-08-02 追加、build v3.54.15): 右側に常設していた選択ノードの読取りウィンドウ (summary/source_ref/body表示、カバレッジの欠落警告、メモ一覧、接続しているノード一覧、行内エッジ削除ボタン) を全廃した。desktopのマップは固定幅の予約列 (旧 `lg:grid-cols-[minmax(0,1fr)_380px]`) を持たず、利用可能な横幅いっぱいに広がる。ノードの読み書きは、通常クリックで開くマップ内編集オーバーレイ (title/summary/kind/layer/status/source_ref/body_md をフォーム編集) と、選択ノード下の操作帯 (「編集」「メモを追加」ボタン、`Cmd`/`Ctrl` 接続) の2つに一本化した。エッジの読み取り一覧・カバレッジ欠落警告・メモの常設一覧は他画面へ移設せず、機能として廃止した。
- 小型インスペクタ (build v3.55.3): 作成・閲覧・編集はマップ内の同一小窓。既存ノードはまず表示モードで、本文とメモのLaTeXをKaTeXとして組版し、メモの改行を保持する。`編集する`でだけフォームへ切り替える。ヘッダーのグリップで小窓を動かせるが、実ブラウザ可視帯の外へは出ない。閉じる×は置かず、**小窓そのもの以外**（マップ空白、フィルタ、ページ余白、別ノード）を押すと閉じる。別ノードの通常クリックは、そのまま対象ノードへの切替を続ける。小窓内の入力・ボタン・ヘッダードラッグは閉じない。小窓は小さい文字・短いcontrol・内部スクロールで密度を保つ。
- map 表示: `react-force-graph-2d` ( `next/dynamic({ssr:false})` ) による力学グラフ。ノード塗り色は kind、形もkindの非色情報、半径は接続本数、layer ごとの列分けと同一 layer 内の縦分散を持つ。statusは一覧表示・編集オーバーレイの文字ラベルへ退避し、ノード外周に点線・破線を重ねない。中心略字は表示しない。
- list 表示: フィルタ通過ノードを kind/layer/status バッジ + 接続本数付きで一覧する。スマホでは list を初期表示する。行クリックはそのノードの編集オーバーレイを開く。
- フィルタ・検索: id/title/summary/source_ref 全文検索、layer・status・relation type のトグルフィルタ、フィルタ解除ボタン。検索と layer/status は map/list へ作用し、relation type は map のエッジだけを絞る。
- 詳細契約: `/spec/2-6-bzm-theory-map-current-spec`。

回帰防止:

- `/bzm/map` route、editor API/components、DB store、migration、snapshot parser、2本のvalidatorを消す変更は、`FEATURE_REGISTRY.md`、`/spec/2-1`、`/spec/2-2`、`/spec/2-3`、`/spec/2-6`、`pwa/bzm/9-5-appendix-changelog.md` を同時に更新する。
- 画面・validator に真偽判定・統合スコア・合成指標を追加しない。件数・接続数を経営判断や評価軸の代わりに使わない。
- メモ追加を「1メモ=1ノード+1エッジ」を作る経路へ戻さない (2026-08-02 に撤回した誤仕様)。メモは `bzm_theory_node_memos` だけへ書く。
- ノード削除を物理DELETEへ戻さない。`archived_at` によるrecoverable soft-deleteを維持し、確認画面 (ノード名・接続件数・メモ件数・影響説明) を経ずに即時削除するUIを作らない。
- 常設の選択ノード台帳 (右側読み取りウィンドウ、固定幅予約列、カバレッジ欠落警告、常設メモ一覧、常設接続ノード一覧、行内エッジ削除ボタン) を復活させない (2026-08-02 撤去)。ノードの読み書きはマップ内編集オーバーレイと選択ノード操作帯 (編集・メモを追加・Cmd/Ctrl接続) に一本化し、地図はマップ/一覧いずれの表示でも右の固定幅予約列を持たない。

## /knowledge-map

目的: 元素・鉱物・鉱石・樹脂・高分子を「材料 → 供給 → 用途 → AMDとの接点」で横断し、高校生でも世界の材料事情・研究テーマ・事業機会・供給リスクを数分で比較できる AMD Materials workspace として使う。従来の L2 / manual / spec / BZM ノウハウ地図も `材料マップ` tab に残す。

必須機能:

- read-only route: `/knowledge-map` は DB write、LLM 呼び出し、外部 NotebookLM 同期を行わない。
- materials lenses: `全体` / `元素` / `鉱物・鉱石` / `樹脂・高分子` / `材料マップ` / `比較` を切り替えられる。画面内の質問フォームや生成AI回答欄は持たない。
- periodic table: 118元素を周期表配置で表示し、`注目度` / `需要` / `供給不安` / `AMD相性` の4軸を切り替えられる。注目度は淡黄→黄→橙→赤→深紅の熱色で表示し、評価済みcellには平易な日本語の主用途を置く。供給不安4は`警戒`、5は`危機`をcell上端と供給警報帯に常時重ね、選択中のheat軸にかかわらず見落とさせない。周期表cell内に`4/5`等の数値は表示せず、評価値は初期の定性評価として根拠を併記し、未評価を低評価やゼロと混同させない。
- element insight: 評価済み元素cellを押すと、用途・供給警報・直近公表相場・5年価格推移・産出国構成を絞って示すmodalを即時表示する。`詳細を開く`で同じmodalを拡張し、評価理由、埋蔵・需給、循環・代替、AMD接点、sourceを追加表示する。相場は代表取引品目、単位、公表時点、非live値であることを明示する。世界生産が非公表の品目は輸入供給元等の代替指標であることを円グラフの注記に出す。
- ranking and navigation: `全体`の元素・鉱物・樹脂入口は矢印だけでなくカード全面を操作対象にする。総合値は独立した編集点を持たず、`注目度 + 需要 + 供給不安 + AMD相性` の4指標合計（20点満点）とする。周期表以外の全体ランキング、鉱物一覧、樹脂一覧、比較列は4指標合計の高い順に並べる。別に、全材料を横断する`需給の崩れランキング`を専用の偏りの強さ（5点満点）で並べる。不足だけでなく供給過剰と価格乱高下も区別し、原因、供給が詰まる工程、評価時点、確からしさを併記する。
- materials detail: 重要元素、代表的な鉱物・鉱石、代表的な樹脂・高分子は、項目を押した直後に要点の小窓を開き、特徴、用途、需給の向き・原因、供給国を示す。`詳細を開く`で同じ小窓を拡張し、埋蔵・供給、代替・循環性、材料の流れ、AMDとの接点、出典リンクを読める。本文と固定ラベルは高校生が辞書なしで読める自然な日本語を原則とし、化学記号や業界略称は日本語の説明を添える。樹脂は追加で、何を原料に、どの重合・重縮合・硬化等の工程で作るかを平易な日本語で示す。
- compare: familyを跨いで2〜4件を比較trayへ追加し、同じ評価軸と用途・供給・循環性・AMD相性を並べて見られる。
- data sources: `protocols` / `project_knowledge` / `member_knowledge` / `project_meeting_summaries` / `project_strategy_signals` / `project_xrl_evidence` / `monthly_reports` / `textbook_insight_candidates` の件数と直近代表 row を読む。
- graph controls: 検索、domain filter、expand/collapse、zoom、node detail panel、source route link を持つ。
- source hygiene: メール全文、Slack全文、議事録全文、資料全文を fetch / 保存 / 表示しない。短い L2 text、summary、source ref、status だけを使う。
- source discipline: 市況・埋蔵量・生産国・需給は source と評価時点を示す。精密値を持たない初期版では、推測値を実測値のように表示しない。

回帰防止:

- `/knowledge-map` route、GlobalNav 導線、materials workspace、118元素の周期表、比較tray、`KnowledgeMapView`、`fetchKnowledgeMapData` を消す変更は、`FEATURE_REGISTRY.md`、`/spec/2-1`、`/spec/2-2`、manual 2-5 を同時に更新する。
- NotebookLM export を追加するときも、OS 側が正本で、NotebookLM はコピー先という境界を崩さない。

## /business-cards

目的: スマホで受け取った名刺を撮影し、OCR候補を人が確認してPJへ紐付け、連絡先台帳とPJナレッジを同時に育てる。

必須機能:

- GlobalNav: 動かすグループに `名刺` と `/business-cards` を置く。
- iOS tab: `MainTabView` は旧 `月次ルーティン` tabを出さず、`PJ進捗 / 名刺 / 設定` の順で `BusinessCardsView` を置く。native shell は `/native/business-cards` を使う。
- mobile capture: カメラ起動 (`capture="environment"`) と写真ライブラリ選択を持ち、送信前に長辺1800px目安へ縮小する。
- review gate: OCR結果を自動確定しない。`processing` → `needs_review` または `ocr_failed` に止め、氏名と1件以上のPJを人が確認した PATCH だけ `confirmed` にする。
- PJ link: 複数PJを選択でき、確定時に `business_card_project_links` と `project_knowledge(category='people', source='business_card', status='active')` を同期する。
- private boundary: 画像は private Storage `business-cards` に置き、認証済みAMDメンバー向け image route だけで読む。email / phone / address / 画像 / raw OCR は `project_knowledge` に複製しない。
- prompt management: OCR prompt は `llm_prompts.prompt_key='business_card.ocr'` を正本にし、コードへ本文を直書きしない。
- recovery: OCR失敗でも画像と名刺行を残し、手入力して確定できる。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が route、GlobalNav、iOS tab順 / 旧月次ルーティンtab不在、カメラ入力、確認ボタン、PJ複数選択、private image route、OCR prompt migration、D-3同期を検査する。
- この導線を削除・薄くする変更は、`/spec/2-5`、manual 2-8、`/spec/2-1`、`/spec/2-2`、D-3 current spec を同時に更新する。

## /poc

目的: Seeds の研究シーズとPoC先を一次入力として持ち、その掛け合わせからヒアリング論点、PoC条件、謝礼、契約、資金、収益分配まで案件化する。

必須機能:

- GlobalNav: 探索グループに `PoC` と `/poc` を置く。Seedsの下流にある案件化棚なので、Admin配下へ移さない。
- data sources: `seeds` / `poc_companies` / `poc_matches` / `projects` / `members` を読む。`seeds` は研究シーズ正本として再利用し、PoC固有のPoC先・条件・質問は `poc_*` に置く。
- source hygiene: Notion議事録、Gmail、Slack、Drive、Web本文やURLを保存・表示しない。`source_ref` / `source_note` は短い参照名だけにする。
- add seed: シーズ名、機関、地域、PI/研究者、領域、用途・業界タグ、キーワード、概要、担当、状態、次アクションを `seeds` に保存できる。
- add PoC destination: PoC先名、規模感、地域、業界タグ、PoC相性、過去PoC/紹介経路、謝礼、担当、状態、次アクションを `poc_companies` に保存できる。
- PoC destination list: PoC先を比較表で表示し、業界タグ、地域、規模感、状態、PoC相性、謝礼・履歴、案件数、次アクションを横並びで比較できる。業界タグ、地域、規模感、状態などのタグで候補先を絞り込める。
- pairing queue: シーズごとに既存案件と上位PoC先候補を並べ、候補の `案件化` で相性仮説・ヒアリング論点・PoC条件の初期案を `poc_matches` に生成できる。
- inline status: 案件候補とPoC先の状態は一覧上で更新できる。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `/poc` route、GlobalNav導線、`seeds` / `poc_companies` / `poc_matches` data access、シーズ追加、PoC先追加、PoC先候補リスト、案件化キューを検査する。
- `/poc` を消す/薄くする変更は、`poc_matching.md`、`SPEC_pwa.md`、manual 2-5 / 5-1、`/spec/2-1` を同時に更新する。

## /admin/japanese-culture-map

目的: 日本文化コンテンツを admin 側の知識ビューとして読み、`jp_culture_items` をマインドマップと日本地図で俯瞰する。

必須機能:

- admin-only route: 実画面は `/admin/japanese-culture-map` に置き、`/admin` layout の admin gate を通す。通常の資料ナビと共通左サイドナビの Admin group からは外し、admin 画面内の `AdminSidebar` にだけ置く。
- read-only view: 画面は `jp_culture_items.status='active'` を読み、DB write、LLM 呼び出し、外部同期を行わない。
- 2 view: 大分類→中分類→アイテムのマインドマップと、都道府県→市区町村の日本地図を切り替えられる。
- legacy redirect: 旧 `/japanese-culture-map` はブックマーク互換だけ残し、`/admin/japanese-culture-map` へ redirect する。

回帰防止:

- GlobalNav の一般「資料」グループや共通 Admin group へ戻さない。導線は `AdminSidebar` にだけ置く。
- `pwa/scripts/check_pwa_critical_ui.cjs` は `GlobalNav.tsx` に `日本文化` / `/admin/japanese-culture-map` / `/japanese-culture-map` が戻ったら落とす。
- route を消す変更は、`FEATURE_REGISTRY.md`、`/spec/2-1`、`/spec/2-2`、manual 2-6、`design/os_manual.md` を同時に更新する。

## /admin/* shell

目的: admin 業務画面では admin 専用メニューと本文だけを表示し、通常の共通メニューと admin メニューが横に二重表示される状態を作らない。

必須機能:

- menu replacement: `AppShell` がブラウザ側の現在 pathname を見て、`/admin/*` では `GlobalNav` の代わりに `AdminSidebar` を同じ左端へ表示する。ダッシュボードから画面内リンクで admin へ移動しても通常メニューを残さない。
- single sidebar: `(app)/admin/layout.tsx` は admin 権限 gate と本文余白だけを持ち、`AdminSidebar` を追加描画しない。
- exit: `AdminSidebar` の先頭から `/dashboard` へ戻れ、現在の build version も確認できる。
- width: admin 本文は単一サイドバーの残り幅を使う。二重サイドバーや入れ子の横レイアウトで業務表を圧迫しない。
- mobile: 狭い画面では48pxの操作レールだけを残し、adminメニューは開閉式のdrawerにする。常時192px幅で本文を圧迫しない。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `AppShell` の menu replacement と、admin layout に `AdminSidebar` が無いことを検査する。

## /admin/contracts

目的: admin が、AMDと各PJの契約関係を「1行 = 1契約」で追い、契約書を開かずに期間・金額・支払・業務範囲・成果物・費用負担・知財・利用・秘密保持・再委託・解除・責任・押印証跡を確認する。

必須機能:

- 対象境界: 主台帳は `relationship_scope='amd_contract'` のみ。AMD以外の当事者間契約、雛形、当事者未確認の自動候補は隔離し、AMDという文字列だけで昇格させない。
- 契約台帳: Drive folder、MTG、議事録、版違い、形式違いを行として混ぜず、1契約1行で状態・期間・金額/支払・業務/成果物・費用/報告・権利/制限/リスクを比較できる。
- 契約同一性: `canonical_contract_id` で同じ契約の既存記録を結ぶ。移行前だけ契約名から作業語を除いた候補で暫定集約し、送付確認・微修正・DocuSign依頼は契約詳細の関連記録として読む。
- 実務条件モーダル: 行を開くとモーダルを表示し、契約当事者・状態/押印・PJ反映を先頭に、期間/更新、金額/支払、業務/成果物、費用負担、知財/利用、秘密保持/制限、解除/責任、文書/根拠を確認できる。文書と版、関連記録は別タブに分ける。
- PJ反映: `is_current_for_project=true` の契約を `projects.contract_terms_json.currentContracts[]` へ契約ID単位で同期し、各PJコックピットの「契約上の実行条件」に表示する。複数契約の条件は混ぜない。コックピット用短文は `terms.cockpitSummary` に分離する。
- 幅: 台帳は本文の利用可能幅をすべて使う。狭い画面は押し潰した表や横スクロールにせず、契約ごとの要約表示へ切り替える。
- 固定列: 一番左を PJ、次を契約名とし、この2列は横スクロール中も左に固定する。
- evidence boundary: 契約書ファイル、修正案、押印版、会議上の言及は `contract_documents` / `contract_signals` / `contract_terms` の証跡として扱い、台帳行を水増ししない。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が台帳の最低幅、PJ → 契約名の列順、2列の固定表示、AMD当事者境界、1契約1行、実務条件モーダル、PJコックピット反映の実装アンカーを検査する。
- 表示境界を変える時は、`/spec/5-6` と manual 6-7 を同時に更新する。

## /admin/invoices

目的: admin/きよが、締め済み稼働月の請求書発行を上から処理する。旧 `/admin/billing` は廃止済みで、互換のため `/admin/invoices` へ redirect する。

必須機能 (2026-08-12 二列レイアウトへ再設計):

- 左メニュー導線: AdminSidebar には `請求書発行` と `/admin/invoices` を置く。`Billing` 表記へ戻さない。
- 2列レイアウト: 左列＝PJ名を主表示した選択リスト（請求先名と未完了件数も表示）。右列＝①契約条件、②13か月請求台帳、③選択月ワークベンチ。デスクトップは document 全体をスクロールさせず内部領域へ収め、モバイルは縦積みへ再配置する。
- 台帳の対象: `projects.status IN ('active','ended','frozen')` の各PJについて直近13か月を生成し、`billing_cycles` をleft joinする。期間外・freeze後・未来月は出さない。cycle欠損は空白にせず `月次台帳未生成`、金額欠損は `請求額なし` blockerとして出す。
- 契約条件: 全PJに `立替の請求上乗せ` を `上乗せ可 / 上乗せ不可 / 未抽出` の3状態で必ず表示する。メモだけから可否を推測しない。立替がある月で未抽出なら請求額を確定せず、契約台帳での抽出を発行blockerにする。
- 金額表示: 台帳は「基本額 / 上乗せ立替 / 請求候補」を月ごとに並べる。選択月ワークベンチはさらに「承認済立替 / 請求上乗せ / 未承認参考」を分離する。請求候補は **基本額 + 契約上『上乗せ可』かつ承認済みの立替**。上乗せ不可のPJは立替発生額を一覧へ残すが候補額とfreee明細へ加えない。基本額は `contract-money.ts` の正本ロジックを使い、`budget_yen` は使わない。
- 立替一覧と承認: 選択月の立替を全ステータスで一覧表示する。`submitted` は担当PMまたはadminが「PM承認」、`pmApproved` はadminが「admin承認」できる。明示クリック時だけ `/api/reimbursements/decision` を呼び、共有サーバー処理がactive member、担当PJのPM/admin、遷移元statusを再検証して条件付き更新する。自動承認・自動通知はしない。
- blocker明示: `freee取引先未設定` / `請求額なし` / `立替上乗せ条件未抽出` / `立替未承認 N件` / `月報未確定` を独立して発行不可理由にする。未承認立替が発行を止めるのは契約上 `上乗せ可` のPJだけ。`上乗せ不可` のPJでは内部精算として表示・承認できるがクライアント請求を止めない。
- 請求書作成・発行: blockerがない未発行月（過去滞留を含む）に `請求書作成` ボタンを置き、作成モーダルを開く。freeeへ実送信するモーダル最終操作だけを `請求書を発行` とする。`過去滞留` 状態自体をクリックでき、詳細で正しい請求月へ更新するか請求書作成へ進める。`issue-invoice` Edge FunctionはDBの `claim_invoice_issue` でblocker検査と発行claimを原子的に行い、並行リクエストは409で拒否する。freee送信後は同じclaim保持者だけが発行済みへ更新できる。送信後に成否不明の障害が起きたらclaimを自動解放せず、freee照合なしの再発行を止める。画面・モーダル・実発行の立替集合は `billed_ym` 優先（なければ`date`）かつ `上乗せ可 + approved/paid` へ統一する。
- 書込み境界: `reimbursements` のauthenticated RLSは読取と「本人のsubmitted申請・編集・削除」だけを許可し、承認metadataはNULL固定。承認遷移は認証済みserver route/Edge Functionが役割とstatusを検査してservice-roleで反映する。ブラウザからの直接承認updateへ戻さない。
- 発行モーダル: iOS `InvoiceStepView` / 旧 GAS `cpOpenInvoiceModal` と同じ発行仕様を維持する。件名、基本明細行、契約月額との差分確認、前月明細引き継ぎ、承認済み立替の読み取り専用明細、調整行、請求日、支払期日、備考、発行済み情報、発行取消を出す。件名とヘッダーは `client_name` を使い、`project_name` / `project_id` など AMD 内部呼称を請求書発行モーダルや freee 件名に出さない。単なる件名/日付/全行だけの薄いモーダルへ戻さない。
- 請求額表示: 明細合計 (`invoice_base_lines_json`) を最優先し、なければ `contract-money.ts` の `contractBackedClientAmount`（確定請求額 `budget_reported_amount` > 契約スケジュール金額 > 月額固定契約の `projects.fee_amount`）へ fallback する。PJ 予算 (`budget_yen`) は AMD 側の原資/報酬予算なので、請求発生判定や請求額 fallback に一切使わない。
- 明細保存: 発行前に `invoice_base_lines_json` / `invoice_subject` を保存できる。発行後は `invoice_issued_at` / `freee_invoice_number` / `invoice_pdf_url` が `billing_cycles` に反映される。
- 互換: `/admin/billing` は画面を持たず `/admin/invoices` に redirect する。
- 通知: この画面の操作ではSlack/メール通知を一切送らない。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `/admin/invoices` route、`/admin/billing` redirect、AdminSidebar 導線、`AdminInvoiceIssueQueue`、`FreeePartnerPicker`、`AdminInvoiceIssueDialog` の `基本明細行 / 立替精算 / 調整行 / 備考 / 発行を取り消す`、`issue-invoice` anchor を検査する。2026-08-12 の二列レイアウト再設計でも、これらの文字列アンカーは維持したまま実装している。
- 旧 PM 月次 routine へ請求書発行を戻さない。請求書発行・送付は admin 業務のままにする。
- 立替の権限・状態遷移判定は画面へ複製せず、`reimbursement-decision.ts` の共有サーバー処理を正本にする。Slack受け口も同じ処理へ委譲する。

## /monthly-agreement

目的: 月初に、本人が「何への合意か」「何を確認するか」を迷わず理解し、各PJの担当内容と予定額を対応づけて合意できるようにする。

必須機能:

- 状態の明示: ヘッダー直下に横幅いっぱいの状態欄を置き、`合意状態：未合意 / 条件更新あり / 合意済み / 対象外` のいずれかと、その理由を一文で表示する。状態値だけの `未確認` と、対象が曖昧な `確認不要` は使わない。
- 変更点の明示 (`条件更新あり` のみ、2026-07-21 追加): 状態欄の直後・`確認して合意する内容` の直前に `data-testid="monthly-agreement-change-summary"` の「今回の変更点」を置く。前回合意時の `snapshot_json` と現在snapshotをPJ・MS単位で比較した件数 (`monthly-agreement-change-count`) と、PJごとの「前回 → 今回」を表示する。前回記録が無い/旧形式なら理由メッセージのみ表示し、生JSON・生hashは出さない。純粋関数 `diffMonthlyAgreementSnapshots()` (`src/lib/monthly-work-agreement-diff.ts`) で計算する。
- 予定額変更の理由 (`条件更新あり` のうち `expectedRewardYen` 変更、2026-07-28 追加): 管理者がPJごとに8文字以上の説明を入力し、`ym × member × PJ × 現在snapshot hash` へ保存する。メンバー側は `monthly-agreement-change-reason` で金額比較より先に理由を読み、理由が無いPJは「変更理由を確認中」と修正要望導線を出す。未入力の理由が1件でもあれば合意API・合意ボタンとも止めるが、報酬計算・payout gateの算定値・非金額だけの再合意は変えない。理由を推測・自動生成しない。
- 確認対象の明示: 状態欄の直下に `確認して合意する内容` を置き、`01 担当する仕事` と `02 その対価としての予定額` をそれぞれ独立した全幅セクションとして縦に並べる。`01` は全PJの担当内容、`02` は予定額合計と同じPJ順の内訳を表示する。契約上の必須事項を表の小見出しへ戻さず、必須領域では12px未満の文字を使わない。
- 操作順序: `確認して合意` は全PJの確認内容より後ろに置く。`修正要望` は隣の副操作から必要なときだけ開く。合意済み / 対象外では主ボタンを状態表示へ切り替える。
- 参考情報: 支払い状況、予定額の根拠、snapshot ID、PJ詳細は、合意操作より後ろの初期状態で閉じた `参考情報` にまとめる。
- 回帰確認: `320 / 375 / 768 / 1280px` で横overflowがないことに加え、5秒で `01` と `02` の位置と内容を指せること、縮小表示でも状態→`01`→`02`→主操作の順序が判別できることを完了条件にする。
- 強制表示: 背景クリックで一時的に閉じても合意状態は保存しない。未合意のまま再入場した場合は再表示する。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が状態接頭辞、4状態、確認する2点、各PJ内の担当内容と予定額、主操作より前に確認領域があること、参考情報の折りたたみ、修正要望の開閉を検査する。
- `320 / 375 / 768 / 1280px` で document の横overflow、状態欄の圧縮、別PJをまたいだ確認項目の並び替わりを認めない。

## /admin/finance

目的: 法人の支払義務を出金前から一元管理し、きよへのSlack通知と月次資金繰りを同じ正本から動かす。

必須機能:

- 支払義務台帳: `company_payment_obligations` を正本に、金額・期日未確認の候補も `needs_review` で保持する。
- 自動収集: 継続支払い、承認済み経費、報酬通知、finance/tax系 action item、active admin + info Gmail を日次同期する。
- 二重計上防止: 各義務に `additive` / `included_in_budget` を持たせ、追加流出だけ月次CFとcashを減らす。
- きよ通知: `members.code_name='きよ'` を動的に解決し、期限前・当日・期限超過・要確認をDMする。通知履歴の一意制約で同じ段階をexact-onceにする。
- 管理操作: `/admin/finance#payment-obligations` で作成・修正・支払済み更新ができる。継続支払い・領収イベントの既存機能は維持する。
- 月次表示: `/management-score` の月次試算表に展開可能な `支払義務` 行を置き、支払内容、期日、金額状態、予算内/追加流出を表示する。

正本仕様: [`pwa/manual/6-9-company-payment-obligations-spec.md`](../manual/6-9-company-payment-obligations-spec.md)

### /admin/finance#会計照合レール

- 週次freee会計照合: 毎週木曜10:00 JSTの`GET /api/cron/freee-accounting-weekly`が`freee_reconciliation_runs/findings/actions`を正本に、口座残高差・同期停止・未処理明細・変な仕訳・役員報酬未消込・内部振替候補を検出する。監査ソースがfreeeアプリ権限不足で読めない場合は0件扱いにせず`audit_source_unavailable` blockerを残し、他の照合を継続する。findingは週次runごとにoccurrence行としてinsertされ、過去runの証跡を保持する（`UNIQUE(run_id, finding_key)`）。
- finding解消: 今回正常に評価できたtypeで再現しなかった過去`pending`/`blocked` occurrenceは削除せず`resolved`にする。読取ソースがunavailableなtypeは未検査なので勝手に解消しない。
- freee読取フィールド: 口座一覧は`with_balance=true`で`last_balance`（同期残高）/`walletable_balance`（登録残高）を取得し、口座明細の処理状態は公式`status`（1=消込待ち、2=消込済み、3=無視、4=消込中、6=対象外）を正にする。`deal_id`/`transfer_id`の有無だけで全明細を未処理扱いしない。
- 4回review gate: `triggered_by='cron'`の完了run数+1が5以上になるまでは、判定ロジックが動いてもexecutorは呼ばれない（完全review-only）。
- 5回目以降allowlist: `eligibleForAutoApply=true`になりうるのは、役員報酬の完全一致消込（`officer_compensation_unreconciled`、期待額は`company_finance_recurring_items`のitem_kind='salary'/category='executive'明示分類が正本、氏名一致だけの紐付けは対象外）と、双方口座特定済み・同額・許容日差以内の内部振替（`internal_transfer_candidate`）のうち完全一致のものだけ。曖昧・同額複数候補・残高差の直接補正・勘定科目の推測は必ずレビュー対象にする。
- freee書込み安全境界: `isActionTypeSafelyExecutable`は2026-07時点で両action typeともfalse固定。内部振替はfreee公式APIに安全な書込み手段が無く、役員報酬もwallet_txnのaccount_item_id更新が「消込」を正しく表現することを公式に検証できていないため、**freeeへの実書込みは現時点で一切発生しない**（常にblocked、`freee_reconciliation_actions`に理由を記録）。
- 正本境界: AMD OSの財務テーブルがSOT、freeeは実行先兼一次証跡。きよの収支スプシは`AMD_FINANCE_REFERENCE_SHEET_ID`設定時のみread-only参照し（セル値は保存せずrowCount/非空セル数だけをsanitize）、正本を上書きしない。
- 管理操作: `/admin/finance`会計照合レールでpending/blockedのfindingを承認/却下、dry-runプレビュー実行ができる。承認/却下はOS内の監査記録の保存のみで、freeeへの書込みは伴わない。

正本仕様: [`pwa/manual/6-10-freee-accounting-reconciliation-spec.md`](../manual/6-10-freee-accounting-reconciliation-spec.md)

## /admin/payouts

目的: 支払月単位で、対象cycleの報酬確認、PJ別収支確認、支払データ同期状態、支払通知書発行、入金確認nudgeを一画面で運用する。

必須機能:

- 支払月選択: `ym=YYYYMM` でメンバー支払月を選び、稼働月 `billing_cycles.ym` を `/admin/projects` の `projects.payment_due_rule` / `payment_due_day` で集約する。`billing_cycles.invoice_ym` はクライアント請求月なので、メンバー支払月判定には使わない。
- 高速初期表示: 通常GETは `billing_cycles.reward_summary_json` の報酬キャッシュを読むだけにする。毎回 `syncRewardSummariesForBillingCycles()` を再計算しない。先12か月の capped 投影 (`forecastCapped`) も `forecastCycles.reward_summary_json` から集計し、画面を開いただけで全PJの `computeForwardCappedMemberCosts()` を走らせない。
- SSR初回データ: `/admin/payouts` page は `loadTargetData(currentYm, { includeAgreementGate: false })` をサーバー側で実行し、`AdminPayoutsClient initialData` へ渡す。初回 client GET を省き、月変更・手動再計算・write後の再取得だけ `/api/admin/payouts` を使う。
- 月初合意gateの後追い表示: 初期表示GETは支払データ本体を先に返し、月初合意gateは `gateOnly=1` の別GETで後追い取得する。保存・発行・送付などのwrite actionではサーバー側gateを必ず実行する。
- 報酬キャッシュ再計算: 明示的な「報酬キャッシュ再計算」操作または保存系処理だけが `refreshRewards=1` / `refreshRewards: true` で再計算する。
- 0円キャッシュ: 報酬対象メンバーがいない月も `reward_summary_json.members=[]` の0円キャッシュとして保存し、`forecastCapped` では key 有りの0円として扱う。`null` の未計算扱いにして budget fallback へ落とさない。
- 報酬キャッシュ日次更新: `payout-reward-cache-refresh` cron が毎日03:05 JSTに、前月 + 当月から先12か月のメンバー支払月、および同じ窓内の稼働月について `billing_cycles.reward_summary_json` を再生成する。支払月対象は `projects.payment_due_rule` / `payment_due_day` で判定し、`billing_cycles.invoice_ym` は使わない。手動で `ym=YYYYMM&lookahead=11` を付けると、指定月から先12か月のキャッシュを作れる。
- 予定担当比率のみ: 報酬計算は MS の期間按分で当月消化ptを出し、`milestone_responsibility.share` で分配する。活動ログ由来の実績配分や通常月の手入力報酬 override は使わない。
- 旧制度の事前合意額: `source_ym <= 202606` に限り、`legacy_reward_payout_amount_override_events` の最新append-only eventを支払snapshot/PDFへだけ適用する。MS・pt・share・`billing_cycles.reward_summary_json`・stock/carry・将来差額は変更しない。画面は `事前合意額：通常 X円 → 固定 Y円` を表示し、0円は監査snapshotを残すが通知書を発行しない。
- 支払額の同期: 画面の支払額・PDF生成の元データは、最新の `billing_cycles.reward_summary_json` から計算した値を正にする。`monthly_reward_payout.total_pay` と `payout_notices.total_yen` は保存済み額で表示値を固定するためではなく、夜間の先回り生成または正式PDF発行時に同期される税抜スナップショットとして扱う。`/admin/payouts` を開いただけでは保存しない。送付操作は同期・PDF生成を行わず、保存済み正式PDFが最新DBと一致するかだけを照合する。
- 月初合意支払gate: `member × 稼働月 × PJ` で未合意 / 条件更新あり / 修正要望中を server-side に止める。2026年6月以前の稼働月 (`source_ym <= 202606`) は導入前/移行月として gate 上 `合意済` 扱いにし、2026年7月以降から通常判定にする。6月は契約改定前かつシステム未完成期間だったため、合意条件として支払いを止めない。移行月だけで blocker が無い場合、admin UI は個別メンバー一覧ではなく「対象支払行 / 移行月スキップ / blocker 0」の summary を表示する。
- 縦型PJ収支表: 「全体収支」列とPJ列を並べ、クライアント支払、バッファ、本契約cap、本契約支払、別財布支払、役員分、役員相殺、本契約残り、メンバー別支払を確認できる。
- 先12か月 目的別4表 + メンバー別支払予定: `/admin/payouts` と `/management-score` 下部表の両方で、先12か月を `先12か月 キャッシュ支払` / `先12か月 会社留保` / `先12か月 報酬債務` / `先12か月 cap超過チェック` の4表に分ける。会社留保を支出扱いしない。会社留保は `cap/売上枠 - 外部支払` として読む。報酬債務は外部メンバーへの月末残高なので12か月分を足さず、各月残・ピーク・最終月残で見る。役員の未充当繰越は会社留保側の内部検算に寄せ、報酬債務には混ぜない。cap超過チェックだけが `報酬需要 - cap/売上枠` を見る。`/admin/payouts` には、行=非役員・支払対象メンバー、列=稼働月の `先12か月 メンバー別支払予定` 表も置き、セルクリックで PJ 別 `totalPay` / 本契約 / 別財布 / 発生額 / pt / 未払い残を確認できる。
- 手入力予算確定なし: `/admin/payouts` では請求額や追加支払枠を手入力して `billing_cycles.budget_yen` を書き換えない。契約から解ける通常capと、MSタグから解ける別財布発生だけを表示する。
- 別財布支払: ZMP のような月額固定PJは通常枠を `fee_amount × 65%` に保ち、OkuDoor追加開発など追加受託分は `tag='cap_extra'` のMSとして `extraBasePay` / `extraPaidYen` / `extraCompanyReserveYen` に分ける。画面上で `本契約発生` / `別財布発生` / `別財布使用` を別表示する。
- 支払データ同期: `payout-notice-prebuild` cron と正式な発行 action が、`monthly_reward_payout` に明細、`payout_notices.total_yen` にメンバー別通知額を税抜で保存する。cron は同期後に正式PDFまで先回り生成し、画面表示中の自動POSTは行わない。開きっぱなしの画面は 60 秒ごとに read-only 再取得し、同期状態だけ追随する。役員または `exclude_from_payout_notice` のメンバーは通知対象外にする。保存時点ではメール送信せず、金額が変わった未送付PDF、現行テンプレート更新時刻より古い未送付PDF、または `members.updated_at` より古い未送付PDFを再生成対象へ戻す。最新支払計算に対応する明細が無い未送付 `payout_notices` は孤立レコードとして削除する。送付済み通知書は `force=true` でも上書きしない。通常の差分は発行時に自動同期するため状態バッジを出さず、月初合意gate・本契約cap blocker がある場合だけ `同期できない` と表示し、admin override または blocker 解消を待つ。
- メンバー別支払の税区分: `メンバー別支払` / 月初合意 gate / 先12か月メンバー別支払予定の詳細で、`支払額` は税抜と税込を併記する。DB (`monthly_reward_payout.total_pay` / `payout_notices.total_yen`) は税抜を保存し、GAS PDF生成時に消費税10%を上乗せする。
- 支払通知書発行: `メンバー別支払` は admin/payouts の主作業表なので、サマリ直下・報酬債務台帳より上に置く。「メンバー別支払」行に `支払通知書発行` / `PDF確認` / `送付` の3操作を置き、見出しにも `全員分PDF一括発行` / `確認用PDF生成` / `強制再発行` を置く。行の `PDF確認` は保存済み正式PDFを開くだけで、PDF生成は行わない。PDF未生成またはメンバー台帳更新後で古い場合は先に `支払通知書発行` / `強制再発行` で正式PDFを作り直す。`確認用PDF生成` は `PREVIEW-...` の確認用PDFを作るが、`payout_notices` には保存しない。`payout-notice-prebuild` は支払データ同期後に正式PDFを事前生成する。正式な個別 `支払通知書発行` と全員分PDF一括発行は、サーバー側で最新計算額を同期し、その後 DB を再読込して最新の `members.contractor_name` / `member_address` / `invoice_registration_number` を使ってから `payout_notices.notice_no` / `pdf_url` を保存する。`強制再発行` は金額差分が無くても最新メンバー台帳と現行テンプレートで作り直す。一括PDF生成で失敗または強制再発行なのに未送付PDFが未再生成になった場合は `ok: false` として画面に失敗を出す。`送付` はPDF生成・再生成を行わず、保存済み正式PDFが最新DBと一致し、確認用PDFではなく、未送付である場合だけ確認モーダルを開く。確認モーダルの `はい・送信` は保存済み正式PDFを添付して `keiri@team-armada.jp` から実メール送信し、成功後に `sent_at` を保存する。PDF URLの手入力欄は置かない。
- 支払通知書PDFフォーマット: 正本は `gas/064_PayoutFreeeNotice.js` の `payoutBuildNoticePdfBlob_`。2026-04改善版の白地・青アクセント・正本ロゴ画像フォーマットを維持する。admin/payouts の支払額 (`monthly_reward_payout.total_pay` / `payout_notices.total_yen`) は税抜として扱い、PDF上で消費税10%を上乗せして「お支払金額」「合計（税込）」に表示する。宛先は `members.contractor_name` (= 未設定時は `member_name` / `code_name`) と `members.member_address`、メンバー側インボイス登録番号は `members.invoice_registration_number` を使い、PDF上のラベルは宛先側・発行者側とも `登録番号` とする。発行者側の値には AMD のインボイス登録番号 (`T7021001064067`) を表示する。必須要素は、中央青見出し `支払通知書`、青ライン、右上右寄せの `作成日` / `通知書番号`、右端に揃えた公式ロゴ画像 (`PAYOUT_LOGO_FILE_ID` / `PAYOUT_LOGOTYPE_FILE_ID`) と会社名・住所・`登録番号`、「お支払金額」サマリbox、青ヘッダ明細表 (`摘要` / `数量` / `単価` / `金額`)、右寄せ合計 (`小計（税抜）` / `消費税（10%）` / `合計（税込）`)、`支払予定日` / `支払方法` / `備考`。PDFには振込先欄を出さない。旧GASの黒罫線フォーマット、テキストで作った `team ARMADA` ロゴ、PDF URL手入力欄へ戻さない。
- 入金確認nudge: `payment-confirm-nudges` を手動実行でき、Slack DMの `/payment-confirm` とつながる。
- 月次モーダル導線: cycle明細やPJ収支表の稼働月から `CockpitMonthlyModal` を開き、報酬根拠に戻れる。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `/admin/payouts` の支払通知書PDF確認、報酬キャッシュ、報酬キャッシュ日次cron、縦型PJ収支表、GAS側の改善版支払通知書PDFフォーマット anchor を検査する。
- `pwa/scripts/check_pwa_critical_ui.cjs` が `本契約発生` / `別財布発生` / `regularBasePay` / `extraBasePay` anchor も検査する。
- `npm run test:payout-amount-overrides` が、旧制度月上限・最新event優先・clear・0円・元reward cache非破壊を検査する。
- 支払通知書PDFの golden PNG は `pwa/scripts/__fixtures__/payout_notice_golden.png` (改善版フォーマットの 1 ページ目を PNG 化したもの) を正本とし、`pwa/scripts/__fixtures__/payout_notice_golden.png.sha256` に SHA256 を固定する。`npm run test:critical-ui` が golden の存在と SHA256 一致を検査し、 fixture が壊れていれば落ちる。
- 改善版PDFを意図的に更新したら、まさが新PNGを目視確認したうえで `payout_notice_golden.png` と `payout_notice_golden.png.sha256` を再生成して commit する。新規 PDF を PNG 化したファイルとの突合は `npm run test:payout-notice-pdf -- --diff <input.png>` で同じスクリプトを再利用する。
- この画面で UI を削る変更は、`FEATURE_REGISTRY.md` と `SPEC_pwa.md` を同時に更新する。

## /admin/kiyo

目的: きよが月次経理の立替精算、請求書、メンバー支払を `/admin/kiyo` から離れず完了する作業台。

必須機能:

- 名前: adminメニュー、ページtitle、見出しは `きよ`。用途説明を名称へ足して `きよ月次経理` に変えない。
- 作業順: `01 立替精算`、`02 請求書`、`03 メンバー支払` の3taskを排他的に切り替える。切替後もURLは `/admin/kiyo?task=...` のままにし、別routeへ移動させない。
- 立替精算: `/reimburse` と同じ実装を埋め込み、申請・編集・削除、PM承認・差戻し、admin承認・却下を実行できる。権限とstatus遷移は既存API/RLSを変えない。adminだけに、admin承認済み（`status=approved`、互換で`paid`も含む）の全メンバー分を並べる常設台帳を持つ。件数・合計金額の見出し、発生日・PJ・摘要・申請者・費目・金額・PM承認・admin承認・領収書の9列、admin承認日降順（欠測時は発生日で安定sort）、丸角・影のないborder-onlyの高密度tableで、既存の申請・承認write workflowとは独立した表示（書込み経路を増やさない台帳）にする。
- 請求書: `/admin/invoices` と同じ `AdminInvoiceIssueQueue` / `AdminInvoiceIssueDialog` を埋め込み、発行前確認、freee取引先保存、請求書発行、発行取消まで実行できる。
- メンバー支払: `/admin/payouts` と同じ `AdminPayoutsClient` を埋め込み、報酬キャッシュ再計算、支払通知書発行・PDF確認・送付、入金確認nudgeまで実行できる。
- 正本境界: きよ用に別のwrite endpoint、支払計算、請求判定、承認遷移を作らない。各専用画面の現行componentとserver-side guardをそのまま共有する。

回帰防止:

- `test:admin-kiyo` が3task、3つの既存作業UI、`きよ` の名称、`読み取り専用` の不在、tablist/tab/aria-selected、丸角カードgridへの回帰禁止、4/8/12px系の余白に加えて、admin承認済み台帳の見出し・admin限定表示・対象status（`approved`／互換`paid`）・合計金額・丸角なしborder-onlyを検査する。
- `test:surface-catalog-contract` がAdminSidebarの `きよ` と `/admin/kiyo` を検査する。
- 3つのtaskを状態一覧やリンクだけへ縮退させない。専用画面にある主操作を `/admin/kiyo` でも同じcomponentで使えることを必須とする。
- 3taskの切替UIは横並びタブ（`role="tablist"`/`role="tab"`/`aria-selected`、角丸ゼロ・影ゼロ・border-only）。PCは同じ幅の3区画が隣接し、縦の境界線（`border`の1px重ね）で区切る。選択中の区画は上辺・左右辺を持ち、下辺は作業面の背景色で消して下の作業面と前面でつながる（`-mb-px` + `border-b-background`）。非選択の区画は薄い面色（`bg-muted/30`）と下辺を持つ。各区画は1段目に番号+名称、2段目に工程説明を置き、説明の長さで区画幅を伸ばさない（幅は3等分固定、説明は`truncate`）。丸角個別カード（`rounded-lg` な3カードgrid）へ戻さない。mobileは各tabに安定した`min-width`（168px）を持たせ、タップ領域44px以上を保った横スクロール1行タブにする。`AdminInvoicesPage` / `AdminPayoutsPage` は `embedded` propで自身の重複見出しを省き、きよ内では密な余白（4/8/12px系）で表示する。専用route側の表示・write workflow・権限・URLは変えない。

## /admin/season-pl

目的: 1 PJ × 1 シーズン (plan cycle) ごとに「請求額がいくらで、内訳がどうで、差し引きどうなるか」を全 PJ で常時確認できる予実表。シーズン頭に「予算」を確定し、毎月の入金・支払で「実績」が埋まる。pt単価過大・未割当pt・cap/原資不整合 (ZMP)・役員 stock 非収束といった歪みを、まさが不安なく一目で検知できる安全網にする。設計正本は `pwa/design/season_budget_actual.md`。

必須機能:

- 一覧 + 詳細の 2 段: 一覧トップは全 active plan cycle を 1 行ずつ (PJ / 請求額 / バッファ / 原資 / pt単価 / 閉じ✓✗ / 未割当pt / 原資=Σcap / 役員収束) で出し、警告ありの行を上に持ち上げる。行クリックで①収入②配分③メンバー別のフル予実表を出す。
- 集計の正本ロジック: `src/lib/season-pl.ts` の `computeSeasonPl` 純関数。`reward-summary.ts` の月次集計 (`buildRewardSummary`) を plan cycle 全期間に集約し、cap + stock 繰越連鎖を内部で効かせる。API `GET /api/admin/season-pl` は一覧 (`mode=list`)、`?planCycleId=` で詳細 (`mode=detail`) を返す。手入力で `budget_yen` を書き換える導線は持たない (= 表示・検算専用)。
- ① 収入: 契約期間内の billing 月を `contractBackedClientAmount` で集約した請求額 (税抜・シーズン合計) を予算、`payment_confirmed_at` 済みの合計を実績入金として出す。契約開始前の事前稼働月は請求額に乗らない。
- ② 配分: バッファ内訳 (`value_plan_cycles.buffer_breakdown_json` の `{items:[{label,amount}],total}`、未設定時は原資から逆算)、メンバー原資 (= `value_plan_cycles.budget_yen` = (請求額−バッファ)×65%)、AMD マージン (35%) を並べ、`バッファ + 原資 + マージン == 請求額` の閉じ検算を必ず出す。
- ③ メンバー別 pt 比予実: 獲得pt (シーズン累計) × pt単価 = 予算取り分、実支払累計 (非役員=現金 / 役員=会社留保)、最終月末の未払い残 stock、差 (実支払+stock − 予算取り分, 最終的に 0 が正) を member 行で出す。役員 (`is_officer`) は「会社留保」、非役員は「現金支払」で分ける。member 名は `/mypage?memberId=` リンク。
- 別財布 (cap_extra) プールの pt単価分離 (2026-07-09 pt正本更新): plan cycle に `tag='cap_extra'` の MS があるとき、pt単価を `regularPtUnitYen` (= 本契約原資 ÷ シーズン期間月数×10pt) と `extraPtUnitYen` (= Σ `billing_cycles.extra_budget_yen` ÷ Σcap_extra effective pt) に分離する。cap_extra effective pt は、明示 `points > 0` があれば明示pt、未設定/0なら MS期間月数×10pt。member の予算取り分は `regularEarnedPt×regular単価 + extraEarnedPt×extra単価` で計算 (`budgetShareYen` / うち `extraBudgetShareYen`)。これをしないと別財布 pt も `原資÷total_points` で薄める旧汚染と同型になり member 収束Δが全員ズレる。`computeSeasonPl` は `regularPtUnitYen` / `extraPtUnitYen` / `extraPoolBudgetYen` / `extraPointsSum` を返す。
- 検算フラグ (この画面の目的): `閉じ検算` (バッファ+原資+マージン=請求額)、`未割当pt` (total_points − Σ MS points、宙吊り pt / 担当無し MS)、`原資=Σ月cap` (`budget_yen` = Σ `billing_cycles.budget_yen` = 本契約のみ、別財布 `extra_budget_yen` は別列で混ざらない)、`pt単価` (regular pt単価 = (請求額−バッファ)×65%÷regular pt と一致)、`役員stock収束` (最終月で役員 stock が 0)。別財布が完済すれば最終月の対象 member の extraStock=0、残 stock は本契約 regularStock のみになる。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `/admin/season-pl` の一覧・詳細・検算 anchor (`シーズン予実表` / `closes` / `unassignedPt` / `budgetMatchesMonthlyCaps` / `officerStockConverges`) と、`computeSeasonPl` / `buffer_breakdown_json` の集計 anchor を検査する。
- AdminSidebar の `シーズン予実` 導線、`/admin/season-pl` route、`/api/admin/season-pl` を消す変更は、`FEATURE_REGISTRY.md` と `pwa/design/season_budget_actual.md` を同時に更新する。
- 「未割当pt」は `Σ(earnedPt) < total_points` ではなく `total_points − Σ(MS points)` で見る (= 期中は消化が total 未満で正常。MS で裏打ちされない pt 単価分母の穴だけを検出する)。この判定を earnedPt 比較へ戻さない。

## /admin/ms-overview

目的: 全 active plan cycle の MS 設計を 1 画面で一望し、MS 本体・期間・pt・tag・担当 share と設計額を編集・確認する設計画面。「pt 配分が他 MS と比べて妥当か」「メンバー間の担当量と設計額がおかしくないか」をシーズン予実表 (= 実消化) と切り離して、`plannedShare` ベースの pt 配分で判断する。`/admin/season-pl` が「閉じてるか」を見る安全網なのに対し、こちらは「設計の歪み」を見る設計画面。仕様正本は `pwa/manual/6-8-admin-ms-overview-spec.md`。

必須機能:

- 表示単位: 全 active plan cycle (`status in (active, confirmed, fixed, draft)`) を `budget_yen` 大きい順に並べたアコーディオン。各 PJ ブロックは初期折りたたみ、先頭 PJ のみ開いた状態で出す。
- 集計の正本ロジック: `/api/admin/ms-overview` (`src/app/api/admin/ms-overview/route.ts`) と `src/lib/admin/ms-overview-calc.ts` は、MS pt と `plannedShare` から **pt配分と設計額** を組み立てる。支払額に見える円換算 (`points × pt単価` を支払確定額として見せる、メンバー年額など) は作らない。実際の支払額は `reward-summary.ts` / `/admin/season-pl` / `/admin/payouts` 側だけが正本。設計額は `effectivePoints × plannedShare × designUnitYen` の目安表示。
- PJ ブロック構造: ① メトリクスカード 4 枚 (合計pt / 本契約pt / 別財布pt / PJ予算残または不足額。設計額・設計単価・保存前支払検算を含む。個人名カードは出さない)、② 全MS (pt順) (MS 名 + 期間 + tag、pt、設計額、pt比、担当 share)、③ メンバー別 pt配分 / 設計額 (`codeName` + 本契約濃 + 別財布淡 の積み上げ横バー + 合計pt + 設計額)、④ tag 凡例 (normal #1D9E75 / routine #888780 / cap_extra #7F77DD)。
- 別財布 (cap_extra) プールは pt 配分として分離。`tag` が cap_extra 系 (`cap_extra` / `extra_contract` / `contract_extra` / `cap_outside` / `uncapped`) の MS は extraPt に積み、通常 MS は regularPt に積む。
- 編集モード (2026-07-09 更新): PJ ブロックごとに「編集モード」トグル。MS 名 / pt / tag / 期間 (`period_start_ym` / `target_ym`) / 完了条件 / 担当 share・役割・担当タスク / MS追加・無効化を編集できる。編集カードは左に MS 基本情報、右にメンバー share 表を置く 2 ペイン構成。pt は編集カード内の数値入力 + pt配分スライダーに加え、MS 一覧先頭の **全MS 編集テーブル** でも調整できる。全MS 編集テーブルは MS名 / tag / 期間 / pt / pt配分スライダー / 設計額 / メンバーのエフォート / 無効化を同じ行に出し、上段だけで MS名・期間・pt と金額感を確認できる。通常 MS のスライダー範囲は編集開始時点の最大 pt × 1.5 を右端に固定し、ドラッグ中に max を変えない。通常 MS の pt を動かすと `regularPointBasis - Σ(non-cap_extra MS effectivePoints)` の **残り割り振り可能pt** をリアルタイム表示する。担当 share 表は 2 カラムにせず、メンバー1人=1行で `メンバー / share / 役割 / 担当pt / 担当設計額 / 担当タスク` を並べる。`cap_extra` の pt は「別財布pt」として編集でき、明示 `points > 0` なら明示pt、未設定/0なら MS 期間月数×10ptで自動算出する。変更のたびに `src/lib/admin/ms-overview-calc.ts` の `recomputeMsOverview` で メトリクス / MS pt比 / MS設計額 / 担当 share 行の担当pt・担当設計額 / メンバー別 pt配分・設計額 を JS 側でリアルタイム再計算する (= API 不要)。算定式は `memberPt=ΣeffectivePoints×share`, `memberDesignYen=ΣeffectivePoints×share×designUnitYen`。`milestone_monthly_contribution_allocations.actual_share` は読まない (plannedShare のみ)。編集モード ON 直後の上部保存バーとフッターの両方に「↻ DB値に戻す」「保存して DB へ反映」を持ち、保存先 DB / reward 再計算 / 保存前支払検算まで表示する。cockpit 側には MS 設計の保存口を置かない。
- 保存前支払検算: `POST /api/admin/ms-overview/{planCycleId}` (body `{ milestones: [...], deletedMilestoneIds: [...] }`) は DB を書き換えず、編集後MS案で protected 月の旧 reward cache と新計算値を比較し、`rewardPreview.status` (`safe` / `warning` / `blocked`) を返す。画面は `blocked` の間「保存不可」として保存ボタンを無効化し、`memberImpacts` でメンバー別の追加支払 / 差額控除 / 本契約・別財布内訳を表示する。`budgetImpact` では支払済み固定 / 実績未照合 / これから支払予定 / 外部メンバーの期末未払い残 / 原資上限 / PJ予算残を表示し、クライアント支払は本契約 + 別財布売上、schedule_based 契約では `contract_terms_json.monthlySchedule.amountTaxExcl`、バッファは `value_plan_cycles.buffer_breakdown_json` 優先で読む。期末未払い残は支払通知対象の外部メンバー分だけを blocker 判定し、役員繰越は会社留保側の内部検算へ寄せる。メンバー支払義務のPJ予算超過、または `PJ予算 > (クライアント支払 - バッファ) × 65%` の原資超過も blocker とする。freee銀行出金と `monthly_reward_payout` 明細が一致した支払済み実績を固定した結果の赤字見込みを warning にする。旧 reward cache が無い、次回精算先の未保護月が無い、差額控除がシーズン内で吸収できない可能性がある、または支払済み印はあるが実支払証跡と明細額が未照合の月がある場合は blocker とする。別財布は protected 月に `extraPaidYen` がある場合だけ本人別差額を作り、未払い在庫だけの端数差では差額台帳を作らない。閲覧モードで cycle を開いた時点でも同じ POST 検算を走らせ、既存状態が blocked なら MS 一覧の上に `MS編集停止中` を出す。`source_ym < 202607` の旧制度合意済み月は新ポイント制の差額控除へ使わない。
- 編集モード保存: `PUT /api/admin/ms-overview/{planCycleId}` (body `{ milestones: [...], deletedMilestoneIds: [...] }`) は保存前支払検算をサーバー側でも再実行し、`blocked` なら 409 で保存前に止める。通過したら (a) `value_milestones` を upsert / 無効化 (`cap_extra` points は明示ptがあれば明示pt、未設定/0なら MS 期間×10ptへ正規化)、(b) `milestone_responsibility` を保存値で置換、(c) `value_plan_cycles.total_points = シーズン期間月数×10 + Σcap_extra effective pt` 再計算、(d) protected 月の差額を `reward_member_liability_offsets` に本人別で記録、(e) 変更前後の MS / 担当 share 差分と保存前支払検算サマリを `milestone_change_events` に記録、(f) `syncRewardSummariesForProject` で未保護月だけ reward 再計算する。protected 月差額が出る場合は `milestone_change_events.revision_id` と `reward_member_liability_offsets.revision_id` を一致させる。payload の MS が同じ plan_cycle に属するかを `plan_cycle_id` 突合で検査し、他 PJ への巻き込み更新を防ぐ。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `/admin/ms-overview` の route / page / client / sidebar anchor (`MS一覧` / `全MS` / `メンバー別 pt配分` / `設計額` / `担当設計額` / `cap_extra` / `編集モード` / `MS追加` / `担当share` / `担当pt` / `全MS pt配分スライダー` / `pt配分スライダー` / `残り割り振り可能pt` / `保存先 DB` / `保存前支払検算` / `保存不可` / `MS編集停止中` / `memberImpacts` / `budgetImpact` / `PJ予算残` / `支払済み固定` / `実績未照合` / `これから支払予定` / `期末未払い残` / `milestone_change_events` / `DB値に戻す` / `保存して DB へ反映` / `recomputeMsOverview` / `memberPointTotals`) を検査する。
- 上段メトリクスは 4 枚固定。3枚化、または 4 枚目を個人名カードへ戻す変更は禁止。4 枚目は `budgetImpact` 由来の `PJ予算残` / `不足額` / `予算不足` / `原資超過` を表示する。
- AdminSidebar の `MS一覧` 導線、`/admin/ms-overview` route、`/api/admin/ms-overview` (GET / PUT)、`src/lib/admin/ms-overview-calc.ts` を消す変更は、`FEATURE_REGISTRY.md` と `pwa/manual/6-8-admin-ms-overview-spec.md` を同時に更新する。
- 支払額に見える円換算を `/admin/ms-overview` に戻さないこと。`/admin/ms-overview` の円表示は設計額の目安に限り、実支払額は reward cache / season-pl / payouts 側だけで見る。
- 別財布判定 (cap_extra 系 tag) は `src/lib/admin/ms-overview-calc.ts` の `isCapExtraTag` を共有し、`season-pl.ts` の `CAP_EXTRA_MILESTONE_TAGS` と一字一句揃える (= 増やすときは両方更新)。

## /admin/private-wiki

目的: admin だけが、AMDメンバー・取引先・クライアント・研究者・外部協力者などの人物単位の趣味・プライベート・関係性メモを PJ ごとに保存・検索・更新できる。

必須機能:

- admin-only route: `/admin` layout の admin gate 内に置き、通常 PJ cockpit、公開ページ、研究機関外部 workspace から参照しない。
- PJ別グルーピング: `private_wiki_entries.project_id` で PJ 別に grouping し、null は AMD 全体 / 未紐付けとして扱う。
- 手作業編集: 追加 / 編集 / archive が UI からできる。直接削除を主導線にしない。
- フィルタ: 検索、PJ、人物種別、status で絞り込める。検索対象には人物名、所属、関係性、誕生日、出身地、居住地、接点、家族、タブー、本文メモ、source を含める。
- 人物文脈: 誕生日 (`birthday_label`)、出身地 (`origin_label`)、居住地 (`residence_label`)、接点 (`contact_context`)、家族 (`family_note`)、タブー (`taboo_note`) を本文メモとは別に表示・編集できる。
- evidence 表示: `source_kind` / `source_ref` / `source_excerpt` / `confidence` / `updated_by` を一覧上で確認できる。
- source hygiene: `source_excerpt` は短い抜粋だけ。メール全文・議事録全文・資料全文を保存する場所にしない。
- API 境界: browser 直接DB writeではなく `/api/admin/private-wiki` の `requireAdmin()` + service_role 経由で list/create/update/archive する。

回帰防止:

- `private_wiki_entries.visibility` は `admin_private` 固定。別画面へ再利用するときは `FEATURE_REGISTRY.md` と `/spec/2-1` を先に更新し、admin-only を崩さない。
- 旧 `tags` は既存互換のため DB に残すだけ。UI/API の入力欄・フィルタ・必須アンカーとして戻さない。
- seed は入れない。必要なテストデータはダミーだけにする。

## /admin/management-knowledge

目的: admin だけが、PJ横断で再利用できる経営ノウハウを判断カードとして保存・検索・更新できる。

必須機能:

- admin-only route: `/admin` layout の admin gate 内に置き、通常 PJ cockpit、公開ページ、研究機関外部 workspace から参照しない。
- PJ任意紐付け: `management_knowledge_entries.project_id` は nullable。null は AMD 全体で再利用する知見として扱う。
- 手作業編集: 追加 / 編集 / archive が UI からできる。直接削除を主導線にしない。
- フィルタ: 検索、PJ、category、maturity、tag、status で絞り込める。
- 知見の骨格: `title` / `category` / `route_type` / `maturity` / `summary` / `body_md` / `reusable_when` / `next_check` / `tags` を持つ。
- evidence 表示: `source_kind` / `source_ref` / `source_excerpt` / `confidence` / `updated_by` を一覧上で確認できる。
- source hygiene: `source_excerpt` は短い抜粋だけ。メール全文・議事録全文・資料全文を保存する場所にしない。
- API 境界: browser 直接DB writeではなく `/api/admin/management-knowledge` の `requireAdmin()` + service_role 経由で list/create/update/archive する。
- 初期カード: `/Users/masa/projects/AMD/kagawa/2026-07-03_roundtable_commercialization_type_memo.md` から Proto-RT / Roundtable 型の知見を seed する。

回帰防止:

- AdminSidebar の `経営ノウハウ` 導線、`/admin/management-knowledge` route、`/api/admin/management-knowledge`、`management_knowledge_entries` を消す変更は、`FEATURE_REGISTRY.md` と `/spec/2-1` を同時に更新する。
- `maturity` は `raw_note` / `hypothesis` / `field_tested` / `playbook` に分け、思いつきと再利用可能な型を同じ状態として扱わない。
- 人物の趣味・関係性メモは `/admin/private-wiki` に置き、この台帳へ混ぜない。

## /dashboard

目的: 研究機関・シーズという母集団を横断し、まさと司令塔が今日判断・着手できる対象を最初に見る入口。PJは両者から契約成立後に生まれる運用レイヤーとして下段に置く (2026-08-02 研究ポートフォリオ中心IAをホームへ正式採用、旧 `/portfolio-preview` を統合)。

必須機能:

- 先手 TODO バッジ: dashboard 上段に `ProactiveTodoBadge` を出し、`proactive_todos.status='open' AND attention_state='approved' AND attention_type IN ('decision','masa_action')` の件数だけを `/proactive` へ送る。期限超過 / red件数は `due_basis='explicit'` に限定し、collectorの仮期限を緊急扱いしない。バッジは admin (= `members.is_admin=true`) のみ表示で、非 admin は枠ごと消す。詳細仕様: [`pwa/spec/2-4-proactive-todo-current-spec.md`](../spec/2-4-proactive-todo-current-spec.md)。**旧 `ProactiveQueuePanel` 経由の `proactive_outbox` 表示 (= 5段ループ盤面の実行段) は 2026-06-27 廃止**。司令塔セッション運用の消滅と完了UI不在が理由 (詳細: spec 2-4 末尾)。dashboard 上段 (= 旧 `LoopKernelBoard` / 旧 `ProactiveQueuePanel`) からは両方とも除去済み。
- 研究ポートフォリオ優先キュー (`PortfolioPulse`、`ProactiveTodoBadge` 直下): 研究機関・シーズ・PJ運用の統計strip + 3パネルキューを表示する。パネル順は研究機関 (PJ化検討中) → シーズ (検討中・SPS評価済み) → PJ運用 (稼働中) の順で、PJを初期画面の主役に戻さない。研究機関・シーズの全件は出さず、上位候補だけを表示する (全件正本は `/institutions` `/seeds`)。データは client component から [`/api/dashboard/portfolio-pulse`](../src/app/api/dashboard/portfolio-pulse/route.ts) を叩いて取得し、`fetchErsBundle` / `fetchAllResearchInstitutionSeeds` の default browser client 直叩きは禁止 (**migration 213 の RLS closure で anon/default client がシーズ0件を返す既知障害があるため**、server-side の `createAdminClient()` 経由に一本化)。API route は `requireMember()` の認証に加えて `getCurrentMemberAccess()` が `portfolio` scope の場合だけ通し、project scope の外部PJメンバーへ横断母集団を返さない。service client はこのscope gateの後にだけ生成する。ECR系 (`fetchErsBundle`) とシーズ系 (`fetchAllResearchInstitutionSeeds`) は `Promise.allSettled` で障害分離する — 片方が失敗してももう片方は返し、失敗した側だけ `PortfolioPulse` 側のパネルに「読み込めなかった」inline通知を出す (0件と偽らない)。ECRとSPSは合算しない。並び順ロジックは [`src/lib/portfolio-pulse.ts`](../src/lib/portfolio-pulse.ts) の `buildPortfolioPulseModel` (旧 `/portfolio-preview` の `buildPreviewModel` を移設)。
- PJ運用アンカー (`#pj-operations`): `PortfolioPulse` の「PJ運用を開く」、GlobalNav の「PJ運用」navはどちらも `/dashboard#pj-operations` へ実接続する (表示だけの偽ボタン禁止)。アンカー先は既存のPJ一覧 (下記) そのもの。
- PJ一覧: `#pj-operations` アンカー配下に Active / Sales-Draft / Ended-Frozen の横長 stripe 一覧を維持する。AMD全体 (`p00`) だけを除き、`institution_projects` に入る研究機関型PJも含むPJ台帳の全行を表示する。すべての行は内部用 `/project/[projectId]/cockpit` を開き、共有workspaceへ直接送らない。
- 経営指標・接続状況 (折り畳み `<details open>`): `DashboardScoreOverview` / `FundingStatsCard` / `ExtractionStatusCard` (admin限定) / `FreeeConnectionStatusCard` (admin限定) をまとめる。既定で開いた状態のため情報は失われないが、優先キュー・PJ一覧より視覚的に軽い扱いにする。`ExtractionStatusCard` は Gmail / Drive / Calendar / Slack / Notion ごとの「OSへ最後に保存された証跡」と「MTG抽出で実際に使えた時刻」を分け、月次対象PJのメール・Slack・Drive設定不足は「設定が必要」に集約して `/admin/projects` へ送る。保存証跡の古さを接続異常と扱わず、未読の再認証通知・PJ設定不足・Calendar接続エラーだけを対応事項として出す。`project_config_gap` は採否通知に出さない。`FreeeConnectionStatusCard` は接続記録と `company_actual_monthly` の freee P/L・口座残高実績を別々に確認し、両方が24時間以内なら `正常`、接続記録なしは `未連携`、それ以外は `要確認` とする。認可情報や秘密値をブラウザへ返さず、画面から同期を実行しない。`FundingStatsCard` は資金調達ラウンドと助成金・補助金を会社別/行別に表示し、累計値は `amd_contribution_status in ('full','partial')` の AMD貢献額だけで計算する (`none` / `unreviewed` はリストには残すが累計に入れない)。投資家別内訳・持株比率・cap table snapshot は dashboard API に返さない。
- PJ台帳の Slack CH 列: `projects.slack_channel_not_required=true` を「チャンネルなし」チェックで編集できる。これは未設定ではなく意図的にSlackチャンネルを使わないPJを示し、抽出状況の設定不足から外す。チェック時は古い `slack_channel_id` を空にする。PJ台帳の見出し行は縦横スクロール中も固定する。
- 左メニューのボード: GlobalNav は研究ポートフォリオ (ホーム / 研究機関 / シーズ / PJ運用) を最上位グループに持つ。「ホーム」(`/dashboard`) はマウスオーバーまたはキーボードフォーカスで、右側に全アクティブPJの一覧を出す既存フライアウトを維持する。各行は対応するPJコックピットへ遷移し、一覧は固定せず `projects.status='active'` を読む。フライアウトはナビのスクロール領域にクリップされない上位レイヤーで表示し、画面下端では一覧部分だけをスクロールさせる。既存の探索・自分・Admin・資料グループの全導線は維持する。
- 研究機関リスト: 契約有無に依存しない `institutions` カタログが正本だが、`/dashboard` には全件表示しない (`PortfolioPulse` は上位候補だけ)。閲覧・ECR比較・機関コックピットへの入口は独立した `/institutions` に置く。対応は固定ID表でなく `institution_projects` から解決し、ECRはSPSと合算しない。
- Company Content shelf: PJ台帳・要対応・経営指標・運用状態の後に、`CompanyContentShelf` を4カラムで表示する。列はメンバー / 沿革 / メディア掲載 / photo。`member_profiles` / `company_history_events` / `media_assets` の approved rows を優先し、未適用環境では既存 `members` + `project_members`、`project_events` / `project_ventures`、photo permission placeholder に fallback する。Notion photo URL や個人情報本文は表示しない。
- MyPage embed: `/dashboard` 右カラムでは `<MyPageContent embedded showMonthlyProjects={false} />` を使い、「今週やったこと」より下の月別PJカードを出さない。`/mypage` 単体では従来どおり月別PJカードを維持する。desktop (xl+) は右カラムを360–400pxに留め、`sticky` + 独立 `overflow-y-auto` にして左カラムのスクロールと分離する。mobile/tablet では埋め込み自体を隠す代わりに「マイページを開く」実リンクカードを出し、導線を落とさない。埋め込み時の loading/error 表示は `min-h-screen` を使わず短い `min-height` に留める (2026-08-02 まさ追加監査反映、埋込枠が画面いっぱいに伸びる不具合の修正)。
- Dashboard運用面: PJ一覧の後に明示 action queue と Management Score を維持する。月次ルーティン由来の自動タスクは生成しない。
- 配色: `/dashboard` は `amd-home-page-skin` (白 / graphite / 濃紺 / AMD blue / cyan、borders-only、4pxベース) を使う。旧 `amd-desk-page-skin` の淡いベージュパレットは使わない (他ページの `amd-desk-page-skin` は変更していない)。

回帰防止:

- `npm run test:portfolio-home-contract` (旧 `test:portfolio-preview-contract`) が redirect化・GlobalNav統合・API route の認証/portfolio scope gate/障害分離・ECR/SPS分離・パネル順・右MyPage幅・borders-onlyを検査する。
- `npm run test:critical-ui` の既存 `expectNotIncludes("src/app/(app)/dashboard/page.tsx", ["fetchErsBundle", ...])` guard は維持 (= `/dashboard/page.tsx` 自体は institutions/seeds fetch を直接持たず、`PortfolioPulse` 経由に閉じる)。

## /notifications と /proactive — まさの注意面

目的: 自動抽出の保管庫ではなく、まさの判断または本人にしかできない具体行動だけを、理由・操作・効果つきで出す。

必須契約:

- 先手TODOの汎用自動生成は停止中。既存 Codex automation `amd-os-proactive-heartbeat` は未審査L2 candidateを正本採否カードへ仕上げる。別automationは作らない。provider APIや従量課金tokenを使わず、DB更新はvalidator後の非LLM applierだけが行う。
- `/proactive` の未対応・ブロック中とdashboard badgeは、`approved` の `decision` / `masa_action` だけを読む。MTG prep、チーム作業、相手ボール、保存完了、待ち、復旧作業、根拠不足は混ぜない。
- `/notifications`、GlobalNav未読数、Realtime popup、iOS/macOS通知は、`l2_notifications` の `approved AND requires_masa_decision=true` と、Codex審査済みかつまさ本人の完全なaction contractを持つ `app_notifications` だけを対象にする。直接の再認証URLがある `connector_auth` だけは復旧を遅らせない例外。
- `meeting_notifications` は会議記録として保持し、注意面・未読数・OS通知には出さない。会議準備はCodex task内で行い、OSへ重複TODOを作らない。
- `due_basis='explicit'` の期限だけを期限超過・redへ使う。明示期限がなければ `none` / null。期限原文が元証跡に実在しない出力はvalidatorが拒否する。
- 正本: [`pwa/spec/2-4-proactive-todo-current-spec.md`](../spec/2-4-proactive-todo-current-spec.md)。回帰検査: `npm run test:proactive-heartbeat`。

## /tasks (deprecated)

目的: `/tasks` 画面が廃止済みであることと、残す互換面を明示する。

廃止済み:

- GlobalNav の `タスク` 導線。
- `/tasks` page route。
- `TasksClient` のマインドマップ / ガント / 業務デスク UI。
- `npm run agent:tasks` helper。

残すもの:

- `tasks` table は cockpit legacy kanban / H-1 next action 互換のため残す。
- `/api/tasks` は既存 caller 互換として残す。browser client から直接 `tasks` を更新しない。
- `/api/task-calendar/register-tasks` は H-1 の次アクション登録として残す。通知・Slack nudge は廃止済み `/tasks` ではなく対象 PJ cockpit へ向ける。
- `/api/task-calendar/schedule-plan` は Calendar 作業枠 dry-run planner として残す。

回帰防止:

- `/tasks` 画面や GlobalNav 導線を復活させない。
- `tasks` 既存カンバン列 (`task_id`, `project_id`, `title`, `status`, `assignee`, `priority`) の意味を変えない。
- DB物理削除、DROP、既存 row の一括削除はしない。
- DDL変更時は `pwa/scripts/migrations/` と `/spec/5-7-task-management-current-spec` を同時更新する。

## /project/[projectId]/cockpit

目的: PJの現在地、MS進捗、資料、経営ハイライト、月次カード、MTGサマリを一画面で見る。

必須機能:

- レイアウト: `max-w-[1600px]` の幅広 container、上 Header → hero (PJ Status) → 進捗管理本文へ進む案C系構成。通常時は MS / 月次側と、資料 / 経営ハイライト / ガバナンス / 助成金 / MTGサマリ側の 2 カラム。凍結中 / 再開予定などのステータスバッジがある時だけ右カラムを出す。`max-w-[1060px]` + 左 720 / 右 220 の旧 2 カラムには戻さない。最下段の旧 TODO かんばんと旧 `ProactiveQueuePanel` は主要導線から外す。
- Header契約実行サマリー: `CockpitHeader` はPJ名/status/分類/PJメンバーに加え、現行契約ごとに `契約期間` / `請求・振込` / `業務・成果物` / `経費申請` / 必要時だけ `推進条件` の最大5項目を表示する。請求・振込タイミングは必須表示。短文は `projects.contract_terms_json.currentContracts[].terms.cockpitSummary` の `invoiceTiming` / `paymentTiming` / `scope` / `deliverables` / `expense` / `execution` を正本にする。知財、秘密保持、解除、責任など常設しても判断量が増えない法務条項は通常契約のサマリーへ出さず、`/admin/contracts` に残す。NDAは `契約期間` / `利用目的` / `運用条件` を表示する。コックピットから `/admin/projects` や旧configへ飛ばす導線は置かない。
- KUTE年度内ロードマップ: `projectId === 'p25'` では Header 直下に `CockpitKuteAnnualRoadmap` を表示する。6/11キックオフ資料 / `PROJECT_BRIEF` の年度内スケジュールを根拠に、規程整備 (`2027-01` 完了目途) とシーズ発掘 / after GTIE (`2027-03` 型化目途) を同じ横軸で見せる。研究機関コックピット `/institutions/inst_kute/cockpit` でも同じ `CockpitView` 経由で表示する。
- 上 hero: PJ ごとに出し分け。p00 (= AMD 会社全体) は `CockpitManagementScoreHero` で AMD Management Score の時系列折れ線 + 最新値カード。SU 系 PJ は `CockpitVentureStatus` 内の最上段に`Bzm22CockpitSummary`を置き、$J/P/Q/S$、数式、肯定形の一文を主スコアとして表示する。従来SPS折れ線とM/P/R/Sカードは初期閉じの「旧SPS履歴」トグル内に残し、XRLは常時表示する。
- Hero 下タブ: SU 系 PJ は `進捗管理` / `スコア詳細` を切り替える。BZM 2.2 / XRL hero はタブ外に置いて常時表示する。`スコア詳細` の最上段は `Bzm22ProvisionalObservatory` の `BZM 2.2` とし、慎重/基準/強気scenarioのJ/P/Q/S、ブラウザ内だけの方針比較、選択・イベントの5レーン時間軸、7群103項目を省略しない全パラメータ台帳を出す。方針比較は保存済み計算bundleが揃う候補を丸ごと切り替え、元の進め方との差を表示するだけでDBへ保存しない。時間軸は各イベントへ試算で前提にした進め方、または「方針選択ではない」を表示する。全パラメータ台帳は内部keyと英語の機械状態を主表示から外し、日本語名、理論記号または非数式項目の明示、関係式、登録値、3ケース、値の決め方、開ける根拠資料を表示する。画面最下部には `現行SPS / BZM 2.1`、`BZM 2.0`、`SPS 1.0 / Legacy AMD` を独立した初期閉じトグルでこの順に残す。各childは初回open時だけmountし、閉じた状態で重い取得を走らせない。BZM 2.2取得不能でも既存モデルを失敗させず、旧 `/venture-map/amd-score/[projectId]` は同じscore-detailへredirectする（`p99`デモを除く）。
- 今期MSリスト: `CockpitGoalsCompact` / `MilestoneGanttChart` でMS期間、pt、担当、sub item、MS単位の `設計額`、担当者ごとの `担当設計額` を表示する。設計額は通常MSなら `value_plan_cycles.budget_yen`、`cap_extra` なら同期間の `billing_cycles.extra_budget_yen` 合計を、それぞれの有効ptで按分する目安で、支払確定額ではない。MS 設計編集は `/admin/ms-overview` に集約し、cockpit / HUD cockpit からは編集しない。
- MS変更履歴: `CockpitMsChangeHistory` を今期MSの直下、`CockpitSeasonFinance` の手前に初期折りたたみで表示する。正本は `/admin/ms-overview` 保存時に追加される `milestone_change_events` と、2026-07-09 backfill の `source='migration'` 基準線。表示は確認専用で、変更日時、記録者、追加/無効化/更新されたMS、担当share差分、保存前支払検算の状態、追加支払/差額控除の合計を出す。契約本文、メール全文、議事録全文、raw source は保存・表示しない。cockpit 側には MS 設計の保存口を置かない。
- 今シーズン収支: `CockpitSeasonFinance` をMS変更履歴の下、月次カードの手前に表示する。シーズン合計と月次行で `クライアント支払` / `バッファ` / `原資上限` / `PJ予算` / `メンバー支払` / `期末未払` / `収支` を出す。クライアント支払は `contractBackedClientAmount` + 別財布売上、schedule_based 契約では `contract_terms_json.monthlySchedule.amountTaxExcl`、バッファは `value_plan_cycles.buffer_breakdown_json` 優先、原資上限は `(クライアント支払 - バッファ) × 65%`、PJ予算は `budget_yen + extra_budget_yen`、メンバー支払・未払残は `reward_summary_json` を正本にする。`期末未払` / `未払残` は支払通知対象の外部メンバーへ将来払う残高だけを表示し、役員の未充当繰越は会社留保側の内部検算へ寄せる。`収支` は現金主義で `クライアント支払 - バッファ - メンバー支払` とし、役員向け報酬相当額や未払残は含めない。役員向け報酬相当額は検算には含めるが、PJ cockpit では表示しない。期末未払または原資超過が 1 円でも残る場合は `不足` 表示と赤い停止帯を出し、報酬計算側の自動上乗せでゼロに見せない。
- 先手TODO: 旧 `proactive_outbox` 由来の `ProactiveQueuePanel` は通常PJ / institution cockpit に表示しない。`資料作成済み` など旧司令塔状態の手動seedがPJ状況面に残ると読解ノイズになるため。先手TODOの棚卸しは `proactive_todos` + `/proactive` + dashboard 上段バッジで扱う。
- 経営ハイライト: MSリスト横の col2 として `CockpitStrategySignals` を表示する。`重要な動き` は internal candidate/confirmed、`採用リサーチ` はつくよみ外部リサーチの confirmed だけ。外部候補は `/notifications` で1件ずつ「採用/見送り」し、未採用をcockpitへ混ぜない。日付・type/category・impact・summary・source refsを表示する。
- 月次モーダル: 月次カードから `CockpitMonthlyModal` を開き、report / reward / invoice を確認できる。右上の操作は `社内版を確認・編集` (`template=internal`) と `提出版を確認・編集` (`template=submission`) の2つで、どちらも同じ「開いた紙面を確認し、本文の見出し・段落・表をその位置で編集してからPDFとして保存する」体験に統一する。社内版の進捗表・ガント等の自動表示は紙面上で直接変更しない。提出版は `monthly_reports_external.body_md` へ保存して構成・内部用語を検査する。社内版は draft へ保存し、確定版の置き換えは確認付きの明示操作だけにする。生成・再生成・AI修正ボタン、提出先名を埋め込んだ操作ラベル、モーダル内の別Markdown編集パネルは置かない。routine step 起動は廃止済み。p00 (= AMD 会社全体) でも他 PJ と同じく月次カード + 月次モーダルが出る (`billing_cycles` を 12 行 backfill 済)。
- PM月次ルーティン: 廃止済み。cockpit の col3 に月次 step/TODO は出さず、月次状態は月次カード + `CockpitMonthlyModal` で確認する。
- 旧 nudge カード: 通常PJ cockpit には `CockpitNudge` / `tsukuyomi_nudge_queue` 由来カードを出さない。通知・軽い確認は Slack や専用面へ寄せ、コックピット本文の読解を妨げない。
- MTGサマリ: `CockpitMeetingSummary` 各行に source link (Notion / Slack / Drive / Gmail / Calendar event の元データへ直リンク) を `元 ↗` の形で出す。各行クリック時は `?meeting=<meeting_id>` を URL に反映し、共有 URL から同じ detail modal を auto-open できるようにする。`summary_short` は一覧カードの2行サマリとして出す。通常PJ cockpitの一覧本体には `max-height` と `overflow-y-auto` を置かず、カードが増えた場合はコックピット全体のページスクロールで読む。`source_kinds='upcoming'` の row は「予定MTG / 準備中」ブロックに出し、`決めること / 準備物` を表示する。H-1が現在時刻の前後24時間にある確定Calendar予定を、M系メンテが今後60日の確定Calendar予定を `POST /api/meeting-prep/calendar-sync` に渡すことで、前回議事録が空のPJでも未来MTGカードを作る。`source_kinds='upcoming_tentative'` や `meeting_id` が `upcoming:` で始まるだけの row は日程未確定の仮置きとして「日程調整中MTG」ブロックに残し、確定予定 count には含めない。予定MTG詳細では、配列を箇条書きで並べるのではなく `narrative_md` の「初見ブリーフ」を主役にし、「会議後に残したい状態」「いまの状況」「当日までに揃えるもの」「必ず確認すること」を文章カードとして見せる。既存 `risks` の内容は破壊せず、この「必ず確認すること」として表示・編集する。MTG詳細モーダルの「表示内容を編集」は、表示している section を同じ位置で textarea 化する。通常MTG / dialogue は `narrative_md` が表示されている場合は `narrative_md` を編集し、raw 配列表示の場合だけ `decided / progress / next_actions / risks` を編集する。予定MTGは同じ `POST /api/meeting-prep`、通常MTG / dialogue は `POST /api/meeting-summary/manual-update` で保存する。MTG詳細モーダルには「つくよみに修正依頼」を置かず、LLM再解釈ではなく手動編集を正本にする。詳細モーダル上部には `PDF保存` / `議事録コピー` / `準備メモコピー` / `共有URLコピー` を置き、PDFは共有用DOMを `html2canvas` + `jsPDF` で直接保存、議事録コピーは `narrative_md` / fallback section から会議後サマリだけのプレーンテキスト、準備メモコピーは `## 参考: 会議前準備メモ` 以降または紐づく prep row の本文、URLは既存 `?meeting=` deep link を使う。詳細モーダルの「添付資料」は `meeting_assets` に、ファイル選択 / drag & drop / clipboard paste / browser screen capture の4経路で一般ファイルを保存できる。新規添付実体はDriveの `PJフォルダ / YYMMDD_会議名` に置き、保存先をカード上に表示する。旧Storage添付は互換表示し、`POST /api/meeting-assets/insert-markdown` で `narrative_md` に Markdown 画像/リンクとして挿入できる。`dialogue:*` で始まる meeting_id は「提案整理」チップ付きで識別し、`CockpitMeetingDetailModal` でラベルを「提案前の論点整理 (チームへの相談)」に置き換える (= 「決まったこと」と書かない)。dialogue meeting は `narrative_md` があれば 1 本の Markdown narrative として表示する。`narrative_md` は `POST /api/dialogue-meeting/narrate` で生成する。各 TopicList の項目は border-l フレーム枠ではなく、`<ul>` + 太字 / マーカー / 見出しの強弱で読ませる。
- 開催済みMTG本文の表現契約: `## 🎯背景` / `## 📊経緯` は段落、`## ✅決まったこと` / `## ▶️次の一手` / `## ⚠️残課題` は1項目1論点の `- ` 箇条書きにする。番号付きリストとチェックボックスは使わない。
- MTG PDFの添付契約: `PDF保存` は共有用本文の後ろに `meeting_assets` の PDF / PNG / JPEG を `sort_order` 順で連結する。投影資料を先、参加者共有資料を後に並べ、資料へのリンク一覧だけで済ませない。
- MTG詳細Markdownのメンバーリンク: `CockpitMeetingDetailModal` で表示する `narrative_md` / `summary_short` / raw 配列 / 予定MTGブリーフは `MarkdownView memberLinks` を通し、active AMDメンバーの `members.code_name` が standalone mention として出る場合だけ `/mypage?memberId=<members.member_id>` へリンクする。既存 Markdown link / code / pre は対象外で、`しかるべき` の `かる`、`こうして` の `こう` のような部分一致はリンクしない。
- 研究機関連携シーズ一覧: `CockpitKuteSeeds` はKUTE専用の別台帳を持たず、`seeds` と全国共通 `seed_sps_assessments` を読む。研究機関PJの対象スコープは `institution_projects.institution_id` から解決する。`/seeds` は全175件を1シーズ1行のフラット表で表示し、研究機関・研究者/PI・PJ状態を通常カラムにする。固定優先順はPJ化済み → PJ化検討中 → PJなし・SPS評価済み(`latest_sps.status==="ready"`) → その他で、機関/研究者/PJ有無のgroup rowは作らない。`spun_off` / `declined` も除外せず、SPS未評価を0にしない。SXは会社未設立の `discussing` / `pre_incorporation`。ECRとSPSは単一スコアへ合算しない。詳細は [`seeds.md`](seeds.md) と [`institution_seed_project_model.md`](institution_seed_project_model.md) 参照。
- **KUTE「規程・内規」タブ** (`CockpitKuteRegulations`, build v3.68.2): `p25`だけに表示する。契約対象の7規程（兼業、知財・大学発SUライセンス、出資・新株予約権、共同研究、利益相反、認定、共有機器利用）を上段の独立セクションとして出し、認定委員会内規・支援細則・認定審査チェックシートは随伴する認定運用文書として下段へ分離する。進捗の正本表示は `S0 対象確定 → S1 原典確認 → S2 AMD原案 → S3 学内調整 → S4 決裁・施行`。各行は進捗順に並び、`S?/4` と4分割バー、現状、次ゲート、次ゲートの時期、実在する資料、途中版数だけを小さな文字で示す。行ごとに一律の完了目標を置かず、全体の工程線で8月の残り6規程素案、9/4の修正案・進捗確認、9〜11月の学内議論・修正、1月の施行版・様式・運用フローを示す。AMD Blueは段階が進んだ部分、赤はS0だけに使い、状態を色だけで表現しない。docx実体はKUTE共有Driveの `00_規程・内規_版管理` に集約し、コックピットへメール本文、個人情報、秘密情報、実体ファイルを複製しない。KUTE以外のPJへタブやKUTE文書リンクを表示しない。削除禁止理由: 契約上の受託範囲・未着手・原典と版の取り違えを、PJの運用面から一目で防ぐ唯一の導線。

回帰防止:

- `pwa/scripts/check_pwa_critical_ui.cjs` が `経営ハイライト`、`CockpitStrategySignals`、`project_strategy_signals`、`project_strategy_signal`、`CockpitMsChangeHistory`、`MS変更履歴`、`milestone_change_events`、`CockpitSeasonFinance`、`今シーズン収支`、`クライアント支払`、`期末未払` の anchor を検査する。
- 研究機関連携シーズ一覧の動的スコープ・PJ優先表示・非公開フィールド除外は `npm run test:kute-seeds-scope`、2種のPJ物理分離・二重分類防止・ECR/SPS非更新は `npm run test:institution-seed-project-domains` で検査する。SPS計算の0/NULL区別・欠損軸のmissing扱い・捏造禁止は `npm run test:seed-sps-score` で検査する。
- MTGサマリの予定MTG block / `POST /api/meeting-prep` / `POST /api/meeting-prep/calendar-sync` / `MeetingPrepInlineEditor` / `POST /api/meeting-summary/manual-update` / `MeetingSummaryInlineEditor` / `MeetingAssetsPanel` / `POST /api/meeting-assets` / `PDF保存` / `議事録コピー` / `準備メモコピー` / `共有URLコピー` も `check_pwa_critical_ui.cjs` で検査する。
- 案C レイアウト anchor (`max-w-[1600px]`、`lg:grid-cols-[minmax(0,1.2fr)_minmax(0,1.2fr)_300px]`、`xl:flex-row` Hero) も `check_pwa_critical_ui.cjs` で検査する。`max-w-[1060px]` や旧 left/right 2 カラム構造に巻き戻ったら `npm run test:critical-ui` で落ちる。

## /project/[projectId]/report/[ym]/print — 月次報告書 印刷 + 編集履歴 (build v3.57.0、migration 223)

目的: 提出前の紙面をそのまま見ながら、社内版・提出版の本文をその場で編集・保存・確定し、誰がいつ何を変えたかを静かな校正台帳として確認できるようにする。

必須機能:

- 印刷ツールバー: 固定 `.toolbar` (`編集する` / `下書きに保存` or `提出版を保存` / 社内版のみ `確定版に反映` / `PDFとして保存`) は維持し、PDF保存が主役の位置づけを変えない。
- 最終更新帯: ツールバー直下に no-print の `MonthlyReportHistoryPanel` (`monthly-report-history-panel.tsx`) を常時表示し、更新者・日時・操作と「編集履歴を見る」ボタンを出す。保存・確定が成功するたびに `historyRefreshToken` を進めて即座に最新化する。
- 履歴パネル: desktopは右側固定パネル、mobile (`max-width: 760px`) は下からのシート。社内版・提出版をタブで分け、時系列一覧 (更新者・日時・操作・自動生成か人による編集か・変更した章) を表示する。一覧は軽量 (`GET /api/monthly-report/history?projectId=&ym=`、本文全文を含まない) で、行を開いた時だけ `id` 指定の detail fetch (`&id=`) で変更前後の全文を取得し、行単位の正確な差分 (`diffMonthlyReportLines`) を表示する。復元操作は無い。Escapeで閉じ、閉じると開いたボタンへフォーカスを戻す。
- 書き込み経路: `manual-update` (社内版下書き保存) / `report/fix` (確定) / `external-manual-update` (提出版保存) の3ルートは全て `requireAdmin` (admin-only)。本文保存 + `monthly_report_edit_history` への追記は RPC (`monthly_report_internal_save` / `monthly_report_external_save`) 内で1トランザクションにまとめ、一方だけ成功する状態を作らない。RPCを経由しない直接書込み (将来の routine / GAS / script) も AFTER トリガーが `automation` として最低限捕捉する。`report/fix` は確定者 (`confirmed_by`) を必ず保存する。
- 版・発行履歴 (印刷PDF内、旧 改訂履歴): §07 添付資料・参照の末尾に、初版 (自動生成) / 確定 (`confirmed_by`) / 本書発行 の3節目だけを残す。個別の下書き保存・編集履歴はPDFへ出さない。
- legacy seed: migration 223 適用時点の既存 `monthly_reports` / `monthly_reports_external` 行は `content_before/after=NULL`, `detail_available=false` の legacy 記録として1回だけseed済み。UIは「詳細差分なし」を表示し、過去の正確な差分を捏造しない。

回帰防止:

- `npm run test:monthly-report-history` が、章内変更検出 (`changedMonthlyReportSections`)・行差分の可逆性 (`diffMonthlyReportLines`)・migration の RPC/トリガー/`confirmed_by`保存・3保存APIの `requireAdmin`+atomic RPC使用・履歴一覧が全文をselectしないこと・詳細取得が`id`指定時だけ全文を返すこと・履歴UIの`role="dialog"`/Escape/タブ分離・印刷物が「版・発行履歴」に改名済みであることを検査する。
- `npm run test:monthly-report-quality` が、下書き保存はRPCの`draft_save`アクションを使うこと、RPC内で確定済みステータスを崩さないことを検査する。

## /institutions/[institutionId]/cockpit

目的: 研究機関カードから、機関の箱を保ったまま関連PJの進捗・月次・MTG履歴へ入る。

必須機能:

- KUTE / NIMS / 愛媛大学カードは `institution_projects` の関係を使って機関コックピットへ遷移する。KUTE=`p25`、NIMS=`p28`、愛媛大学全体=`p30`。研究機関PJは通常PJリストへ二重表示せず、機関行のPJ運用レイヤーとして表示する。
- CX `p20` と `p26` は種別未確認のため、名称や過去の固定対応から研究機関PJ・シーズPJへ自動分類しない。
- 研究機関コックピットは `institution_projects` から対象PJを動的に解決し、既存PJコックピットの `CockpitView` を同画面にマウントする。これによりMS進捗、月次カード/モーダル、MTGサマリを既存データのまま使う。
- 上部にECR充足率、関連PJ、今期MS件数、MTG履歴件数を出す。
- `project_meeting_summaries` を月ごとに束ねたMTGツリーを表示し、各行から通常PJコックピットのMTG詳細 (`?meeting=`) へ遷移する。
- `/institutions/[institutionId]` の詳細画面からも研究機関コックピットと通常PJコックピットへ戻れる。
- **「土壌×シーズ」タブ** (`CockpitSoilSeeds`、2026-07-30追加、build v3.52.0): `進捗管理` / `スコア詳細` に続く3本目のタブ。機関ECR8軸と所属シーズSPSを、実観測日unionの断面 (`観測断面台帳`、最大24断面) で同じ観測基準日として整列するが、ECR元評価日とSPS元評価日は別表示し、単一スコアへは絶対に合成しない (`terminology_glossary.md` §4 / `BZM_2_0_REVISION_REQUIREMENTS.md` §3-5)。相関/因果/予測モデルは出さない。所属は `seeds.institution_id` (migration `202_soil_seeds_institution_link.sql`、KUTE=`org_name`工学院大学のみbackfill、SPS値は未変更)。純粋関数は `pwa/src/lib/institution-soil-seeds.ts`、テストは `npm run test:institution-soil-seeds`。KUTEのみ所属シーズ上位ランクから既存p25 GTIE申請支援検討フローへの導線を持つ。削除禁止理由: ECRとSPSを機関単位で継続観測しつつ合成単一スコア禁止を守るための唯一の画面。

## 株主・ガバナンス + 要対応 (2026-06-15 追加、2026-07-16 会社概要タブへ統合)

設計: `pwa/design/governance_action_items.md`。DB: migration `137_governance_and_action_items.sql` (`action_items` / `project_shareholders` / `project_valuation_rounds` / `project_shareholder_meetings`)、`174_project_company_overview_and_equity_ledger.sql` (`project_company_profiles` / `project_equity_transactions` / `project_equity_entries` / `project_convertible_instruments` / `project_financial_periods` + `amd_os_is_member()` gate 関数)。

- **PJ cockpit 常設「会社概要」タブ** (`CockpitCompanyOverview`、`CockpitView` のタブ列 `進捗管理` / (`スコア詳細`) / **`会社概要`** の3本目として全PJ常設。2026-07-16、col2 の `CockpitStrategySignals` 直下にあった旧 `CockpitGovernance` を削除しこのタブへ統合): 基本情報 (商号/法人番号/資本金/機関設計/決算月等、`project_company_profiles`)、株式イベント台帳 (`project_equity_transactions` + `project_equity_entries`) 由来の cap table、発行済/完全希薄化後 (FD) 株式数、確定株式イベントを積み上げた 100% 資本構成推移タイムライン、資金調達ラウンド (`project_valuation_rounds`)、J-KISS/SAFE/CB 等の転換前証券 (`project_convertible_instruments`)、総会・取締役会 (`project_shareholder_meetings`)、年度決算 (`project_financial_periods`)、Excel (`downloadCompanyOverviewXlsx`) / PDF (html2canvas+jsPDF) 出力を1画面に集約する。`status='planned'` の計画株式イベントは法的現在値の cap table (`buildCapTableSnapshots`) に混ぜず、転換前証券の転換見込株式数 (`convertibleScenario`) も現在の持株比率には混ぜない。`project_company_profiles.registered_issued_shares` (登記上の発行済株式数) と株式イベント台帳合計の tie-out を `capTableTieOut` で常時表示する。**まさ確定仕様により、全PJを members 登録済みの AMD メンバー全員が閲覧・編集できる (admin 限定ではない)**。データは `/api/governance` (`requireMember` gate。従来の admin 限定から 2026-07-16 に変更) から client fetch。既存 `project_shareholders` は互換表示用に残し、cap table の opening balance へ非破壊 backfill 済み。削除禁止理由: 終了後も残る AMD 持分・ガバナンス可視化 + 会社の基本情報から資本政策までを1つの正本に集約する (まさ確定 2026-06-15 / 2026-07-16)。
- **会社概要「キラー要素カタログ」** (`CockpitKillerFactorCatalog`、migration `246_killer_factor_catalog.sql` / `249_killer_factor_operating_model.sql` / `250_killer_factor_graduated_assessment.sql`): `killer_factor_catalog` は全PJ共通マスタ、`project_killer_factor_states` はPJ別状態。共通要素は、AMDが発生前に塞ぐ `prevention`（予防統制。打ち手・完了時機必須）と、兆候を継続確認する `monitoring`（常時監視）へ分け、UIはfilterで片方を隠さず二群を同時表示する。予防統制状態は `unchecked / not_started / in_progress / implemented / controlled` の成熟度4段階と `breached`（統制逸脱）、常時監視状態は `unchecked / clear / watch / warning / occurred` の悪化度4段階。状態日・記録/文書に基づく根拠メモ・ログイン中の記録者・記録日時を必須とし、予防統制だけPJ目標日を任意で持つ。`summarizeKillerFactorRisk()` は `critical > attention > unknown > watch > stable` の優先順で全体判定と重大・要対応・要観察・未確認・安全の件数を返し、未確認を安全扱いしない。desktopは先頭の全体判定と初版7件を`1440×900`内で一覧できる44〜64px行、mobileは横スクロールなしの短い縦積み。反復行へ大型の危険ボタンを置かない。`/api/governance/killer-factors` は `requireMember` gate。今回は記録と表示までで、自動通知・成功確率再計算・LLM呼び出しへ接続しない。全体の密度規約は `/spec/2-7-ui-design-code-current-spec`。削除禁止理由: AMDの予防責任と継続監視を分け、事前確率へ混ぜずに再測定起点を残す全PJ共通台帳だから (まさ確定 2026-08-09 / 2026-08-10)。
- **SX「事業計画」タブ** (`CockpitBusinessPlan`, `src/lib/sx-business-plan.ts`, 2026-07-27追加): `p21` のみ、`進捗管理 / スコア詳細 / 事業計画 / 会社概要` の4タブ目として表示する。最上段は4行（事業開発 / 技術開発 / 組織開発 / 資金調達）×5列（PSI / Seed→A / A→B / B→C / C→IPO）のフェーズマトリクス。各セルに活動、費用、出口条件、関連XRL、列見出しに総額・調達源・固定費バーン上限・5軸XRLを表示する。直下の `data-testid="sx-phase-matrix-xlsx-export"` は、同じフェーズ表だけを `.xlsx` としてブラウザ内で出力する。`src/lib/sx-business-plan-xlsx.ts` は数式開始記号を文字列として無害化し、先頭2行と開発レーン列を固定した横長1シートを生成する。保存API・DB更新は持たない。その下へ100%株主構成推移と資本政策表 (`CapitalPlanWorkspace`)、最後に年次試算表を置く。共通の枠は直接的な表題・項目名だけで構成し、SX固有の事業仮説はフェーズのセルと計画データに置く。SXの計画資本政策だけは会社概要タブからこのタブへ移し、確定済み会社情報と法的株式台帳は会社概要に残す。migration `196_sx_capital_policy_draft.sql` は同名プランが無い場合だけ編集可能な計画を投入し、既存編集を上書きしない。
- **SX workspace→AMD運用接続** (`src/lib/sx-workspace-operating-context.ts`, `/api/project-workspace/p21/automation-context`): `project_management_*`の変更履歴を、保存時LLM・別台帳・件数上限なしで既存D-6/H-1へ渡す。完了状態の決定・成果・停止・技術結果・資金・関係先事実だけをdeterministicにD-6レビュー対象へ印付けし、通常の進行中TODOは除外する。H-1では全変更を会議文脈として読むが、会議sourceなしに会議決定へ昇格させない。Cockpitには作業一覧を複製せず、採用済み経営ハイライトの根拠から元workspaceへ戻る。
- **SX「事業計画」タブの投資判断表現** (2026-07-28 更新): 配色は白・slate・indigoを基調とし、緑/黄/赤は成功/警告/エラー、複数色は株主構成棒と凡例だけに限定する。GRLは内閣府SIPの Governance Readiness Level（制度・規制・標準・ガイドライン、1〜8）で、フェーズ到達値を `1 / 3 / 5 / 6 / 8` とする。組織の採用・役割・内部統制はHRLへ置く。`CapitalPlanMatrix` は `overflow-x-auto` のみでページ縦スクロールを妨げず、各株主はFD比率サマリー1行を初期表示、`aria-expanded` 付きボタンで金額・株数・発行済・FD株数の詳細を開く。年次試算表は百万円・整数・3桁区切りで、売上原価、役員報酬、給与・賞与、研究開発費、販管費、助成金の特別利益/圧縮損、助成金入金を分け、会計PLと資金繰りを混同しない。直下の初期閉じ `data-testid="sx-annual-parameters"` は保存しないローカル試算で、年次の売上・原価・助成金・人員と役員/社員別の報酬/旅費/消耗品費・R&D/販管費・工場以外設備・IPO以外調達、段階別の自社工場投資額/年度、IPO年度/公募額を入力に、PL・capex・調達・期末現預金を`src/lib/sx-business-plan.ts`の純粋関数で再導出する。変更あり表示と初期値へ戻す操作を持つが、資本政策・株主構成・会社情報には書き込まず再読み込みで初期値に戻る。
- **cap table 統合ワークスペース `CapitalPolicyWorkspace`** (2026-07-16 追加、同日 UX 再設計で `CapitalTimeline` / 別枠 `CapTableHistoryMatrix` / 選択イベントだけを再掲する冗長 `cap table｜{event}` セクション / 表から離れた `NextRoundSimulator` を統合・廃止): 会社概要タブの cap table は「資本政策」という単一セクションに集約されている。イベント列見出し (設立/Seed/QST現物出資等の日付+ラベル、実 `<button>` で focus-visible・選択時は下線) の直下に、confirmed 状態の株式イベントから積み上げた holderName ベースの縦積み100%持株構成バーを共通0-100%スケールで揃えて配置し、列をクリックすると次回ラウンド試算のベース断面が切り替わる。その下に `data-testid="cap-table-history-matrix"` の履歴行 (発行済株式・新規発行・調達額・発行価額・pre/post-money → holderName別 `shares株 · pct%` の compact 1行表記) を sticky label 128px / event列 140px のコンパクト幅で並べる。正史が `incorporation` から始まらない場合は `capTableOriginWarning` が `data-cap-table-origin-warning` の警告をワークスペース内に出し「創業時のシェアを遡って再現できない」旨を明示、創業時株式イベントの入力導線 (`onAddFoundingEvent`) を提示する。Excel 出力 (`downloadCompanyOverviewXlsx`) も UI と同じ計算エンジンを再利用し、`ラウンド別cap table` シートに履歴マトリクスを書き出す。**このワークスペースは confirmed 済みの法的な現在値 cap table 専用のベースライン表示であり、将来ラウンドの試算・編集は行わない**（旧 `data-testid="next-round-simulator"` の `次回ラウンド（試算）`列・保護株主/目標比率コントロールは **2026-07-17、下記 `資本政策プラン台帳 CapitalPlanWorkspace` へ置き換え・廃止** 済みで、このワークスペースにはもう存在しない）。削除禁止理由: 確定済みの法的 cap table の経緯を1つの正本タブ・1つの統合グリッドに揃えるため、編集→スクロールして確認→また編集の往復を無くすため (まさ確定 2026-07-16)。
- **資本政策プラン台帳 `CapitalPlanWorkspace`** (2026-07-17 追加、旧・単発未保存の `NextRoundSimulator`「次回ラウンド試算」列と保護株主概念を置き換え): `CapitalPolicyWorkspace` の cap table 統合グリッド直下に置く、全 AMD メンバーが閲覧・編集できる名前付きシナリオ台帳。DB: migration `179_project_capital_plans.sql` の `project_capital_plans` (作業中シナリオ、`amd_os_is_member()` gate) / `project_capital_plan_versions` (freeze 時の append-only 確定版、`UPDATE`/`DELETE` は trigger で拒否)。migration `180_freeze_capital_plan_derived_document.sql` (build v3.44.5) で `freeze_capital_plan()` の旧4引数シグネチャを `DROP FUNCTION` した上で `p_document_json` 引数を追加した5引数版へ置換済み (テーブル・データは無変更、置き換え対象は関数シグネチャのみ)。ロジック: `src/lib/capital-plan.ts`。必須機能: (1) シナリオに名前を付けて複数保存できる、(2) 編集は800msデバウンスで自動保存され `revision` 列の楽観ロックで同時編集競合を検知する、(3) `incorporation` から `equity_issue` / `option_pool` / `convertible_issue` / `convertible_conversion` / `secondary` / `share_split` / `ipo` までの複数ラウンドを1本のイベント列で連結し、上流イベントの変更を `deriveCapitalPlan` が下流ラウンドへ再計算・伝播する、(4) イベント/割当のすべての数値フィールドは `EditableValue{value, source}` を持ち、`source` は `input`/`calculated`/`imported`/`confirmed`/`override` のいずれかで、`override` は元の自動計算値 (`calculatedValue`) を保持したまま手動置換できる、(5) **保護株主 (protect-holder) 概念は存在しない** — 目標比率は割当ごとの `targetOwnershipPercentage` を編集するだけで、特定株主を自動保護する仕組みは作らない、(6) cap table 履歴マトリクスと同じイベント列に揃えた holderName ベースの縦積み100%グラフを表示する、(7) `checkPublishEligibility` (`validateCapitalPlan` + `validateSubmissionCompleteness`) が返す `blockingIssues`/`warnings` はワークスペース上に「検証結果（エラー N件 / 警告 N件）」の折りたたみ一覧としてクリック可能な形で可視化され (エラー赤・警告アンバー)、`severity='error'` が1件でもあれば freeze ボタンを無効化しツールチップで理由を示す。freeze API は保存済み `document_json` をまず `deriveCapitalPlan` で再導出し `checkPublishEligibility` を再検証してから、derive済みドキュメントを `p_document_json` として `freeze_capital_plan()` (migration 180版、5引数) SECURITY DEFINER 関数に渡す。この関数が revision検証・version insert・`latest_frozen_version`更新を単一トランザクションで行うため、未derive のまま生の値が history に固定されることはない、(8) VC向け Excel 出力 (`createCapitalPlanXlsx`) は frozen version からのみ生成でき作業中シナリオからは出力できない。出力は「提出情報」(サマリー: プラン名・凍結version・source_revision・公開日時/者・検証結果件数・provenance凡例・丸め方針・金額単位・比率表示基準)・「資本政策」(イベント列 × 指標行のcap table。評価額・調達額・転換条件などの前提もこのシートに載り、縦積み100%持株構成のネイティブ棒グラフ (`percentStacked` bar chart) をこのシート上に埋め込む)・「投資家別」(投資家ごとの出資・保有スケジュール)・「潜在株式・譲渡」(転換前証券・セカンダリ等の潜在希薄化)・「検算」(バリデーション結果・source override 一覧・イベントごとの整合検算) の5シート構成、(9) `CapitalPlanMatrix` は `md:hidden` でも表示され続け、選択1イベント表示に切り替わることはない。`max-h-[70vh] overflow-auto` の bounded スクロールコンテナで全イベント列にアクセスし、行ラベル列 (`sticky left-0`) とイベント列ヘッダー行・FD比率バー行 (`sticky top-0`、測定済みヘッダー高さでオフセット追従) はスクロール中も固定表示され続ける。折りたたみ式「株主・イベント詳細設定」内の別コンポーネント `EventEditor` (割当の詳細編集フォーム) だけが `md:hidden` 幅で◀/▶ (`onPrev`/`onNext`) の選択イベント切替ナビゲーションを持つ、(10) basis driverの編集可否と自動算出値は `calculationBasis` ごとに決まり、自動算出セルは通常グレー「自動」バッジ、`source:'override'` 時はアンバー「上書き」バッジを表示、✎ボタンで直接上書き入力 (`forceOverride`) でき✕「上書きを解除」で戻せる、(11) 新規株主追加は割当なしの行を作るだけで、`valuation_and_investment` basisのイベントで金額欄を空欄・0のまま残しても株数は `source:'calculated'` を保持し、金額入力で `deriveCapitalPlan` が単価から株数を自動算出する、(12) `changeCalculationBasis` によるbasisの直接切替は、切替先で driver となるフィールドを `source:'input'` に、output となるフィールドを `source:'calculated'` に張り替えて古い `override` を意図的に消去する正規化を行う、(13) `secondary` イベントは売り手側 `shares` をマイナス値で表現し (買い手はプラス)、イベント内合計が0でなければ `secondary_net_not_zero`、代金不一致なら `secondary_amount_mismatch` でバリデーションエラーになる、(14) `convertible_conversion` は basis セレクタがUIに出ず `deriveEvent`/`validateCapitalPlan` が `manual` 以外を拒否するmanual専用イベントで、負の `convertible` 割当と正の発行割当の手動入力が無いと `convertible_conversion_missing_consumption`/`_missing_issuance` でブロックされる。削除禁止理由: 保存されない単発試算と保護株主という決め打ち概念では複数ラウンドを跨いだ資本政策の意思決定を積み上げられなかったため、名前付きシナリオ + 全値editable + freeze版という設計に置き換えた (まさ確定 2026-07-17)。
- **資本政策表の可読性規約** (2026-07-31): `CapitalPlanMatrix` の金額・株数は表示時に3桁ごとのカンマで区切り、セル幅が狭くても自動算出金額を `…` で省略しない。FD比率の縦積み棒と凡例は、色覚の違いを含めて区別しやすいカテゴリ配色、凡例swatchの外周、隣接segmentの細い境界線で株主を見分ける。色は株主識別専用で、成功・警告・エラーの意味を持たない。
- **migration `175_lst_cap_table_history.sql`** (2026-07-16): LST (`p07`) の cap table 正史を `xlsx:LST_captable_250415.xlsx` から復元。migration 174 の自動生成 opening_balance を void にし、創業 (`incorporation`, 星野毅 普通株30,000株)・Seed新株発行 (`new_issue`, UMI3号投資事業有限責任組合 シード種優先株式15,000株)・QST現物出資 (`in_kind_contribution`, QST 普通株式2,500株) の3件の confirmed 株式イベントを `project_valuation_rounds` (Seed / QST ラウンド) と紐付けて再構築する。最終断面は合計47,500株、星野毅63.1579% / UMI 31.5789% / QST 5.2632%。この復元内容は LST 固有の事実であり、他PJへの一般ルールではない。
- **要対応（期日順）面** (`ActionItemsPanel`): `/dashboard` の上段 action queue と `/notifications` 先頭。全PJ横断 + personal/company scope の `action_items` を期日順 + 「あと何日/期限超過」chip で表示、「対応済にする」で `status=responded`。成功時は押した行だけを即座に外し、一覧全体の再読込はしない。更新失敗時だけ元の位置へ戻す。データは admin 限定 API `/api/action-items`。削除禁止理由: 期日付き inbound 義務を埋もれさせない導線 (まさ確定 2026-06-15)。会社概要タブにも同じ confirmed action_items を「会社運営の要対応」として表示する (PJ cockpit 側の要対応面)。
- **`/admin/governance`**: 株主/ラウンド/総会/要対応の手動記録 CRUD。AdminSidebar に「🏛 株主・ガバナンス」。ページ自体は Admin route 配下のナビからのみ導線があるが、下流 `/api/governance` は 2026-07-16 以降 `requireMember` (members 登録済み AMD メンバーなら誰でも書き込み可) のため、admin 専用の書き込み経路ではなくなっている。`action_items` を扱う `/api/action-items` は引き続き `requireAdmin`。
- **`/api/governance/extract`**: Gmail/Drive/Calendar 等から抽出された総会・取締役会・書面決議候補の受け口。既定は `l2_coverage_gaps` review candidate、`apply=true` のときだけ `project_shareholder_meetings` に canonical insert。`attachments` に `content_base64` / `data_url` があれば、確認済み反映時に `projects.drive_folder_id` 直下の `YYMMDD_会議名` folder へ保存し、Drive link を `attachments_json` に残す。削除禁止理由: LST の取締役書面決議のようなメール由来ガバナンス履歴を資料リンク込みで OS 化するための入口 (まさ依頼 2026-06-16)。
- **`/admin/projects` の「総会」「役会」checkbox + `/api/cron/governance-email-sweep`**: `projects.governance_watch_shareholder_meetings` / `governance_watch_board_meetings` がONのPJだけ、`report_emails` とのGmailやりとりを総会/役会keywordで狭く検索し、`/api/governance/extract` に candidate / apply を渡す。削除禁止理由: D-14G の検索範囲をPJ台帳から明示的に制御し、全メール横断の誤検知・取りこぼしを減らすため (まさ依頼 2026-06-16)。
- 既存 `tasks` は H-1 / cockpit 互換の旧データレーン。`action_items` は5生データ抽出 + 採否ループ + personal scope を持つ inbound 義務で、`tasks` table を置換しない。

## /admin/schedule

目的: 契約・法定支払義務・報告書・確定 action item・会社運営ファクト・公式期限ルールから、年間の締切を自動生成するadmin read model。

必須機能:

- `AdminSidebar` に `運営カレンダー` → `/admin/schedule` を置く。admin layout/auth gateと既存full-width shellを使う。
- 既定の`年間運営`は法定納付を主役にする。先頭は`今から要対応の口座流出`（要照合+未来の未完了納付）とし、`納付済み`、`要照合`、`これからの口座流出`、年間総額を混ぜずに表示する。12か月の支払額レールは要照合+今後分だけ、月ごとのカードもこの3レーンと`その他の運営`を分ける。納付行は日付、正式名称、支払先、完全な金額、確定/概算、状態を省略しない。社会保険料はカレンダーでは本人預り分を含む口座流出総額、PLでは会社負担コストとして別指標にする。
- 12か月すべての実日付7列グリッドは補助タブ`日付カレンダー`として残す。xlは3か月×4段、md/lgは2か月、モバイルは1か月ずつ表示し、前月/次月・月選択・今日・選択日agendaを備える。
- 会社単位の法定納付を主役にする。`company_payment_obligations` は `tax` / `social_insurance` だけを運営カレンダーへ取り込み、PJ別の請求書発行/送付・入金確認 (`billing_cycles`) は生成対象から除外する。
- カレンダーから予定・日付・金額・担当者を追加、編集、削除しない。元正本の修正後に `/api/admin/schedule/rebuild` で再生成する。
- detail drawer/sheet は期限精度 (`day` / `month` / `period` / `unknown`)、金額役割 (`outgoing` / `incoming` / `contract_reference` / `informational`)、担当、正本リンク、公式根拠、生成状態を表示する。不明額を0円表示しない。
- `company_payment_obligations` は `notification_owner='payment_obligation'` として既存通知台帳を所有する。運営カレンダー通知は `AMD_OS_SCHEDULE_NOTIFICATIONS_ENABLED='1'` の明示opt-in時だけ `company_schedule_notifications` の一意キーで送信する。
- migration `178_admin_operating_calendar.sql` の5テーブルは、facts/rules/occurrences/notificationsをservice_role writer、actionsを`requireAdmin`済みadmin APIがservice-roleでappend-only insertするwriterに限定する。browserからのauthenticated直書きは不可。middleware/proxyだけを認可根拠にしない。

回帰防止:

- `npm run test:admin-schedule` が法定期限の純粋日付計算、2026年の年間納付集計、公式URL、amount role、notification owner、年間運営UI、手入力禁止のanchorを検査する。
- 予定追加ボタン、カレンダー上の日付/金額/担当者編集、支払義務の重複通知を戻さない。ルールの変更は公式一次情報、版、確認日、テストを同じ束で更新する。

## /project/[projectId]/workspace — SX COO統合経営ダッシュボード

対象: `p21` を初期seedとする、PJ横断へ一般化可能な `project_management_*` 管理台帳。

必須機能:

- 初期表示は全体判定（順調 / 注意 / 危機 / 未評価）、理由最大3件、今週決めること最大3件、次期限、最終確認日、必須充足率を一続きで表示する。充足率だけで順調にしない。
- 目的 → 4本柱の成果目標 → KPI/先行指標 → マイルストーンをPJ内の関連情報として接続し、KPIに基準値・目標・実績・単位・閾値・判定ルール・測定日・頻度・根拠・確度を持たせる。`gte` / `lte` / `between` の判定はDB/API/計算ロジック/UIで同じ意味にする。
- 4本柱は同一軸（現在ゲート、derived状態、予定/予測/差分、担当、次の成果、最大論点、確認日、確度）で比較する。現在ゲートは完了済みの先頭に固定せず、未完了の依存順・開始状況・期限順で決定する。
- タイトル「事業化ロードマップ」は、データから導出した絶対期間（現在は2026年7月〜2027年3月、固定の「9か月」表記は使わない）を見出しに表示し、マイルストーン行選択から関連論点・複数仮説・証拠・検証・意思決定・次action・関係先・担当・依存先へ遷移できる。計画/予測/実績/仮置き凡例と遅延日数を文字で示す。390pxは期限順カード/縦ロードマップへ再構成する。
- 論点・仮説台帳は事実/仮説/決定事項を文言で分離し、根拠・反証/不足・次検証・担当・期限・確度・確認日を表示する。決定後はactionの担当・期限・完了条件・次回確認・完了証跡まで閉じる。
- 関係先リストは大学・金融機関・試作先・PoC候補先・VCを含む唯一の一覧で、独立したPoC/VCリストは持たない。`全関係先 / PoC候補先 / VC` は同じ台帳を絞る排他的タブ。VCは保存済みroleの`role_kind='shareholder_investor'`だけで判定し、名称から推測しない。PoC候補先/VCタブでは役割groupを比較主軸から外し、対象全件を1本の高密度registerへまとめる。主列は`関係先 / 現在の状況 / ゴール / 詰まり・PJ影響 / 次にやること / 担当・期限 / 現在地の根拠 / 接点の経緯 / 履歴・保有`（`接点の経緯`は日次で読まない履歴属性なので右端側に固定し、左から2番目へ戻さない）。`接点の経緯`は`connection_context`と`introducer_label`の固定2段で、個別接点の行為者や内部出典を紹介者へ流用しない。一覧は現在地stepと目標状態だけを別列で表示し、管理者は表示中の編集可能cellをその場で修正する。行全体や右端件数はボタンにしない。確認済み接点（古い順）→現在地→未完了work item→次の一手→ゴールの可変`進行状況`は、会社名下の短いsegment railだけから開く閲覧専用モーダルへ置く。railとモーダル内の進行blockは同じ配列から描画し件数・順序を一致させる。固定段階filter、固定分母、`x/7`は使わない。停止・期限超過・7日以内・保留・情報更新要・担当未確認・期限未設定は進行stepとは別の関係先単位判定として表示し、当方ボールは独立表示と要対応内の優先順に使う。情報更新要は確度未確認または最終確認14日超。要対応件数は該当先の絞り込み操作を兼ね、列見出しは長い一覧でも固定表示する。ボールは側と担当者を併記し、現在行の会社名だけを限定文型で「先方」へ短縮する。全履歴・保有事項は行下でなく閲覧専用モーダルへ出し、編集対象は一覧cellの表示値と一致させる。表示中集計は選択タブとquick filterへ追随する。p21のVCはPartners Fund、DAVP、BNV、いよぎんキャピタルを別partnerとして持ち、確認できない接触段階・合意・ボール・期限・ゴールは未確認のままにする。ユーザー向けラベルとして「次の受け渡し」「受け渡し先」は出さず、旧`next_ball_owner`/`handoff_to`列はDB互換のため内部に残すが編集フォーム・表示ラベルには出さない。
- 技術試験、資金スナップショット、組織役割、RACI、容量、field audit、更新履歴は実データを画面に描画し、各行から管理者が編集できる。週次エフォートも柱/ゲート/次の成果へ接続するが、経営判定の主役にはしない。
- derived判定はcritical/high KPIの欠落・閾値外・鮮度切れ・期限超過・停止・循環依存を必ず反映する。依存はDB triggerで同一PJと循環を検査し、必須/任意・lag・開始日を区別する。手入力statusはderivedを上書きしない。
- `project_management_*` だけを正本とし、旧 `project_sx_*` の二重台帳を作らない。重要表はsoft deleteとfield-level auditを持ち、authenticatedの物理DELETEを拒否する。member_selectは`deleted_at IS NULL`を必須にする。
- management APIはGET/PATCHに加えてPOSTを持ち、新規論点から仮説・根拠・検証・判断・actionまで親情報を指定して追加できる。portfolio/adminだけが作成し、project scopeは閲覧だけにする。raw本文、契約原文、報酬、メール本文、内部交渉メモ、source URLはDTO/UIへ出さない。
- `/project/:id/workspace` は月初合意の全画面ゲートを起動せず、GlobalNavサイドバーを使わない埋め込みシェルで表示する（GlobalNavが無いだけで、desktopではワークスペース内部専用の152px幅セクションナビ`lg:grid-cols-[152px_minmax(0,1fr)]`を持つ。mobile/tabletでは同じナビが横スクロールの上部帯になる。「左サイドバーが一切無い」という記述は誤り）。390pxでは期限順カード・縦ロードマップ、768pxではカード、lg以上で比較表を使い、選択中の経営文脈へ戻れるstickyセクションナビを置く。
- 運用準備は重要ゲート、KPI/技術試験、資金、役割/研究側、今週エフォートの5項目を不足件数つきで表示し、1件でも未確認ならトップ判定を未評価へ閉じる。
- Round 3の初期表示はヘッダー直後に経営判定を置き、390/768/1440で理由・最大の詰まり・今週決めること・次期限・鮮度/不足をスクロールなしで読む。運用準備の詳細は判定後に置く。stickyナビは横クリップで分断せず、管理者は非表示化した共有情報を一覧し、履歴を残して復元できる。決定済みとSX側の次アクションの必須項目はmigration 185のDB CHECKとAPIの合成検証で守る。
- Round 4では、成果目標・KPI・ゲート完了条件・資金/協力機関の可視文に残る内部語 `runway` を「資金残存月数」へ表示時変換する。関連ゲートはPJ内マイルストーンのID/slug共通mapで名称表示し、未知値は「名称未確認」へ閉じる。`audit:sx-management-visible-terms` はSCRIPT/serialized dataを除外した390/768/1440の可視DOM監査を再実行できる。
- Round 5（2026-07-22 SX_COO_DASHBOARD_SPEC_20260719.md準拠）: 上部4本柱信号帯（`SxReactorPanel`）は各柱ごとに 状態 / 証拠充足バー / 予定との差 / 次期限 / 最大の詰まり を同一ボタン内に同時表示する（`sxTrackEvidenceCompleteness` / `sxFormatDelta`）。証拠充足は担当・完了条件・測定済みKPI・鮮度確度の4チェックから算出し、進捗率の主観値を混ぜない。
- 三つの証明（`SxProofOutcomes`）は `PoC用リアクター仕様確定 / 確定仕様での動作確認 / ユニットエコノミクス証明` を独立カードで表示し、各カードに証拠充足率（全テーマ未評価なら「未評価」表示で0%にしない）・状態分布・判断期限・不足情報を持つ。7開発テーマ×3証明の接続はDB非依存の型付き中央マップ `src/lib/sx-proof-mapping.ts`（`SX_THEME_PROOF_MAP`）から導出し、7×3マトリクスで可視化する（`scripts/test_sx_proof_mapping.mjs`で全テーマが1つ以上の証明へ接続していることを検査）。
- 協力機関パイプライン（`SxPartnerPipeline`、2026-07-24 二車線ボール管制へ再実装）は全機関を1本の共有罫線台帳で表示する。優先度低・保留（`deferredLowPriority`＝primary roleの`relationship_state==='on_hold'`）の機関も末尾の`<details>`「保留・低優先（重要経路外）」内へ、他機関と同じ`PartnerRow`で1行表示し、全件管制からは消さない。地理名を付けた旧称は表示レイヤーで一切出さない（`sxPartnerDisplay`はDB値の`partner.name`をそのまま使い、slugベースの上書き辞書は廃止。DB側名称は migration 190 で既に「ファインケム」へ補正済み）。
- Round 6（2026-07-23 関係先台帳化、migration `191_sx_partner_ledger_upgrade.sql`）: `project_management_partners` へ `current_ball_side`（`sx`/`partner`/`shared`/`none`/`unknown`）・`current_ball_owner`・`next_ball_owner`・`target_state`・`due_date_precision`（`day`/`month`/`unknown`）を additive 追加し、新設の `project_management_partner_interactions`（面談/メール/合意/成果物/引き継ぎ/状況更新/メモの履歴、他 `project_management_*` 表と同水準の soft delete/RLS/parent guard/field audit）と合わせて、機関ごとに「誰がボールを持ち」「次に誰へ渡り」「いつまでにどの状態になるか」を初見で読める関係先台帳へ再設計した。`due_date`/`due_date_precision`（partners）と`occurred_on`/`occurred_on_precision`（interactions）はDB CHECK（`project_management_partners_due_date_consistency_191` / `project_management_partner_interactions_date_consistency_191`）とmanagement APIの合成検証（`assertDatePrecisionConsistency`）の両方で、unknown精度に日付あり・day/month精度に日付なしを拒否する。
- Round 8（2026-07-24 COO向け二車線ボール管制、migration `192_sx_partner_role_and_work_items.sql`。COO/研究開発/UIUXの3監査でP0不合格 → 同日中に是正した確定版）: 新設の `project_management_partner_roles`（役割`role_kind`×関係状態`relationship_state`の多対多正規化、1partnerにつき`is_primary=true`は有効行内で最大1つ＝unique partial index `project_management_partner_roles_one_primary_192`、`source_kind`/`source_ref`列はAPI契約と揃えてCREATE TABLE時から必須＝`project_management_partner_roles_source_kind_check_192`）と `project_management_partner_work_items`（当方/先方/双方/未確認の`side`別に task/question/deliverable/decision/approval/response を複数保持する保有事項台帳、`completion_criteria`/`handoff_to`/`related_milestone_id`はnullable、DB CHECK `project_management_partner_work_items_due_date_consistency_192`。加えて`completed_on`/`completion_evidence`/`accepted_by`/`accepted_on`で完了を監査可能にし、`status='completed'`は`completion_criteria`/`completion_evidence`/`completed_on`必須＝`pm_partner_work_items_completion_check_192`、`item_kind='deliverable'`かつ完了は`accepted_by`/`accepted_on`も必須＝`pm_partner_work_items_acceptance_check_192`（2026-07-24 4th audit是正: 旧名は65/64byteでPostgresの63byte識別子制限に抵触し実DBでは黙って切り詰められていたため短縮名へrename、legacy truncated名からのRENAME CONSTRAINTをidempotentに実行）。DB CHECK・API `assertWorkItemCompletionRequirements`・型・編集フォームで揃える）、既存 `project_management_partner_interactions` への `actor_side`/`actor_label` additive追加（DB CHECK `project_management_partner_interactions_actor_side_check_192`。「行為主体」は`ball_side_after`から逆算しない別データ、既存p21 seed のうちcurrent truthとして確定できる4件だけbackfill: NDA=shared、試作リアクター製作完了=partner、初回面談=shared、窓口移管方針=sx。「2026年8月の納品予定を確認した」はボール保持側から推測できないため`unknown`のまま明示的に残す）で、機関ごとの現在値を「当方が抱える保有事項｜中央の受け渡し｜先方が抱える保有事項」の二車線へ再設計した。role/work item seedのWHERE NOT EXISTSは`source_ref`単独判定（`deleted_at IS NULL`条件を外した）にし、ユーザーが後からsoft-deleteした行を再適用で新規active行として復活させない。verification（DO $$ブロック）も同じくsource_ref基準の履歴件数で判定し、正当なsoft-delete後の再適用を失敗させない。`role_kind`は既存自由記述から推測せず未確定は`unclassified`、p21 current truth (`source_ref='user:2026-07-24#partner-role-normalization'`) は愛媛大学=大学・研究機関、SMBC=金融機関、Partners Fund=株主・投資候補（`relationship_state='candidate'`を明示指定）、ダイキアクシス/EWIR候補機関A/ファインケムは`unclassified`のまま。既存 `project_management_partner_commitments`（相手の約束/SX側の次アクションの境界）は不変で、UIでは`sxHoldingsForPartner()`（`pwa/src/lib/sx-partner-holdings.ts`）が有効commitmentを対応側の保有事項へ統合表示するだけ。management APIに resource `partner_role`/`partner_work_item` を追加し、`PARENT_FIELDS`で`partner_id`（両方）と`related_milestone_id`（work_itemのみ、任意）の親PJ検証を行う。`related_milestone_id`の別PJ guardは`scripts/test_project_management_rls.mjs`で、partner_idは自PJ有効だがrelated_milestone_idだけ他PJのゲートを指す直接probeを持つ。partner role/work item seedはp21主分類role 6件・保有事項4件（ダイキアクシス2件・SMBC2件）を`source_ref='user:2026-07-24#partner-role-normalization'`/`'user:2026-07-24#partner-work-items'`で冪等投入し、再適用可能である。
- Round 9（同日、監査是正後のCOO集計・二車線/履歴UI確定版）: 管制帯（`sxComputeControlBandCounts()`）を単位別へ再設計し、`totalPartners`/`activePartners`/`deferredPartners`/`unclassifiedPartners`はpartner単位の品質集計として保留（`deferredLowPriority`）を含む全関係先を対象にし、`totalHoldings`/`sxHeld`/`partnerHeld`/`sharedHeld`/`sideUnknown`/`overdue`/`dueSoon`/`dueMonthPrecision`/`dueUnset`/`ownerUnconfirmed`/`bothSidesHeldPartners`はholdings単位でactive（非deferred）partnerの保有事項だけを対象にする（緊急期限系だけdeferredを明示的に除外）。`overdue`/`dueSoon`は`due_date_precision='day'`のみを対象にし（`sxIsHoldingOverdue`/`sxIsHoldingDueSoon`）、month精度は`dueMonthPrecision`という別バケットへ分離、Holding表示（`HoldingRow`）も同じ判定を使うため未来のmonth精度期限は赤くならない。初期表示は`sxGroupPartnersByPrimaryClassification()`でactive partnerを主分類（`roleKind`×`relationshipState`）ごとにgroupByし、見出し・件数・分類内は共有の表示ソートキー（`sxPartnerDisplaySortKey`＝active holdingの直近期限→partner期限→未設定、行の表示期限と完全一致）で並べる。「共同開発先」と「共同開発候補先」のように同じ`roleKind`でも`relationshipState`が違えば必ず別groupにし、`unclassified`は常に末尾。`CategoryNav`は補助filter chipとして残り、選択すると対象`roleKind`のgroupだけに絞る。保留グループは末尾の`<details>`に同じ`PartnerRow`で残し、`opacity`は一切使わず`border-l-4`＋淡い背景＋「・保留」文言で示す。二車線列は390px基準を`grid-cols-[minmax(0,1fr)_56px_minmax(0,1fr)]`（gap 4px, `gap-x-1`）へ修正し、旧`grid-cols-[44%_12%_44%]`（列合計100%+gapでクリップしていた）を廃止。768/1440は維持。共有保有事項は左右へ複製せず、行の下に中央をまたぐ1本の全幅行（`sharedHoldings`）として1件だけ表示し、`side=unknown`も件数バッジ止まりをやめてタイトル・担当・期限を持つ専用全幅行（`unknownHoldings`）へ必ず表示する。保有事項は角丸カードをやめ、共有罫線＋左ステータス線（`HOLDING_STATUS_LINE`）の高密度行（`HoldingRow`）にし、当方/先方の各lane初期表示は期限昇順の優先2件＋「ほかN件」ボタン（クリックで台帳の詳細・編集へ展開）。各保有事項行は`relatedMilestoneId`からゲート名（`gateAndProofForItem`）を表示し、そのゲートが7開発テーマに該当すれば証明名まで解決、未接続は項目単位で「ゲート未接続」。やり取り履歴は本文（`InteractionTimeline`）を分類編集/保有事項編集/約束カードから分離し、別`<details>`「台帳の詳細・編集」（`PartnerLedgerDetails`）へ移した。履歴イベント行（`InteractionRow`）はmobile中央列を56〜60px（`grid-cols-[minmax(0,1fr)_60px_minmax(0,1fr)]`）、md以上は130pxへ広げ、`unknown`行も`shared`行と同じく要約/outcomeを中央をまたいで必ず表示する（旧: 件数注記のみを廃止）。編集ボタンは44px角アイコン（`h-11 w-11`）にし、aria-labelへ関係先名と対象名/要約を含める（`${partnerName} - ${summary}を編集`等）。text-[9px]は全廃し10px以上に統一、行全体の`opacity`は使わずbackground/文言/線だけで保留を示す。二車線集計/分類/グルーピングの純粋関数（`sxHoldingsForPartner`/`sxComputeControlBandCounts`/`sxPrimaryRoleKindCounts`/`sxRoleDisplayLabel`/`sxGroupPartnersByPrimaryClassification`/`sxPartnerDisplaySortKey`/`sxIsHoldingOverdue`/`sxIsHoldingDueSoon`/`sxIsHoldingMonthPrecision`/`sxIsHoldingDueUnset`）は`pwa/src/lib/sx-partner-holdings.ts`へ分離し、`sx-management.ts`（`import "server-only"`）を跨がずclient component（`sx-visual-shared.tsx`が re-export）とテストの両方から使える。build v3.49.0。
- Round 10（同日、v3.49.0是正版に対する2回目のUIUX/COO再監査是正）: やり取り履歴行（`InteractionRow`）を旧3レーングリッド＋角丸色面ブロックから、shared/unknown/通常すべて共通の1イベント1本・全幅罫線行（`min-h-[64px]`、上下2段なし）へ全面書き換えし、同一行に日付・種別／行為主体（`共同`/`行為主体未確認`/actorLabelまたは`当方`/`先方`）／要約・結果（`actor_side`に関わらず常にフル表示）／以後のボールを横並びで持ち、44px編集ボタンは行末へ独立配置（本文の行高に影響しない）。保有事項の並びは期限昇順のみから`sxSortHoldingsByPriority(items, today)`（`pwa/src/lib/sx-partner-holdings.ts`。blocked最優先→day精度期限超過→day精度→month精度→期限未設定の5tier）へ変更し、停止中の項目が期限の都合で「優先2件」の枠外に押し出されないようにした。共有/未確認の保有事項も当方/先方と同じ`HoldingsCell`（優先2件＋「ほかN件」）へ統一し、無制限列挙をやめた。行を展開すると「台帳の詳細・編集」が既定で開き（`open`属性）、新設の`sxAllHoldingsForPartnerAudit(partner)`（全ステータスのwork itemを`SxHoldingItem`形へ、完了・停止も含む）による「保有事項の詳細（完了・停止を含む全件）」一覧が権限に関係なく全員へ表示される。`SxHoldingItem`型に`detail`/`completedOn`/`completionEvidence`/`acceptedBy`/`acceptedOn`を追加し、値がある場合は誰でも読める。編集ボタン（`Pencil`アイコン）だけ`canManage`条件。`sxRoleDisplayLabel`は`unclassified`でも常に関係状態と複合表示（「未分類・候補」「未分類・成立」等、旧・状態を無視した一律「未分類」は廃止）し、`in_progress`/`established`のように複合ラベルが同じ文字列（「XX先」）になる分類済みroleも、分類グループ見出し（`<h4>`＋`（状態）`併記）と各行の役割行（`・${state}`併記）の両方で状態を必ず視覚区別する。`relationship_state='ended'`を`sxIsPartnerEnded()`で判定し、`deferredLowPriority`（`on_hold`）とは別単位として`activePartners`/holdings系集計から除外しつつ`totalPartners`には残し、末尾に専用`<details>`「終了（対応中から除外）」を追加、保留・終了どちらもCategoryNavの`activeRoleKind`絞り込みを継承する。管制帯（`ControlBand`）を「母数／ボール／緊急」の3行へ再構成（mobileは行ごとに`overflow-x-auto`の単一行横スクロール）し、`endedPartners`（終了）・`unorganizedPartners`（当方/先方いずれかのレーンの保有事項が0件で「確認済み0件」と断定できない未整理機関数）・`organizedCoveragePct`（両レーンとも確認できているactive機関の割合）・`blockedHoldings`（停止件数、side問わず）を追加。「未完了保有事項」→「未完了事項」、「双方に保有ありの関係先」→「双方保有先」に短縮。当方/先方レーンの空表示は「まだないよ」（確認済みゼロと誤読される文言）をやめ「未整理（確認未実施）」にした。`CategoryNav`は`aria-pressed`・選択チップの`✓`グリフ（色だけに依存しない選択表現）・`focus-visible`リング・mobile単一行横スクロールを追加。各機関行は`<article aria-labelledby="sx-partner-name-${partnerId}">`へ、分類グループ見出しは`<p>`から`<h4>`へ変更した。純粋関数側は`sxSortHoldingsByPriority`/`sxAllHoldingsForPartnerAudit`/`sxIsPartnerEnded`を`pwa/src/lib/sx-partner-holdings.ts`へ追加し、`npm run test:sx-partner-holdings`へblocked優先tier・ended判定・audit全件包含・unclassified状態複合・endedPartners/unorganizedPartners/organizedCoveragePct/blockedHoldings集計のテストを追加した。
- Round 11（同日、4回目のUIUX/COO再監査是正）: `InteractionRow`のmobileグリッドを`grid-cols-[56px_minmax(0,1fr)_64px_44px]`（日付/本文/ボール/編集）へ、行為主体は独立列を持たず本文先頭の小ラベルに統合。`md`以上は`md:grid-cols-[56px_68px_minmax(130px,1fr)_64px_44px]`で行為主体を独立68px列へ戻し、本文列は最低130px確保。日付・行為主体・要約・結果・ボールはすべて`truncate`+`title`属性で全文が読める（外部閲覧者もsummary/outcomeを常時視認可能なのは維持）。行高は`h-16`固定＋`overflow-hidden`。`InteractionTimeline`のヘッダーを`grid-cols-[minmax(0,1fr)_44px]`へ再構成し、左は集計を横一行`overflow-x-auto`（`mobile折返し禁止`）＋右端フェード、右は44px角アイコンの追加ボタン（テキストボタンから変更）。「台帳の詳細・編集」の`<details>`から`open`属性を外し、履歴を開いても既定では閉じた44pxの見出しのみ表示、ユーザー操作で開く（履歴本体には角丸カードを戻さない）。`ControlBand`の順序を「緊急→ボール→母数」へ並び替え（旧: 母数→ボール→緊急）、`ControlBandRow`/`CategoryNav`の横スクロール領域に右端フェード（`[mask-image:linear-gradient(...)]`、背景色に依存しない）と`tabIndex=0`のキーボード到達性を追加。分類グループ見出し`<h4>`を10px→11pxへ、見出し行に`border-l-4`の左罫線を追加。低コントラストな`#777166`（10px使用箇所）を`#69665d`へ統一。管理者向け「保有事項を追加」ボタンを`min-h-11`のテキストボタンから`h-6 w-6`のアイコンのみボタンへ圧縮し、初期行高を押し上げないようにした。management APIのPATCHを、通常編集では`meta.hasSourceRef`な種別に`source_kind='manual'`/`source_ref='PWA共有管理画面'`を再スタンプするよう戻した（直近ラウンドで「編集してもsource不変」に倒していたのを、field auditが手動編集を示さなくなる問題として再監査で指摘され是正。soft-delete/restoreは引き続きsourceを変えない。固定seed idなので復活はしない）。migration 192のやり取り履歴backfill検証を、固定count(4)依存からライブ行の状態判定（`source_ref`＋既知summaryでlive かつ`actor_side='unknown'`が1件でも残っていればFAIL）へ変更し、ユーザーが編集・削除した行があっても正しく再適用できるようにした。`sxPrimaryRoleKindCounts()`は保留・終了を除外せず全partnerを集計するよう変更し、対象が保留・終了機関しかいない分類のCategoryNavチップが二度と出ない問題を解消（filter到達可能性の担保）。新設`sxPartnerHasBlockedHolding()`/`sxPartnerPrioritySortKey()`（`pwa/src/lib/sx-partner-holdings.ts`）で、停止中の保有事項を持つ機関を分類内・分類間ソートの両方で最優先にし、行に「停止」バッジを追加。管制帯の「未整理」を「台帳0件先」、「組成率」を「登録率（対応中N先中）」へ改称し、当方/先方レーンの空表示を「未整理（確認未実施）」から「台帳0件（未整理/確認済み0件は未区別）」へ変更（reviewed flag相当の列が無いデータなので、レーンが空であることを「確認済みでゼロ」と断定しない）。reviewed flag追加のDDLは今回実施しない。`scripts/test_project_management_rls.mjs`のprovenance probeを新しいsource再スタンプ契約へ更新し、`project_management_partner_work_items`のsoft-delete→migration再適用相当probeを追加（completion/related_milestone_id等が変わらないことを検査）。`scripts/test_sx_partner_holdings.mjs`に`sxPrimaryRoleKindCounts`の新挙動・`sxPartnerHasBlockedHolding`・`sxPartnerPrioritySortKey`・blocked優先グルーピングのテストを追加。build v3.49.1。
- Round 12（同日、最終監査残件の是正。build versionは変更なしv3.49.2のまま）: `PartnerLedgerDetails`の「約束・次アクション」カードへ、status（open/in_progress/completed/blocked/cancelledを`HOLDING_STATUS_TONE`/`HOLDING_STATUS_LABEL`で識別）・`completedOn`・`nextReviewOn`・`confidence`（`sxConfidenceLabel`）・`promisedOn`と`dueDate`の両方・相手/SXの両ownerを常に表示するよう拡張（旧: commitmentKindで片方のownerだけ条件表示していたのを両方併記に変更）。`commitment.evidence`は引き続き「一次根拠」とだけ表示し「完了の証拠」とは呼ばない。`SxHoldingItem`（`pwa/src/lib/sx-partner-holdings.ts`）へ`sourceKind`/`sourceRef`/`lastVerifiedAt`/`confidence`を追加し、work item由来・commitment由来ともネイティブの4フィールドをそのまま転写する（Round 14で判明: この時点の実装は`SxPartnerCommitment`型/`mapCommitment()`が`source_ref`列を転写しておらず、commitment由来は誤って`sourceRef`常に`null`固定になっていた。実際には`project_management_partner_commitments`も`source_ref`列を持つ）。新設`sxSourceKindLabel()`/`sxSourceRefDisplayLabel()`（raw URLは「外部リンク（詳細は編集画面で確認）」に、`user:YYYY-MM-DD#topic`形式の内部tracking keyは「手動記録 YYYY-MM-DD」に整形し、rawなsource_refやURL・secretを画面に漏らさない）を追加し、`HoldingRow`（開いた優先2件・全件監査リスト両方で共有）に「出典: ○○（△△）・最終確認 YYYY/M/D・確度 高/中/低/未確認」の行を追加した。横スクロール領域の`SCROLL_HINT_CLASS`/`ALWAYS_SCROLL_HINT_CLASS`から、末尾コンテンツ自体を薄くしてしまう`mask-image`によるcontent-fadeを撤去し、十分な右paddingと`focus-visible`リング、新設`ScrollHintArrow`（aria-hidden・pointer-events-noneの非フェード矢印`▸`、`ControlBandRow`/`CategoryNav`はsm+で非表示、`InteractionTimeline`集計ストリップは常時表示）へ置き換え、末尾指標のコントラストを落とさずにスクロール可能性を示す。mobile touchでも`InteractionRow`のtruncateされた全文へ到達できるよう、既定で閉じた「台帳の詳細・編集」内に新設`InteractionFullRow`による「やり取り履歴（全文・全件）」一覧を追加（occurred date/kind・行為主体・summary/outcome全文・以後のボールを非truncateで表示、編集ボタンのみ`canManage`）。`InteractionTimeline`の初期timeline 3件+header<=260予算、`InteractionRow`のh-16固定はそのまま維持。`scripts/test_project_management_rls.mjs`の2件の残存問題を修正: (1) provenanceプローブが、実DB上のseed行が既に`manual`/`PWA共有管理画面`になっている場合に偽失敗していたのを、`BEGIN`内で先にprobe行のsourceを`current_truth`/`rls-test-fixture`へ強制リセットしてから通常編集相当の更新・soft-delete/restoreを検証する形へ変更し、standalone「before」SELECTとその否定assertを削除。(2) やり取り履歴actor_side backfillの検査を、固定`count(*) >= 4`から、migrations/192のverification DO блокと全く同じ「live（`deleted_at IS NULL`）かつ既知`source_ref`+既知summaryなのに`actor_side='unknown'`が0件」判定へ変更し、正当な編集でsummary/source_refが変わった行やsoft-delete済み行を検査対象から自然に除外する（固定件数依存を撤廃）。`scripts/test_sx_partner_holdings.mjs`にprovenanceフィールドの正規化（commitmentのsourceRef=null固定含む）・`sxSourceKindLabel`・`sxSourceRefDisplayLabel`のunit testを追加。
- Round 13（2026-07-24、COO最終差し戻し3点の是正。build v3.49.2据置）: `InteractionTimeline`の表側previewを`sorted`全件描画から`preview = sorted.slice(0, 3)`固定へ変更し、件数が3件を超えても「3件+header<=260」の静的高さ予算を割らないようにした（旧実装は件数が増えるほど予算を超えていた）。header集計（記録/合意/成果物/引継ぎ）は`sorted.length`/`partner.interactions`全件基準のまま変えず、`sorted.length > 3`の時だけpreview直下に「表示3 / 全N件・全文は「台帳の詳細・編集」へ」という短い注記を追加。既定閉の「台帳の詳細・編集」内`InteractionFullRow`による「やり取り履歴（全文・全件）」一覧は変更しない。`InteractionTimeline`ヘッダーのgridを`canManage && onAddInteraction`（`canAddInteraction`）で分岐させ、真のときだけ`grid-cols-[minmax(0,1fr)_44px] gap-2`で44px追加ボタン列を確保し、read-only（`canManage=false`または`onAddInteraction`未指定）は`grid-cols-1`のみでgap/空44px列を持たない（`InteractionRow`が既に持つread-only grid切替と同じ設計）。`PartnerRow`の「台帳の詳細・編集」`<summary>`文言を`canManage ? "台帳の詳細・編集" : "台帳の詳細"`へ分岐し、編集導線が無いread-only閲覧者へ「編集」を含む文言を見せないようにした。`ScrollHintArrow`の矢印色を低コントラストな`#a49d8c`から`#69665d`へ統一。
- Round 14（2026-07-24、RD最終差し戻し1点の是正。build v3.49.2据置）: Round 12で「`SxPartnerCommitment`型に`source_ref`列が実在しないので`sourceRef`は常に`null`」と実装していたのが誤りで、`project_management_partner_commitments`は`source_ref`列を持ち、management APIの`live(...)`クエリも既に`source_ref`を含めて取得していたのに、`SxPartnerCommitment`型（`pwa/src/lib/sx-management.ts`）と`mapCommitment()`がその値を転写しておらず、`commitmentToHoldingItem()`（`pwa/src/lib/sx-partner-holdings.ts`）が常に`sourceRef: null`で捨てていた。`SxPartnerCommitment.sourceRef: string | null`を追加し`mapCommitment()`が`nullableString(row, "source_ref")`を転写、`commitmentToHoldingItem()`が`commitment.sourceRef`をそのまま`SxHoldingItem.sourceRef`へ転写するよう修正（`HoldingRow`は元々`sxSourceKindLabel()`/`sxSourceRefDisplayLabel()`経由で安全表示していたため、上流のデータ欠落だけが原因）。`PartnerLedgerDetails`の「約束・次アクション」全件カードにも`HoldingRow`と同じ形式の「出典: ○○（△△）・最終確認 YYYY/M/D・確度 高/中/低/未確認」行を追加し、`sourceRef`はここでも`sxSourceRefDisplayLabel()`を必ず経由してraw値を出さない。`scripts/test_sx_partner_holdings.mjs`の「commitmentは`source_ref`列を持たないのでholdingは常にnull」という誤前提のテストを、実際に転写された値・値が無い場合はnull据え置き・safe label masking（URL/内部tracking key双方）の3点を検証する内容へ書き換えた。
- Round 15（2026-07-24、追加是正1点。build v3.49.2据置）: `InteractionTimeline`の`sorted.length > 3`注記（「表示3 / 全N件・全文は「台帳の詳細・編集」へ」）が、Round 13で`PartnerLedgerDetails`の`<summary>`文言を`canManage`分岐にした際に同期されず`canManage`に関わらず常に「台帳の詳細・編集」固定のままだったのを是正し、`{canManage ? "台帳の詳細・編集" : "台帳の詳細"}`へ分岐して`<summary>`の実在ラベルと一致させた。critical UIに両分岐のanchor（`'全文は「{canManage ? "台帳の詳細・編集"'`/`': "台帳の詳細"}」へ'`）を追加。
- 低頻度面（運用準備・4本柱詳細比較表・目的-KPI・意思決定の記録・判断ループ・測定/資金/体制・非表示履歴の復元）は `id="management-ledger"` の単一`<details>`「管理台帳・編集」へ集約し、個別の折りたたみボックスを並べない。各サブ機能（`ObjectiveKpiSection` / `DecisionLoopSection` / `MeasurementSection` / `DeletedManagementSection` / CREATE_RESOURCES新規追加）は集約後も同一のまま動作する。
- Round 8（2026-07-25 経営状況図、v3.49.9、`SxExecutiveControlDeck` 再設計）: 初期画面のPJ管制盤を、文章カードと小区画の集合から一続きの経営状況図へ再構成した。読み順は `経営判定（判定語を帯内最大サイズの色面で表示） → 4本柱信号 → 経営状況図 → 次の経営介入`。経営状況図は重要経路を一方向レールに描き、ノード間隔は予測日ベースの日数比例（`deriveSxStateMap` の `gapDaysFromPrev` をgrid frへ変換、min幅floorあり）、`今日`マーカー（`asOf`）を現在地ノード直前に置く。各ノードは 状態記号+状態ラベル / 柱アクセント色（点の縁色+柱略称テキスト、色だけに依存しない） / 予定→予測 / 遅延幅バー（共通スケール `SLIP_PX_PER_DAY=2`・上限56日、`SlipBar`。差0以下はバーを描かず文字のみ） / 担当 を持ち、仮日程は破線・現在地は塗りチップ+左太罫で示す。停止・期限超過・技術試験停止・検証/対応停止・相手先保留・関係先対応待ち・論点滞留（`FLAG_KINDS`）は該当ノード直下の旗（`sx-blocker-flag`、ボール側+実名+期限）として物理接続し、`milestoneId` が可視ノードへ解決しない行は `経路ノード未接続の対応待ち`（`sx-unattached-blockers`）へ明示する。担当未確認/鮮度切れ/評価未完はノード上のstate mark（担当未確認はノードの担当行へランクバッジを統合）として描き、第三者旗にしない。次の経営介入は上位3件へ絞り、①②③の番号（`RankBadge`）を状況図の旗・マークとリストで共有する。直近アクションは1行の薄い帯（`sx-upcoming-queue`）へ降格し、対応待ちとの2枠並置をやめた。デスクトップ（lg+）は状況図→介入、lg未満は介入→状況図（縦レール `sx-critical-path-rail-vertical`、時間は上→下）の順。ボール表示は `ballDisplay` で「未確認・未確認」「担当 担当未確認」の重複を作らない。
- Round 9（2026-07-25 統合タイムライン、v3.49.10。まさ差し戻し「ほとんど改善されていない」+PM/UXUI両サブエージェント監査を受けた全面再構成）: ページ内に3枚あった時間軸（Round 8の経路レール / 事業化ロードマップ`SxNineMonthTimeline` / 折りたたみ全件ガント）を、1枚の統合タイムライン（`SxUnifiedTimeline` + `deriveSxUnifiedTimeline`）へ統合し旧3枚を廃止。初期画面は `判定バー → 統合タイムライン → 今週の意思決定+次の経営介入` の3帯。判定バー（`sx-verdict-bar`/`deriveSxVerdictSummary`）は業務判定（停止>期限超過>遅延見込み。未評価だけの経路をオンスケにしない）と運用判定（判定不能・充足%・重大未確認）を分離し、STEP2消化（金額未確認なら「未確認」、ランウェイ機構は作らない=まさ確定 2026-07-25）と設立判断までの残日数を並べる。統合タイムラインは月グリッド・今日線・設立判断旗の共通座標系に柱4レーン（事業→技術→体制→資金。レーンヘッダーへ旧4本柱信号の予測差・詰まりを吸収）×日付付き全マイルストーン（バー=計画開始→予定、縦線=予定日、◇=予測日・中抜き=仮、橙バー=遅延幅）を置き、重要経路は太字+黒左罫+行間の破線接続（`criticalPoints`をSVG polylineへ、行高固定44px=グローバル操作領域規則とy座標を一致）。①〜⑤ボールピンは各介入の自身の期日位置（橙=相手側/黒=当方、月精度はリング、近接は`MIN_PIN_GAP_PCT`で位置だけ補正）。日付未登録・完了は描かず件数で明示（`undatedCount`/`completedCount`）。介入キューはtop5へ拡張し、重要経路外の柱の現在ゲート（`pillarGates`、停止/期限超過/担当未確認/鮮度切れ/評価未完のみ・行の発明なし）を同じキューに載せ、最大遅延柱のクォータ（`applySxInterventionPillarQuota`、候補ゼロなら`sx-quota-note`）で資金+35日が初期画面から消えないようにした。今週の意思決定（`sx-decision-queue`）は論点台帳の意思決定待ちと同じ判定で全件昇格。旧・直近アクション1行帯は廃止。ラベル列は内部横スクロール中もsticky固定。三つの証明は3行ストリップ+共通不足バナー（`sx-proof-shared-missing`）へ圧縮し7×3は既定閉のdetails付録へ、7テーマ表は経路ストリップ重複を削除し「未登録」系の赤を灰へ（赤=事業リスク専用）。全体構成は 経営サマリー→計画詳細（全マイルストーン詳細表+選択文脈、id=`management-plan`維持）→技術証明（`management-proof`新設）→論点・仮説→関係先→実行・体制（週次エフォート入力はサマリー付き`sx-effort-entry-details`へ降格、研究側メンバー未確認の警告は維持）。ヘッダーの鮮度表記は基準日+データ最終確認の2値へ縮約し、内部コード`p21`の露出を削除。共有面の「〜だよ/〜してね」口調を常体へ。批評の正本はehm-os `SX_DASHBOARD_CRITIQUE_20260725.md`。同日v3.49.11で再監査是正: 三証明バーの未評価塗り廃止（0評価の満杆誤読禁止）/ 柱詰まりは時間エリア側レーンヘッダーで全文表示 / 予測差ラベルは紙色背景で破線と重ねない / 重要経路最終行にも予測差0日 / ボール未確認ピンは白抜き / STEP2消化セルは管理台帳資金欄への導線を持つ / 論点台帳は未決decision保有行へ「意思決定待ち」バッジ併記（ヘッダ件数と行タグ一致）/ 見出しは「意思決定待ち（期限順）」。
- Round 10（2026-07-25 まさフィードバック3件、v3.49.12）: **(1) 遅延語の3状態分離** — `sxEcdClassifySlip`（純粋関数）が `overdue`（予定日超過で未完了）/ `confirmed_slip`（予測>予定かつ日付確定 or 見直し理由が実質登録済み）/ `provisional_slip`（予測>予定だが仮日程で理由も定型・未確認）/ `none` を返す。`isPlaceholderForecastReason` が「未確認」「仮置き」「初期Seed」を含む理由を根拠なしとみなす。判定バーは confirmed_slip があるときだけ「遅延見込み +N日」（warn）、仮置き差だけなら「予測差 +N日」+detail「期限超過なし・仮日程の見込み差」（unknownトーン）へ落とす。タイムラインの遅延幅バーとラベルも `sxFormatSlip` で 赤=期限超過 / 橙=根拠のある遅延見込み / 灰=仮日程どうしの予測差 に分け、凡例へ明記。p21の全9マイルストーンは7/14投入の `forecast_change_reason='初期Seed。予測日は仮置きで、変更理由は未確認'` なので、実測の遅延ではなく仮置き差として中立表示になる。**(2) ゲート詳細はモーダル** — タイムライン行クリックで `MilestoneDetailModal`（`sx-milestone-detail-modal`）を開く。旧実装の `#selected-management-context` へのスクロール移動は廃止（図から視線が飛ぶため）。モーダルは Esc / 背景クリック / 閉じるボタンで閉じ、開いた瞬間に閉じるボタンへfocus。ヘッダーに 予定/予測/日程の確度/担当、直下に `予測日の根拠:`（forecast_change_reason）を表示し、本文は既存 `SelectedMilestoneContext` をそのまま使う。**(3) 図から手動で追加・編集** — 管理者はレーンヘッダーの `＋`（hover/focusで出現、`onCreateMilestone(track)`）でその柱にマイルストーンを追加、行の `✎`（同）で日程・担当を編集、モーダルヘッダーの「日程・担当を編集」からも同じ `EditPanel` を開く。計画詳細節の詳細表にも `sx-plan-manual-edit`（マイルストーンを追加 / 依存関係を追加 / 選択中ゲートを編集）を置く。いずれも既存の management API（resource `milestone` / `dependency`）を使い、新しい書き込み経路は作らない。
- Round 11（2026-07-25 実使用フィードバック5件、v3.49.25）: **(1) 方向が読める遅延語** — `sxFormatSlip` を「予定より N日後ろ（仮置き）/ 予定より N日遅れ見込み / 期限超過 / 予定より N日前倒し」へ改訂（「予測差+N日」は方向が読めないため全廃）。判定バーの仮置きだけのケースは「実際の遅れなし」+「仮置きの見込みは予定より最大N日後ろ」（中立トーン、緑にしない=仮日程を健全へ変換しない原則）。**(2) 破線の自己説明** — 重要経路破線の最初の区間中点へ「重要経路の順序」ラベルを直書きし、凡例を「破線=重要経路の順序（前のゲートが終わってから次へ進む依存のつながり）」へ。**(3) 関係の流れ**（`PartnerJourneyFlow` / `sx-partner-journey`） — 各関係先行の7列register直下に これまで（やり取り履歴を`occurredOn ?? createdAt`昇順、種別+精度付き日付+要約）→ 現在地（ボール側+実名+期限。当方ボールは赤二重枠）→ これから（次の一手）→ ゴール（目標状態、未登録は未登録表記）の一本線。ステップの発明はしない。**(4) 当方ボール強調** — `currentBallSide='sx'` 行は行頭に白抜き赤バッジ「当方ボール」+ `border-l-4` 赤左罫 + 淡赤背景、介入キューのボール列は「当方ボール・実名」を太字赤。`sxPartnerPrioritySortKey` を blocked(0) > 当方ボール(1) > その他(2) の3tierへ拡張（「ボール持ったまま待つことは基本的に許されない」2026-07-25 まさ確定）。**(5) 三つの証明の位置づけ1行**（`sx-proof-positioning`） — 論点・仮説台帳（経営の検証ループ・全柱）との役割分担を本文へ常設。
- Round 12（2026-07-27 実使用フィードバック6件、v3.49.31）: **(1) 遅延語は「遅れ/前倒し」の二択** — 「予定よりN日後ろ」は方向が伝わらないため全廃し、`sxFormatSlip` は 期限超過 / 予定よりN日遅れ見込み / 予定よりN日遅れ（仮置き）/ 予定どおり / 予定よりN日前倒し のみを返す。判定バーのdetailも「仮置きの見込みは予定より最大N日遅れ」。**(2) ピンはhover表示** — ガント上部の①〜⑤はクリックで下方向へスクロールさせず、hover/focusで `sx-pin-hovercard`（対象・ボール・期限・止まるゲート）を出す。`onPinClick` は廃止し、pinへ `gate` を持たせた。**(3) 人名は名字のみ** — `sx-name-normalize` の対応表をフルネームから名字へ（まさ→山地、かる→輕部、ちこ→遠藤）。**(4) SMBCの事実訂正** — 「窓口移管方針を確認した」は未合意（石原先生から確認が出ただけ）なので非表示化し、進捗は初回面談までへ戻す。ゴールをEWIR参画へ。**(5) Partners Fund** — ゴール=出資確定、その前提としてデューデリジェンスをwork itemとして登録。**(6) PoC候補先リスト**（`sx-poc-board` / `management-poc` 節） — 多量排出事業者リスト（2026-06-23）由来の候補38件と、ルート別状況（2026-07-21）由来の接触先12件を既存の管理API経由でpartnerとして登録し、排液の調達に近い順（調達済>合意済>相談中>未接触）のボードで一覧。未接触の候補は7列の関係先台帳から外し（`sxIsUntouchedPocCandidate`）、台帳には件数と導線だけ残す。母集団・必要社数は台帳に無いので画面へ書かない。
- Round 13（2026-07-27 実使用フィードバック5件、v3.49.33）: **(1) 計画バーを塗らない** — 実線バーが「今日線より右まで終わっている」と読まれたため、計画期間は枠線だけで描き、内側の塗りは登録済み `progressPct` からのみ出す（p21は全9件0%・未評価なので枠だけになる）。凡例も「バーの枠=計画期間（開始→予定。終わった分ではない） ・ 枠内の塗り=登録済みの進捗」へ。**(2) 破線の呼称** — 造語の「重要経路の順序」をやめ「前提のつながり」へ。凡例は「前のゲートの結果が次の前提になる。台帳の依存登録から描画。日程上は並行して進む区間もある」とし、finish_to_startの断定をやめた（p21の登録6件のうち4件は後続の計画開始が先行の計画終了より前で、FSとして矛盾している）。**(3) 「設立判断」→「設立」** — 設立自体はAMD参画時点で決定済み（2026-07-27 まさ確定）なので、残日数・旗・見出し・目的日ラベルから「判断」を外す。**(4) 関係の流れへ未完了の保有事項** — 「これから」を次の一手だけでなく未完了work items（例: 出資確定の前提であるデューデリジェンス）を期限順で並べる形へ。完了・中止は出さない。**(5) 関係先7列目** — 履歴の省略行（最新記録）を廃止し列見出しを「関連ゲート・証明」へ。履歴全文は関係の流れと詳細トグルが担う。
- Round 14（2026-07-27 まさ再指摘、v3.49.34）: **計画バーは二段塗り** — 枠線だけの白抜き（Round 13）は「終わってないからって色を消すのはデザイン悪すぎる」と差し戻し。薄い塗り=計画期間（確定 `accent66` / 仮置き `accent3d`）、濃い塗り=完了した範囲（`progressPct`、フル彩度）へ。0%でも柱の色が見え、濃い塗りだけが完了の主張。凡例は「バー=計画期間（開始→予定）で薄い塗り ・ 濃い塗り=完了した範囲（登録済みの進捗。すべて薄いのは進捗未登録のため）」。Round 13の不変条件（完了の主張は登録済み進捗からのみ・均一単色で計画を完了に見せない）は維持。
- Round 18（2026-07-27、v3.50.0。v3.50.1本番監査是正）: **PJ週次管制を別URLで追加** — `/project/[projectId]/weekly-control`。既存`/workspace`を置換せず、上段を「先週→今週 / 今回の会議 / 決定→介入」の3段レール、中央を4本柱の例外行、主面を論点・仮説の`要整理→検証中→判断待ち→決定・棄却`に固定。担当・次期限・最終更新・更新切れ・期限超過を表示し、仮説/根拠/検証/判断/決定後actionを既存management APIへ追加・更新できる。判断済みでも未完了actionは要フォローに残す。カードの次の動きは停止→期限超過→期限順で決定後action/判断/検証から選ぶ。親論点の鮮度は仮説・根拠・判断・actionの最新確認日も含め、次期限は子要素を含む最短日から導出。週次差分未接続は0件でなく`抽出接続待ち`、既存値は`現行台帳の仮表示`と明示。`projectScopedPathAllowed`、埋め込みshell/月初合意除外、SSR/client titleを両route対応。v3.50.1で週表示をUTC固定してSSR/client hydrationを一致させ、カード詳細の小さい補助文字・操作領域を拡大。migrationなし。
- Round 19（2026-07-27、v3.50.2）: **週次管制の見出し意味論とガントを是正** — コピー調の見出し「論点と仮説を、忘れられない流れに乗せる」等を廃止し、`週次差分・判断・介入` / `全体ガント` / `論点・仮説リスト` / `データ接続状況` の業務名へ統一。Round 18で既存画面だけへ残していた統合ガントを新URLでも`SxUnifiedTimeline` / `deriveSxUnifiedTimeline`の再利用によって表示し、別の時間モデルを作らず、全マイルストーン・計画・予測・今日線・重要経路・依存を確認できるようにした。週次画面では対応先のない介入ピン行だけ`showPins={false}`で隠す。migrationなし。
- Round 21（2026-07-29、v3.51.19）: **週次管制ガントと論点操作を実務化** — `project_management_tasks`（migration 200）を追加し、工程→タスク→子タスクを任意階層で折りたためるWBSガントへ拡張。各行に`予定 / 進捗 / 予測 / 差`を直接表示し、濃色=完了範囲、縦線=計画完了日、◇=完了見込み日、線分=両日付の差で進捗ではないことを凡例と行内表示で固定した。予測根拠がない仮置きは「遅れ」と断定せず`完了見込み +N日（仮）`とする。選択詳細はガント下からPC右側インスペクター（tablet/mobileは最大70vhの下部シート）へ移し、ガントの工程・タスクをその場で追加/編集できる。論点・仮説は常時見える文字付き追加/編集ボタンから開き、論点追加の初期分類を事実確認へ是正した。
- Round 23（2026-07-30、v3.51.22）: **メール専用の関係先リストを廃止し1面へ統合、PoC候補先を罫線一覧へ再構築** — Round 22で追加した独立`SxPartnerEmailLedger`（`sx-partner-email-ledger`）を削除し、代わりに`SxPartnerPipeline`の各`PartnerRow`へ`SxLatestContactStrip`（`data-testid="sx-partner-latest-contact-<id>"`）を追加して直近接点（日付・種別・往復方向・要点・結果・次の一手・接点後ボール/現在ボール）を1行で表示する。台帳の詳細・編集（`PartnerLedgerDetails`）内の全文・全件履歴（`InteractionTimeline`）は縮退させず維持し、ストリップは履歴の代替ではなく先頭サマリとして機能する。表示語は引き続き`sxNormalizePublicName`経由の公開安全な語のみで、本文・メールアドレス・URL・内部`source_ref`は出さない（プライバシー契約は無変更）。`SxPocCandidateBoard.tsx`は`SxPocCandidateList.tsx`へリネーム（`deriveSxPocBoard`→`deriveSxPocList`、`sx-poc-board`→`sx-poc-list`）し、カードグリッドから罫線区切りの一覧へ再構成: desktop/tabletは`<table>`（候補/現況/最終接点/次の一手/ボール担当/編集の6列）、mobileは同じデータを2段リスト（`sm:hidden`）で表示し、未接触グループの展開ボタンは`min-h-[44px]`のフル幅罫線ボタンに統一した。migrationなし。`scripts/check_pwa_critical_ui.cjs`をこの契約へ更新（`SxPartnerEmailLedger`の不在検査を追加）。build v3.51.22。
- Round 24（2026-07-30）: **関係先の初期表示を1社1行へ統合** — `SxPartnerPipeline`のdesktop registerを5列（関係先/進行状況/直近接点/現在ボール・期限/関連・操作）へ変更し、旧4重複列（当方の保有事項/先方の保有事項/次の一手/目標状態）と、進行状況・直近接点の独立subrowを廃止した。値と追加・編集は`PartnerLedgerDetails`/展開操作へ維持。旧`PartnerJourneyFlow`は`PartnerProgressFlow`へ改名し、`buildPartnerProgressSteps()`が接点・現在地・未完了work items・次の一手・ゴールを1回だけ構築する。会社名下`PartnerStageRail`と進行状況blockへ同じ配列を渡し、`data-step-count`/`data-progress-segment`/`data-progress-step`で1対1対応を回帰検査できる。`SxLatestContactStrip`は同じmain grid内の`SxLatestContactCell`へ変更。tablet/mobileは同じarticle内で再配置し、page全体の横overflowを作らない。migrationなし。
- Round 25（2026-07-30、v3.51.26）: **PoC候補先を関係先リストへ統合** — 独立`SxPocCandidateList.tsx`、`management-poc`節、PoC候補先ナビを廃止し、未接触を含む全PoC候補を`SxPartnerPipeline`へ1社1行で統合した。通常の役割groupと別に`deriveSxPocList()`の段階順（排液調達済/合意済・取得前/相談中/未接触）で台帳内groupを作り、`sx-partner-filter-poc`から候補だけへ絞り込める。未接触groupは同じ台帳内で初期6件+`sx-partner-poc-expand`に折りたたむ。各候補行は`PoC候補・<段階>`badgeを持ち、既存の進行状況・直近接点・現在ボール・詳細編集とrail/block 1対1契約をそのまま使う。母数帯へ候補総数/接触済/排液調達済/未接触を追加し、未接触候補は全関係先母数には含めるが`operationalPartners`から外して緊急・ボール件数を水増ししない。workspaceの同じ関係先節にPoC候補追加、weekly-controlの同じ節に関係先編集導線を維持。migration・public-safe境界は変更なし。
- Round 26（2026-07-30、v3.51.27）: **PoC専用の進捗分類を廃止し、横断属性へ修正** — Round 25で別リストは消したが、台帳内に`調達済/合意済・取得前/相談中/未接触`というPoC専用groupを残したため、関係先の共通分類から依然分離していた。`deriveSxPocList`/`sxPocStageLabel`/専用group/未接触展開を削除し、PoC先を含む全partnerを同じ`sxGroupPartnersByPrimaryClassification()`と`sxPartnerPrioritySortKey()`へ通す。候補・接触済み・調達済みを含むumbrella labelを`PoC先`、行badgeを`PoC`とし、「候補」の誤用と段階の重複表示を解消。filterは`表示`（全関係先/PoC先）と`役割`の2行へ分け、互いを解除せずANDで組み合わせる。母数帯のPoC固有表示は総数だけ。未接触PoC先の実行中管制件数除外、5列、rail/block同数、詳細編集、public-safe境界は維持。migrationなし。
- Round 27（2026-07-30、v3.51.28）: v3.51.27本番3幅監査で見つかった週次画面の関係先セクション説明だけの旧語`PoC候補`を`PoC先`へ揃えた。構造・データ・migration変更なし。
- Round 28（2026-07-30、v3.52.1）: **PoC比較レンズを共通7段階で再構築** — 3名の独立監査で、PoC属性判定とrole分類をそのまま比較軸にした結果47/53先が「未分類」へ落ち、接点件数を可変stepとして数えて進捗に見せていた構造を不合格とした。PoCは同じpartner台帳の横断属性のまま維持し、filter中だけ役割groupを外して候補→情報交換→条件整理→面談調整→検証準備→合意確認→実行中の固定7段階へ全件を一本化。既定の関係段階順と要対応順、段階別件数filterを追加。要対応は停止・日精度期限超過・7日以内・保留・情報更新要・担当未確認・期限未設定を関係先単位で判定し、当方ボールは独立表示/tie-breakとして段階と分離した。再監査で検出した行密度・集計分母・二重rail・担当者消失も是正し、PoC専用compact rowとモーダル詳細、表示中集計、側＋担当者表記へ変更。最終再監査では要対応件数から対象社名へ直接到達できない問題、平常保留の誤分類、古い情報の不可視、長スクロールで比較基準が消える問題、モバイル行高を是正し、件数ピルの絞り込み・14日鮮度判定・sticky帯・1段圧縮・通常一覧の担当者表示を追加。過去段階は完了証跡と誤認させる`✓`を使わず、railの塗りと現在位置だけを示す。接点記録の有無はroleLabelでなく`interactions`/`lastContactDate`から導出。会社名短縮は限定文型だけで保存値・詳細履歴・第三者名を変えない。migrationなし。
- Round 29（2026-08-01、v3.53.11）: **週次管制を予測差ビューから必須ゲート管制へ変更し、関係先を介入チェーン化** — PM/履歴意味論/UIUXの独立監査で、固定段階名だけでは「誰が何を抱え、どこを止めるか」が読めず、ガントの予測◇・差分線・重要経路破線も判断ノイズと判定。週次ガントへ`dateMode="planned_only"`を追加し、完了見込み・予定差・予測◇・差分線・破線接続を表示/範囲/並びから撤去。必須dependencyを前提ゲートとして一覧・各行・詳細に`充足/総数`表示し、前工程completedかつ完了証跡ありのみ充足とした。migration 210はNewCo設立判断の最低条件として「有償PoCの口頭合意」「出資の口頭合意」を未評価・担当/期限/証跡未確認で追加し、達成済みを捏造しない。ガント上で工程/タスク/必須条件、関係先一覧で関係先/履歴/具体的保有事項/分類を同画面編集でき、通常編集は既存APIのmanual provenanceを使う。関係先行は`関係先・現在地 / 詰まり・PJ影響 / 次にやること / 担当・期限 / 現在地の根拠 / 履歴・保有`へ変更し、primary interventionの行動・側・担当・期限・関連ゲートを同一保有事項から選択。上部へ担当別未完了/危険/情報不足、詳細へ全履歴・保有事項・合意/未合意・目標・出典を表示し、行内PoC badgeを廃止した。
- Round 30（2026-08-01、v3.53.11監査追補）: **必須ゲートと担当負荷を運用可能な単位へ是正** — NewCo前提の未評価を`未達`でなく`未確認`へ分離し、条件行から前提工程の状態・担当・日付・証跡とdependency接続条件を別々に編集できるようにした。有償PoC/出資の口頭合意は、先方または投資家・合意内容・確認日・根拠の4項目が揃う場合だけ充足する。工程/タスク/ゲート/論点/仮説/検証/判断/action/関係先保有事項を共通作業単位へ正規化したPJ全体担当帯を追加し、停止・期限超過・7日内・期限なし・未完了・影響ゲートを担当別に展開表示する。mobile/tabletの横ガントは縦の工程一覧へ置換し、関係先行の履歴列も本文下の全幅操作へ再配置。構造化保有事項のない関係先は、関連ゲート配列の先頭を行動の影響先として推測表示しない。
- Round 31（2026-08-01、v3.53.12）: **通常工程と停止条件を分離し、関係先の進捗比較を復旧** — `project_management_milestones`を一律ゲート表示せず、停止条件は有償PoC口頭合意・出資口頭合意の2件だけを上部のマイルストーン行へ固定する。通常工程の◇・ゲート表記を除去。工程、タスク、論点、仮説、関係先は、独立した編集ボタンや鉛筆を経由せず名称・状態・担当・日付などの値自体を選んで修正する。関係先は初期状態でPoC候補先全件を表示し、1社1行の同じ列位置に7段階の直線目盛りと現在地を置く。旧pill rail、行別PoC badge、役割groupによる分断を使わず、進捗・詰まり・次の行動・同一保有事項由来の担当/期限・PJ影響・直近根拠を横並び比較する。
- Round 32（2026-08-01、v3.53.13）: **関係先の固定7段階を撤回し、可変進行とVCタブを復元** — Round 31の固定7段階では、保存済みの接点・未完了作業・現在地・ゴールの増減が比較面から失われたため、関係先ごとの可変stepへ戻した。確認済み接点→現在地→未完了work item→次の一手→ゴールを唯一の配列として組み立て、会社名下segmentと進行blockの件数・順序を一致させる。固定段階filter・固定分母・`x/7`・要対応内の段階tie-breakを撤去。既存の全関係先/PoC候補先タブと同じ台帳絞り込みとしてVCタブを追加し、保存済み`role_kind='shareholder_investor'`だけを対象にする。1社1行、PoC badgeなし、直接編集、詰まり・行動・担当/期限・PJ影響・根拠・履歴/保有は維持。migrationなし。
- Round 33（2026-08-01、v3.53.14 / migration 211）: **一覧を現在値の比較面へ整理し、詳細を重畳化** — 関係先一覧の可変`進行状況`blockを詳細モーダルへ移し、一覧は`現在の状況`と`ゴール`を独立列にした。行全体を単一の詳細ボタンとし、会社名から履歴件数までどこを押しても同じ詳細へ入る。会社名下segmentは詳細内の全進行stepと同じ配列を維持する。VCへ別交渉主体のDAVP、BNV、いよぎんキャピタルを未確認状態で追加し、既存ダイキアクシスの技術納品履歴へ混ぜない。週次ガントは通常バー末尾の縦線を削除し、右ペイン詳細をガント上の中央重畳モーダルへ置換。desktop/mobileで同じ情報階層、Esc/背景/閉じる、値から編集へ移る際のdialog非重畳を契約化した。
- Round 34（2026-08-01、v3.53.18）: **ガント・関係先の詳細編集を同一モーダル内へ統合** — Round 33の実装では詳細値の選択時に共通editorが別backdrop/dialogとして重なっていたため、ガント詳細と関係先詳細の本文を埋め込み編集フォームへ切り替える構造に変更した。詳細を開いてから保存・キャンセルまで`aria-modal=true`のdialogとbackdropは各1つだけを維持し、操作後は同じ対象の詳細へ戻る。埋め込み時は最初の入力欄へfocusを移し、未保存変更がある状態の外側×・Esc・背景クリックは共通の破棄確認を通す。ガント詳細portalへ週次画面の色変数を明示的に渡し、画面外の既定色へ崩れる問題と装飾的な英語見出しも除去。追加操作など詳細外から始める編集だけは独立editorを維持する。migrationなし。
- Round 35（2026-08-01、v3.53.21）: **視覚上も詳細編集を1枚の面へ修正** — Round 34はDOM上のdialog/backdropを1つにした一方、共通`editorPanel`の枠・別背景・重複した編集見出し・対象工程/関係先ブロック・内側フッターを外側詳細へそのまま埋め込み、操作上は第二のモーダル面に見えていた。埋め込み時はfields/error/actionsだけを詳細本文へ直接描画し、内側のpanel/header/contextを生成しない構造へ変更。ガントと関係先の双方で、外枠1つ・見出し1つ・スクロール本文1つ・操作フッター1つを視覚契約とし、誤った`.editorPanel.editorInline`を必須化していたcritical guardも禁止契約へ反転した。migrationなし。
- Round 36（2026-08-01、v3.53.23）: **ガントと関係先を表示位置で直接編集** — ガント詳細の名称・担当・状態・計画期間・進捗・実績・内容・詰まり・完了条件と、設立前提の各表示値は、押した位置だけを入力欄へ差し替える。保存・キャンセル・APIエラーも同じ位置に置き、詳細本文全体を編集フォームへ差し替えない。PATCH payloadは選択したfieldだけに限定し、期間だけ`planned_start`と`planned_end`を組で送る。新規タスク追加は従来の複数fieldフォームを維持する。関係先は行全体の詳細/editモーダルを廃止し、一覧の表示cell内で直接編集する。会社名下の可変segment railだけを進捗・全履歴の入口とし、開くモーダルは閲覧専用。行余白・読み取り専用cell・右端件数からはモーダルを開かない。migrationなし。
- Round 37（2026-08-02、v3.56.3 / migration 221・222）: **ガントの手動依存線・固定ヘッダー・高密度モーダル** — 既存のNewCoゲート判定用`project_management_milestone_dependencies`とは別に、task/MS右端→別task左端の手動`finish_to_start`線だけを持つ`project_management_schedule_dependencies`を追加した。通常の移動/resizeと排他な`依存を接続`モードで描画・soft deleteし、mobile/keyboard/offscreen接続は起点→接続先の2操作を使う。線はgeometry非依存の一覧からも解除可能。DBは同一PJ・active端点・XOR・自己接続・重複・task循環・MS自身配下への接続を拒否し、端点の後続削除/移動でも不変条件を閉じる。端点行をUUID順でロックしてからPJ単位advisory lockを取る順序へ全経路を統一し、循環検査の直列化と端点更新の並行実行をデッドロックさせない。desktopガントは内部縦横スクロール化し、月/設立日/`工程 / タスク`の先頭ヘッダーと左工程列をsticky固定。root editorは640/760px・最大720px、PlanInspectorは820px・最大720px、desktop入力36px/footer38pxへ圧縮し、640px以下は44px操作・16px入力を維持する。point-MS追加は`接続する成果 / MS名 / 予定日`だけ必須、`完了条件`は任意とし、重複する到達点と次の成果物を作成時に要求しない。
- Round 22（2026-07-29、v3.51.20）: **週次管制へ関係先・PoC候補先・メール接点を復帰** — 新URLから欠落していた既存`SxPartnerPipeline`と`SxPocCandidateBoard`を`#partner-ledger`へ表示し、`SxPartnerEmailLedger`でメール接点のある関係先を最新接点順に一覧化。1行に関係先・最新要点・送受信方向・現在ボールを置き、展開時は確認済み要約を時系列で表示する。migration 201は2026-07-29に共有Gmailで確認したPoC関連6先・11接点を`interaction_kind='email'`/`source_kind='imported'`で投入し、メール上は接触済みだが台帳に無かったマルトモ・オカベ・ヤマキを未合意のPoC接触先として追加する。本文・アドレス・URL・個人連絡先はDB/UIへ保存せず、面談結果がメールに無い過去予定は完了扱いしない。`sxSourceRefDisplayLabel()`は`gmail-thread:*`を固定の安全ラベルへ変換し内部参照を表示しない。
- Round 20（2026-07-28、v3.51.2）: **成立条件ナビゲーションを別URLで追加** — `/project/[projectId]/navigation`。既存`/workspace`/`/weekly-control`を置換せず、`src/components/project-navigation/`配下に完全独立した視覚コンポーネント（`SxNavigationDashboard`・独自CSS変数の`navigation.module.css`）を持つ。**(1)** 判定は予定内/要介入/判定不能の3値（`on_track→予定内`、`attention・crisis→要介入`、`unassessed→判定不能`）に加え、**予測差分（対計画）を独立軸で別集計**（`forecastComparableCount`/`forecastSlipCount`/`maxForecastSlipDays`/`maxForecastSlipTitle`）。全マイルストーンが`unassessed`でも`plannedEnd`→`forecastEnd`の乖離だけは見落とさず、比較可能な日付が0件なら「予測差分未算定」とする。**(2)** 全体依存ガント「依存航路」は節点を基準計画(`baselineSegment`)・完了帯(`completeSegment`)・**予測延伸**(`slipSegment`=計画超過分だけを独立レイヤーで赤枠+`+N日`表示)・**前倒し**(`pulledInDays`、延伸と混同しない別マーカー)に分離。重要経路は実線、前提のつながりは破線。**(3)** 関係先リストは識別→完了済み/直近接点→現在(段階・ボール)→次の受け渡し→期限・目標の順で全56件をデフォルト全件表示（役割グループ見出し＋検索input、行44px以上）。完了済み(commitment/workItem)とinteractions(直近のやり取り)を混同しない。役割グルーピングはprimary roleがあれば構造化(`roleKind×relationshipState`)、無ければ`roleLabel`に「PoC候補/PoC接触」が明記される時だけ「PoC候補・接触（表示ラベル由来）」、それ以外は正式分類を捏造せず「役割未分類」。**(4) RSC最小化**: `src/lib/sx-navigation.ts`の`buildSxNavigationViewModel()`が唯一のRSC境界越え窓口。`ProjectWorkspaceBundle`/`CurrentMemberAccess`全体をClient Componentへ渡さず、`sourceRef`・メンバー名簿・工数・意思決定の`context`/`rationale`/`changedBy`を含まない最小`SxNavigationViewModel`だけを渡す。タイムラインスケールは関数を持たないserializable版（軸目盛り事前計算済み）にし、pending decisionは`id`/`title`/`dueDate`/`ownerLabel`/`isThisWeek`のみ。`projectScopedPathAllowed`、埋め込みshell/月初合意除外、SSR/client titleを対応。単体テスト`scripts/test_sx_navigation.mjs`で判定・タイムライン・節点レイヤー分離・依存線・クリティカルパス・関係先(完了/直近接点分離・役割グルーピング捏造禁止)・制約ボード・pending decisionの最小フィールド化・view modelのJSON.stringify可能性/sourceRef非含有を検査。migrationなし。
- Round 21（2026-07-28、v3.51.7）: **SX PJ管制ダッシュボードをゼロベースで再構成** — 同じ別URL `/project/[projectId]/navigation` の旧試作UIと旧`src/lib/sx-navigation.ts`を廃止し、`sx-navigation-v2.ts`の階層表示モデルへ置換。初期画面は重要経路を展開し、4本柱を期間要約バーで畳む。柱を展開すると `マイルストーン → 技術試験・論点・履歴` へ掘れる。ガントは1タスク1本（予定期間＋予定終了ティック＋進捗位置＋同一バー末尾の遅延）へ統一し、重要経路は専用グループ・赤レール・`CRITICAL`・直角`M H V H`接続で明示、曲線と二段積み上げ棒を廃止した。関係先は文章カードから共通7段階軸の比較表へ変え、`x/7`・合意・現在ボール・期限・次の約束・目標状態を全件同一列で比較可能にした。マイルストーン、論点、仮説、根拠、検証、判断、action、技術試験、関係先、保有事項を選択文脈内で追加・更新できる。migration `198_sx_navigation_v2_current_truth_seed.sql`で、マニホールド型は「6/24確認で現行ラインから除外」（停止日とは断定しない）、Dy回収は「吸着可能性が見えた」を確認済み根拠とし、実排液選択性・酸脱着・1/3/5サイクル耐久を未評価の別試験として登録した。
- loading / empty / error / disabled / selected / focusを用意し、操作は44px以上。状態は色だけで伝えず、日本語表示で内部status・confidence・source・entity名を漏らさない。
- Round 16（表示構造の文書整合）: desktopのワークスペース内部専用152pxセクションナビ、事業化ロードマップのデータ導出絶対期間表示（固定「9か月」表記は使わない）、関係先リストの7列固定/二車線契約、`nominalizeSxActionLabel`によるaction系ラベルの名詞止め表示（地の文・根拠は非加工）、DB互換のみで残る`next_ball_owner`/`handoff_to`非表示の実装差分をFEATURE_REGISTRYとspec 3-8へ反映した（build v3.49.3、UI・ドキュメント同期のみでmigrationなし）。
- Round 17（SX PJ管制盤`SxExecutiveControlDeck`、2026-07-24 最終監査是正。build v3.49.7）: 重要経路レール（`deriveSxCriticalPathRail`）は`未評価/不明確度/鮮度切れ`・`要注意`（attention/at_risk）・`期限超過`・`停止`を明示stateへ分離し、現在地ノードであっても偽の緑（`current`扱い）を返さない（真にオンスケな場合だけ`current`/`future`）。ノードは予定(`plannedEnd`)と予測(`forecastEnd`)＋差分日数を別フィールドで保持し1本の日付へ潰さない。可視ウィンドウは「直前完了1件以内＋現在＋次3件」に加え、経路の最終ノード（設立判断等）を`finalNode`として常に末尾へ含め（既にウィンドウ内なら追加しない）、先頭に隠れた完了分は`leadingMarker`（`+N完了`）、現在ウィンドウと最終ノードの間に隙間があれば`trailingMarker`（`…N`）で明示。ノード間は色だけでなく`ArrowRight`のコネクタ線で接続。対応待ちキュー（`deriveSxInterventionQueue`）は真偽ブールでなく厳密な項目単位に再設計: 技術試験は`milestoneId`で紐づく重要ゲートのblocked/failedを最優先、unassessedは`評価不能`として低優先度に分離し、試験自体は期限を持たないため親ゲートのforecast/plannedを`dueContextLabel="親ゲート期限"`付きで表示。検証run/対応actionは`issue.validationRuns`/`issue.decisions[].actionItems`から個別導出し、issueのtitle/owner/dueへ集約しない（`method`/`title`・`ownerLabel`・`dueDate`・`gate`・`issueId`を厳密に転記）。相手先保有事項は`item.side`＋`item.ownerLabel`を一次のボール源とし、`partner.currentBallSide`/`currentBallOwner`は使わない（SX側保有事項も非表示にしない）。`relatedMilestoneId`を持たない相手先レコードのため、`partner.relatedMilestoneSlugs`が重要経路と交差し`currentBallSide`がpartner/shared/unknownかつ有効な`dueDate`/`nextCommitment`を持つ場合の`partner_fallback`行を追加（milestone関係をデータに無い形で推測しない）。重要ゲート/論点行はボール不在時に`SX側`を決め打ちせず、列見出し「ボール / 担当」＋`ballSide="担当"`・owner不明時`未確認`で表示。各行は`entityType`/`entityId`/`milestoneId`と、milestone行は`management-plan`、technical_test行は`management-capacity`、issue行は`#sx-issue-<id>`、partner行は`#sx-partner-<id>`という厳密anchorを持ち、`anchor === "management-plan"`の行のみ`onSelectMilestone`を呼ぶ（technical_test/issue/partner行は呼ばない）。それら以外の行のクリックは`onSelectMilestone`を呼ばず`focusAnchorRow()`（`location.hash`更新＋`scrollIntoView`＋`.sx-anchor-highlight`の一時フラッシュ、`globals.css`に`@keyframes sx-anchor-flash`を追加）で該当DOM行（`ProjectWorkspaceDashboard.tsx`の`id="sx-issue-${issue.id}"`、`SxPartnerPipeline.tsx`の`id="sx-partner-${partner.id}"`、scroll-mt-24付き）へフォーカス遷移する。右ペインを「対応待ち（優先順、最大3件）」＋区切り線下の別セクション「直近アクション（最大3件、`deriveSxUpcomingQueue`）」の2ブロックへ分離（統合しない）。直近アクションは`management.actions`/未決定`decisions`/planned・running `validationRuns`から導出し、期限超過/7日以内/14日以内/30日以内/期限未設定の窓でソート（同窓内は重要経路紐付き→停止中→期限→ラベル順）、対応待ちに既出の項目は`dedupeKey`（`action-<id>`等）で`excludeKeys`除外し二重表示しない。トップ判定バッジは`sxVerdictDisplayLabel()`で内部judgment値（`on_track`/`attention`/`crisis`/`unassessed`）をDB非破壊のまま表示専用テキスト（オンスケ/要注意/危険/判定不能）へ写像し、`unassessed`を緑/オンスケへ誤写像しない。レスポンシブは対応待ちを`sm:table`のdesktop表と`sm:hidden`のmobile 2行リスト（`InterventionRowMobile`）へ分岐し、`data-testid`をそれぞれ`sx-intervention-queue`/`sx-intervention-queue-mobile`で分離、重要経路レールは`lg`未満で内部縦スクロール領域（`max-h-[420px] overflow-y-auto`）を持ち390px幅でページ本体をクリップしない。mobile DOM順は 判定→4本柱→対応待ち/直近アクション→重要経路 を維持（`order-1`/`order-2`）。全操作行`min-h-11`＋`focus-visible:outline`を維持。単体テスト`scripts/test_sx_executive_control_deck.mjs`をこの契約へ全面拡張（false-green各state、直前完了/現在/次/最終のウィンドウ構成、技術試験/検証/actionの厳密フィールド、相手先side/owner優先順位＝SX側非表示テストを撤去し逆に「SX側は表示される」ことを検査、重要経路紐付きの相手先fallback、優先順位の安定ソート、直近アクションの窓/ソート/重複除外、実名正規化の表示境界を検査）、`scripts/check_pwa_critical_ui.cjs`に`sx-intervention-queue-mobile`/`sx-upcoming-queue`/`deriveSxUpcomingQueue`/`sxVerdictDisplayLabel`/`直近アクション`/`ボール / 担当`/`focusAnchorRow`/`sx-anchor-highlight`/`ArrowRight`/`InterventionRowDesktop`/`InterventionRowMobile`のanchorを追加。【同日 build v3.49.6据置、追加監査是正】担当未確認/鮮度切れ/評価未完を1つの`owner_unconfirmed`kindへ潰していた問題を是正: `SxEcdInterventionKind`に`gate_stale`/`gate_unassessed`を追加し、`ownerMissing`は`owner_unconfirmed`、それ以外で`isStale`は`gate_stale`、それ以外の`unassessed`は`gate_unassessed`へ分岐（owner既知の鮮度切れ/評価未完ゲートを「担当未確認」と誤表示しない）。この行の`ballOwner`は常に`ownerOrUnconfirmed(milestone.ownerLabel)`（既知の担当は保持し未確認時のみ「未確認」）、`dueDate`は`forecastEnd || plannedEnd`、forecast使用時は`dueContextLabel="予測期限"`、`dateCertainty`も伝播。表示ラベルは「担当未確認」/「情報更新切れ」/「ゲート評価未完」。技術試験行（`technical_test_blocked`/`technical_test_unassessed`）にも親ゲートの`dateCertainty`を伝播（従来欠落）。重要経路レールのノード色以外の意味論を是正: 現在地(`current`)は`TriangleAlert`でなく`CircleDot`、`attention`stateには`TriangleAlert`を明示付与（従来は既定の丸枠にフォールバックし無区別だった）。フォーカス遷移を全ジャンプへ統一: `UpcomingRow`は`#`付き/素のセクションid両方で`preventDefault`＋`focusAnchorRow`を使用（従来は`#`無しアンカーで素通りのブラウザ内遷移のみだった）。`management-plan`行（`anchor==="management-plan"`）は`onSelectMilestone`呼び出し後に`requestAnimationFrame`で`focusAnchorRow("management-plan")`を追加実行し、`ProjectWorkspaceDashboard.tsx`の`section#management-plan`/`section#management-capacity`に`tabIndex={-1}`を追加（従来は選択のみでフォーカス遷移が無かった）。単体テスト`scripts/test_sx_executive_control_deck.mjs`へ`gate_stale`/`gate_unassessed`分岐（owner既知の鮮度切れ・評価未完それぞれのkind/owner保持/予測期限/dateCertainty伝播）と技術試験行の`dateCertainty`伝播テストを追加。【同日 build v3.49.8、本番1440x900実測レイアウト是正・現行契約】主段レイアウトを`lg:grid lg:grid-cols-[58%_42%]`（重要経路レール左58%＋対応待ち/直近アクション右42%の2カラム）から、(1)`lg`以上は4本柱信号帯直下に重要経路レールを全幅横帯として配置（`data-testid="sx-critical-path-band"`、className`"order-2 min-w-0 border-b border-[#e4ddd0] px-3 py-2 lg:order-1"`）、(2)その下に対応待ちキューと直近アクションを`grid-cols-1 lg:grid-cols-2`の2カラム（`data-testid="sx-queue-columns"`、className`"order-1 grid min-w-0 grid-cols-1 lg:order-2 lg:grid-cols-2"`）へ再構成。対応待ち・直近アクションはそれぞれ独立した`<section>`（統合しない）とし、カラム間は`border-r`/`border-b`の罫線のみで区切りカードは追加しない。`lg`未満（mobile/tablet）はCSS `order`でキュー列（対応待ち→直近アクション）を重要経路レールより先に表示する既存の優先順（判定→4本柱→対応待ち/直近アクション→重要経路）を維持。対応待ちキューのdesktop表/mobileリストの切替閾値を`sm:`から`lg:`へ変更（768pxのtabletで4列desktop表を半幅2カラムへ押し込まず、`lg`未満は一貫してモバイル2行リスト`InterventionRowMobile`を使用）。既存`data-testid`（`sx-critical-path-rail`/`sx-intervention-queue`/`sx-intervention-queue-mobile`/`sx-upcoming-queue`/`sx-four-pillar-signal-strip`）、`data-sx-anchor`、44px操作行、focus-visible outline、導出ロジック（`deriveSxCriticalPathRail`/`deriveSxInterventionQueue`/`deriveSxUpcomingQueue`）・データは無変更。本番1440x900実測（reflow前）でrail高さ104.5pxに対し右カラムが292〜735pxまで伸び、左下に約330pxの空白が残っていたスペース浪費を解消。`scripts/check_pwa_critical_ui.cjs`に`sx-critical-path-band`/`sx-queue-columns`と全幅レール/2カラムキューの主要classNameのexpectIncludesを追加。build v3.49.8。

回帰防止:

- `npm run test:sx-management` はKPI 0件、充足率Goodhart、critical未知、閾値内外、actual=0、between不足/逆転、依存待ち/停止、任意依存、期限超過、完了証跡、未完action、DAG cycle、現在ゲート切替を検査する。
- `npm run test:sx-executive-control-deck` は重要経路レール（false-green禁止、仮日程、担当保持、月精度期限、相手先ボール）に加え、`deriveSxStateMap` の旗接続（rowの`milestoneId`一致のみ、推測接続なし）、未解決行の`unattached`退避、flag/state分類（担当未確認等を第三者旗にしない）、gap日数の予測日比例、①②③ランクの図・リスト共有を検査する。
- `npm run test:project-management-rls` (`npm run test:sx-management-rls`) は24個のsoft-delete対象member_select（`project_management_partner_interactions`/`project_management_partner_roles`/`project_management_partner_work_items`/`project_management_tasks`含む）、184/185/191/192の分類・約束・決定・現在ボール・期限精度・行為主体側・`source_kind`・完了必須条件・成果物受入必須条件CHECK、補正履歴、partner_interactions/partner_roles/partner_work_items各表のtrigger（block_delete/parent_project_guard/field_audit/touch_updated_at）とtasks表の同一PJ・親子循環guard/block_delete/field_audit/touch_updated_at、列存在（`completed_on`/`completion_evidence`/`accepted_by`/`accepted_on`/`source_kind`/`source_ref`含む）・soft-delete可視性・親PJ guard（`related_milestone_id`が他PJのゲートを指す直接probeを含む）、`project_management_partner_roles_one_primary_192`（1partner1primary roleのunique partial index）、p21 primary role seed 6件・保有事項seed 4件、interaction actor_side backfillは固定件数ではなくライブ行のsource_ref+既知summaryでactor_side='unknown'が残っていないかで判定、`project_management_partner_roles`/`project_management_partner_work_items`双方のsoft-delete→migration再適用相当probe（idの重複・復活が無いこと、work_itemはcompletion/related_milestone_id等が変わらないことも検査）、provenance probeは通常編集が`source_kind='manual'`/`source_ref='PWA共有管理画面'`へ再スタンプしsoft-delete/restoreはそれを変えないことを検査、`status='completed'`/`item_kind='deliverable'`の未確認完了を拒否するDB CHECK probe、authenticated roleによるPJ境界を検査する。migration 200未適用のDBではtasks表の新規assertionが失敗する。
- `npm run test:sx-partner-holdings` (`scripts/test_sx_partner_holdings.mjs`) は `pwa/src/lib/sx-partner-holdings.ts` の純粋関数（`sxHoldingsForPartner`のwork item/commitment統合とside振り分け、`sxAllHoldingsForPartnerAudit`の全ステータス包含、`sxComputeControlBandCounts`のpartner品質集計＝全件対象/deferred・ended除外なし・holdings単位緊急集計＝active（非deferred・非ended）限定・day精度限定のoverdue/dueSoon・month精度別バケット・bothSidesHeldPartners・endedPartners/unorganizedPartners/organizedCoveragePct/blockedHoldings、`sxPrimaryRoleKindCounts`のunclassifiedデフォルトと全partner集計（deferred/ended含む、category navのfilter到達可能性を担保）、`sxRoleDisplayLabel`のunclassified含む複合ラベル生成、`sxIsPartnerEnded`のon_hold非混同、`sxSortHoldingsByPriority`のblocked優先5tier、`sxPartnerDisplaySortKey`のactive holding優先ソート、`sxPartnerHasBlockedHolding`/`sxPartnerPrioritySortKey`のblocked partner最優先判定、`sxGroupPartnersByPrimaryClassification`のroleKind×relationshipState別group・unclassified常に末尾・deferred/ended除外・blocked partner最優先ソート、`sxSourceKindLabel`のcurrent_truth/manual/imported表記、`sxSourceRefDisplayLabel`のraw URL/内部tracking key非露出、work item/commitment双方の`sourceKind`/`sourceRef`/`lastVerifiedAt`/`confidence`正規化＝commitmentも自身の`source_ref`をそのまま転写し値が無ければnull、Round 14で追加した`sxSourceRefDisplayLabel`のcommitment sourceRef safe-label化テストも含む）を検査する。
- `npm run test:critical-ui` は目的-KPI、測定、決定ループ、POST/新規追加、運用準備、選択文脈、lg以上の比較表・lg未満のカード、effort接続、PJ-scoped effort APIのanchorに加え、`SxProofOutcomes` / `SxPartnerPipeline` / `sx-proof-mapping.ts` / 管理台帳・編集への集約anchor、二車線ボール管制（全関係先/対応中/双方保有先/月精度期限/分類別件数ナビ/保留・低優先グループ/終了（対応中から除外）/行為主体未確認/台帳の詳細・編集/ゲート未接続/台帳0件先/登録率/停止バッジ/`sxHoldingsForPartner`/`sxAllHoldingsForPartnerAudit`/`sxComputeControlBandCounts`/`sxRoleDisplayLabel`/`sxIsPartnerEnded`/`sxSortHoldingsByPriority`/`sxPartnerHasBlockedHolding`/`sxGroupPartnersByPrimaryClassification`/`sxIsHoldingOverdue`/`sxIsHoldingMonthPrecision`/`endedPartners`/`blockedHoldings`/`unorganizedPartners`/`organizedCoveragePct`/`aria-pressed`/`aria-labelledby={nameHeadingId}`）・ControlBand順序（緊急→ボール→母数）・InteractionRow/Timelineグリッド（`grid-cols-[56px_minmax(0,1fr)_64px_44px]`/`md:grid-cols-[56px_68px_minmax(130px,1fr)_64px_44px]`/`grid-cols-[minmax(0,1fr)_44px]`）・`open`属性不在・`#777166`不在・management APIのprovenance再スタンプ（`meta.hasSourceRef`ブランチ）・migration 191/192（date precision整合CHECK・`ON CONFLICT ... DO UPDATE`・`actor_side`・`project_management_partner_roles_one_primary_192`・`source_kind`/完了/成果物受入CHECK・固定count非依存のbackfill検証含む）・management APIの`interaction`/`partner_role`/`partner_work_item` resourceと`assertDatePrecisionConsistency`/`assertWorkItemCompletionRequirements`のanchor、現行の二車線desktop/tablet/mobile responsive grid-cols anchor（`grid-cols-[minmax(0,1fr)_minmax(0,1fr)] md:grid-cols-[1fr_1fr] xl:grid-cols-[128px_minmax(130px,1fr)_minmax(130px,1fr)_120px_120px_108px_124px]`）、`text-[9px]`/`opacity-80`/`組成率`/旧「未整理（確認未実施）」文言の不在、「中央受け渡し」「協力機関の進捗」の不在を検査する。加えて「やり取り履歴（全文・全件）」/`ScrollHintArrow`/`出典`/`最終確認`/`確度`の存在を検査する。Round 13分は`preview = sorted.slice(0, 3)`/`preview.map((interaction)`/`表示3 / 全`/`canAddInteraction`/`grid-cols-1`/`{canManage ? "台帳の詳細・編集" : "台帳の詳細"}`の存在と、`text-[#a49d8c]`の不在を検査する。Round 14分は`sx-management.ts`のmapCommitmentが`source_ref`を転写する行の存在と、`SxPartnerPipeline.tsx`の約束カードに`sxSourceKindLabel(commitment.sourceKind)`/`sxSourceRefDisplayLabel(commitment.sourceRef)`が存在することを検査する。Round 15分は`InteractionTimeline`の3件超注記が`'全文は「{canManage ? "台帳の詳細・編集"'`/`': "台帳の詳細"}」へ'`の両分岐を持つことを検査する。
- `node --experimental-strip-types scripts/test_sx_proof_mapping.mjs` は7テーマ全件が`SX_THEME_PROOF_MAP`で1つ以上の証明へ接続していること、`computeSxProofOutcomes`が全未評価テーマ集合を0%ではなく`null`（未評価表示）で返すことを検査する。
- Round 37（2026-08-02、v3.54.10）: **担当負荷の列独立化・root editorトークン復元・edit_validation追加**。**(1)** `SxProjectOwnerWorkload.tsx`の閉じた担当者行を、`lg:grid-cols-2`直下に`<details>`を暗黙行でround-robin配置する構造（片側展開が共有行を伸ばし、反対列の後続カードのY座標まで動かしていた）から、`OwnerLoadColumn`という完全に独立したflex-col要素2つ（配列を`Math.ceil(length/2)`で前半/後半split）へ再構成。desktopの外側`grid lg:grid-cols-2`は各列1グリッドアイテムのみを持つため、片側の高さ変化は反対列の描画に影響しない。mobile（`lg`未満）は2つのcolumn要素が縦に並び、`left→right`の連結が元の配列順序と一致する。各項目のaria-labelを実動作（元の編集文脈を開く）どおり`${item.title}を編集`へ変更し、`data-work-unit-kind`/`data-work-unit-id`を追加。**(2)** root editor（`IssueEditor`のembedded/inlineFieldでない分岐）はcreatePortalでdocument.body直下へ描画され、`.page`が定義する`--sheet`/`--ink`/`--muted`/`--line`/`--green`/`--red`等のCSS変数の継承チェーンから外れていた（`.editorEmbedded`/`.inlineFieldEditor`は既にローカル再定義済みだったが、実際にportalされる`.editorBackdrop`だけ欠けていた）。`.planInspectorLayer`と同じトークン一式を`.editorBackdrop`へ再定義し、生成り`#fffdf7`（`rgb(255,253,247)`）の不透明dialog背景・濃墨文字・深緑CTAを復元。閉じるボタン（40→44px）・primary/secondaryボタン（42→44px）・input/select/textarea（42→44px）・checkbox行（42→44px）を44px以上へ拡大し、`:focus-visible`に2px `var(--green)`のoutlineを追加（3:1以上のコントラスト）。**(3)** 検証（`SxValidationRun`）work unitクリックが`edit_validation`を持たず親issue編集へ黙ってフォールバックしていた穴を修正。`EditorState`へ`edit_validation`（`issue`/`hypothesis`/`validation`、`resource: "validation"`のPATCH、fieldsは既存`create_validation`と同一）を追加し、`navigateToWorkUnit`の`unit.kind === "validation"`分岐で押した`SxValidationRun`そのものを開く。仮説・検証・判断・actionのID不一致は、関係先provenance監査と同じく`notifyWorkUnitNotFound()`のnoticeで終え、親issue編集への黙ったフォールバックを廃止（issue自体が見つからない場合も同様）。論点系root editorを開く直前に必ずガント側選択（`selectedMilestoneId`/`selectedTaskId`）を解除し、`#issue-hypothesis`への`scrollIntoView`を廃止（モーダルは全画面backdropで背景を覆うため、担当負荷から開いたときにscrollYが動くバグの原因だった）。root editor dialogへ`data-editor-kind`/`data-editor-resource`/`data-editor-record-id`を追加し、実ブラウザ監査でクリック対象と開いたeditorの一致を検査できるようにした。migrationなし。回帰防止: `scripts/check_pwa_critical_ui.cjs`に`OwnerLoadColumn`/`splitAt`/`leftLoads`/`rightLoads`のcolumn-stackアンカー、`.editorBackdrop`内の`--sheet:#fffdf7`/`--ink:#24231f`/`--green:#235f4b`トークン定義（`expectPattern`）、44px各要素・`:focus-visible`outline、`edit_validation`/`resource: "validation"`/`issue.validationRuns.find(`、論点系ID不一致のnotice分岐、`#issue-hypothesis`への`scrollIntoView`不在を追加した。
- Round 38（2026-08-02、v3.54.12）: **Round 37の3件の後続監査是正（担当負荷2カラムの並び順・root editor文字色・navigateToWorkUnitの状態保全）**。**(1)** `SxProjectOwnerWorkload.tsx`の`OwnerLoadColumn`分割を、Round 37の前半/後半split（`splitAt = Math.ceil(length/2)`）から偶数index=左列・奇数index=右列のrow-major分割（`partitionOwnerLoadColumns()`、`src/lib/sx-project-owner-load.ts`へ切り出し）へ修正。前半/後半splitは旧CSS gridの暗黙行ペアリング（item0/item1が同じ行の左右）が作っていた見た目のペア順と一致しなかったため。mobile（`lg`未満）はこの分割結果を連結せず、常に元の`loads`順を単独DOMで描画する（連結だと`[0,2,4,1,3,5]`という別順序になる）。desktop用2列DOMは`hidden lg:grid`、mobile用単列DOMは`lg:hidden`で常に片方だけをa11y treeへ出し、重複ノードを作らない。片側`<details>`の展開は引き続き反対列の高さ・Y座標を動かさない。単体テスト`scripts/test_sx_project_owner_load.mjs`に`partitionOwnerLoadColumns([0..5])`が`left=[0,2,4]`/`right=[1,3,5]`（奇数長は左が多い側）になること、分割が元配列を破壊しないことを追加。**(2)** `weekly-control.module.css`の`.editorPanel`へ`color: var(--ink)`を明示。Round 37で`.editorBackdrop`へCSS変数トークン一式（`--sheet`/`--ink`等）を再定義したが、`.editorPanel`配下のh2/strong/label/本文自体は`color`を明示していなかったため、bodyのdark themeを継承し、不透明な生成り`#fffdf7`背景の上で文字が薄くなるバグが残っていた。`.editorPanel`直下でcolorを固定し、dark theme下でも濃墨で読めるようにする。**(3)** `SxWeeklyControlDashboard.tsx`の`navigateToWorkUnit`を、論点/仮説/検証/判断/actionだけでなく工程・タスクも含め、対象issueと子record・task・milestoneのID/存在をすべて検証し`nextEditor`（またはtask/milestoneの対象ID）を組み立ててからだけ、`workloadFilter`/`detailEditor`/`planFieldEditor`/`editorFieldKeys`/`selectedMilestoneId`/`selectedTaskId`の既存overlay stateを一括解除するよう修正（partner分岐の既存パターンを踏襲）。Round 37時点では、issue検証や工程/タスクのnav先クリックの直後・分岐前に無条件でこれらstateを解除しており、無効IDクリック（notifyWorkUnitNotFound()のnoticeだけを出すはずが）が既存の詳細/editorを巻き添えで閉じていた。`unit.kind === "issue"`分岐にも`unit.id === issue.id`検証を追加し、navIssueId一致だけで別issueへ取り違えないようにした。migrationなし。回帰防止: `scripts/check_pwa_critical_ui.cjs`に`partitionOwnerLoadColumns`のimport/使用・`hidden lg:grid lg:grid-cols-2`/`lg:hidden`の存在、`.editorPanel`内`color: var(--ink);`（`expectPattern`）、`let nextEditor: EditorState;`・task/milestone存在確認・`unit.kind === "issue"`のID検証・状態clear後のsetEditor呼び出し順のアンカー、旧「検証前にoverlay stateを一括clearする」パターンの不在（`expectNotIncludes`）を追加した。
- Round 33（2026-08-02、v3.54.4）: **週次管制のH1厳密化・ガント3レーン統合・関係先クリック契約の provenance 化**。**(1)** `SxWeeklyControlDashboard.tsx`のH1を`SolvioraX PJワークスペース`固定へ変更（プロジェクト名の動的埋め込みを廃止）。ヘッダー/セクション見出し/六指標帯（`workloadBand`）を圧縮し、desktopでの帯高さを64px以下に収める（各ボタンは`min-height:44px`のヒットターゲットを維持）。週次フローレール（`flowStep`）の`min-height`を326pxから約200pxへ圧縮し、内部padding/アイコン/余白も比例して縮小。`SxProjectOwnerWorkload`は閉じた担当者行を`lg:grid-cols-2`（desktop 2カラム、mobile 1カラム）へ再構成し、padding/min-heightを圧縮。**(2)** `SxUnifiedTimeline.tsx`のガントは常に**事業開発／技術開発／組織開発の3レーンだけ**を描く（旧4本柱＋独立`FOUNDING_LANE_META`「設立前提」レーンを廃止）。資金調達(funding)は独立レーンを持たず組織開発へ統合（`DISPLAY_LANE_LABEL`/`DISPLAY_LANE_ORDER`）。設立前提の2件（有償PoC口頭合意・出資口頭合意）は`BLOCKING_MILESTONE_LANE`でそれぞれ事業開発／組織開発レーンへ強制配置し、他の全マイルストーン/タスク行は元のレーン対応（`displayLaneKeyForTrack`）どおり1回だけ描画する。両ブロッキングMSは引き続き初期展開。日程未設定でも◇（マイルストーン）マーカーを消さず、位置は日付比例（left:X%）ではなく既存の「日程未設定」テキスト直後という非時間的な位置に置く（捏造日付/Xマークにしない）。`ROW_H`を68→48pxへ、月/凡例/ピン行/ツールバーも比例縮小。**(3)** 関係先由来の共通母集団クリック（`navigateToWorkUnit`）はスクロールせず、必ず`unit.originResource`（`SxHoldingItem`/`SxProjectWorkUnit`が保持する新規discriminator。`"partner_work_item"|"commitment"|"partner_next_action"`）で判定し、`recordId`/`partnerId`から対象レコードを直接引いて単一の編集モーダルを開く。IDの一致だけで`partner.workItems`→`partner.commitments`の順に探索するフォールバックは行わない（provenance不一致は他resourceへfall throughしない）。`partner_work_item`は既存`edit_partner_work_item`、`commitment`は新設`edit_commitment`（`resource: "commitment"`、`project_management_partner_commitments`をPATCH）、`partner_next_action`（構造化保有が無いpartnerの`sxPartnerPrimaryIntervention`フォールバック行）は既存`edit_partner`を`fieldKeys`で`next_commitment`/`owner_label`/`due_date`/`due_date_precision`/`current_ball_side`/`current_ball_owner`の6項目だけへ絞った`PARTNER_NEXT_ACTION_FIELD_KEYS`で開く（関係先編集の全項目フォームは出さない）。開く前に`workloadFilter`/`detailEditor`/`planFieldEditor`/`editorFieldKeys`を必ずクリアし、モーダルは常に1つだけになる。migrationなし。回帰防止: `npm run test:sx-project-owner-load`は`originResource`/`recordId`/`partnerId`の3 provenance（work item/commitment/fallback）と非partner unitのnull化を検査、`npm run test:sx-partner-holdings`は`sxHoldingsForPartner`が返す`SxHoldingItem.originResource`を検査、`npm run test:critical-ui`は3レーン専用識別子（`DISPLAY_LANE_ORDER`/`DISPLAY_LANE_LABEL`/`BLOCKING_MILESTONE_LANE`/`事業開発`/`技術開発`/`組織開発`）の存在と旧`FOUNDING_LANE_META`/`FOUNDING_LANE_KEY`/`founding-prerequisites`の不在、H1の`SolvioraX PJワークスペース`固定文言、`edit_commitment`/`PARTNER_NEXT_ACTION_FIELD_KEYS`/`unit.originResource`分岐/旧`data-sx-anchor`スクロールの不在を検査する。
- Round 39（2026-08-02、v3.54.13）: **担当負荷desktop 2カラムの実ブラウザCSSバグ是正（紫背景のgrid行stretch）**。Round 37/38で右detailsのheight/Y座標固定はコード上達成していたが、まさが本番実ブラウザで確認すると、外側`<div className="hidden gap-px bg-[#d9cfde] lg:grid lg:grid-cols-2">`の既定`align-items: stretch`により、各`OwnerLoadColumn`（`flex flex-col gap-px bg-[#d9cfde]`）がgrid行の高さ（＝左右どちらか展開している側の高さ）までstretchされ、`OwnerLoadColumn`自身の紫背景（`#d9cfde`）が伸びた分の空白として反対列の下に巨大な紫ブロックを作っていた（gap-pxの背景色トリックで境界線を作る実装が、意図せず大面積の紫stretchも副作用として持っていた）。`SxProjectOwnerWorkload.tsx`のdesktop 2列gridへ`lg:items-start`を追加して各列を自列の内容高さだけに止め、外側gridと外側`<section>`の大面積背景を生成り`#fffdf7`（section地と同色）へ変更。左右境界の1px線はgapの背景色トリックをやめ、`relative`コンテナ内に`aria-hidden="true"`の独立absolute divider（`className="pointer-events-none absolute inset-y-0 left-1/2 w-px -translate-x-1/2 bg-[#d9cfde]"`）として切り出した（grid行トラック高さは`items-start`でも従来どおり最大子要素高さのままのため、`inset-y-0`は常に両列の高さいっぱいに伸びる）。`OwnerLoadColumn`内部の各details間1px区切り（列ローカルな`gap-px bg-[#d9cfde]`）は列が自列高さで自己完結するため無変更で維持。row-major分割（`partitionOwnerLoadColumns()`）・mobile単列描画（`lg:hidden`、元配列順）・右detailsのheight/Y座標固定（Round 37/38）は無変更。migrationなし。回帰防止: `scripts/check_pwa_critical_ui.cjs`のexpectIncludesへ新DOM構造（`<div className="relative hidden lg:grid lg:grid-cols-2 lg:items-start">`、absolute dividerのclassName全文、`className="border border-[#c9bfd0] bg-[#fffdf7]"`）を追加し、expectNotIncludesへ旧`"hidden gap-px bg-[#d9cfde] lg:grid lg:grid-cols-2"`と`bg-[#f7f3f7]`を追加した。
- Round 40（2026-08-02、v3.55.0）: **週次管制ガントの直接ポインタ編集（移動/リサイズ/MS配置）、root editor/task・milestone detailモーダルのレコードシート統一、work-in（milestone/task）楽観的並行制御を追加**。**(1) migration 220**は`project_management_milestones.timeline_kind`（`'phase'|'milestone'`、既定`'phase'`、CHECK制約）を追加し、既存の設立前提2milestone（`business-paid-poc-oral-agreement`/`funding-investment-oral-agreement`）を`'milestone'`へbackfillした。合わせて`planned_start<=planned_end`（両方非NULL時）のCHECK制約を追加。`project_management_milestones`/`project_management_tasks`は既にmigration 183/200由来の自動増分`version`列と`project_management_touch_updated_at`トリガーを持っており、本Roundはこの既存列を使う（新規version列は追加していない）。**(2)** `src/lib/sx-gantt-drag.ts`を新設し、厳密なYYYY-MM-DD実在日付判定（`isStrictCalendarDate`、2/30等を弾く）、px⇔日変換、バー中央ドラッグ（`computeBarMove`、期間保存）、左右ハンドルリサイズ（`computeBarResizeStart`/`computeBarResizeEnd`、相手側日付を越えたら相手側でclamp）、マイルストーンドラッグ（`computeMilestoneMove`）、クリック/ドラッグ閾値判定（`isWithinClickThreshold`、既定4px）、`planned_start<=planned_end`検査（`isValidPlannedRange`）、MSを置くクリック位置→日付変換（`dateFromPct`）を純関数として提供。`scripts/test_sx_gantt_drag.mjs`（`npm run test:sx-gantt-drag`）で全関数を単体テストする。**(3)** `SxUnifiedTimeline.tsx`は`timelineKind`/`version`をDisplayRowへ追加し、`row.isBlockingMilestone || row.timelineKind === "milestone"`のときだけ◇（マイルストーン）を描画する（スラグ推測をやめ、設立前提2件のslug特例は`isBlockingMilestone`として維持）。`canManage`かつ日付ありの通常バー行はポインタドラッグで中央移動・左右ハンドルでリサイズでき、`pointerdown`でpointer captureし`pointermove`でローカルpreview（`plannedStart`/`plannedEnd`を仮の値へ差し替えてpct再計算）、`pointerup`で4px未満なら通常クリック（`justDraggedRef`で直後のnative clickへselectionを委譲し、二重発火・click/drag競合を防ぐ）、4px以上ならPATCH（`expected_version`同梱）を送る。Escapeまたは`pointercancel`で`drag` stateを破棄し何も保存しない。保存成功で`onManagementChange(bundle,message)`、409は`refetchManagementAfterConflict()`でGET再取得し通知する。新規「MSを置く」トグルボタンは`canManage`かつ`projectId`ありのときだけ表示し、有効化中はレーン領域全体に配置用オーバーレイを重ね、クリック位置のX座標→`dateFromPct`で日付、Y座標→`laneAtOffsetY`でレーン判定し、`timeline_kind:'milestone'`のPOSTで新規milestoneを作成後すぐ選択して詳細を開く（`objective_id`/`outcome_id`はPJの目的・対象track成果から自動解決、未確認ならnoticeで中断）。`SxUnifiedTimeline`の`projectId`/`onManagementChange`はoptionalで、渡さない埋め込み（`SxExecutiveControlDeck.tsx`の経営状況図）は従来どおりクリック選択専用のまま変化しない。**(4)** APIは`route.ts`の`dateValue()`を`isStrictCalendarDate`ベースへ強化し、milestone/task の create/patch で merge後`planned_start<=planned_end`を検査、milestone作成時`timeline_kind`を`'phase'|'milestone'`のenumとして受理（既定`'phase'`）。PATCHは`milestone`/`task`リソースで任意の`expected_version`を受け取り、指定時は現在の`version`と不一致なら`{error, code:"version_conflict", currentVersion}`を409で返す（未指定の呼び出し元＝旧`/workspace` `ManagementEditor`は従来どおり無検査で動く、後方互換）。**(5)** `sx-management.ts`の`SxManagementMilestone`/`SxTask`型へ`timelineKind`/`version`を追加し、selectへ`timeline_kind,version`/`version`列を追加。**(6)** `IssueEditor`（root/embedded/inlineField共通）と`PlanInspector`をivory record-sheet系へ統一。overlay `rgba(36,35,31,.38)`+`blur(2px)`、opaque `#fffdf7`ダイアログ、1px warm border、3px深緑top rule、6px radius、fixed header(76px)/footer・bodyだけscroll（`.editorBody`新設）、root editor幅約760px（`create_milestone`/`edit_milestone`/`create_task`/`edit_task`のみ`data-editor-width="wide"`で約920px）、control 44px/13pxのivory inset（`box-shadow: inset`）。フィールドは`title`/`statement`/`name`/`commitment_text`のうち存在する1つを本文最上段の単独primary fieldへ、残りを`分類・接続先/日程・進捗/状態・担当/内容`の4意味groupへ自動分類（`groupFormFields`、キー/type基準、editor種別ごとの個別マップ不要）。`editorContext`の対象見出し行は、その値が本文fieldとして実際に見えているとき（`hasVisibleField(key)`）だけ抑止し、`title`/h2/入力欄への三重表示を防ぐ。dirty close は`window.confirm`・第二dialogを開かず、footerを「変更を破棄する？ / 戻る / 破棄」へその場で差し替える（`confirmingDiscard` state）。`PlanInspector`側（外側の詳細closeボタン/Esc/背景クリック）でdirtyな内側editorがある場合は、`requestDetailClose()`がwindow.confirmを出す代わりに内側editorの「キャンセル」を疑似clickし、同じinline確認へ委譲する（第二の確認UIを増やさない）。初期focusはclose buttonではなく最初の編集可能fieldへ移し、必須未入力エラー時は該当fieldへfocusする（`focusField`、`name`属性ベース）。migrationは(1)のみ。回帰防止: `npm run test:sx-gantt-drag`（新規）、既存`npm run test:sx-weekly-control`/`test:sx-project-owner-load`/`test:sx-gate-requirements`/`test:sx-management`/`test:sx-executive-control-deck`/`test:critical-ui`は無回帰。

- Round 41（2026-08-02、v3.55.0）: **Round 40の追加監査是正（PlanInspector共有tray化・ガント二重PATCH/誤ドラッグ起点/lostpointercapture・MS配置の即時POST廃止・expected_version必須化・補償ロールバックCAS化）**。詳細は`spec/6-1-appendix-changelog.md`のv3.55.0是正エントリを正本とする。要点のみ: `factEditableValue`/`FACT_SLOTS`を廃し`editableValue`へ統一、破棄確認を`role=status`/`aria-live`＋`requestCloseRef`契約へ、600px以下でroot editorもbottom sheet化、ヘッダーgradient撤去、パンくず/状態チップ重複を削除。RowBarの取っ手をpointerdown-onlyへ（move/up/cancelは親1箇所）、行全体からの誤ドラッグ開始を廃し実バー/◇のみの44px以上hit-targetへ、`lostpointercapture`を`shouldCancelOnLostPointerCapture`で判定。一般MSは`computeMilestoneMove`で常に一点化、設立ゲート2件は新設`computeGateEndMove`で完了日のみ移動。`placeMilestone`の直接POSTを全廃し`onCreateMilestone`プレフィル→既存フォーム→保存時のみPOSTへ。milestone/task PATCHの`expected_version`を必須化（欠落・不正400）し`SxNavigationDashboard`/`ProjectWorkspaceDashboard`の呼び出し元も追従、履歴insert失敗時の補償ロールバックを`updatedVersion`へのCASへ変更。migration 220はp21スコープ・inverted-range事前検査・point-MS CHECKを追加（未適用）。回帰防止: `npm run test:sx-gantt-drag`拡張＋新設`npm run test:sx-gantt-ui-contracts`。
- Round 42（2026-08-02、v3.55.0）: **Round 41以降の最終監査是正（dragRef同期・狭いbarの外付けresize hit target・一般MS単一予定日・projectIdなし埋め込みの誤保存・MS作成prefill保持・MS種別の視覚差・requestCloseRef/dirty intent・edit trayの文脈・focus管理・embedded scroll・409再同期・NewCo例外のp21スコープ・parent-integrity DB trigger）**。詳細は`spec/6-1-appendix-changelog.md`のv3.55.0監査是正エントリ群を正本とする。開始・完了がある通常バーは同日/数日でも3操作を維持し、最低16pxの中央hit領域を確定した外側へ16pxの開始/完了hit targetを置く。日付領域の左右22pxを操作余白とし、同日バー・表示期間端の取っ手・44pxの◇をgrid内へ保つ。DBは子milestoneの作成/復元だけでなく、親outcome/objective側のproject・所属・track・soft-delete変更もtriggerで監視する。回帰防止: `npm run test:sx-gantt-drag`（日程計算/`shouldCancelOnLostPointerCapture`）、`npm run test:sx-gantt-pointer-gesture`（純関数群を使ったpointerdown→move→upの複数イベントジェスチャー検証）、`npm run test:sx-gantt-ui-contracts`拡張（外付けhandle、作成prefill、409再同期、dirty intent、p21スコープ、parent-integrity triggerの構造的guard）、`npm run test:critical-ui`。

- Round 43（2026-08-02、v3.55.1）: **MS作成導線もガントの3レーン契約へ統一**。本番で「MSを追加」を開くと、ガント本体の事業開発／技術開発／組織開発とは別に、旧DB trackの資金調達／体制構築を含む4択「柱」が露出していたため削除した。`create_milestone`は「接続する成果（配置レーン）」だけを選び、表示labelは`ganttLaneLabelForTrack()`で3レーンへ正規化する。POSTするraw `track`は選択outcomeから導出し、funding/organizational_buildingは同じ組織開発表示へまとめながら`milestone.track = outcome.track`のDB親整合を維持する。PlanInspectorのパンくずも3レーン名に揃えた。migrationなし。

- Round 44（2026-08-02、v3.55.1）: **wide root editorのmobile幅を100%へ固定**。600px以下のmedia queryで`.editorPanel[data-editor-width='wide']`も明示的にwidth 100%へ上書きし、MS/工程/タスク作成・編集だけ左右24pxずつ空いた浮遊sheetへ戻るCSS詳細度競合を防止。全root editorが同じbottom sheet契約を満たす。migrationなし。

- Round 45（2026-08-02、v3.55.1）: **複数outcomeを含む表示レーンではMS親成果を暗黙選択しない**。`placeMilestone()`は同じ3表示レーンに属するoutcomeが1件だけのときに限りprefillし、funding/organizational_buildingの2候補を含む組織開発では未選択でフォームを開く。利用者が正確な接続先成果を選ぶまで保存できない。migrationなし。

- Round 46（2026-08-02、v3.55.7）: **ガントの追加操作を行内へ統合し、全モーダルを同じ操作面へ再設計**。各3レーンの末尾へ権限者向け48px固定行 `＋ 新規タスク` を1行ずつ常設し、ツールバーと工程/タスク詳細の追加ボタンを廃止。追加フォームは表示レーンから保存先を推測せず、そのレーン内でガントに表示中の未完了phase/設立ゲートを `接続する工程` として必須選択する（候補1件だけ事前選択、0件は保存不可）。親タスク候補は選択工程の配下だけに連動し、工程変更時は以前の親選択を解除する。新規MSは専用ボタン/配置モードを廃止し、ガント行の実バー/◇以外の空白日付ポイントを直接クリック→`MSを追加する？ Y/N`→Yで既存IssueEditorを開く。mobileはレーン内小型日付軸、keyboardはfocus面の中央日付で同じ入口へ入り、Y/Nが上端で切れる場合は下へ反転する。N/Esc/外側クリックは無書込みで閉じ、Yでもフォーム保存まではPOSTしない。バー/◇の詳細選択とdragは兄弟buttonへ分離し、空白クリックとの二重発火を防止。IssueEditor・PlanInspector・関係先履歴を生成りsheet、12px角丸、1px暖色border、rgba(36,35,31,.46)+4px blur、固定header/footerへ統一し、640px以下は同じbottom sheet・16px controlにする。フォームは接続先→内容→状態/担当→日程の順と左見出しrailへ整理。migrationなし。
- Round 47（2026-08-02、v3.55.8）: **本番実操作で検出した未日程行のMSクリック遮蔽を解消**。日程未設定の工程・設立ゲート行で、右側タイムライン下部を覆っていた全幅の詳細ボタンを廃止し、実バー／◇がない日付面は全域をMS追加候補へ渡す。工程詳細は左側の名称行から従来どおり開ける。必須タスク要約は`pointer-events:none`として空白日付面を遮らない。構造テストで未日程の全幅詳細ボタンが復活しないことを固定。migrationなし。
- Round 48（2026-08-03、v3.57.0 / migration 223）: **月次報告書の編集履歴を本文と同時に保存し、印刷ビューで確認可能にした**。ツールバー直下の最終更新表示から、desktopは右パネル、mobileは下シートを開く。社内版／提出版タブ、更新者・操作・時刻・変更章、展開時だけ取得する変更前後の行差分を提供する。migration前の既存記録は値を推測せず詳細差分なしとする。本文と履歴はRPCの同一transactionで保存し、RPC外writerもtriggerで捕捉する。初期版はadmin限定、復元操作なし、PDFは個別編集を載せず`版・発行履歴`として節目だけを残す。
- Round 50（2026-08-05、v3.58.0 / migration 233-235）: **SX関係先台帳へPoC営業ファセットと試料台帳を統合**。PoC候補先スプシ（26社×20列+履歴40件+サンプル21件）をp21関係先リストへ取り込み、スプシ側の独立管理を廃止できる状態にした。`relationship_stage`をPoC営業段階（first_contact/hearing/technical_review/sample_acquisition/declined追加の13値）へ拡張し、`activity_state`（対応中/先方待ち/社内待ち/停滞/保留/見送り/未確認）と`poc_category`（PoC候補先/技術協力先/試料提供元/試料提供ルート）を新設。`poc_category`保存行は`現在の状況`cell上段に段階/状態/確度の3チップを常時表示し、押すと同位置の2x2 select群で1PATCH編集。PoC候補先タブは保存済み区分を最優先で母集団判定し、`区分`/`段階`filterチップ行が営業ファネルKPI（段階別件数）を兼ねる。タブ3枚は不変。試料は新テーブル`project_management_partner_samples`（1社N試料の軽量台帳）で、`履歴・保有`列に件数、履歴モーダル内`試料台帳`節で行内編集+試料名だけの追加（第二モーダル禁止のまま）。スプシの要確認内容27件はwork_item(question)化し、担当未確認をownerなしで担当不明バケットへ浮上させる。未確認値は未確認のまま取り込み（確認済み=high/要確認=low）。H-0038〜40（関係先に紐付かない全体メモ3件）は対象外。
- Round 49（2026-08-03、v3.57.2）: **SX関係先台帳のdesktop密度退行を是正**。広幅8列への切替を内側containerの`@min-[1280px]`で判定していたため、1440px相当でも実一覧幅1261pxでは条件を満たさず、8項目が6段へ折り返されて1社約504pxまで膨張していた。8列の実必要幅に合わせてcontainer breakpointを1248pxへ修正し、1261pxでは1行化、1248px未満では右端をclipさせず2列へ戻す。会社名・最終確認・可変segment railは先頭cell内へ横並びに収め、行padding/gap/履歴列も圧縮した。`現在の状況`は224pxを確保し、固定2slot編集、rail件数、8列の意味は不変。構造テストは旧`@min-[1280px]`の再導入を禁止し、compact row anchorと1248px gridを固定する。migrationなし。
- Round 50（2026-08-03、v3.57.5）: **SX関係先名の全文表示、接点経緯の追加、無変形インライン編集**。9列を`関係先236px / 接点の経緯148px / 現在の状況196px / ゴール104px / 詰まり96px / 次にやること132px / 担当・期限72px / 現在地の根拠104px / 履歴72px`へ再配分し、先頭cell内でも名称本文に160pxを確保。名称の`truncate`を廃止して自然改行で全文表示する。名称は汎用`InlineCellEditor`から専用`InlinePartnerNameEditor`へ分離し、編集時は同じ位置の文字だけを透明textareaへ置換する。背景・下罫線・行高・最終確認日・segment railを維持し、旧緑左枠、編集中だけの「関係先名」ラベル、大型保存/取消行を出さない。Enter/blur保存、Esc取消、IME変換中Enterの誤保存防止を実装。blur後の次操作再現はpointerdown済みの対象だけに限定し、Tab移動で勝手に別editorや履歴を開かない。`project_management_partners`へ`connection_context`と`introducer_label`を追加し、一覧で固定2段表示・同位置編集する。migration 201で`agreed_scope`へ仮置きされていた伊予銀行紹介3社だけをmigration 224で移し、他社は推測しない。1248pxのcontainer切替と9列1行契約は維持。
- Round 51（2026-08-04、v3.57.8）: **SXガントのネスト導線を明示し、既存タスクも付け替え可能に**。各3レーン末尾の唯一の`＋ 新規タスク`行は維持。作成フォームは接続工程の直後に`ネスト先（親タスク）`を出し、`工程の直下（親なし）`または`〜の子にする`を文章で選べるようにした。タスク詳細にも同じ値を常設し、選択工程内の親への付け替えと、工程直下への解除へ実際に到達できる。migrationなし。
- Round 52（2026-08-04、v3.57.9）: **SXガントのタスクネストを選択肢から直接ドラッグへ置換**。各タスク名の左にだけ階層用グリップを置き、親にしたいタスク行へ重ねて子にする。工程行へ落とすと工程直下へ戻る。日程バーの左右dragとは発火面を完全に分離し、同じ工程以外・自分自身・子孫・現親はdrop targetにしない。drop時はtaskの`parent_task_id`だけをexpected_version付きPATCHで更新し、保存後は新しい親を自動展開する。mobileも同じグリップと受け皿を使い、Esc／pointer cancelは無書込みで中止する。フォーム内の親タスクselectと詳細の二次編集導線を廃止。migrationなし。
- Round 53（2026-08-04、v3.57.12）: **SXガントのMS◇を見えている印そのものから編集可能に**。◇は表示専用の`pointer-events:none`要素ではなく、44px以上のクリック／タップ／keyboard targetを持つbutton内へ置く。ドラッグなしの◇クリックは該当MSの既存詳細編集を開き、ドラッグだけが日程変更となる。依存接続モードのportと空白日付面のMS追加導線はそのまま分離する。階層グリップは`project_management_tasks`にだけ出す。工程・MSは親子task fieldを持たないため、見かけだけの階層dragを追加しない。migrationなし。
- Round 54（2026-08-04、v3.57.13）: **日程未設定MSの◇も直接編集に統一**。日程が無いMSは時間軸上に正直なX座標を持てないため、従来どおり「日程未設定」の左に◇を置く。ただしこの印を`pointer-events:none`の飾りに残さず、44pxのbutton内へ移し、押すと同じMSの既存詳細を開く。これにより、日程未設定の◇を押して下層の空白日付面（新規MS追加）へ誤って流すことはない。日程あり◇と日程未設定◇は同じ`data-gantt-milestone-marker`契約で回帰を検出する。migrationなし。
- Round 55（2026-08-05、v3.57.18 / migration 232）: **ガント依存線を可視オブジェクトの端点へ厳密に接続し、9親タスク名を圧縮**。依存線は日付座標を直接使わず、最低表示幅16pxを含む可視タスクバーの右端中央から後続バーの左端中央へ接続する。後続開始が近い／前にある場合は5区間の直交迂回とし、終端直前を必ず左→右にする。MSは日付座標へ◇中心を固定し、右頂点を起点・左頂点を終点にする。矢印マーカーは8pxの`userSpaceOnUse`・`refX=8`として先端だけを境界へ接触させる。行高48px・操作領域44pxは維持し、バーは行中央の10pxへ拡大。9親タスクの一覧名は短縮し、元の長文を空の`description`へ保存して既存詳細モーダルの「内容」で表示・編集する。詳細パンくずは互換用親MSでなく`task.track`を優先する。経路計算は`src/lib/sx-gantt-dependency-route.ts`へ分離し、通常・同日・逆転・最低幅・task/MSを単体検査する。

- Round 56（2026-08-05、v3.57.21）: **SX週次管制の既存レコード保存を画面先行にした**。タスク・MS・論点系の詳細／セル編集と、関係先台帳の直接編集は、PATCH値を同じ画面モデルへ即時反映して編集面を閉じ、DBの書込み・全体bundle再構築は背景で行う。成功応答の古い全体bundleは後から画面へ戻さず、直後の次編集を消さない。失敗・409だけはDBの最新bundleへ戻し、理由と再読込が必要な状態を通知する。新規作成はDB採番IDが必要なため従来どおり保存完了を待つ。`sxApplyOptimisticManagementPatch`の単体検査と、保存順序／失敗再同期の構造検査を追加。migrationなし。

- Round 57（2026-08-05、v3.57.22）: **SXガントの依存線を短い経路とホバー解除へ整理**。終点を可視オブジェクト境界に接続する契約は維持したまま、通常の前方判定を44pxから11px、迂回時の始点側の逃がしを20pxから5px、終点側の進入を16pxから4pxへ縮小し、不要な大回りを抑える。desktopは描画線を細いまま保ち、透明10pxの当たり判定だけで線を拾う。ホバー中の線だけを強調し、ポインタ近くに小さな`外す`を出して既存のsoft deleteを呼ぶ。接続モード中は新規接続を優先してこの操作を表示しない。mobile／keyboardには既存の`依存関係`一覧の解除操作を残す。ルート単体検査に短い逃がしと12px前方接続を追加し、構造検査でhover契約を固定。migrationなし。
- Round 58（2026-08-06、v3.58.1）: **確度の4値化・確度順ソート・接点の経緯の右寄せ・週次管制の配色コントラスト是正**。確度は`SX_PARTNER_CONFIDENCE_ORDER`（high/medium/low/unknown = 確認済み/推定/要確認/未確認）を正本とし、従来`medium`を「確認済み」へ丸めていた2値表示を廃止する。3チップは「段階」「状態」「確度」の軸名をチップ内に必ず持ち、値だけを並べない。`SxPocComparisonSort`へ`"confidence"`を追加し、件数バンドの`data-partner-sort-trigger`で「要対応順（既定）/ 確度順」を切り替える（インライン編集中は切替を保留）。9列順は`関係先→現在の状況→…→現在地の根拠→接点の経緯→履歴・保有`で、grid定数2本・ヘッダーspan順・行内cell位置の4箇所を必ず同時に動かす。`Sx*.tsx`/`sx-*.tsx`の色トークン429箇所を機械置換し、WCAG AA未達だった補助文字色（#8f8a7e/#928c80/#9b9487 ≈3:1）を#65604f（6.18）へ、主要罫線#d6cebfを#ada18a（1.54→2.53）へ引き上げた。色相と意味の割当（赤=事業リスク、琥珀=待ち・要確認、緑=段階、青=確度高）は変えない。migration 236でいづつ重複行のsoft delete・誤`connection_context` 3件のNULL化・H-0038〜40の論点登録を実施。
- Round 59（2026-08-06、v3.58.2 / migration 237）: **PoC実現可能性・顧客有望度の判断2軸と優先度バッジ**。`confidence`は情報の確からしさであって商談の見込みではない（実装上も`sxPartnerNeedsRefresh`の鮮度判定にしか使わない）ため、見込みを相乗りさせず`poc_likelihood`（PoCを実施させてもらえる可能性）と`customer_value`（顧客としての有望度＝支払意欲・回収価値）をhigh/medium/lowのcheck制約付き列として新設し、判断理由を`value_note`（240字）へ持たせた。未評価はNULLで、推測で埋めない。`sxPocPriorityTier`が2軸をhigh=3/medium=2/low=1で合成し6→S・5→A・4→B・3→C・他→D、片方でもNULLなら`unrated`（表示「未」）。会社名の直前に`data-poc-priority-tier`バッジ、`現在の状況`チップ群へ「実現」「顧客」と`value_note`の理由行を追加し、列は9本のまま増やさない。`SxPocComparisonSort`へ`"priority"`を追加し**既定sortをpriorityへ変更**（まさ「このリストの上の方を優先的にアタックしていく設計になっていれば、見る方はすごく楽」）。インライン編集は同じ位置のまま段階/状態/確度/区分/実現/顧客の6select＋理由inputを1PATCHで送る。
- Round 60（2026-08-06、v3.58.3 / migration 238）: **関係先の評価を最左の独立カラムへ・排液プロファイル・議事録の全文保存**。`poc_likelihood`×`customer_value` の合成でS〜Dを出す方式は「排液がもらえるか」を過大評価していたため廃止し、`poc_grade`（s/a/b/x、未評価はNULL）を人が直接付ける独立列にした。判断軸は (1) ペインの高さ (2) 単価の高い（需要の高い）重金属が排出されているか の2つで、排液提供の可否は大幅に劣後させる（まさ 2026-08-06）。`sxPocPriorityTier` は `pocGrade` だけを読み、表示は S/A/B/✕/未。評価は会社名cellから外し、行の最左34pxの `data-partner-grade-cell` へ置いてinline selectで直接変更する。`effluent_components` / `effluent_volume_annual` / `effluent_cost_annual` を追加し、`現在地の根拠` と `接点の経緯` のあいだへ `排液` cellを新設（未入力は「成分 未確認」等と明示）。列は11本になり、内側9列は 1248px から padding/評価/履歴/gap を引いた1046px以内（現行1036px）へ収める。`project_management_partner_interactions.detail_md`（40000字）へ議事録本文を全文保存し、履歴モーダルで折りたたみ表示する（plaud等の共有リンクは失効し `source_ref` だけでは内容が残らないため）。
- Round 61（2026-08-06、v3.58.4 / migration 239）: **関係先台帳の次回面談カラムと、評価同点時の情報鮮度ソート**。`project_management_partners`へ`next_meeting_on`(date) / `next_meeting_time`(text) / `next_meeting_mode`(`onsite`/`online`/`hybrid`/`phone`のcheck、NULL=未定) / `next_meeting_place` / `next_meeting_prep`を追加。面談予定は`next_commitment`+`due_date`（こちら側の作業）と別fieldで持ち、混ぜない。時刻は「午後」「9月上旬」のように確定度が揃わないため`time`型で縛らずtext。型`SxMeetingMode`は`sx-management.ts`（server-only）だが、`SX_MEETING_MODES`/`SX_MEETING_MODE_LABEL`はclient componentからも読むため`sx-partner-progress.ts`へ置く（server-onlyモジュールから値をexportしclientでimportするとturbopack buildが落ちる）。UIは`次にやること`の右に104pxの`次回面談`cellを新設（`InlineCellEditor`、`keyFor("next-meeting")`、日付/時刻/形式select/場所/準備すべきものの5項目を1 PATCH、空文字はNULLへ戻す）。列は12本、`PARTNER_CONTROL_HEADER_GRID`の内容合計は1144pxで`@min-[1248px]`予算（1248−gap-2×11−px-2）ちょうど。`sxComparePartnersForPoc`の`priority`は第2キー`lastVerifiedAt`降順・第3キー`lastContactDate`降順を追加し、評価同点の中で情報が新しい先を上へ出す。management APIのpartner patchは`takeDate`/`takeOptionalText`/`enumValue`で検証。build v3.58.4。
- Round 62（2026-08-06、v3.58.5）: **✕を未評価より下へ・評価cellのpopoverを右開きへ**。`PRIORITY_TIER_ORDER` を `["s","a","b","unrated","x"]` にし、「優先度順」の並びを S → A → B → 未 → ✕ にする。`✕` は顧客候補として適さないと判明済みであり、まだ判断が入っていない `未` より劣位として扱う（まさ 2026-08-06「『✕』は未より下にソーティングして。顧客候補としては適してないことが判明したわけだから」）。入力時の選択肢順 `SX_POC_GRADE_CHOICES` は S/A/B/✕/未 のまま変えない（見つけやすさとソート順は別要件）。`InlineCellEditor` に `align?: "center" | "start"`（既定 `center`）を追加し、popoverを `left-1/2 -translate-x-1/2` と `left-0` で切り替える。最左の評価cellは幅34pxしかなく中央寄せだと320pxのpopoverがviewport左外へはみ出していたため `align="start"` を渡し、cellの左端から右へ開かせる。今後左端に狭いcellを足す場合も同じ扱い。migrationなし。
- Round 63（2026-08-06、v3.58.6）: **関係先台帳の省略なし表示・列幅のUI調整・先頭列先頭行の固定**。台帳のcellから `truncate` / `line-clamp-2` を全廃し `break-words` へ変え、`現在の状況` の下段トラックも `minmax(30px,auto)` にして内容の行数ぶん伸ばす（まさ 2026-08-06「各カラムの情報は、『…』で省略せずに表示して」）。履歴モーダルの `PartnerProgressFlow` チップだけは固定幅の別UIなので `line-clamp-2` を残す。列定義の正本を `PARTNER_LEDGER_COLUMNS`（key/label/title/defaultWidth/minWidth）配列にし、固定Tailwind文字列 `PARTNER_CONTROL_HEADER_GRID` とヘッダーspanの手書き列挙を廃止。TailwindのJITは動的 `grid-cols-[…]` を出力できないため、幅は `partnerLedgerGridStyle()` が root の `@container` へ `--sx-pl-inner` / `--sx-pl-header` / `--sx-pl-row` / `--sx-pl-total` を流し、gridは `@min-[1248px]:[grid-template-columns:var(--sx-pl-*)]` で読む。見出しcell右端の2px `role="separator"`（`data-partner-column-resize`）をpointerドラッグして `minWidth` まで縮められ、pointerupで `localStorage["sx-partner-ledger-column-widths-v1"]` へ保存する（DBへは保存しない。共有すると他の閲覧者の列まで動くため）。既定から動いている間だけ「列幅をリセット」ボタンを出す。横スクロールは許容し（まさ「横に長くなる分には全然問題ないよ」）、行集合を `@min-[1248px]:overflow-x-auto` + `min-width:var(--sx-pl-total)` の枠で包む。**先頭行の見出しは枠の外**に置き `onScroll` で `scrollLeft` だけ転記する（枠の内側へ入れて高さを固定すると `InlineCellEditor` の `absolute top-full` popoverが切れるため。同じ理由で枠に `pb-72`）。**先頭列（評価）は `sticky left-0`** ＋不透明背景で固定し、グループ見出しと保留/終了の折りたたみラベルも `sticky left-3` で左端に留める。v3.58.3/3.58.4の「内側列を1046px/1144px以内へ収める」予算計算は撤廃。migrationなし。
- Round 64（2026-08-06、v3.58.9）: **ガント依存線は常設の「＋」ポートから引く（`依存を接続`モード廃止）**。Round 37 で入れたモードトグルを廃止し、`SxUnifiedTimeline` の `dependencyMode` state を `dependencyDrawingEnabled`（`canManage && Boolean(projectId)` 由来の静的な可否）と `dependencySource != null`（描画中の一時状態）へ分解する。編集権限があれば日程を持つ全タスクと全MSの右側に常設の「＋」ポート（`data-gantt-dependency-source`）が hover / focus で現れ、押したままドラッグして他タスク・MS上で離すと `finish_to_start` を1本作る。ポートは既存の当たり判定と重ならない位置に置く — タスクはバー右端 `calc(right + 16px)`（終了日リサイズ取っ手の外側）、MSは◇中心 +22px（`DRAG_HIT_HEIGHT/2`、44pxドラッグ箱の外側）から始まる幅24px。日程ドラッグ・階層ドラッグ・空白点MS追加の抑止条件は「モード中」から「起点をarm中」へ移したため、通常時はこれらが全て効く。レーン縦断MSマーカーは◇本体を日程ドラッグ／詳細クリックのまま残し、`dependencySource` がある間だけ接続先として振る舞う（起点化は隣接する専用の＋ボタンが担う）。mobile一覧の起点ボタンは `Link2`→`Plus`、接続先ボタンは起点arm中だけ表示して行の密度を保つ。DB側は無変更（MSを接続先にする `successor_type` / `successor_milestone_id` は migration 230 で導入済み）。`test_sx_gantt_ui_contracts.cjs` は `dependencyMode` を assertNotIncludes へ移し、モードトグルの復活を回帰として落とす。
- Round 70（2026-08-09、v3.69.2 / migration 247-248）: **関係先・論点の低摩擦入力と議論履歴**。関係先の排液調達はモーダルへ飛ばさず、nullable `effluent_procured`を行内PATCHする（NULLだけ既存試料導出へfallback）。新規関係先は名前＋任意分類だけ、役割は新規・編集の両フォームから削除し、APIが互換NOT NULL列を安全な既定値で補う。履歴列名`poc_grade`は保持しつつ、評価の意味を全分類共通のPJ優先度（最優先/有望/継続確認/現時点対象外）へ変更。論点表はAMD Blue 4階調へ統一し、種類・期限を明示、種類＋本文＋任意期限だけで作成できる。`project_management_issue_discussions`は論点ごとの議論を追記保存し、詳細内の進捗ログと検索・最終活動日へ反映する。期限表示と期限超過判定は同じ`sxWeeklyIssueNextDueDate()`を読む。
- Round 71（2026-08-09、v3.70.2 / migration 250）: **高密度の論点ワークベンチ**。論点表の内容・仮説・議論・判断・次の一手からellipsisを廃止して全文表示する。行から開く詳細は、左=背景/ネスト仮説、中央=過去/今回の議論と派生仮説、右=判断/次の一手/embedded編集の3ペインdialogへ統合。`project_management_issues.background`へ発生背景を固定保存し、議論ログと分離する。解決済み操作は同じdialog footerに置き、resolved行を一覧末尾へ送る。
