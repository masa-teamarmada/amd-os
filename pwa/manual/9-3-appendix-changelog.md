# 附則（変更履歴）

> この章は AMD OS マニュアル本則（1-1 〜 9-2）の **追加・変更・削除をすべて記録する append-only の変更履歴**。まさ指示（2026-05-29）で新設。**マニュアル正本が勝手に消されたり簡略化されたりしたのを検知するための仕組み**。

## 運用ルール（厳守）

- **マニュアル本則のどの章でも、内容を追加・変更・削除したら、必ずこの附則に1行追記する**。コミットと同じ作業単位で記録する。
- 記録項目: `日付 / 対象章 / 種別（追加・変更・削除）/ 内容 / 理由 / 誰が`。
- **削除は特に必ず残す**。「なぜ消したか」「まさの承認があるか（実発言の引用）」を書く。承認のない恒久仕様の削除は禁止（→ [`AGENTS.common.md` 移植・リファクタ時の機能保全] / [3-2 §カレンダー色→PJ判定]）。
- この附則自体は **append-only**。過去エントリを書き換え・削除しない。
- 番号（「#71」等）だけで「まさが決めた」と書かない。判断の根拠はまさの実発言を引用する。

## 変更履歴

| 日付 | 対象章 | 種別 | 内容 | 理由 | 誰が |
|---|---|---|---|---|---|
| 2026-07-13 | 2-5 探索・知識アセット / 5-1 Research Assets | 追加 | `/seeds` に「深掘り資料」列を追加。確認済みの共有資料だけを一覧アイコン・詳細 modal・編集欄から扱えるようにした。build v3.39.66 | シーズごとの事業化検討資料を、一次ソース本文や生URLを混ぜずに追えるようにするため | えいみ |
| 2026-07-10 | 2-3 / 9-3 | 修正 | 要対応の行単位完了表示を手順へ明記し、通知・要対応のcloseout記録を同期。build v3.39.60 | 次の担当が、対象行だけを消すUXと現在の運用証跡を同じ正本から確認できるようにするため | えいみ |
| 2026-07-10 | 2-1 はじめて使う人向け | 修正 | dashboard / 通知画面の「要対応」で「対応済にする」を押したとき、一覧全体を読み直さず押した行だけを即時に消すように変更。更新失敗時だけ元の位置へ戻す。build v3.39.59 | 他の要対応まで一瞬消えてから戻る表示をなくし、処理した対象を明確にするため | えいみ |
| 2026-07-10 | 3-3 通知とつくよみ | 修正 | 抽出状況から根拠のない48時間の要確認判定を撤去。保存証跡とMTG抽出での利用時刻を分け、既読のNotion再認証通知を現在の障害として出さないようにした。build v3.39.57 | 保存時刻の古さを接続障害と誤認させず、実際の抽出結果を確認できるようにするため | えいみ |
| 2026-07-10 | 8-3 L2 Extraction Routines / 先手TODO | 変更 | 先手TODOの PWA cron を、MTG起点に加えて Gmail の期限つき依頼 (`email_action_request`) も `proactive_todos` へ保存する運用へ更新。build v0.39.54 | KUTE 平本さんのメールのような「メールで新しく発生したAMD側TODO」を `/proactive` で拾えるようにするため | えいみ |
| 2026-07-10 | 8-3 L2 Extraction Routines / 設計入口 | 変更 | 先手TODOの現行設計入口を更新し、旧 commander outbox / heartbeat 案ではなく `proactive_todos` + `/proactive` + 既存Vercel cron daily 09:15 JST + Gmail `email_action_request` が現行であることを design/spec 側にも同期。build v3.39.58 | handoff 前に、使い方・仕様・設計書の3層で「メールTODOは非LLM PWA cron内」と読める状態にするため | えいみ |
| 2026-07-10 | 3-3 通知とつくよみ | 変更 | dashboard の「抽出状況」で48時間以上保存がない Calendar / Slack / Notion 等に、再認証・PJ設定不足・接続確認済みの保存停止を理由として表示し、再認証・PJ台帳・抽出運用へのリンクを追加。build v0.39.53 | 「要確認」だけでは何を直すべきか判断できないため | えいみ |
| 2026-07-10 | 3-3 通知とつくよみ / 2-6 adminオペ | 変更 | PJ台帳のSlack欄に「チャンネルなし」を追加。使わないPJは明示チェックで抽出状況の設定不足から外し、チェック時は古いチャンネルIDも消す。PJ台帳の見出し行は表を縦横に動かしても固定するように戻した。build v0.39.51 | Slackチャンネルを作るほどではないPJを設定漏れとして出し続けず、横長台帳でも列の意味を見失わないようにするため | えいみ |
| 2026-07-10 | 4-8 MS進捗 / D-2計画遅延通知 | 修正 | `ms_schedule_delay` の遅延判定で、期限後の当月行が無い場合はその月以前の最新進捗を current_pct として使うように変更。build v0.39.49 | SXの2026年6月期限MSが6月時点で100%済みなのに、7月行が無いだけで「現在0%」の計画遅延通知になっていたため | えいみ |
| 2026-07-10 | 3-3 通知とつくよみ | 修正 | `/notifications` の詳細表示で、D-11メディア掲載は `project_media_mentions` の保存済み行を表示し、未対応kindでも通知本文をfallback表示するように変更。`saved_count >= total_count` の保存済み通知は「はい・反映」ではなく「はい・確認済み」と表示する。build v0.39.48 | D-11掲載が既に保存済みなのに「抽出された行が見つかりませんでした」と出て、未反映に見えたため。他の通知でも同じ空表示が誤解を生むため | えいみ |
| 2026-07-10 | 2-1 はじめて使う人向け / 3-3 通知とつくよみ | 変更 | dashboard に「抽出状況」を追加し、Gmail / Drive / Calendar / Slack / Notion のOSへの最終保存時刻と、月次対象PJのメール・Slack・Drive設定不足をまとめて見せるようにした。設定不足は通知で「はい・いいえ」を選ばず、PJ台帳から直す導線へ変更 | 設定不足通知は、採否ボタンを押しても設定が直らず、次に何をすべきか判断できなかったため | えいみ |
| 2026-07-10 | 2-2 メンバー日常導線 / 6-6 Member Ops / 7-1 報酬計算 | 修正 | 月初合意モーダルから、月次の実データではない `到達目標` 表示を撤去。PJごとに `担当内容` を一度だけ置き、その右に担当MSを列挙する形へ変更。予定額の合計とPJ別表は内容幅に合わせた固定列に縮め、列間の余白をなくした。build v0.39.46 | MS名（例: CEO業務）を月次目標として表示すると事実と違い、同じラベルをMSごとに繰り返し、予定額表も内容より大きい幅に引き伸ばされて読みにくかったため | えいみ |
| 2026-07-10 | 2-2 メンバー日常導線 / 6-6 Member Ops / 7-1 報酬計算 | 修正 | 月初合意モーダルの必須枠へ、各PJの担当内容と到達目標を実データで表示。`確認して合意` を主ボタンにし、その右に小さい `修正要望` を配置。支払い状況は初期閉じにし、開いた中へPJごとの支払い内訳を統合して、下段PJカードを廃止。build v0.39.44 | 必須枠が「下の詳細を確認」と案内するだけで担当内容が空欄に見え、修正要望の方が合意より選びやすく、同じPJ情報が複数のカードに分かれていたため | えいみ |
| 2026-07-10 | 2-1 はじめて使う人向け | 修正 | 左メニュー `ボード` の全アクティブPJフライアウトに、画面下端でも一覧内だけがスクロールする高さ制御を追加。build v0.39.43 | ナビ外レイヤー化後も、表示位置によって一覧が画面下へ伸びすぎないようにするため | えいみ |
| 2026-07-10 | 2-1 はじめて使う人向け | 修正 | 左メニュー `ボード` の全アクティブPJフライアウトを、ナビのスクロール領域にクリップされない上位レイヤー表示へ変更。build v0.39.42 | v0.39.41 でマウスオーバーしてもフライアウトが見えない状態になっていたため | えいみ |
| 2026-07-10 | 2-2 メンバー日常導線 / 6-6 Member Ops / 7-1 報酬計算 | 修正 | 月初合意モーダルの必須カード名を実データに合わせて `担当内容・到達目標` へ変更。常設の `直してほしいこと` カードを廃止し、合意ボタン横の `修正要望` から必要時だけ入力欄を開く。参考情報は独立カードをやめ、短い区切り見出しにした。build v0.39.42 | `conditions` が常に空なのに発注条件が入るように見え、修正要望と説明用カードが常時並んで画面のまとまりを崩していたため | えいみ |
| 2026-07-10 | 2-2 メンバー日常導線 / 6-6 Member Ops / 7-1 報酬計算 | 修正 | 月初合意モーダルの `合意前に必ず確認すること` に、予定額合計と全PJの予定額を実数で集約。必須枠より下は `参考情報・修正依頼` 見出しで、発注条件の詳細・予定額の根拠・支払い状況・修正依頼を明確に分けた。build v0.39.41 | 必須確認カードが「金額を見る」と説明するだけで、実際に確認すべき金額が枠外へ散っていたため | えいみ |
| 2026-07-10 | 2-1 はじめて使う人向け | 追加 | 左メニューの `ボード` にマウスを重ねると、全アクティブPJを右側の一覧から直接開けるようにした。build v0.39.41 | ダッシュボードへ戻らず、現在地から目的のPJコックピットへすぐ移動できるようにするため | えいみ |
| 2026-07-10 | 6-6 Member Ops / 月初合意 | 修正 | `/admin/monthly-work-agreements` と月初合意の支払説明で、`billing_cycles.invoice_ym` をメンバー支払月として扱わず、PJ台帳の支払条件から現金支払月を計算するように変更。build v0.39.40 | `invoice_ym` は請求書発行月なのに支払月として優先していたため、ZMP 202606 の報酬が 202607 支払一覧から落ち、月初合意一覧の `今月支払` が 0 円に見えていたため | えいみ |
| 2026-07-10 | 2-2 メンバー日常導線 / 6-6 Member Ops / 7-1 報酬計算 | 修正 | 月初合意モーダルの必須確認を「担当する内容と到達目標」「予定額」の2点に固定。未合意/条件更新ありでは `確認して合意` を押すまで支払いに進めないことを明記し、担当割合・今月のpt・支払い状況は初期折りたたみの参考情報へ分離 | 情報量が多い画面でも、メンバーが支払いに必要な確認だけを迷わず完了できるようにするため | えいみ |
| 2026-07-09 | 6-6 Member Ops / 月初合意 | 修正 | `/monthly-agreement` の `未払いがどう残るか` を、縦積みのバー/明細から、左に項目・右に稼働月列を並べる横長マトリクスへ変更。build v0.39.34 | 右側の余白が大きいのに1か月あたり複数行を使っており、未払残の月次推移を横に追いにくかったため | えいみ |
| 2026-07-09 | 6-4 Finance / Payment Confirm | 修正 | 入金確認nudgeを入金日当日の候補だけ送信する仕様へ変更し、Slack/確認画面/freee同期失敗DMの表示を `支払月` から `入金月` へ修正。build v0.39.33 | 入金日前は実際の入金確認ができず、毎日nudgeされても判断できないため。AMDが受け取るお金なので表示も入金月が自然なため | えいみ |
| 2026-07-09 | 2-5 探索・知識アセット / 5-1 Research Assets | 修正 | `/poc` の一次入力を `シーズ` / `PoC先` に整理。画面ではシーズ追加、PoC先追加、シーズ x PoC先マトリックス、空セルからの `案件化` を正規導線にした。既存OS情報からKUTE系シーズ6件とPoC先26件を初期投入する。build v0.39.32 | まさ指摘「『マッチ案件』『企業候補』ではなく『シーズ』『PoC先』の2つにすべき」「OS内の情報から入力できるものは入力して」を反映するため | えいみ |
| 2026-07-09 | 2-6 adminオペ / 6-3 請求書発行 / 6-6 Member Ops / SPEC_pwa / FEATURE_REGISTRY | 修正 | `/admin/invoices` の `要確認 / 設定不足 / 過去滞留` から発行前チェック詳細を開けるようにし、設定不足では freee取引先IDを保存できるようにした。過去滞留判定を稼働月ではなく請求月 (`invoice_ym || ym`) 基準に変更。build v0.39.31 | きよが状態名だけでは何を解消すべきか分からず、請求月が対象月の繰延行まで過去滞留として表示されていたため | えいみ |
| 2026-07-09 | 2-6 adminオペ / 6-3 請求書発行 / 6-6 Member Ops / SPEC_pwa / FEATURE_REGISTRY | 修正 | `/admin/invoices` の初期表示を `発行待ち` から `未完了` (= 発行待ち / 要確認 / 設定不足 / 過去滞留) に変更。請求書発行モーダルとfreee件名fallbackは `client_name` を使い、AMD内部の `project_name` / `project_id` を出さないようにした。build v0.39.29 | 初期表示が押せる1件だけに絞られてZMPしか見えず、さらに発行モーダルの件名・ヘッダーにAMD内部呼称が出ていたため | えいみ |
| 2026-07-09 | 2-5 探索・知識アセット / 5-1 Research Assets | 追加 | `/poc` PoC案件化を追加。Seedsの下流で、企業候補、相性仮説、ヒアリング論点、謝礼・PoC費用、契約、資金、収益分配、次アクションを扱う画面として記載。build v0.39.28 | PoCサービスMTGの内容を、議事録全文ではなく次に使える案件化台帳としてOSに残すため | えいみ |
| 2026-07-09 | 2-6 adminオペ / 6-3 請求書発行 / 6-6 Member Ops / SPEC_pwa / FEATURE_REGISTRY | 修正 | `/admin/invoices` の請求額判定から `budget_yen / 0.65` fallback を削除し、明細合計 → 確定請求額 → 月額固定契約額 (`projects.fee_amount`) の順に変更。対象月より古い未発行行は `発行待ち` から外して `過去滞留` に分離。`AdminInvoiceIssueDialog` は iOS/GAS 由来の前仕様へ戻し、基本明細行、承認済み立替、調整行、備考、発行済み情報、発行取消を復元。build v0.39.27 | BWE 2026年2月が AMD 側予算 `budget_yen` から請求額扱いされ、SE 2026年4月のような古い未発行行が今月の発行待ちに混ざっていたため。あわせて請求書発行モーダルが前の実務仕様から薄い明細編集UIへ変わっていたため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット / 6-8 Admin MS Overview | 追加 | `milestone_change_events` の既存MS履歴を backfill。active / fixed の11 plan cycle、110 MSを、`value_milestones.created_at` ごとの作成バッチとして19 eventに分けて追加した。ログ導入前の pt/share 更新時刻は復元せず、担当shareは backfill 実行時点の現行値として扱う | `MS変更履歴` を導入した後、これまでのMS変更・作成履歴もメンバー向け cockpit で確認できるようにするため | えいみ |
| 2026-07-09 | 2-6 adminオペ / 3-1 architecture / 6-3 請求書発行 / 6-6 Member Ops / design routine / FEATURE_REGISTRY | 修正 | `/admin/invoices` をきよの発行フローに合わせ、締め済み稼働月・請求額ありの行だけを表示する仕様へ変更。未来月、請求なしPJ、稼働期間外、freeze後の空cycleを発行対象から除外し、`発行待ち / 要確認 / 設定不足` を分離。`金額 / 報告 / 立替` のきよ確認と、freee取引先未設定の設定不足を明示する。build v0.39.25 | まさ指摘「8月の稼働分まで表示」「請求が発生しないPJも出てる」「きよの立場でUIUX設計」を反映し、実務で押せる請求書だけを発行待ちに出すため | えいみ |
| 2026-07-09 | 2-6 adminオペ / 6-3 請求書発行 / 6-6 Member Ops / FEATURE_REGISTRY | 修正 | `/admin/invoices` の中身を旧 billing matrix から請求書発行キューへ変更。未発行 / 発行済み / 送付済み / 入金済みで絞り込み、行の主操作を `発行` / `請求書を発行` に寄せた。請求額は明細合計、確定請求額、PJ予算逆算の順で表示する。build v0.39.24 | まさ指摘「ページ名は請求書発行になってるけど、中身はbilling matrixのままだよ」を反映し、ページ名だけの置換ではなく実務画面として請求書発行に寄せるため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット / 6-2 Admin Projects | 修正 | `立替精算` 欄を契約可否表示ではなく、発生額/不可を表示する仕様へ変更。空欄は `0円`、金額入力ありはその文字列、不可PJは `不可`。ZMP は実務上OKだが契約書上は明示条項なしのため、OS上はOK運用として扱う。build v0.39.23 | まさ確認で「この欄に書くべきはOKかどうかではなく発生額。立替不可PJの場合は不可」と確定したため | えいみ |
| 2026-07-09 | 2-1 / 2-2 / 2-3 / 2-6 / 3-1 / 5-2 / 6-2 / 6-3 / 6-4 / 6-5 / 6-6 / design routine / FEATURE_REGISTRY | 変更・削除 | 旧 `/admin/billing` を廃止し、請求書発行の主入口を `/admin/invoices` に変更。左メニュー・ページタイトル・マニュアル・設計登録簿を `請求書発行` 表記へ同期し、旧URLは互換 redirect のみにした。build v0.39.22 | まさ指示「billingページは廃止して請求書発行ページを作って。billingってもう使わなくない？」を反映し、請求書発行導線を業務名どおりに固定するため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット / 6-5 Admin Payouts / 6-8 Admin MS Overview / 7-1 報酬計算 | 修正 | `期末未払` / `未払残` を支払通知対象の外部メンバーへ将来払う残高に限定し、役員の未充当繰越は会社留保側の内部検算へ寄せる仕様へ更新。build v0.39.22 | ZMP OkuDoor の未払残に役員留保分が混ざり、外部開発支払の最大額 (あび・うめ各20万円) より大きく見えていたため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット / 6-8 Admin MS Overview | 追加 | `/admin/ms-overview` の保存成功時に `milestone_change_events` へMS差分・担当share差分・保存前支払検算サマリを記録し、PJ cockpit の今期MS直下に折りたたみ `MS変更履歴` を表示する仕様を追加。build v0.39.22 | まさ指示「MSの変更履歴をコックピットに表示」「勝手に変更されて報酬額が変わったらメンバーから不満」「新しい業務委託契約書では記録しておくことがAMD側の責任」を反映するため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット | 変更 | 今期MSリストのバー上メンバーchipに、各担当者の `担当設計額` を追加。MS全体の設計額だけでなく、担当shareごとの金額感を一覧で確認できるようにした。build v0.39.20 | まさ指示「あとここに各メンバーの金額も表示されるようにして！」を反映するため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット | 変更 | 今期MSリストの各MS行に `設計額` を表示。本契約MSは plan cycle 予算、`cap_extra` は同期間の別財布予算から按分し、支払確定額ではなく MS 設計額の目安として扱う。build v0.39.19 | まさ指示「コックピットのMSリストのところに、各MSに割り当てられている予算がいくらなのかを明示してほしい」を反映するため | えいみ |
| 2026-07-09 | 2-6 adminオペ / 左サイドナビ | 変更 | 日本文化マップを一般「資料」導線から外し、`/admin/japanese-culture-map` の admin-only 画面へ移動。旧 `/japanese-culture-map` は admin route へ redirect。build v0.39.18 | まさ指示「『日本文化』のページだけど、adminに移動させてほしい」を反映するため | えいみ |
| 2026-07-09 | 4-9 研究機関ERS / Dashboard | 変更 | NIMSカードの関連PJをCX `p20` から正式NIMS OS導入PJ `p28` へ変更。`p28` は研究機関エコシステム構築PJとして通常PJ一覧ではなく研究機関ERSリスト側へ寄せ、CX `p20` はNIMS Pilotの初期ユースケースとして分けて扱う。build v0.39.17 | まさ指示「NIMSを正式にp28としてOSにも登録」「NIMSカードは入ってるけど、まだPJ番号と紐づいてないので、紐づけ」を反映するため | えいみ |
| 2026-07-09 | 6-8 Admin MS Overview / 7-1 報酬計算 | 修正 | 金額ベースで先に決まる別財布だけ、cap_extra の明示ptを期間×10ptより優先する特例を追加。ZMP OkuDoor は 130pt / extra単価10,000円 / total_points 250へ是正し、別財布未払い在庫だけの保護月差額は台帳化しない。build v0.39.15 | 1,300,000円の別財布原資を60ptで割ると円単位の丸めで設計額が1,300,020円に見え、金額固定の予算なのに端数が出るため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット / 2-4 先手TODO | 削除・変更 | 通常PJコックピット右カラムから旧 `proactive_outbox` 由来のTODO欄を削除。先手TODOの棚卸しは `/proactive` と dashboard 上段バッジで扱い、PJコックピットは資料・経営ハイライト・ガバナンス・助成金・MTGサマリに絞る。build v0.39.15 | 旧TODO欄の `資料作成済み` などが、完了/外部送付/内部メモのどれか分からず、古い手動seedがPJ状況面のノイズとして残っていたため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット / 3-2 データと抽出 / 8-3 L2 routines / spec 3-3 meeting flow | 変更 | W-Prep Launch を、毎週水曜15:00 JSTに Calendar + DB の7日窓を照合して visible prep thread を作るレーンとして明文化。DBだけを見て完了扱いにしない、thread title変更・pin必須、第一声は「流れ / 位置づけと着地点 / まさがやること」の3点完了報告、共有フォルダ資料はAMD OSデザインコード準拠のHTML主成果物に統一する仕様へ更新 | `int) PoCサービス（依頼側）` などCalendarにはあるがDB未同期のMTGがprep対象から漏れたこと、prep threadが会議冒頭セリフ案だけで待機したこと、資料形式がGoogle Docs / Markdown等に分散したことを再発防止するため | えいみ |
| 2026-07-09 | 2-3 PJ コックピット | 削除 | 通常PJコックピット右カラムの「つくよみメモ」を削除。`tsukuyomi_nudge_queue` 由来カードは通常コックピットに出さず、ステータスバッジがある時だけ右カラムを表示する。build v0.39.14 | まさ指示「コックピットにある『つくよみメモ』を削除して。」を反映し、コックピット本文の読解を邪魔しないようにするため | えいみ |
| 2026-07-08 | 6-8 Admin / MS Overview | 変更 | 上段メトリクスの回帰禁止と旧文言削除を本番確認できるよう、BUILD_VERSION を `v0.39.13` に更新。4枚目は `budgetImpact` 由来の `PJ予算残` / `不足額` / `予算不足` / `原資超過` カードとして固定する | まさ指示「それをやると巻き戻ることが分かってる。絶対に再発させないためには…書いてるところをすべて削除するほうがいい」「いや上げないとだめじゃん」を反映し、設計上の再発防止が本番版として判別できるようにするため | えいみ |
| 2026-07-08 | 2-3 PJ コックピット | 変更 | PJ cockpit `今シーズン収支` の月次最終列を `残` から `収支` へ変更し、現金主義で `クライアント支払 - バッファ - メンバー支払` を表示する仕様へ更新。役員向け報酬相当額と未払残は現金流出ではないため収支列に含めず、裏の不足判定には引き続き使う。build v0.39.12 | まさ指示「その定義の『残』は、全然いらないかも。現金主義で、その月の収支を書いてほしい」を反映するため | えいみ |
| 2026-07-08 | 6-8 Admin / MS Overview | 変更 | 上段メトリクスの回帰禁止を明文化。4枚構成を3枚化しないこと、4枚目を個人名カードへ戻さず `budgetImpact` 由来の `PJ予算残` / `不足額` / `予算不足` / `原資超過` カードとして固定することを追記。build変更なし | まさ指示「また巻き戻る可能性あるから設計書に入れといて」を反映し、同じ個人名カード復活を防ぐため | えいみ |
| 2026-07-08 | 6-8 Admin / MS Overview | 訂正・変更 | 直前の3枚化を訂正し、上段メトリクスを4枚構成へ戻した。4枚目は個人名カードではなく、保存前支払検算 `budgetImpact` の `PJ予算残` / `不足額` / `予算不足` / `原資超過` を出すカードに変更。build v0.39.11 | まさ指示「本来４枚だった。このカードをなくして、代わりになにか入れてたはず」を反映し、個人名カードを戻さず支払安全カードへ差し替えるため | えいみ |
| 2026-07-08 | 6-8 Admin / MS Overview | 削除 | `/admin/ms-overview` の上段メトリクスから個人名カードを削除し、メトリクスカードを合計pt / 本契約pt / 別財布pt の3枚に変更。build v0.39.10 | まさ指示「MS編集画面のこの欄は削除してってお願いしてあったはず」を反映し、個人名カードは下段のメンバー別 pt配分 / 設計額で代替するため | えいみ |
| 2026-07-08 | 2-3 PJ コックピット / 6-5 Admin Payouts / 7-1 報酬計算 | 修正 | PJ cockpit の `今シーズン収支` から `会社留保` 表示を削除。役員向け報酬相当額は検算には含めるが、メンバー向けコックピットと支払通知書には出さない仕様へ明記。build v0.39.9 | まさ指示「内部留保っていうか役員がもらうはずの報酬額なので、メンバーにわざわざ見せなくてもいい」を反映するため | えいみ |
| 2026-07-08 | 2-3 PJ コックピット | 修正 | MTGサマリ一覧を枠内スクロールにせず、カードが増えた場合は一覧全体を縦に伸ばしてコックピット全体のページスクロールで読む仕様へ更新。build v0.39.8 | MTGサマリの内側スクロールが使いにくく、自然に読み進められないため | えいみ |
| 2026-07-08 | 6-1 / 8-3 / OSデータ一覧 / D-10 route | 変更 | D-10 `member-weekly-activities` の定期経路を `GET ?mode=evidence` → Codex automation合成 → `POST activities[]` 保存へ変更。route 内 Anthropic 合成の例外扱いをやめ、legacy `interactive=1` GET 一発実行は保存に使わない current truth へ更新 | `/mypage` の週次活動に fallback 由来のメール本文・HTML・runner marker が表示されたため。D-10合成を定額内のCodex automationへ移し、古いroute triggerが後から上書きしないようにするため | えいみ |
| 2026-07-07 | 2-3 PJ コックピット | 修正 | 「日程調整中MTG」別欄を廃止し、日程未確定の予定は「予定MTG / 準備中」欄にまとめ、日付欄を `日程未確定` と表示する。`meeting_id` が `upcoming:` でも開催済みソースを持つ row は準備カード扱いしない。build v0.39.7 | 日程未確定は予定欄内の状態で足り、別レーンがあると古い仮置き・開催済み内容が亡霊のように残って見えるため | えいみ |
| 2026-07-07 | 2-3 PJ コックピット / 先手 TODO | 修正 | 予定MTG欄を開始時刻ベースにし、会議開始後の予定カードを「準備中」に残さない。開催済み議事録へ表示する会議前準備メモは、手動準備または prep worker 成果があるものだけに限定。`next_meeting_prep` TODO は紐づくMTG開始後に自動終了する。build v0.39.6 | 開催済みMTGに薄い `MTG準備情報` や期限切れの `agenda / 進行案` TODO が亡霊のように残っていたため | えいみ |
| 2026-07-04 | 2-4 AMD Cockpit / Dashboard | 削除 | `/dashboard` の `PJ先手レーダー` を廃止し、`ProactiveRadarBoard` と dashboard 差し込み、レーダー用回帰ガード、仕様契約を削除。build v0.39.5 | 現状の内容は `先手TODO` と重複し、実情に合う先手判断はえいみOS側で実装した方が機能しそうなため | えいみ |
| 2026-07-04 | 2-4 AMD Cockpit / Dashboard | 修正 | `PJ先手レーダー` の自動対象を `active` / `sales` / `draft` に絞り、`frozen` / `ended` / 旧PJは明示的な未対応先手TODOがある場合だけ表示する。材料ゼロの平常監視行も非表示にした。build v0.39.4 | r3kt のような停止済み・実務外PJが「空白提案」として混ざり、現実の先手判断からズレたため | えいみ |
| 2026-07-04 | 2-4 AMD Cockpit / Dashboard | 追加 | `/dashboard` 上段に `PJ先手レーダー` を追加。`proactive_todos` の未処理だけでなく、MTG準備、経営ハイライト、不在検知、月次詰まりから「空白提案」を出す。build v0.39.3 | 全PJで「見えている未処理」だけでなく、誰もまだTODO化していない段階の先手候補を最初に見えるようにするため | えいみ |
| 2026-07-04 | 2-2 Member Workflows / 6-6 Member Billing Prompts / 3-14 月初合意 | 修正 | 月初合意モーダルの背景クリックによる一時 dismissal を復帰。dismissal は保存せず、未合意/条件更新ありならダッシュボード等を開き直すたびに再表示する。build v0.39.2 | モーダル背面の画面を一時確認したい操作は許可しつつ、合意完了とは混同させないため。前回の「背景クリック禁止」方針は、画面確認の逃げ道まで塞いでいたため | えいみ |
| 2026-07-04 | 6-7 契約管理 / 7-1 報酬計算 | 修正 | monthly_fixed 契約の Contract Apply が、バッファなしの場合に未確定の `billing_cycles.budget_yen` と現行 `value_plan_cycles.budget_yen` を契約 cap (= 月額税抜×65%) へ整合するルールを追記。KUTE p25 の「2026-05-08 一括生成値を旧 Contract Apply が温存し、2026-07-01 当月自動確定だけが65%へ直した」原因メモも追加。build v0.39.1 | 月によってPJ予算の計上が違って見えた原因は手入力ではなく、古い一括生成経路と当月自動確定経路の混在だったため。契約反映済みなのに未来月だけクライアント支払額がPJ予算に残る状態を再発させないため | えいみ |
| 2026-07-04 | 2-6 adminオペ | 追加 | `/admin/management-knowledge` 経営ノウハウ台帳を追加し、`management_knowledge_entries`、admin-only境界、maturity、source hygiene、Proto-RT 初期カードを追記。build v0.39.0 | 日々生まれる経営ノウハウを会話やローカルメモで消さず、OS内の再利用カードとして蓄積するため | えいみ |
| 2026-07-03 | 2-3 PJ コックピット / 6-8 Admin MS Overview / 7-1 報酬計算 / 3-14 月初合意 | 修正 | CryoX のような schedule_based 契約では `contract_terms_json.monthlySchedule.amountTaxExcl` を予定クライアント支払として収支に入れる。PJ cockpit と MS編集検算に `原資上限 = (クライアント支払 - バッファ) × 65%` を追加し、PJ予算が原資上限を 1 円でも超える場合も赤停止・保存不可にする。build v0.38.12 | KUTE のように期末未払が 0 でも PJ予算自体が原資上限を超えていれば、AMD運営費が見えないところで削られるため | えいみ |
| 2026-07-03 | 2-2 Member Workflows / 6-6 Member Billing Prompts / 3-14 月初合意 | 修正 | 月初合意の必須モーダルを、背景クリックなどで先送りできない確認画面として明記し、実装も合意完了後だけ閉じる形へ戻した。build v0.38.11 | 未合意または条件更新ありのままOS操作へ進めると、「合意完了までは最初に月初合意を確認する」という月初運用が崩れるため | えいみ |
| 2026-07-03 | 2-3 PJ コックピット / 6-8 Admin MS Overview / 7-1 報酬計算 / 3-14 月初合意 | 修正 | PJ cockpit と MS編集検算の `クライアント支払` に別財布売上を含め、`バッファ` はシーズン原資の `buffer_breakdown_json` を優先するよう更新。MS Overview は閲覧モードで cycle を開いた時点でも現行案を検算し、不足がある場合は MS 一覧の上に `MS編集停止中` の赤表示を出す。契約自動確定/予算承認は、シーズン原資にバッファ内訳がある PJ で月次バッファを二重控除しない。build v0.38.10 | ZMP で別財布売上200万円がクライアント支払に乗らず、SX でシーズンバッファ織り込み済みなのに月次側でも200,000円を引いて期末未払を発生させていたため | えいみ |
| 2026-07-03 | 2-3 PJ コックピット / 6-5 Admin Payouts / 6-8 Admin MS Overview / 7-1 報酬計算 / 3-14 月初合意 | 訂正・変更 | 同日直前の「最終月に自動精算枠を足してゼロ着地させる」方針を撤回。報酬計算は未使用cap繰越だけを使い、期末未払残を自動上乗せで隠さない。MS編集は編集開始時点からクライアント支払・バッファ・PJ予算・メンバー支払・会社留保・期末未払・不足額を表示し、期末未払またはPJ予算不足が1円でもあれば保存不可にする。PJ cockpit には `今シーズン収支` を追加し、月次とシーズン合計で同じ収支を見せる。build v0.38.7 | まさ確定「ゼロ着地にして」「シーズン最初（またはMS修正時点）でゼロ着地できるか分かる」「不足額がある状態ではMS編集を終えることができない」「AMD運営側が認識していないところで運営費が勝手に削られる設計にしない」をOS最重要仕様として固定するため | えいみ |
| 2026-07-03 | 2-2 Member Workflows / 6-6 Member Billing Prompts / 3-14 月初合意 | 変更 | 未合意または条件更新ありでOS内の他画面を開いた場合、`/monthly-agreement` へ即遷移させず、開いた画面を背景に残したまま月初合意の必須モーダルを前面表示する仕様へ変更。build v0.38.4 | ダッシュボード等を開いた本人が突然別ページへ飛ばされたように感じず、「いま開いた画面」と「先に必要な確認」を同時に理解できるようにするため | えいみ |
| 2026-07-03 | 6-8 Admin MS Overview / 3-14 月初合意 | 修正 | 支払済み実績を `freee_wallet_txn_verified:` 証跡 + `monthly_reward_payout` 明細が一致した行だけに限定し、`reward_paid_at` だけの月は `実績未照合` として別枠化。月初合意の支払実績/支払予定は税込額で表示し、明細は税抜/税込併記。MS編集の保存前支払検算は未照合の支払済み月がある場合 `blocked` にする。build v0.38.3 | 実際の振込額と同一だと確認できない計算キャッシュを支払実績扱いにすると、MS変更時の赤字判定と過去支払表示を信用できなくなるため | えいみ |
| 2026-07-03 | 6-5 Admin Payouts / 7-1 報酬計算 / 3-14 月初合意 | 修正 | すべての plan cycle で終了月の `carryOverYen = 0` を絶対条件にし、通常capと未使用cap繰越で不足する場合は `regularFinalCapTopUpYen` / `extraFinalCapTopUpYen` を最終月へ自動追加する。`/admin/payouts` 先12か月表は、報酬債務表で `ゼロ着地 + 最終精算`、cap超過チェックで不足月として表示し、精算上乗せを隠さない | SX(p21) の 2027年3月に未払残が残っていた。シーズン終了時に未払残高がゼロでない状態は資金繰り・債務管理上の重大リスクで、未払残があるのに「ゼロ着地」と見せる表示も危険なため | えいみ |
| 2026-07-03 | 2-2 Member Workflows / 6-5 Admin Payouts / 6-6 Member Billing Prompts | 変更 | `is_admin=true` のメンバーは `exclude_from_payout_notice=true` でも月初合意対象に含め、本人合意が未完了/条件更新ありならOS内の他画面より先に `/monthly-agreement` を表示する運用へ更新。build v0.37.10 | admin本人で合意フローをテストできるようにし、合意完了前に他画面へ進めない体験を確認するため | えいみ |
| 2026-07-03 | 7-1 報酬計算 / 月初合意 | 修正 | `buildRewardSummary` の cap 連鎖を、未払い stock だけでなく未使用の月次capも翌月へ繰り越す方式に変更。基本月次capは `regularCapBudgetYen` / `extraCapBudgetYen` に残し、繰越込みの配分上限を `effectiveRegularCapBudgetYen` / `effectiveExtraCapBudgetYen`、次月へ渡る使い残しを `regularUnusedCapCarryOutYen` / `extraUnusedCapCarryOutYen` として reward summary に保存する。backfill は protected 月をスキップするため、支払済み月は書き換えない | ZMP(p19) しんちゃん(ID026) の月初合意で、年間原資は足りているのに前半の未使用capが捨てられ、後半のMS消化増で未払いがシーズン末に残っていたため。まさ指示「未使用キャップは持ち越せるようにしないと設計上問題があるよね？そしたら持ち越せるようにしよう。」 | えいみ |
| 2026-07-02 | 6-5 Admin Payouts / 7-1 報酬計算 / design season_budget_actual | 追加・変更 | 送付済み/支払済みの過払いを、支払通知書の変更や会社留保ではなく、同一メンバー本人の未払残からだけ相殺する台帳 `reward_member_liability_offsets` を追加。`buildRewardSummary` / 先12か月 capped 投影 / シーズン予実表が同じ相殺台帳を読む仕様へ更新。ZMP 2026シーズンは、しん・こうの小額過払いを許容し、あび・うめの過払いだけ本人stockから相殺する | 5月稼働分は発行・支払済みで変更できないため。シーズン全体では PJ原資 (クライアント支払額−バッファ)×65% 以内に収束させつつ、他メンバーや会社留保へ過払いを押しつけないため | えいみ |
| 2026-07-01 | 6-5 Admin Payouts / 3-14 月初合意 | 修正 | `exclude_from_payout_notice=true` かつ `is_admin=true` のメンバーは合意保存不要のまま、確認用に `/monthly-agreement` でPJ/MS/貢献率/予定報酬を表示する仕様へ更新。画面上の `share` 表記は `貢献率` に変更。build v0.36.41 | まさのアカウントは合意対象外で本人画面を確認しづらかったため。adminは支払い条件に含めず、表示確認だけできるようにする | えいみ |
| 2026-07-01 | 3-1/3-2/3-3 データ・抽出 / 4-1 AMD Protocol / 4-8 MS Progress / 8-3 L2 routines / H-1 MTGサマリ / spec 3-0/3-1/3-9〜3-12/5-3/5-4 | 修正 | 旧 Windows MMO writer を現行運用から外し、D-1/D-2 LLM/D-3/D-4 を `rehome required`、H-1 を local Codex / Codex automation 現行 writer に更新。H-1 が Notion 議事録 DB の `eventId` / `日付` / `PJ` / `メンバー` exact-root property を補完する仕様へ更新し、`test:l6-notion-property-guard`、`nav_meeting_backfillAiPages_`、`nav_meeting_backfillMinutesDefaultMember_`、`NOTION_MINUTES_MEMBER_PROP` / `NOTION_MINUTES_DEFAULT_MEMBER_PAGE_ID` も正本化 | Notion 議事録 DB の `PJ` / `日付` / `eventId` / `メンバー` が大量に空欄で、旧MMO停止後の現行 writer と backfill 手順がマニュアル上も曖昧だったため | えいみ |
| 2026-07-01 | 6-5 Admin Payouts / 支払通知書 | 修正 | 月初合意を支払い条件として通常判定する開始月を 2026年7月稼働分へ変更し、2026年6月以前の稼働分 (`source_ym <= 202606`) は導入前/移行月として合意済み扱いで通す仕様へ更新。build v0.36.39 | 6月は契約改定前かつシステム未完成期間で、まさ判断として合意ステップをスキップする月だったため。契約前/未完成期間をあとから支払い停止に使わないようにする | えいみ |
| 2026-07-01 | 3-2 データ・抽出 / 6-2 Admin Projects / 8-3 L2 routines (M-1) | 変更 | L2M-1 月次報告書 routine を Codex `amd-os-l2` (daily 05:30 JST, gpt-5.5) → Claude routine `amd-os-l2m1-monthly-report` (月末最終日 03:00 JST, opus-4-8+ultracode) へ移行。旧 Codex は PAUSED (復活禁止)。内部保存版 (`monthly_reports.final_content`) + 対外提出版 (`monthly_reports_external.body_md` + PDF) の 2段生成に拡張。`projects.monthly_report_scope` 3値enum (`none` / `internal_only` / `internal_and_external`) 追加、`/admin/projects` の「月報 scope」「エイリアス」「禁止語 allow」「業務内容 JSON」列で編集可能。プロンプト本文は `llm_prompts` の `l2m1.monthly_report.internal.v2` / `l2m1.monthly_report.external.v2` を admin UI で編集。Slack 通知は `scripts/send-eimi-slack.mjs` 経由でまさ DM に集約 (PJ チャンネルには投げない)。migration 159 + 160、build v0.36.37 | 旧 gpt-5.5 daily の生成品質不足を opus-4-8+ultracode で解消し、KUTE実納品形式準拠の対外納品 PDF を月次で自動生成する運用に切り替えるため。プロンプトの SKILL/コード直書きを AGENTS.common.md L510-514 の DB管理原則に整合させるため。まさ 2026-07-01 確定 | えいみ |
| 2026-07-01 | 2-3 PJ コックピット / 8-3 L2 routines / H-1 Phase P / Prep Worker | 修正 | Notion AI Meeting Notes 事前コンテキストに `l6_prep_notion_context_gate` を追加。AI Meeting Notes page が見つかっているのに marker 未挿入の `needs_insert` では `ready` 禁止にし、append-only 追記後の再fetch確認を必須化。`not_found` / `write_failed` / `ambiguous` / `wrong_page` / `skipped_after_meeting` は `prep_readiness_reasons.notion_ai_context` に保存する | KENQ prep で手動貼り付け用 context は作られたが、当日の Notion 議事録 page への実挿入確認が無く、固有名詞メモを「作った」と「Notionへ入った」が混ざっていたため | えいみ |
| 2026-06-30 | 2-3 PJ コックピット / 6-2 Admin Projects / 6-7 契約管理 | 変更 | `月次報告` を長文提出ルール表示から、`要提出` 等の短い状態 + 時期 / 提出期限 / フォーマット / 記載事項 / 根拠の詳細表示へ変更。`projects.contract_terms_json` と `contract_terms.extracted_terms_json` に `monthlyReportSubmissionTiming` / `monthlyReportSubmissionDeadline` / `monthlyReportSubmissionFormat` / `monthlyReportSubmissionRequiredItems` を追加し、KUTEの契約外運用提出も同じ正本に残す。build v0.36.35 | 「月次報告」列で「月次報告書を提出」と書くのは冗長で、フォーマット・必要記載事項・提出期限・指定なし情報まで横断確認できる必要があるため | えいみ |
| 2026-06-30 | 2-4 AMD会社全体 / Dashboard累計実績 | 変更 | `/dashboard` の資金調達・助成金累計を、登録済み総額ではなく AMD貢献額だけで集計する仕様へ変更。会社別/行別表示を追加し、非貢献・未判定のラウンド/助成金もリストには残す。投資家別内訳・持株比率は dashboard には出さない。build v0.36.36 | 会社の全ラウンド・全助成金が AMD の貢献によるものではないため、営業アピール数字を過大表示しないようにする | えいみ |
| 2026-06-29 | 2-3 PJ コックピット / 8-3 L2 routines / H-1 Phase P / Prep Worker | 修正 | MTG prep session の第一声を、保存完了や readiness の技術報告ではなく、「前回/直近の流れ」「今回の着地仮説」「冒頭で確認したいこと」「まさへの確認問い」を含む会議設計スターターにする運用へ変更。会議後も同じ session で H-1 議事録を踏まえた次回MTG相談へ続ける前提を追記 | prep session の目的は、まさがOS内情報を説明し直さずに会議設計の相談を始めることなのに、SX経営会議prepがDB保存完了報告だけで終わり、対話開始になっていなかったため | えいみ |
| 2026-06-29 | 2-3 PJ コックピット / 月次報告書 | 変更 | 月次モーダルの月次報告書導線を、全PJ共通の `社内保存用を編集` と、CX/SX/KUTE だけに出る提出用リンクへ分離。提出用は `template=nims-cx` / `ehime-sx` / `kogakuin-kute` で NIMS・愛媛大・工学院向けの印刷ビューを開く。build v0.36.33 | 社内保存用と提出用を別生成する設計だったのに、画面上は単一の印刷/PDFだけで、提出先別フォーマットを選べなかったため | えいみ |
| 2026-06-29 | 2-3 PJ コックピット / 6-2 Admin Projects / 6-7 契約管理 | 追加 | `/admin/projects` に `月次報告` 列を追加し、`projects.contract_terms_json.monthlyReportSubmissionRule` / `monthlyReportSubmissionNote` を編集・表示する仕様へ更新。D-13契約書抽出は同項目を `contract_terms.extracted_terms_json.monthly_report_submission_rule` / `monthly_report_submission_note` に保存し、Contract Apply 後にPJ正本へ畳む。PJ cockpit ヘッダー最上部にも月次報告提出ルールを表示する。build v0.36.34 | 契約条件抽出済みの中に月次報告書提出ルールが含まれており、admin台帳と各PJ cockpitで横断確認できるようにするため | えいみ |
| 2026-06-29 | 6-5 Admin Payouts / 支払通知書 | 修正 | 行の `送付` クリック時にPDF生成・再生成・支払データ同期を行わず、保存済み正式PDFが最新DBと一致する場合だけ確認モーダルを開く仕様へ変更。未発行・確認用PDF・金額不一致・メンバー台帳更新後・テンプレート更新後で古いPDFは送付できず、先に `支払通知書発行` / `強制再発行` を促す。UIの `発行中` と `確認中` も分離。build v0.36.32 | 送付クリック後にPDF再生成が残っていて画面が固まったように見え、`PDF確認` と同じく「確認系操作は生成しない」という設計と矛盾していたため | えいみ |
| 2026-06-29 | 6-2 Admin Projects / Members / 6-5 Admin Payouts | 修正 | 未送付PDFの `last_generated_at` より `members.updated_at` が新しい場合は、金額差分がなくても再生成対象にする仕様へ変更。行の `PDF確認` は保存済み正式PDFを開くだけにし、未生成または台帳更新後で古いPDFでは再発行を促す。全員分の確認生成は `確認用PDF生成` として正式PDF確認と名前を分離。登録番号は保存時とPDF生成時に全角T・T風文字・空白/ハイフンを正規化する。build v0.36.31 | メンバー台帳で住所・登録番号を修正した後も、既存PDFが古いまま確認され、強制再発行の失敗も見落とされやすかったため | えいみ |
| 2026-06-29 | 6-2 Admin Projects / Members / 6-5 Admin Payouts | 修正 | `/admin/members` のセル編集を browser 直接 `members.update` から `PATCH /api/admin/members` の service_role 更新へ変更し、保存済み row でUIを置き換える仕様に更新。支払通知書の正式発行・強制再発行・送付準備は、支払額同期後にDBを再読込し、最新の契約者名・住所・登録番号をPDFへ反映する運用を明記。build v0.36.30 | メンバー台帳で住所やインボイス登録番号を修正しても、画面だけ更新されたように見えてDB正本へ保存されず、再発行PDFが古い情報のままになる事故を防ぐため | えいみ |
| 2026-06-29 | 4-5 Management Score / Finance Simulation | 変更 | `/management-score` のキャッシュ残高チャートに `実績接続見込み` を追加。最新の freee 口座残高実績をアンカーにし、未来月は見込み月次CFを累積する。既存の予算線は `当初計画残高` として残し、API `/api/finance/live-cash-balances` も主 `cashBalance` を実績接続見込み、`budgetCashBalance` を当初計画として返すよう変更。build v0.36.29 | 今月時点の予実乖離が大きいと、元の計画残高線だけでは未来の資金繰り判断に使いにくいため。予算は差分検出用に残し、意思決定用の残高予測を現実の残高から接続する | えいみ |
| 2026-06-29 | 6-5 Admin Payouts / 支払通知書 | 修正 | 支払通知書PDFの宛先側・発行者側の表示ラベルを `インボイス登録番号` から `登録番号` に変更。DB項目・admin入力欄としてのインボイス登録番号は維持 | PDF上の表記を短くし、まさ指定の文言に合わせるため | えいみ |
| 2026-06-29 | 2-3 PJ コックピット / 8-3 L2 routines / H-1 Phase P / Prep Worker | 修正 | 6/26 に追加した Notion AI Meeting Notes 事前コンテキスト注入仕様を、現行 visible thread 版 worker に復元。会議前 AI Meeting Notes page を検索し、`amd-os:notion-ai-context:{meeting_id}:{digest}` marker 付きで PJ固有名詞・略称・拾うべき論点を insert-only 追記する。見つからない/書けない場合も prep failure にはせず、`prep_readiness_reasons.notion_ai_context` と `prep_draft_md` に手動貼り付け用 context を残す | 6/26 の履歴には仕様が残っていたが、後続の WIP revert / visible thread 修正後の worker 手順から Phase 5.5 相当が欠落し、6/29 MTG prep で固有名詞メモが Notion に事前注入されなかったため | えいみ |
| 2026-06-29 | 2-3 PJ コックピット / 8-3 L2 routines / H-1 Phase P | 修正 | MTG Prep の Calendar 準備枠作成時に、`CFG_ColorPJHistory` を PJコードから逆引きして有効な `color_id` を付ける運用を追加。既存 prep 枠が無色・別色なら次回 H-1 run で色だけ補正し、色解決・色更新だけ失敗しても枠作成と spawn は止めない | PJごとの色を準備枠にも自動付与し、手作業で色を直す運用をなくすため | えいみ |
| 2026-06-29 | 2-3 PJ コックピット / 8-3 L2 routines / H-1 Phase P | 修正 | MTG Prep の spawn を Codex Desktop の visible thread (`create_thread`) 固定へ変更し、`codex exec` の見えない session id を `prep_worker_session_id` に保存しない運用へ修正。ready 未通知を見つけた自動 Slack DM nudge は既定OFFにし、明示依頼時のみ `scripts/send-eimi-slack.mjs` でえいみ名義送信する。さらに OS対象行 / 過去MTG探索 / Calendar / GmailまたはDrive探索を実行しないまま `ready` にできない context completeness gate を追加 | KENQ prep が見えない session として作られ Codex Desktop から探せなかったこと、また 6/29 09:19 に未承認感のある ready 通知が ChatGPT/まさ名義で送られたことの再発防止 | えいみ |
| 2026-06-28 | 2-3 PJ コックピット / 8-3 L2 routines / H-1 Phase P | 修正 | MTG Prep の raw 基準時刻 `meeting_start_at - 24h` が H-1 非稼働日 (土日など) に落ちる場合、同じ HH:mm のまま直前稼働日へ繰り上げる運用へ変更。月曜12:00 MTGなら金曜12:00に prep 枠を作り、その枠開始までに session が立ち上がるよう直前の H-1 run で spawn する。稼働時間外は同日 09:15 / 21:15 へ丸める | H-1 が平日のみ稼働するため、月曜MTGの24時間前が日曜に落ち、カレンダー準備枠はあるのに Codex prep session が立ち上がらない事故を防ぐため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書 | 修正 | `/admin/payouts` の通常同期差分バッジを非表示にし、月初合意gate・本契約cap blocker など発行できない状態だけ `同期できない` を表示する仕様へ変更。メンバー行の未保存説明も削除 | 発行・送付時に自動同期される差分を押せないボタン風バッジで表示すると、操作が必要に見えて迷うため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書 | 修正 | `/admin/payouts` の同期差分表示を `未同期` から `発行時に同期` に変更し、開きっぱなしの画面は 60 秒ごとに read-only 再取得する仕様を追加。6月分の実DB差分が0でも古いタブstateで未同期表示が残る誤解を防ぐ | `未同期` 表示だと支払通知書発行してよいか分からず、バックグラウンド同期後も開きっぱなし画面が変わらないため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書 | 修正 | `/admin/payouts` を開いただけでは支払データ保存を走らせない仕様へ変更。夜間の `payout-notice-prebuild` で支払データ同期後に正式PDFまで先回り生成し、正式発行・一括発行・送付時もサーバー側で同期してから進む | 画面表示時に `自動保存中` と出て保存が始まる体験が不自然で、支払通知書発行を1人ずつ待つ運用も重かったため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書 | 変更 | `/admin/payouts` の支払額を最新計算額から自動で `monthly_reward_payout` / `payout_notices.total_yen` へ同期する仕様に変更。手動の `支払データ保存` を主操作から外し、正式PDF発行・一括発行・強制再発行・送付の直前にもサーバー側で同期する。PDFは発行者側の表記を `インボイス登録番号` に短縮し、右上の作成日/通知書番号、ロゴ、会社名、住所、インボイス登録番号を右揃えにする | 金額が変わるたびに運用者が保存ボタンを押す必要がなく、PDF上部の表記と揃えも見やすくするため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書 | 修正 | `/admin/payouts` の主作業表である `メンバー別支払` をサマリ直下・報酬債務台帳より上へ移動。`支払データ保存` は支払額と通知額を確定しメール送信はしないことを明記し、未保存時でも `保存して全員分PDF発行` で保存→一括発行を連続実行できるようにする | 支払通知書の発行時に、保存ボタンの意味と一括発行ボタンの有効化条件が分かりにくく、1人ずつ発行する運用になっていたため | えいみ |
| 2026-06-27 | 2-6 Admin Ops / admin/kiyo | 変更 | `/admin/kiyo` の表示対象を active PJ のみに変更。メンバー支払内訳、立替精算、請求書送付確認から active 以外のPJを除外する | 非稼働PJを月次経理チェックに出すと、きよの確認対象が増えすぎるため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書PDF | 修正 | 送付ボタン近くと確認モーダルに「確認モーダル→はい・送信で実メール送信」の説明を追加。送信時に送信用PDFを強制再生成し、PDF作成日を送信日 (JST) にする。PDFはタイトル以外のフォントを大きくし、作成日/支払予定日/支払方法のラベルと値を近づけ、振込先欄を削除し、AMDの適格請求書発行事業者登録番号 `T7021001064067` を追加 | 支払通知書送付時に何が起きるか分かりにくく、事前生成日のまま作成日が残り、PDFの文字・項目間隔・振込先表示も読みづらかったため | えいみ |
| 2026-06-27 | 2-6 Admin Ops / admin/kiyo | 追加 | `/admin/kiyo` を追加。`/admin/payouts` の支払月集計からメンバー別支払額、`reimbursements` から全PJの立替精算、`billing_cycles` / `billing_log` から請求書送付と `keiri@team-armada.jp` 証跡を read-only で確認する | きよが月次経理で、支払額・立替精算・請求書送付確認を1画面で見られるようにするため | えいみ |
| 2026-06-27 | 6-5 Admin Payouts / 支払通知書 | 修正 | `/admin/payouts` の `メンバー別支払`、月初合意 gate、先12か月メンバー別支払予定詳細で、`支払額` を税抜 / 税込併記に変更。DB保存値 (`monthly_reward_payout.total_pay` / `payout_notices.total_yen`) は税抜、GAS PDF 生成時に消費税10%を上乗せする境界を明記 | きよから「支払額が税込か税抜か分からない。両方書いた方がよい」と要望があり、画面上の税区分を明確にするため | えいみ |
| 2026-06-27 | 2-3 PJ コックピット / 6-2 Admin Projects / 6-7 契約管理 | 追加 | PJリストに `提出物` / `立替精算` 列を追加し、`projects.contract_terms_json.deliverablesRequired` / `deliverablesNote` / `expenseReimbursementAllowed` / `expenseReimbursementNote` を編集・表示する仕様へ更新。D-13契約書抽出は同項目を `contract_terms.extracted_terms_json` に保存し、Contract Apply 後に PJ 正本へ畳む。PJ cockpit ヘッダー最上部に PJメンバー、契約条件、業務委託料、支払い条件、提出物、立替精算可否を表示する | きよから「各PJのコックピット上部でPJリスト由来の契約・支払・提出物・立替精算情報を見たい」と要望があり、PJリストに無い2項目も契約書から抽出して横断確認できるようにするため | えいみ |
| 2026-06-27 | 3-3 通知・つくよみ / H-1 MTGサマリ | 変更 | `meeting_notifications` の議事録作成通知を廃止。H-1 は `project_meeting_summaries` / MTGカードだけを更新し、migration 156 trigger が古い writer の insert / upsert update を捨てる。PWA一覧・未読バッジ・critical poll からも除外 | 議事録を作っただけの通知は読後アクションがなく、誤り修正は cockpit の手動修正導線で扱うため | えいみ |
| 2026-06-27 | 3-3 通知・つくよみ / 8-2 通知レビュー UI | 修正 | 緊急通知の説明を current rule に更新。右下ポップアップは `connector_auth`、明示 critical metadata、metadata上の blocker/期限超過/再認証、high以上の経営ガードレールに限定し、MTGサマリ・契約予兆・総会/役会・D-11メディア掲載・high importance L2 は通常通知に残すと明記 | MTG本文やL2本文の「事故」「blocker」「再認証」等に反応して通常レビューが緊急ポップアップ化し、まさの画面に不要な緊急通知が連発したため | えいみ |
| 2026-06-26 | 4-5 Management Score | 変更 | Management Score の判定ガードと現状点検運用を追加。`routine_auto` / p00 / points=0 / 月次ルーティン系MSを継続根拠にしない、unknown過多の先手力をscore capする、財務未同期は `finance_data_missing`、方向性未実装は `data_missing`、新規案件はdeal単位で重複排除する仕様を明記。点検コマンド `npm --prefix pwa run audit:management-score -- --ym=YYYYMM --fail-on-actionable` で P0/P1/P2 が0件になることを完了条件にした | AMD Management Score に月次ルーティンMS、unknown高得点、未同期0円悪化、未実装source悪化、pipeline重複が混ざり、会社バイタルとして信用できない状態だったため | えいみ |
| 2026-06-26 | 4-5 収支シミュレーション / 9-2 開発者向け | 追加 | KAGAMI など外部クライアント向けに `GET /api/finance/live-cash-balances` を追加。`company_budget_actual_monthly.cash_amount_yen` 直読みではなく、`/management-score` と同じ live monthly PL simulation を server-side で回し、過去月は `cash_balance` 実績、未来月は live 予算残高を返す。返却は月次残高・runway・actual/forecast source に限定 | KAGAMI 財布画面の AMD 残高スタックが保存済み snapshot を拾い、OS 本体のキャッシュフロー推移より過大に見えたため。外部画面でも OS 本体の live truth を使えるようにするため | えいみ |
| 2026-06-25 | 2-3 PJ コックピット | 追加 | KUTE (`p25`) のヘッダー直下に年度内ロードマップを表示する仕様を追加。2026年6月〜2027年3月の横軸で、規程整備 (`2027年1月目途で整備完了`) と、シーズ発掘 / after GTIE (`2027年3月までに支援実務の型化`) を同時に確認する | KUTE の年度内全体スケジュールが議事録や資料内に埋もれ、コックピット最上段で現在地と年度末の着地を見られない状態だったため | えいみ |
| 2026-06-25 | 2-3 PJ コックピット / MTGサマリ | 修正 | MTG詳細モーダルの共有操作を `PDF保存` / `議事録コピー` / `準備メモコピー` / `共有URLコピー` に更新。開催済み議事録の `narrative_md` 末尾に `参考: 会議前準備メモ` がある場合は議事録本文から分離し、準備メモだけ個別コピーできる仕様に変更。PDFはブラウザ印刷ではなく共有用DOMから直接保存する | メール本文コピーに準備メモが混ざり、PDFも同じページが繰り返される・幅が狭い・メール本文と内容がズレる問題があったため | えいみ |
| 2026-06-25 | 2-3 PJ コックピット / MTGサマリ | 追加 | MTG詳細モーダルに `PDF保存` / `メール本文コピー` / `共有URLコピー` を追加。PDFは印刷画面から保存、メール本文は `narrative_md` / fallback section を外部貼り付け用プレーンテキストへ整形、URLは既存 `?meeting=` deep link をコピーする仕様として記録 | 先方に議事録共有する機会が多く、MTGカードからそのまま共有できる導線が必要になったため | えいみ |
| 2026-06-24 | 8-3 L2 抽出 routines / H-1 Phase P (MTG Prep) / spec 3-3 meeting flow | 修正 | H-1 内 Phase P (MTG Prep セッション自動立ち上げ) の起点判定を **F2+F3 deterministic fallback** に改修。基準時刻 = `meeting_start_at - 24h` を必ず採用し、Calendar `get_availability` (freebusy) が成功した時のみ前倒し。`get_availability` / `get_colors` / `NEXT_PUBLIC_GAS_API_KEY` の失敗を Phase P の blocker として扱わず、各 MTG ごとに基準時刻のまま続行する。Calendar 書き込み自体が失敗しても `prep_calendar_event_id=null` のまま spawn は進める。worker SKILL の `projects.facilitator_member_id` 参照を撤去し、`project_meeting_summaries.facilitator_member_id` (MTG 行単位) のみを見る形に整合 (実 DB には `projects.facilitator_member_id` 列が存在しないため) | 2026-06-22 確定の Phase P が `ACCESS_TOKEN_SCOPE_INSUFFICIENT` / GAS_API_KEY 不在 / freebusy 不能を blocker 扱いして毎時 review_required で全 skip し、11件の prep が 1度も spawn されない事故 (2026-06-24 まさ確認、SX p21 で発覚)。F2+F3 で「外部依存が無くても spawn は走る」設計に改める | えいみ |
| 2026-06-24 | 2-3 PJ コックピット / 株主・ガバナンス | 修正 | 株主・ガバナンス欄の要対応表示を `review_status='confirmed'` かつ未完了 (`open` / `in_progress`) のみと明記し、未確認candidateはレビューキューに留める境界へ更新。`source='meeting_summary'` の会議タイトル由来候補は D-14 要対応対象外とし、会議由来の次アクションは H-1 / MTGカード側で扱う | SXなどの通常MTGタイトルが「要対応候補」として株主・ガバナンス欄に混入し、本来の総会・取締役会・保有株式・外部期限付き義務の目的とズレて見えたため | えいみ |
| 2026-06-24 | 6-5 Admin Payouts / 3-14 月初合意 / FEATURE_REGISTRY / BUGS | 修正 | 月初合意支払 gate で、移行月扱いの行だけで blocker が無い場合は個別メンバー表を出さず、対象支払行数と「移行月スキップ」の summary を表示する仕様へ更新 | 2026/05稼働分のZMP支払行4件だけが `合意済` 一覧に並ぶと、他メンバーが合意対象から漏れているように見えるため。移行月は個別合意一覧ではなく支払月単位のスキップとして見せる | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY / BUGS | 修正 | `/admin/payouts` page が `loadTargetData(currentYm, { includeAgreementGate: false })` を SSR で実行し、`AdminPayoutsClient initialData` として渡す仕様へ更新。クライアントは初回 client GET をスキップし、月初合意gateだけ `gateOnly=1` で後追い取得する。月変更・手動再計算・保存/発行後の再取得は従来どおり API を使う | 報酬キャッシュと gate 分離後も、初回表示が「ページ表示 → hydration → client GET」の二段待ちになり、実ブラウザでデータ表示まで数秒残っていたため。キャッシュ済みデータは最初のサーバー応答に載せて体感待ちを削る | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY / BUGS | 修正 | `/admin/payouts` 初期表示GETから月初合意gateの snapshot 照合を分離。通常GETは支払データ本体だけ先に返し、画面側が `gateOnly=1` を後追い取得して `payoutAgreementGate` をマージする。保存・発行・送付などwrite actionでは従来どおりサーバー側gateを必ず実行する | キャッシュ化後も実ブラウザでデータ表示まで約11〜15秒かかり、重い月初合意gate照合が初期表示をブロックしていたため。表示速度とwrite safetyを分離する | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / 3-14 月初合意 / FEATURE_REGISTRY / BUGS | 修正 | 月初合意支払 gate で、2026年5月以前の稼働月 (`source_ym <= 202605`) を導入前/移行月として `合意済` 扱いにする仕様へ更新。実際の合意 row は偽造せず、gate 表示理由を「導入前/移行月のため合意済み扱い」とする | 月初合意機能は2026年6月途中導入で、2026年5月稼働分は本人が月初に合意できない。5月分が snapshot hash 更新により `条件更新あり` blocker になり支払停止していたため | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY | 追加 | `/admin/payouts` に `先12か月 メンバー別支払予定` 表を追加。行=非役員・支払対象メンバー、列=稼働月、セル=その月に外部支払する `totalPay` 合計。セル選択で PJ 別の支払額 / 本契約 / 別財布 / 発生額 / pt / 未払い残を表示する仕様へ更新 | 支払月単位・PJ単位だけでは、メンバーごとの今後12か月の支払予定とPJ内訳を横断確認しづらかったため | えいみ |
| 2026-06-23 | 6-2 Admin Projects / Members 台帳 / 6-6 Member Billing Prompts / design mypage | 変更 | `/admin/members` の既存「支払対象」列を「役員」(`is_officer`) に改名し、新しい「支払対象」列 (`exclude_from_payout_notice`) を追加。非役員かつ支払対象外は `/mypage` 金額を `ー`、役員かつ支払対象外は金額表示 + `（役員のため支払対象外）`、0円は未計算扱いしない仕様へ更新 | 役員会社留保と非役員の支払通知書対象外を分離し、0円と未計算を混同しないため | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY / BUGS | 修正 | 報酬対象メンバーがいない月を `reward_summary_json=null` ではなく `members=[]` の0円キャッシュとして保存する仕様へ更新。`forecastCapped` では key 有り0円として扱い、budget fallback に落とさない | `p00` / `p09` のように旧重計算でも0円が正しい月が `null` のままだと、通常GETのキャッシュ集計では未計算扱いになり、先12か月表で本来0円のPJに支払予定が出る可能性があるため | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY / BUGS | 修正 | `payout-reward-cache-refresh` の先12か月更新で、支払 ym に紐づくcycleだけでなく、同じ窓内の稼働 ym のcycleも同期対象へ追加。`p00` / `p09` のように支払月判定では窓外になりうるが、先12か月表では稼働月として表示される行の `reward_summary_json` も事前生成する仕様へ更新 | 通常GETをキャッシュ読みにすると、forecast表の正本は `forecastCycles.reward_summary_json` になる。cronが支払月基準だけだと、表示窓内の稼働月cacheが欠けるケースが残るため | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY / BUGS | 修正 | `payout-reward-cache-refresh` の日次対象を前月・当月・翌月から、前月 + 当月から先12か月へ拡張。手動実行は `lookahead` query/body で先月数を指定できるようにし、`/admin/payouts` 先12か月表用の `reward_summary_json` を表示前に作れる仕様へ更新 | 通常GETをキャッシュ読みに寄せるだけでは、日次生成範囲外の将来月で `forecastCapped` の元キャッシュが欠けるため。表示高速化とキャッシュ正本化を両立するため | えいみ |
| 2026-06-23 | 6-8 Admin MS Overview | 修正 | MS pt配分スライダーの範囲を「現在値×1.5」ではなく、編集開始時点の最大pt×1.5を右端にした固定レンジへ変更。全MSまとめスライダーと個別MSカード内スライダーの両方で同じ固定レンジを使う | スライダー操作中にmaxが現在値へ追従し、右側へ動かすほど1pxあたりのpt増加幅が変わっていたため。全域で一定間隔の操作感にするため | えいみ |
| 2026-06-23 | 6-8 Admin MS Overview | 修正 | `全MS pt配分スライダー` の各MS行に `MS金額` を追加。右端を `pt / MS金額` 表示にし、スライダー操作中も `recomputeMsOverview.ptValueYenByMs` の再計算結果を即時反映する | 全MS横断でpt配分するとき、pt数だけでは各MSの金額インパクトを比較しづらかったため | えいみ |
| 2026-06-23 | 6-8 Admin MS Overview | 修正 | 編集モードのMS一覧先頭に `全MS pt配分スライダー` を追加。MS名 / pt数値入力 / スライダー / 現在pt だけを並べる配分専用パネルで、個別編集カード内の `pt配分スライダー` と同じ編集中 state を更新する。通常MSの変更中は `残り割り振り可能pt` も同パネルでリアルタイム表示する | 元仕様にあった「全MSのスライダーだけがまとまった配分導線」が、個別カード内スライダーへ寄ったことで失われていたため。全体比較しながらpt配分できる入口と、詳細カード内での個別調整の両方を残すため | えいみ |
| 2026-06-23 | 6-8 Admin MS Overview | 修正 | 編集カードを左=MS基本情報 / 右=担当share表の2ペインに再編。担当share表は2カラムを廃止してメンバー1人=1行に統一。pt は数値入力に加えて `pt配分スライダー` を復帰し、通常MSの `残り割り振り可能pt = regularPointBasis - Σ(non-cap_extra MS effectivePoints)` をリアルタイム表示する仕様へ更新 | 横長すぎるフォームと2カラム担当表で比較しづらかったため。元々あったスライダーによるMS pt配分導線を消さず、配分残も見ながら調整できるようにするため | えいみ |
| 2026-06-23 | 6-5 Admin Payouts / FEATURE_REGISTRY / BUGS | 修正 | `/admin/payouts` 通常GETで先12か月 capped 投影を毎回 `computeForwardCappedMemberCosts` で全PJ再計算していた処理を停止し、`forecastCycles.reward_summary_json` のキャッシュ集計から `forecastCapped` を返す仕様へ更新。手動「報酬キャッシュ再計算」(`refreshRewards=1`) 時だけ再計算後キャッシュを使う | 「通常GETは読むだけ」という既存仕様に対して、先12か月表だけ実質再計算が残り初期ロードが長くなっていたため | えいみ |
| 2026-06-23 | 6-8 Admin MS Overview | 修正 | 編集モード ON 直後の上部に保存バーを追加し、フッターと同じ `DB値に戻す` / `保存して DB へ反映` を表示。担当 share 行には `MS内金額 = effectivePoints × share × pt単価` をリアルタイム表示する仕様へ更新 | 保存ボタンが下部だけだと長いMS一覧で保存場所が分かりづらく、share調整時にそのMS単体の金額インパクトが見えなかったため | えいみ |
| 2026-06-23 | 2-3 PJ コックピット / 6-8 Admin MS Overview / 7-1 報酬計算 / BUGS | 修正 | MS設計編集を `/admin/ms-overview` に集約。admin編集モードで MS名・pt・tag・期間・完了条件・担当share/役割/タスク・追加/無効化を保存できるようにし、cockpit / HUD cockpit のMS設計保存口を停止。pt単価分母は `期間月数×10pt` に戻し、`total_points = シーズン期間月数×10 + Σcap_extra MS期間月数×10` と明記。ZMP OkuDoorシステム開発は 202605〜202610 の6か月×10=60pt、total 180ptへ是正 | ZMP 別財布 pt が 20pt/67pt の任意値で揺れる/通常MS pt編集で本契約pt単価が変動する問題の再発を防ぐため。write boundary を admin に一本化し、通常MS配分合計を単価分母に使わない正本へ戻した | えいみ |
| 2026-06-22 | 2-3 PJ コックピット / 月次レポート印刷ビュー | 修正 | 印刷ビュー v0.34.2: `/api/project/monthly-report-print/route.ts` の MS 取得を「現役 plan_cycle のみ」に絞り込み。`planCycles.filter(c => status ∈ {active,confirmed,draft,in_progress})` を経由して fixed (= 過去シーズン) 配下の見積明細・旧 MS が §02 表 / §02b Gantt に混入しないようにした | まさ実機確認 (2026-06-22) SX (p21) のスクショで「下の方に出てる『期間未設定』のやつは、MSじゃなくて見積書の明細にあった項目」と指摘。DB 確認の結果、過去 cycle に is_active=true で 75 件 (見積明細含む) 残存。dedup では救えないため API 側で plan_cycle.status フィルタを追加 | えいみ |
| 2026-06-22 | 2-3 PJ コックピット / 月次レポート印刷ビュー | 修正 | 印刷ビュー v0.34.1: MS dedup を milestoneId 単独から「milestoneId + タイトル正規化」の 2 段階に拡張、直近 period_start_ym 優先で残す。Gantt も同 dedup を共用。実施体制の「主な担当MS / 業務内容」から share=0 (= 関与なし) を除外、share 大きい順にソート | まさ実機確認 (2026-06-22) 「同じ MS が複数表示されてるやつは解決してない (= 別シーズン同名 MS が並んだ)。実施体制については関与 0% の MS が担当 MS に書かれているから削除して」を反映 | えいみ |
| 2026-06-22 | 2-3 PJ コックピット / 月次レポート印刷ビュー | 変更・削除 | 月次レポート印刷ビュー (v0.34.0) をクライアント提出物としてスリム化。§01 表紙: 見積書番号削除、契約番号/AMD契約番号/入札番号のみ並列。§01 Exec: COST RAG カードと予算消化メタ・XRL 前月比表を削除、Schedule/Risk の 2 軸 + XRL 5 軸現在値カードへ。§02-§03 統合 (A=業務遂行レポート / B=MS 進捗表 / C=主要成果 / D=議論で固まった事項 / E=採択・メディア) 色付きサブブロック化。§04 関連キーパーソン削除、§05 課題・リスク全削除、§07 5生データ ソース証跡削除。`stripInternalJargon` で「つくよみ / 月次進捗モーダル / MTGページ / 経営シグナル / コックピット / nudge」等の OS 内固有名を中立表現へ印刷時置換し、同時に `/api/report/generate` LLM system prompt に「クライアントから見えない呼称は出すな」を追記 (今後の生成にも適用)。`selectActiveMilestonesForReport` で MS 重複 (同 milestone_id 重複)・buffer タグ・完了済かつ動きなしで target_ym が当月より前の MS を除外 | まさ確定 (2026-06-22): クライアント提出物として「予算・リスクは要らない、OS 内固有名はクライアントから見えないので書いてはいけない、MS が古いものや重複が出ている、当月成果は当月進捗に統合してほしい、もっと色を活用して見やすく」 | えいみ |
| 2026-06-22 | 8-3 L2 routines / 設計 governance_action_items | 修正 | `scheduled-tasks/amd-os-l2-consolidated-evidence/SKILL.md` の Phase 0 で `activeProjects` (status=active) と `allProjects` (status フィルタなし) の 2 種類を取得する運用へ変更。Phase A〜J は activeProjects、**Phase K-C (D-14 要対応) / D-14G (Governance Email Sweep) / Phase M (Coverage Scanner) は `allProjects` を使う**。会社名→PJ 紐付けで ended PJ を取りこぼさない。あわせて拾う signal キーワードに「みなし決議 / 書面決議 / 同意書」を追加。BWE (p11) の `governance_watch_shareholder_meetings` / `governance_watch_board_meetings` を ON に切り替え (= ended でも対象から外さない方針) | BWE 第1回定時株主総会 同意書 (6/18 21:12, 中井遥さん→まさ・比嘉先生、書面決議, 6/22 18:00 締切) が Codex automation の D-14 / D-14G で取りこぼされた事故対応。原因は Phase 0 の active PJ list がそのまま暗黙に使われていたこと (JOYCLE 5/28 取りこぼしと同型)。新規 cron は追加せず、既存定額枠 automation 内で完結する形に修正。BWE 株主総会 1 行 + action_item 1 件を手動 backfill 済 (project_shareholder_meetings.source_ref=`gmail://thread/19eed63f8ddd31b7`) | えいみ |
| 2026-06-22 | 2-3 PJ コックピット / 8-3 L2 routines | 修正 | 同日朝の MTG Prep Worker を再設計。Codex Cloud REST API + Bearer token 経路 (= 定額外トークン課金) を廃止し、**既存 H-1 automation (`amd-os-l6-meeting-flow`) の内部 Phase P** に統合。新 automation は作らず、毎時 H-1 run で post-MTG 即時 timing 判定 → まさカレンダーの空き枠に「＋ <PJ> MTG準備: <タイトル>」枠作成 → 該当時刻に `codex exec` で新規 session を spawn → SESSION_ID を `prep_worker_session_id` に保存 → session 内で worker prompt が draft 生成して `prep_worker_status='ready'` → 同 run 末尾でつくよみ口調 Slack DM 通知。codex は ChatGPT サブスク認証 (= `auth_mode='chatgpt'`) のため定額外トークンゼロ。claude は使わず codex 一本化。prep枠ドラッグ追従、まさカレンダーのみ対象、prep_calendar_event_id 列追加 (migration 151)、prep_worker_session_url 列廃止 (DROP) | まさ確定 (2026-06-22): (a) Codex Cloud REST API は定額外トークン課金を生むため禁止、(b) 既存 H-1 と別 automation を作るのは冗長、(c) 48h前固定 spawn は現実に手一杯になる時があるので post-MTG 即時 + カレンダー空きベースに変更、(d) prep 作業もカレンダー枠として「＋」付きタスクで可視化、(e) codex / claude 使い分け廃止、codex 一本化。「automation は新規 session を生み出す役だけ、prep 本体は session 内で実行」の責務分離を明確化 | えいみ |
| 2026-06-22 | 2-3 PJ コックピット / 8-3 L2 routines | 追加 | MTG Prep Worker (= 自動準備セッション) を追加。翌48h の upcoming MTG ごとに Codex Cloud automation を動的 spawn し、文脈ロード→着地点draft→Drive資料draft→Notion議事録draft→readiness 計算まで完遂して待機。毎朝 07:30 JST につくよみが まさ専用 Slack DM で worker session URL + readiness pill + 空き枠/見積 をまとめ通知。3 SKILL (`amd-os-l6-meeting-prep-spawner` 06:30 / `amd-os-l6-meeting-prep-worker` 動的 / `amd-os-l6-meeting-prep-nudge` 07:30) と `project_meeting_summaries` の prep_* 列 11個を追加 (migration 150) | まさ要求「明日 MTG あるけど準備してない、codex 開いて『背景はこうで…』と毎回説明するのがだるい。codex automation で『このMTGの背景は把握してるよ。準備始めよう。着地点はこれだよね？』みたいなのを自動で話しかけてくれるところからのスタートだとめちゃくちゃありがたい」を OS 側で実装するため | えいみ |
| 2026-06-21 | 2-7 タスク管理 / pwa運用 | 変更・削除 | `/tasks` 画面を廃止済みに変更。GlobalNav導線、page route、TasksClient、mindmap/gantt/業務デスク、`npm run agent:tasks` helper を廃止対象として明記し、`tasks` table / `/api/tasks` / H-1 `register-tasks` は cockpit・H-1互換のため残す境界へ更新 | まさ明示依頼「AMD OS の `/tasks` はもう使っていないので廃止したい」。ただし H-1 next action と既存 cockpit kanban が `tasks` table/API に依存するため、画面廃止とDB/API削除を分ける | えいみ |
| 2026-06-21 | 2-7 タスク管理 | 追加 | `/tasks` 上部に「今日の業務デスク」を追加。期限が今日/今週、期限超過、確認待ち、停止、えいみ/agent由来タスクを既存 `tasks` から read-only 集約し、フォーカスカードと判断キューから通常のタスク詳細へ入れるようにした | 愛大OSの「業務デスク」思想を AMD OS に小さく展開し、L2/MTG由来タスクやagent作成タスクの巻き取り候補をマインドマップ/ガントへ入る前に1画面で把握できるようにするため | えいみ |
| 2026-06-21 | 6-8 Admin / MS Overview | 追加 | PJ ブロックの並び順を **healthy → frozen → inactive** に変更。`projects.status` と `projects.freeze_from_ym` + `project_freeze_periods` (status=active で `freeze_from_ym ≤ JST今月 < restart_ym`) を見て `ProjectHealthState` を判定。各層内は `budgetYen` 降順 → `projectId` 昇順。PJ ヘッダに状態 chip (`❄ freeze {fromYm}〜` / `■ {status}`) を表示。グループ単位の health は「PJ 内で最も良い cycle」を採用 | JC (ended) / BWE (ended) / CTB (freeze 中) が上段に並んでアクティブPJを圧迫していたため、healthy PJ を上に集約する | えいみ |
| 2026-06-21 | 6-8 Admin / MS Overview | 修正 | client で plan_cycle を **PJ 単位にグループ化** し、`period_end_ym` 降順で先頭を「現役シーズン」、それ以外を「過去シーズン」として `▸ 過去シーズン (N件) を表示` トグルに畳む `ProjectCycleGroup` を追加。CX (p20) / SX (p21) のように同 PJ で active + fixed の cycle が並んで block 重複していた事故を修正 | route は status active/confirmed/fixed/draft を全部返す前提のため、PJ 単位の集約は client 側で行う必要があった | えいみ |
| 2026-06-21 | 6-8 Admin / MS Overview | 追加 | MS 一覧を「現役 (target_ym ≥ JST 今月 or 期間未設定)」「過去分 (target_ym < 今月)」に分割し、過去分はデフォルト折りたたみ + `▸ 過去分 (N件) を表示` トグル展開に。閲覧モード・編集モード両方に適用。計算 (メトリクス・メンバー年計・pt 単価) からは過去分を外さない (= 表示だけ畳む)。あわせて KUTE (p25 `PC-p25-202605-202703`) と CX (p20 `PC-p20-202606-202609`) に MS「事務手続き」を追加: KUTE は 2pt きよ100% (= 128,571円 ≒ 12.9万)、CX は 10pt きよ100% (= 117,000円 ≒ 12万、`value_plan_cycles.budget_yen` を 0 → 585,000 (= Σ billing.budget_yen) に同期して pt 単価を確定) | シーズン跨ぎ後に過去 MS が現役と並んで一覧を圧迫しないようにする。KUTE / CX の事務手続き工数 (きよ) を MS として明示し、年計・支払予定に乗るようにする | えいみ |
| 2026-06-21 | 6-8 Admin / MS Overview | 追加 | 編集モードを追加。PJ ブロックごとに「✏ 編集モード」トグルで MS の pt をスライダーで動かせる。`recomputeMsOverview` (= `computeSeasonPl` のメンバー予算配分と同式の純関数) で メトリクス / MS pt価値 / メンバー年計 を JS 側で即時再計算。`PUT /api/admin/ms-overview/{planCycleId}` で `value_milestones.points` を一括更新 → `value_plan_cycles.total_points` 再計算 → `syncRewardSummariesForProject` で全月 reward 再計算 (PAID 月は skip) | スライダーで pt 配分・メンバー序列を動かしながら影響を即視できるようにし、cockpit を行き来する手間を省くため | えいみ |
| 2026-06-21 | 6-8 Admin / MS Overview | 追加 | `/admin/ms-overview` を新設。全 active plan cycle の MS を pt 順で並べ、本契約/別財布の pt 単価を分離してメトリクス・横バー・メンバー年計 (plannedShare 理論値) を表示する設計レビュー画面。`/admin/season-pl` の computeSeasonPl を再利用して算定ロジック乖離を防ぐ。実消化 (`milestone_monthly_progress`) は読まず、設計値そのものを見せる | 2026-06-20 ZMP MS 設計再考セッションで「全 MS pt 順 × メンバー年計のヴィジェットをそのまま admin にして全 PJ 載せたい」とまさが要望したため | えいみ |
| 2026-06-20 | 6-7 契約管理 / 8-3 L2 routines | 追加 | `/admin/contracts` に契約条件候補を表示し、D-13 の出力に `contract_terms` candidate を追加。candidate/pending では売上・予実・報酬capへ反映せず、admin review 後の Contract Apply が本番反映点であることを明記 | CX契約条件抽出の未取り込み差分を main 現行OSへ安全に手移植し、古いbranchを消せる状態にするため | えいみ |
| 2026-06-19 | 2-2 Member Workflows / 6-5 Admin Payouts / 6-6 Member Ops | 追記・訂正 | `members.exclude_from_payout_notice=true` の対象例に あき / ID029 (無報酬稼働) を追加。りり / ID006 と同じく、報酬額表示は `ー`、月初合意・支払通知書・支払 gate は `not_required` として扱う。あわせて 6-5 の月初合意予定報酬ソースを、当月月次予算 × 予定MS消化pt × active member share の合意用予定額へ同期 | まさ指示「あきも除外しておいて。無報酬で働いてるので。」をOSマニュアル正本へ残し、支払通知書の capped 支払額と月初合意用予定額を混同しないため | えいみ |
| 2026-06-19 | 4-5 収支シミュレーション / 6-5 Admin Payouts / 7-1 報酬計算 | 変更 | `/admin/payouts` と `/management-score` 下部の先12か月表を、`キャッシュ支払` / `会社留保` / `報酬債務` / `cap超過チェック` の4表に分解。会社留保は支出ではなく `cap/売上枠 - 外部支払` で表示し、報酬債務は12か月合計ではなく各月残・ピーク・最終月残で読む | 1つの表に外部支払・会社留保・未払い残・capリスクが混ざると、会社留保を増やせているかを見る目的に対して誤読が起きるため | えいみ |
| 2026-06-19 | 4-5 収支シミュレーション / 6-5 Admin Payouts | 修正 | 先12か月の「本契約cap / 別財布」表を、各セル `本契約` / `別財布` の2行 × `入` / `出` / `残` の財布チェック表に整理。MS月割・stock・合算残を主表示から外し、残がマイナスの時だけ `払いすぎ確認` を出す | 目的が「財布ごとにいくら入り、いくら出て、払いすぎていないか」を確認することなのに、複数の監査情報が同じセルに混ざって読みづらかったため | えいみ |
| 2026-06-19 | 6-5 Admin Payouts / 7-1 報酬計算 / design project PL | 追加・変更 | `/admin/payouts` に報酬債務台帳を追加する仕様へ更新。未払い残を `前月残 + 今月発生 - 今月支払 = 月末未払い残` で表示し、原因を `契約前発生` / `繰越+今月発生` / `繰越のみ` / `cap不足` に分類。先12か月表も `stock` ではなく `未払い残` と表示し、PL/cash/支払予定/報酬債務の境界を design に追記 | SX の契約前稼働 202604/202605 由来の未払い残が正しい数字でも、単なる stock 表示だと後から理由を再確認する必要が出るため | えいみ |
| 2026-06-19 | 4-3 AMD Score | 変更 | R(readiness) / XRL checklist / S(survivability) / Triple Helix / 幾何平均の説明を、数式だけでなく日本語・表・例で読める形へ補強 | AMD Score を初見で読む人が `TRL/BRL/GRL/SRL/HRL` や `sigma_SU` の意味を数式から逆算しなくて済むようにするため | えいみ |
| 2026-06-19 | 3-2 / 8-3 / H-1 MTGカード | 修正 | recurring 予定MTGの series 推定を補強。`recurring_event_id` がDBへ残らない既存カードでも、title が `定例` / `月次` / `毎月` / `weekly` / `monthly` 等なら曜日を外して `PJ + title + 開始時刻` で束ね、月次定例の曜日ズレでも次回1枚だけ表示・同期する。build v0.28.8 | v0.28.7 は fallback key に曜日を含めていたため、Google recurring id が残っていない月次/不規則な定例が複数カードとして残るケースがあったため | えいみ |
| 2026-06-19 | 3-2 データと抽出 / 8-3 L2 routines | 変更・削除 | `/admin/calendar-review` を削除し、H-1 次アクションは `POST /api/task-calendar/register-tasks` で `tasks` へ自動登録、担当者本人にだけ Slack DM nudge する仕様へ変更。Calendar作業枠 dry-run は必要時のみ `/api/task-calendar/schedule-plan` に残す。build v0.28.9 | admin が全件 review する運用は負荷が高く、タスク担当者本人に確認を返す方が自然なため。まさ指示「このページ自体削除しようよ。そんで自動でタスクが登録されて、ちゃんとslack nudgeが飛ぶようにして。」 | えいみ |
| 2026-06-19 | 2-2 / 2-3 / 3-1 / 6-2 / 6-3 / 6-6 / design/routine | 変更・削除 | OS 上の PM/PL 月次確認 TODO / nudge を完全廃止として明記。報告書確認の軽い連絡は Slack 側で完結させ、`/mypage` / dashboard / cockpit / action queue へ同期しない。支払 gate から月次ルーティン完了条件も外し、月次カードは read/write の確認面としてだけ残す | まさ指示「そもそも月次ルーティンというもの自体を廃止して。報告書確認nudgeはslack上でやるだけだから、OS側は不要。」に合わせ、前回の「report nudge だけ残す」方針をOS内実装から撤回するため | えいみ |
| 2026-06-19 | 9-2 開発者向け | 変更・削除 | Vercel deploy / main push 前に事前確認で止まる 2026-06-04 ルールを削除し、2026-06-12 以降の「main push = Vercel Git 自動 production deploy」「原則、deploy前の事前確認で止めない」「deploy bundle は事後報告」「`AMD_OS_VERCEL_DEPLOY_APPROVED=1` は人間確認フラグではなく誤実行防止スイッチ」へ統一 | 古いVercel事前確認文言を読んだ worker が本番反映済みと誤解させ、まさが未deployページを探す時間を失ったため。まさ指示「その古いルールをすべて削除して。二度とこういうことが起きないように。」 | えいみ |
| 2026-06-19 | 4-3 AMD Score | 変更 | legacy M-X-F は主表示へ戻すものではなく、PRS の P/R/S を説明する evidence shelf として読む説明を補強 | legacy score が高いからといって PRS Primary の `INPUT NEEDED` を上書きしない境界を、読者が manual だけで判断できるようにするため | えいみ |
| 2026-06-19 | 4-3 AMD Score | 変更 | PRS primary formula の前に、P/R/S を「勝ち筋の大きさ」「届く準備」「生き残る力」として読む説明、式の日本語訳、0-9点の感覚表を追加 | 数式だけだと初見読者が AMD Score を単なる巨大な点数として読みやすいため、PRS が必要条件の同時充足を見るモデルだと manual 上で直感的に理解できるようにする | えいみ |
| 2026-06-19 | 4-3 AMD Score | 変更 | R の `readiness` を「会社として前に進められる準備」として説明し、XRL checklist の「全項目チェック済みが続く最大レベル」を階段の例で補足。S では Triple Helix、産官学合成、幾何平均、三乗根、`+1/-1` の意味を説明 | R/S の数式だけでは、初見読者が「なぜその式になるのか」「何を観測しているのか」を理解しにくかったため | えいみ-worker |
| 2026-06-19 | 2-1 / 2-3 / 2-4 / 3-3 / 4-8 / 6-3 / 6-4 | 変更・削除 | PM向け月次 routine step UI 廃止後の導線へ本文を同期。`/mypage` は月次TODOではなく月初合意・月次報酬確認、PJ cockpit は月次カードから `CockpitMonthlyModal` を開く導線に整理。旧 PL レビュー DM route は削除済みとし、契約由来請求額は `contract-billing-auto-confirm` と admin billing / payouts 側で扱う境界へ更新 | PWA/GAS側で旧 PM 月次 routine component・nudge・PL review route を削除/停止したため、ユーザー向け manual が存在しない step/API を案内しないようにするため | えいみ |
| 2026-06-19 | 7-1 報酬計算 | 修正 | アンカー方式の期間按分で、PM locked アンカーが `period_start_ym` より前にある場合は経過月数の起点を開始直前月へ丸める説明を本文へ追記。回帰チェック `scripts/check_ms_schedule_anchor_boundary.mts` と実装 `ms-schedule-shared.ts` を同期 | 開始前 0% 確認が開始月 100% 完了に化ける境界バグを、報酬計算の読み手にも分かる形で固定するため | えいみ |
| 2026-06-19 | 2-2 Member Workflows / 6-6 Member Billing Prompts / 7-1 報酬計算 | 修正 | 月初合意画面の stock 表示を `未払いストック（今月は支払われない）` の別枠へ変更。PJカードの主表示は `今月支払` とし、合意用予定報酬は補助表示に下げる | stock 分が通常の報酬額と同じ見え方だと、今月支払われるものなのか未払い繰越なのか本人が判断できないため | えいみ |
| 2026-06-19 | 3-2 データと抽出 / 8-3 L2 routines | 追加 | `/admin/calendar-review` を追加。MTGカード/議事録/Gmail TODO/Slack TODO 由来の Calendar 作業枠候補を dry-run planner で確認し、`calendar_writes` / `slack_nudge_candidates` / `review_reasons` をOS上で見て write bundle をコピーできる。画面・route とも Calendar/Slack/Gmail 実writeなし | planner API 実装済みと「実際にCalendarへ入る」状態が混同されていたため、外部write前のreview面を追加し、完了定義を OS 上で見えるようにするため | えいみ |
| 2026-06-19 | 6-5 Admin Payouts / 7-1 報酬計算 | 変更 | 役員会社留保を非役員支払より先取りせず、同じ月次 cap 按分に入れる形へ変更。役員に割り当たった分は `companyReserveYen` / `officerReserveYen` として内部留保にし、支払通知書 `totalPay` は0のまま除外する。役員の未充当分は `companyReserveUnfundedYen` に残し、過去 stock として翌月支払へは繰り越さない | 役員留保だけを優先消化すると、非役員メンバーへの支払/stock返済が不自然に後回しになるため。会社留保の扱いは維持しつつ、月次 cap の取り合いでは他メンバーと同等に扱う | えいみ |
| 2026-06-18 | 6-5 Admin Payouts / 7-1 報酬計算 | 変更 | 会社回収バッファに月次消化上限 `companyReserveBufferMonthlyYen` を追加し、SX の80万円バッファは 202606〜202609 に20万円ずつ消化する形へ変更。契約バッファを一括で全額回収せず、残 cap を役員会社留保・非役員支払/stock返済へ回す | 80万円を初月に全額近く回収すると支払capが硬すぎるため。契約開始前稼働分は回収しつつ、月次キャッシュアウトとメンバー支払を平準化する | えいみ |
| 2026-06-18 | 2-2 Member Workflows / 6-5 Admin Payouts / 6-6 Member Billing Prompts / 7-1 報酬計算 | 修正 | 月初合意の対象判定を freeze overlay と支払対象外メンバーに同期。`project_freeze_periods` / `projects.freeze_from_ym` 到達後のPJ (= CTB p06 202605以降) と `members.exclude_from_payout_notice=true` (= りり / ID006 NIMS 無償出向) は月初合意対象外。SX のように支払予定0円でも `stockYen` が発生するPJは、本人画面に支払予定0円・ストック予定・支払対象額を read-only 表示する | CTB が 202606 月初合意に出ていたのは、月初合意実装が `projects.status='active'` だけを見て freeze overlay を見落としていたため。報酬を受け取れない出向メンバーと、支払0だがストックされるPJを誤って未合意/算定待ちに見せないため | えいみ |
| 2026-06-18 | 6-5 Admin Payouts / 7-1 報酬計算 | 変更 | 月初合意支払 gate を追加。`/admin/payouts` の支払データ保存、PDF生成、一括PDF、通知メール送信、送付済み確定で `member × 稼働月 × PJ` の未合意・条件更新あり・修正要望中を server-side block する。admin override は理由・actor・member/PJ/月を `member_monthly_work_agreement_payout_overrides` に監査保存した場合だけ許可。cron `payout-notice-prebuild` も blocker member を生成対象から外す | 月初合意を「見える化」から「合意していないと支払へ進めない」支払ゲートへ進めるため。契約上の個別発注/SOW/条件確認として hard guard を本番運用するには契約改定・メンバー同意・法務レビューが前提であることも明記 | えいみ |
| 2026-06-18 | 6-5 Admin Payouts / 7-1 報酬計算 | 変更 | 月次cap消化順を「契約バッファ (`budget_buffer_amount`) → 役員当月稼働分の会社留保 (`companyReserveYen` / `officerReserveYen`) → 非役員メンバー支払/stock返済」に変更。役員は支払通知書から除外したまま、当月basePayをAMD内部留保として `reward_summary_json` とPJ収支表に残す。非役員 stock は従来どおり carryIn、役員の過去 stock は支払予定に繰り越さない | 入札案件では契約開始前稼働を stock して後月支払する必要がある一方、役員稼働分まで外部支払/stock返済に食われると会社残高が崩れるため。SX の事前稼働80万円バッファも同じ共通ロジックで消化する | えいみ |
| 2026-06-19 | 2-2 / 2-3 / 2-6 / 6-2 / 6-3 / 6-6 / design/routine / design/mypage | 変更・削除 | PM月次確認を「月次報告書に対する『これでいい？』nudge」だけへ集約。報告会日程調整は完全廃止、2か月に1回の対面ナレッジ会は月次ルーティン外運用、立替精算確認はPMタスクから除外、請求書発行・送付はadmin役割へ移管、CTB見積送付は停止中のため一旦廃止。admin billing matrix は報告書確認 / 立替 / 請求 / 入金 / 支払の管理面だけを残す | まさ指示「報告会は完全廃止でおけ。」「月次報告書は、『これでいい？』ってnudgeだけPMに飛ばせばOK。」「立替精算もそれでOKなので外して。」「請求書発行送付はadminの役割に変更する。」「CTBが停止してるので見積もりは一旦廃止。」に合わせ、PMの月次ルーティン化をやめるため | えいみ |
| 2026-06-19 | 3-3 通知・つくよみ / 8-1 Knowledge Admin / Tsukuyomi | 変更 | Slack上のえいみ / つくよみ返信仕様を追加。えいみ親投稿へのスレッド返信はえいみが返し、つくよみ親投稿へのスレッド返信はつくよみが返す。開発者向けには AMD-Slack GAS `gas-slack/S001_Router.js` が `message` とスレッド内 `app_mention` の親投稿personaを判定し、`replyPersona=eimi|tsukuyomi` を渡す運用を追記 | えいみ親投稿に対してつくよみが返信したSlack事故を受け、Slack Events実入口とpersona分岐を次回誤読しないようマニュアル正本に固定するため | えいみ |
| 2026-06-19 | 2-2 / 2-3 PJ コックピット / 6-2 PJ・メンバー台帳 / 6-3 Invoice/Billing Routine / 6-6 Member Ops / design/mypage | 変更 | `請求額確定` を PM/PL の `/mypage` 月次 TODO と報酬除外判定から外す仕様へ更新。cockpit の `budget` step は削除せず、契約/報酬キャッシュの前提が崩れた時の例外復旧・個別差分入力用に残す。契約未適用・対象月の報酬額未可視は通常のPMタスクではなく、契約台帳/報酬キャッシュのデータ整備対象として扱う | まさ指示「請求額確定については除外しておけ。ただし全PJについて契約書が読み込めてて報酬額が見えていることが前提。」に合わせ、毎月のtap確認をPMタスク化しないため | えいみ |
| 2026-06-18 | 2-3 PJ コックピット / BUGS | 修正 | MTG詳細MarkdownのAMDメンバー自動リンクを、`members.code_name` の単純部分一致ではなく、独立した mention と判定できる箇所だけに限定。`こうした` / `こうして` / `しかるべき` / `かかる` のように短い code_name (`こう` / `かる`) が日本語の単語途中に含まれる場合はリンクしない。必要な場合は本文側で `[りり](/mypage?memberId=ID006)` のような明示Markdownリンクを使う | CX鮫島MTGカードで「藤崎さん」を「りり」へ直す一方、本文中の「こう」「かる」が全てコードネーム扱いで誤リンクされていたため。短い code_name は自然文に頻出するので、単純substringリンクを恒久仕様として禁止する | えいみ |
| 2026-06-19 | 6-5 Admin Payouts / 7-1 報酬計算 | 修正 | cap 配分を「契約バッファ (`budget_buffer_amount`) 控除後、役員内部留保と非役員支払を同じ配分母で扱う」仕様へ修正。役員は支払通知書から除外したまま、割当額を `companyReserveYen` / `officerReserveYen`、未配分分を `companyReserveUnfundedYen` / `stockYen` として同じ carry chain に乗せる。あわせて `extra_revenue_json` の開発期間按分額 × 65% を `cap_extra` reward cap に反映し、regular / `cap_extra` 片方の未使用 cap は同月内の未配分 pool へ補填する | まさ確定「cap配分のときは役員の内部留保分も、他のメンバーへの支払いと同等に扱う」に合わせるため。ZMP/OkuDoor では別財布売上とMS進捗がプロラタなのに、reward cap だけ `budget_yen` 手入力値を見て stock が発生していたため | えいみ |
| 2026-06-19 | 2-2 Member Workflows / 6-5 Admin Payouts / 6-6 Member Billing Prompts / 7-1 報酬計算 | 修正 | 本人画面とadmin月初合意一覧の stock 表示を支払額から分離。本人PJカードは `今月支払` を主表示、`stockYen` を `今月末未払い残（今月は支払われない）` として別枠表示し、前月繰越・今月発生・今月支払の内訳を添える。admin一覧にも `今月支払` / `未払い残` 列と合計カードを追加 | stock が支払われる報酬と同じ見え方だと、今月支払対象なのか未払い繰越なのか本人/adminが判断できないため | えいみ |
| 2026-06-18 | 6-5 Admin Payouts / 7-1 報酬計算 | 変更 | 月次capの会社回収バッファ (`budget_buffer_amount`) を契約台帳上の AMD 回収分として扱う仕様を追加。2026-06-19 訂正により、役員内部留保は非役員支払と同じ cap 配分母で扱い、支払通知書からだけ除外する | 入札案件では契約開始前稼働を stock して後月支払する必要がある一方、契約上の会社回収バッファは cap 前に控除する必要があるため。役員内部留保については後続訂正で、他メンバー支払と同等に扱うことを current truth にした | えいみ |
| 2026-06-18 | 4-5 収支シミュレーション / BUGS | 修正 | `/management-score` キャッシュフローの別財布売上について、PL計上 (`extraRevenue`) とキャッシュ入金 (`extraRevenueCash`) を分離。PL は従来どおり開発期間 `period_start_ym`〜`period_end_ym` で按分し、キャッシュ予算は `invoice_ym` または `billing_date` + PJ支払サイトで解決した入金月にだけ乗せる。OkuDoor(p19) の ¥2,000,000 税抜は 202605〜202610 の PL 按分を維持しつつ、2026-03-31 請求分のキャッシュは 202604 入金見込みへ移る。あわせてチャート線種を「実績=実線 / 予算=破線」に反転 | 予算まで実績値へ置き換えると予実差分が消える一方、PL按分月をそのままキャッシュ入金月にすると実際の銀行残高と乖離するため。予実差分は残し、キャッシュ予算だけ請求・支払サイトに沿わせる | えいみ |
| 2026-06-18 | 4-5 収支シミュレーション / BUGS | 修正 | `/management-score` キャッシュフローの2026年1月「キャッシュ残高(予算)」について、freee `wallet_txns` を確認し、旧GAS snapshotの `loan01` (商工中金 500万円、`disbursementYm=202601`) は未実績ではなく 2026-01-19 実行済み融資と判定。いったん `loan01` を外して再生成した結果、202601 の `cash_amount_yen` が `-101,332` となり、実態と逆にありえないマイナスになったため、`loan01` を予算入力へ復元し `company_budget_monthly` を再生成。202601 の予算は `loanDisbursement=5,000,000`、`net_cash_flow=4,292,245`、`cash_amount_yen=4,898,668`。別途 `sync_freee_cash_balances.cjs` で freee口座月末残高を `company_actual_monthly.category='cash_balance'` へ同期し、202601 実績キャッシュ `4,795,492` を `/management-score` の実績線・実績欄に表示するよう変更 | 予算線は「計画・確定済み前提」、実績線は freee / 入出金実額として分ける必要がある。予算まで実績値へ寄せると予実差分が消えるため、実行確認済み融資は予算入力に残し、実績残高との乖離は実績線・差分で見る | えいみ |
| 2026-06-18 | 4-5 収支シミュレーション / BUGS | 修正 | `/management-score` 下部の「PJ別 先12か月収支」表も、`/admin/payouts` と同じく `computeForwardCappedMemberCosts` の capped 支払予定と `expandExtraRevenue` の別財布按分を使うよう修正。最終収支は `budgetYen + extraRevenueYen - forecastPayoutYen`。ZMP(p19) 202607〜202612 が旧ロジックで `195,000 - 195,000 = 0` に張り付いていたのを解消し、正値 (202607 +¥329,207 / 202608 +¥341,133 / 202609 +¥313,164 / 202610 +¥327,243 / 202611 +¥46,926 / 202612 +¥46,653) を BUGS に記録。build v0.27.7 | 収支系は `/management-score` 月次収支シミュレータ、`/admin/payouts` 先12か月表、`/management-score` 下部PJ別表の3系統あり、過去修正が下部PJ別表へ伝播していなかったため。finance 数字変更時の3系統確認ルールを正本化する | えいみ |
| 2026-06-19 | 6-5 Admin Payouts / 7-1 報酬計算 | 修正 | 月初合意の予定報酬を `reward_summary_json.members[].basePay` / `breakdown[].payYen` に統一。月初合意側の独自報酬再配分を廃止し、cap / carry / stock / 支払確定状態は `/admin/payouts` の精算情報として分離 | 金額に合意する画面で報酬キャッシュと別体系の金額を出すと、本人が合意する金額の根拠が崩れるため | えいみ |
| 2026-06-18 | 6-3 Invoice/Billing Routine | 追加 | 「契約由来の請求額は つくよみ が毎月自動確定する (①案)」節を新設。契約抽出済み (= `contract_terms` applied) PJ は毎月の `請求額確定` を PM が手で押す必要がなく、つくよみ (月次 cron `/api/cron/contract-billing-auto-confirm`、毎月1日 JST 07:00) が契約由来額を `budget_confirmed` まで自動で進め、PM には Slack DM で事後通知する (確認するだけ)。対象条件 (期間内 / end_ym 必須 / schedule_based は contract_source_term_id, monthly_fixed は fee_amount)・金額算出 (schedule_based は budget_yen÷0.65 で月別、monthly_fixed は fee_amount)・触らない月 (reported 以降は不変)・PL レビュー DM との関係を明記。詳細実装は spec 5-6 へリンク | まさ「契約書を抽出できてるなら、もう自動的に金額いれて、その確認だけをPMにslack nudge投げるだけのプロセスにしてほしい」「全PJの仕組みを変えたい」→ ①案確定 (applied=人の確認、以降つくよみ自動確定+PM事後通知) (2026-06-18)。毎月同じ契約額を PM に tap-confirm させるのは無意味で、手入力前提を排除する (= つくよみが生データから自動構築する原則) | えいみ |
| 2026-06-17 | 4-5 収支シミュレーション / BUGS | 既知の未対応 | 上記の別財布按分は `/management-score` 月次収支シミュレータには反映されたが、`/admin/payouts`「先12か月 PJ収支」表 (`AdminPayoutsClient.tsx`、`cycle.budget_yen` ベースの独自 forecast) には未反映。PJ収支コンポーネントが2系統あり片方だけ実装した。次セッションで (B) AdminPayoutsClient の forecast に按分ロジックを追加 (按分 lib の共通化が望ましい)。詳細 BUGS.md 2026-06-17 | まさが deploy 後に `/admin/payouts` で ZMP の按分が乗っていないのを発見。finance の数字を変えたら両画面で目視確認する verify ルールを教訓化 | えいみ |
| 2026-06-17 | 4-5 収支シミュレーション | 変更 | 別財布（別契約）売上の計上方式を**請求月一括 → 開発期間按分 (B-a)** に修正。`extra_revenue_json` のエントリに `period_start_ym`〜`period_end_ym` を持たせると、総額を開発期間の月数で均等按分し (端数は最終月寄せ、pt消化と同じ「期間で割る」思想)、PL計上もキャッシュ入金も同じ按分月に乗せる。按分は live builder 層 (`live-monthly-pl-inputs.ts`、`monthsBetween`/`nextYmInt` ヘルパー追加) で各月行に展開し、エンジンの `extraRevenueForYm` は ym 一致額を素直に合算 (エンジン本体は無改修)。期間指定が無いエントリは従来どおり `billing_cycles.ym` へ一括 (後方互換)。OkuDoor を 202605〜202610 の6ヶ月按分 (≒¥333,333/月・最終月¥333,335) に本番データ更新。build v0.25.1 | 「開発が走った月に売上を分散させたい (単月の収支が実態に近くなる、pt消化と同じ思想)」というまさ確定 (2026-06-17「B-aで」)。請求月一括だと開発6ヶ月分の売上が2026-03に集中し、開発実働月の各単月収支が原価だけ立って実態とズレるため。本番データ書き換えのためまさGO取得済み (「ごー！」) | えいみ |
| 2026-06-16 | 4-5 Finance Simulation / 5-6 契約管理 | 変更 | CX(p20) の売上計上を契約書実態に修正。旧 `monthly_fixed ¥290,000 / start_ym=202511 / end_ym=null` が契約終了後も無期限計上され 202702 以降まで¥290,000を立てていた事故を解消。契約書 (contract_terms `1cf248e3`, NIMS, 契約No W2026004905, masa確定2026-06-15) に基づき `fee_type='variable' / start_ym=202606 / end_ym=202610` へ更新し `contract_terms_json` に契約メタ+月別スケジュール投入。売上は billing_cycles の budget_yen(6月¥50,700/7-9月¥178,100)÷0.65逆算で 6月¥78,000・7-9月¥274,000=税抜¥900,000、10月以降¥0。billing_cycles は既存値が正しいため不変更。あわせて spec 5-6 に「契約抽出→projects/billing_cycles 反映 (Contract Apply)」節を新設し、contract_terms applied を ①contract_terms_json ②fee系 ③billing_cycles の3層へ反映する正本経路と end_ym 必須ガードを定義 (自動反映は未実装=次の実装対象) | CXは6-9月の有期契約で最終振込10月なのに無期限で売上が立ち、予実・CF・法人税が過大に出ていた。根本対応として契約書から projects 契約カラム・売上計上・月別売上まで自動入力する仕組みを設計書に正本化 (まさ 2026-06-16「契約書を見て自動入力する仕組みにしてほしい」「admin/projectsの契約関連カラムにも入れる」)。本番データ書き換えのためまさGO取得済み (「あってる」) | えいみ |
| 2026-06-16 | 4-5 Finance Simulation | 変更 | サブスク/SaaS の `company_finance_recurring_items.amount_yen` を freee 取引実額 (`/api/1/deals?partner_id=`) で棚卸し更新。slack ¥24,000→¥20,828 / claude ¥45,000→¥22,945 (freee正本ルール=freeeに実額があるものはそれ優先。最新2025-10、Max化前のため2026実額が入り次第追従) / conduct ¥44,000→¥48,400 (会計+労務2本→税理士一本に契約変更。社労士法人コンダクトはfreee上2025-10で取引終了=解約確認、独立行は元々無く合算行を税理士単独額に更新) / co-en=つくばまちなかデザイン ¥38,500 据置 (定常月額一致, 費目=地代家賃)。マニュアル4-5に「サブスク額の正本=freee deals」行を追加 | サブスク額は手動入力前提にしない (まさ確定「freeeデータが有るものはそっち優先」)。古い額のまま月次収支シミュレータを回すと固定費が実態とズレて経営判断を誤るため。本番データ書き換えのためまさGO取得済み (2026-06-16「Aで」「社労士側デリートでおけ」) | えいみ |
| 2026-06-16 | 4-5 Finance Simulation | 変更 | `/management-score` の月次収支シミュレータ + 予実表 + グラフをまさ5要望で改善。(1) 社保が常時¥0だった真因 = live builder が全固定費を `costType='taxable'` 固定 → `socialInsBase=0`。`display_name` が `役員報酬/上乗せ/給与/給料/賞与` を含む行を `executive` にマップし社保が乗るよう修正 (実測 base ¥1,167,334 → 社保 ≒ ¥175,100/月)。法人税は元々エンジンが2月決算納付で計上済み (202602 ¥108,000 等) と実データ確認。(2) 予実表を縦スクロール撤廃で全行常時表示。(3) 売上原価行を `▶/▼` トグルで PJ別 内製/外注内訳展開。(4) グラフに実績キャッシュ残高 (緑破線, 実績確定月まで累積) line を追加し予算 line と予実比較。build v0.23.1 | まさ5要望。特に社保¥0は live builder 移行時の costType 欠落バグで、役員報酬を社保ベースに入れないと営業利益・CF・法人税まで過大に出る経営判断ミスにつながるため。発見 (202602 役員報酬まさ ¥100万/¥979,891 の重複行で社保base二重計上) も含め正本に固定 | えいみ |
| 2026-06-16 | 6-1 / 8-3 / OSデータ一覧 | 変更 | OS上の D系データ一覧で `Claude routine` と見えてしまう source 文言を current truth に再修正。D-13/D-14/L3-1 は「route はあるが Codex collector 未実装」、D-10 は「Codex writer起点だが内部 Anthropic API 例外」と読めるように揃えた | まさが本番一覧を見たときに、いま誰が抽出していて何が未実装かを一目で判断できるようにするため | えいみ |
| 2026-06-16 | 6-1 / 8-3 | 変更 | `/admin/settings` の説明と 8-3 を更新し、D-10 は「Codex 側 writer が起点だが内部 route は Anthropic API 使用、ただし例外許容」と明記。あわせてデータ一覧の writer 名を D/M/W/H で読める形に整理 | 「どの automation が何のデータを抽出するか」と「D-10 の例外境界」を OS / manual 上で平易に読み取れるようにするため | えいみ-worker |
| 2026-06-16 | 8-3 L2抽出routine | 変更 | D-10 `member-weekly-activities` は `/admin/settings` 手動起動の対象から外し、Mac/MMO writer も `@anthropic-ai/sdk` を使う PWA route trigger に過ぎないため fixed-price goal 未達と明記 | Codex automation 経由でも従量 Anthropic route を叩く限り「定額トークン内で回す」要件を満たさないため | えいみ-worker |
| 2026-06-16 | 8-3 L2抽出routine | 変更 | Claude routines 依存を外した L1-L3 Codex移植の current truth は spec `5-8` を参照する注記を追加。あわせて D-10 `member-weekly-activities` は `/admin/settings` の Operations から `interactive=1` 付きで手動起動できる運用を追記 | Claude routines を前提にした旧説明だけだと、抽出再開時にどこを起点にすべきか誤読するため。まず D-10 を OS 上から即時実行できる最小単位を残すため | えいみ-worker |
| 2026-06-16 | 4-5 Finance Simulation | 追加 | live builder の実装完了に伴い実測検証を追記。(1) エンジンの「売上0の月は原価も計上しない」制約 (`pjRev===0` でスキップ) が現行 active PJ 全件で無害なことを表で確認 (p00=総pt0/p07・p24・p26=plan cycle無し/p06=過去cycle/p10-21=monthly_fixedで毎月売上/p25=全月billing有り)。(2) `deriveRewardBudgetForPt` の月次報酬予算解決順 (billing budget_yen → fee×0.65 fallback → cycle budget按分) と主要PJの uncapped 月次原価実測 (p19≈¥195k/p20≈¥50.7k/p21≈¥56.8k/p25≈¥654.5k)。p21の26年4-5月は budget_yen=0 で fee fallback が効くことを明記。(3) baseline 表示も `buildLiveGasSimulationResult` で live エンジン駆動 (snapshot は実績列ソース+fallbackとして保持)。build v0.22.9 | live builder を全 active PJ の実データで検証した結果を、チャットに残さず正本に落とす (セレンディピティ依存の出口対策、まさ確定 2026-06-15「調査の発見は正本docに即落とす」)。特に p21 fee fallback と 516行制約の無害性は次セッションが再発見せずに済むよう表で固定 | えいみ |
| 2026-06-16 | 4-5 Finance Simulation / 7-1 報酬計算 | 変更 | 月次収支シミュレータの入力を凍結 snapshot (`company_budget_inputs`) から OS ライブテーブル直読み (`buildLiveMonthlyPlInputs`) へ移行する仕様を記載。固定収益=`projects.fee_amount`、変動収益=`billing_cycles.budget_yen`、固定費=`company_finance_recurring_items`、将来メンバー原価=MS進捗を期間按分した uncapped 報酬。予実管理のため将来月の予測報酬を `billing_cycles.reward_summary_json` へ保存 (A案、将来行INSERTはまさ承認後)。7-1章に uncapped 定義と将来原価シミュ節を追加し、現行PWA実装 (PM_LOCKED進捗 / payable cum / アンカー按分) に同期 | 旧 snapshot 方式は手入力コピーで現実 (新規契約・MS進捗・報酬発生) とすぐ乖離し経営判断に使えなかった。あわせて、えいみが自作の報酬計算ロジックを設計書経由でなくコード探索で再発見していた構造問題 (セレンディピティ依存) の出口対策として 7-1章を実装に同期 (まさ確定 2026-06-15「設計書を正本として作ったはず」) | えいみ |
| 2026-06-15 | 8-3 L2 Extraction Routines | 追加 | 「層 (tier) 軸 と Coverage Scanner (L3)」節を追加。L1/L2/L3 + 非LLM派生の定義、D-12 freee=L1相当、Coverage Scanner=L3 (不在検知 / Phase M / `/admin/coverage-gaps`) を記載 | 個別抽出器 (D-1〜D-14) の上位に「OS化されてない重要情報を検知する安全網」を新設したため。cadence軸だけだとL1同期/L2抽出/L3監視が見分けられず取りこぼしを再生産する事故を防ぐ (まさ確定 2026-06-15) | えいみ |
| 2026-06-15 | 2-7 タスク管理 / pwa運用 | 変更 | ユーザー自身が `/tasks` 画面で作成したタスクのtoastを廃止。えいみがagent helperでタスクを作成した場合は、OS通知任せではなく同じセッション内で `タスク追加: <title> (<task_id>)` と伝える運用に変更 | まさが自分で追加したタスクを自分に通知するのは意味がなく、必要なのはえいみが裏で追加したタスクを会話中に知らせることだったため | えいみ-worker |
| 2026-06-15 | 2-2 / 6-6 | 変更 | 月初合意でbreakdown行がないMSを0円表示にし、当月報酬も担当MSもないPJを非表示にする | 未確定表示が、実際には0円または月初合意対象外を意味していたため、本人が確認すべき対象だけに絞るため | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 / 8-2 通知 | 変更 | `/tasks` の既定status filterをdone含む全status表示(archived除外)に変更し、`archived` statusとarchived込み全表示を追加。progress入力/表示を外し、詳細ウィンドウのSession行は未設定でも常時表示。画面作成時はtoast、agent作成時は `app_notifications(kind='task_created')` へ通知する | 完了タスクも見ながら運用したい一方、archive済みは通常視界から外したい。progress手入力は運用負荷が高く、Session linkは未設定状態も見える方が探しやすいため | えいみ-worker |
| 2026-06-15 | 2-2 / 6-6 | 変更 | 月初合意の本人画面から条件/前提・未確定/要確認枠を外し、MS別予定報酬を表示できるよう修正。MS別金額は `breakdown[].payYen` をそのまま使う | PJ合計は出ているのにMS別予定報酬が未確定に見える不整合を解消し、本人確認情報をPJ/MS/到達目標/予定報酬へ絞るため | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 / pwa運用 | 追加・変更 | Codex / Claude Code のえいみが `npm run agent:tasks` で `/tasks` を確認・作成・更新・session紐づけできる運用を追加。タスク詳細にSession linkを表示する | 会話中に発生した新タスクをチャット内TODOで流さずOS正本に登録し、どのAIセッションで進めているか追えるようにするため | えいみ-worker |
| 2026-06-15 | 2-2 / 6-5 / 6-6 / 7-1 | 変更 | 月初合意の本人表示を担当MS・当月到達目標・想定報酬中心へ変更し、frozen PJ除外、cap/carry-over非表示、修正要望保存を追記 | 報酬が発生しないPJや精算内部情報を月初合意に出さず、本人が条件違いを申告できる運用にするため | えいみ-worker |
| 2026-06-15 | 2-2 / 6-5 / 6-6 / 7-1 | 追加・変更 | 月初タスク・報酬合意を追加。`/monthly-agreement` の使い方、`/mypage` 導線、`/admin/monthly-work-agreements`、`member_monthly_work_agreements`、報酬計算を変えない確認レイヤーであることを追記 | メンバーが月初に遂行対象・想定報酬・条件へ合意し、未合意/条件更新をadminが確認できる運用をマニュアル正本へ残すため | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 | 変更 | マインドマップのノードstatus表示にhoverするとstatus選択ポップアップを出し、選択したstatusをモーダルなしで即時反映・裏保存・Undo可能にした | status変更のたびに詳細モーダル、プルダウン、保存を踏むUXが重かったため | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 | 変更 | マインドマップで新規作成した詳細ウィンドウを実寸に合わせて画面内へ引き上げ、タイトル入力へfocusするよう変更。ノード色をTODO/Doing/Done/Review/Blocked/Pendingのstatus別に分離 | 下の方に生成したタスクのmodalがはみ出す問題と、TODO/Doingが同色で区別しづらい問題を解消するため | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 | 変更 | 詳細ウィンドウを幅480px基準に縮小し、さらに上寄せ。フォームを `label/control/label/control` の横詰めgridに変更し、入力枠・select・dateを28px、Descriptionを56px程度へ圧縮 | まさ指摘「全然高密度になってない」「入力枠が大きい」「p00 AMDの右の空きが無駄」「デフォルト位置をもっと上に」への対応 | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 | 変更 | マインドマップのpinch/trackpad zoomを緩め、親タスク移動時に子孫も追従、edgeを直線矢印化、詳細ウィンドウを上寄せ/高密度/header削除つきに変更。ガントは親→子tree順、indent/枝線、子数badgeで親子関係を明確化 | まさ指摘「pinchが敏感」「親を移動したら子もついてくる」「矢印は直線」「modalの削除が遠い」「ganttの親子関係を明確に」への対応 | えいみ-worker |
| 2026-06-15 | 2-7 タスク管理 | 変更 | マインドマップ詳細のタイトル入力は Enter で即時保存。node の `+` handle click は親node右側へ新規子タスクを optimistic 作成し、drag時は既存どおり既存nodeへの親子edge接続に使う。edge矢印は子タスク→親タスク表示へ反転し、node hoverは右へずらさずその場scaleに変更 | まさ指摘「どんどんタスクを増やしたい」「タイトル入力でエンターキー」「＋を押したら子タスク生成」「矢印の向きを逆」「ズレるのではなく、少し拡大」への対応 | えいみ-worker |
| 2026-06-14 | 2-7 タスク管理 | 追加 | `/tasks` の全PJタスク管理章を追加。マインドマップ / ガント、空白クリック作成、drag/drop親子edge、期間バー、既存カンバンとの関係を記録 | OSに全PJ・全員のタスク状況を横断表示する新機能を追加したため | えいみ-worker |
| 2026-06-14 | 2-7 タスク管理 | 変更 | マインドマップをカード型から円形node + 外側labelへ変更。親子edgeは親から子への矢印にし、edge作成はnode全体dropではなく輪郭hoverで出る `+` ハンドルから別node輪郭へdragする操作へ変更。node clickで詳細モーダルを開く | まさ指摘「四角だとマインドマップ感なくない」「親子関係が分からない」「ドラッグアンドドロップだとUX悪い」への対応 | えいみ-worker |
| 2026-06-12 | 4-8 MS Progress / Revision | 変更 | デフォルト月割りをアンカー方式へ追補 (A案+C案)。確定済みの月がある MS はその確定値起点で `アンカー% + (100/N)×経過月数`、target_ym 超過後も 100% に自動ジャンプせず月割り継続。期限切れ + 100% 未達 MS は毎日「D-2 MS計画遅延」通知 (l2_kind='ms_schedule_delay')、解消で自動消去。トラブル表に「進みすぎ/期限切れMSが100%にならない」行を追加 | まさ確定「AとCでいこう」。SX (p21) で 5月確定 15% の MS が 6月デフォルトで 100% に飛び PJ 進捗が進みすぎに見えた問題の根治。確定仕様は spec 3-10 | えいみ |
| 2026-06-12 | 4-8 MS Progress / Revision | 変更 | MS進捗を schedule_default_revision_v3 へ全面改訂。全MSにスケジュール按分のデフォルト月割り (非LLM cron `/api/cron/ms-schedule-progress`、`source='routine_auto'`)、LLM は乖離±10pt以上で revision 提案 + 通知 (l2_kind='ms_progress_revision') のみ、まさが「はい」で confirm するまでデフォルト通り。抽出フロー mermaid / cron 表 / トラブル表も更新 | まさ確定「Nか月計画なら月100/N%がデフォルト、ズレは通知確認、おれが認めない限りデフォルト通り。巻き戻りはそもそも起きない設計」。確定仕様は spec 3-10 | えいみ |
| 2026-06-12 | 6-2 Projects/Members 台帳 | 変更 | `/admin/members` の「役員」列ヘッダを「支払対象」へ改名。`is_officer=true` → `対象外` / false → `対象` 表示とし、編集 checkbox に「役員 (=対象外)」label を追加 | まさ指示「役員という列のヘッダを支払対象という名前に変えてほしい」。役員報酬 0 円扱い (2026-06-12 確定) に伴い列の実機能 = 支払対象判定を表すため。ヘッダだけ変えると ON badge の意味が反転して読めるので表示も同時に修正 | えいみ |
| 2026-06-12 | 8-3 L2 routines | 変更 | L2 health red/yellow 後に `health:l2:actions` で local action ledger を作り、owner / next action / deadline / close条件 / worker prompt seed を持つ incident に変換する運用を追加 | red報告が来ても誰も復旧worker化しない問題を止め、検知後の起票→owner割当→修復→再チェック→closeを残すため | えいみ-worker |
| 2026-06-10 | 2-3 PJ コックピット | 変更 | MTG詳細モーダルの編集を表示 section と同じ位置の inline edit に変更し、予定MTGの旧「気をつけたい読み違い」を「必ず確認すること」として表示・編集する方針を追記 | 表示内容と編集可能領域がズレ、保存してもカードに反映されない field がある問題をなくすため | えいみ-worker |
| 2026-06-09 | 2-3 PJ コックピット / 8-3 L2 routines | 変更 | MTG詳細モーダルの添付資料を一般ファイル対応へ変更し、新規添付の保存先を Drive の `PJフォルダ / YYMMDD_会議名` と明記。カード上の保存先表示、旧Storage添付の互換表示、DBはmetadataのみ保存する方針を追記 | MTGカード資料アップロードで md 等が古い画像/PDF制限に拒否されていたため。MTG単位の資料置き場をPJ folder配下に整理するため | えいみ-worker |
| 2026-06-09 | 2-3 PJ コックピット | 変更 | MTG詳細モーダルのMarkdown添付 (`.md` / `.markdown`) をOS内モーダルで開ける仕様を追記 | MTG準備メモなどのMarkdown添付をDrive/別タブへ移動せず、カード内で読めるようにするため | えいみ-worker |
| 2026-06-09 | 2-3 PJ コックピット | 変更 | MTGサマリの予定MTG判定を `source_kinds='upcoming'` exact だけでなく、`upcoming+calendar+manual-prep` のような `+` 区切りtokenにも対応すると明記 | 2026-06-10 ZeMA/ZMP定例MTGカードが日時確定済みなのに日程調整中へ落ちた再発防止 | えいみ-worker |
| 2026-06-09 | 6-7 契約管理 | 追加 | `/contracts` の契約予定枠、status、version history、押印版metadata、5生データ予兆dry-run、Slack nudge dry-run、Drive保存先 `共有ドライブ/ARMADA/a3_backoffice/契約` を追加 | 契約締結予兆から押印版保存までをOS上で追い、契約書本文をDBに入れずDrive metadataだけで安全に管理するため | えいみ-worker |
| 2026-06-08 | 2-3 PJ コックピット | 変更 | 資料コーナーの閲覧/追加権限を admin-only から、対象PJの active member または admin へ変更。PJ cockpitを開けるメンバーが資料一覧だけ `Forbidden` にならないよう明記 | PJ資料はPJ内共有機能であり、まさ/adminだけに閉じると目的を満たさないため | えいみ-worker |
| 2026-06-06 | 2-4 AMD 会社全体 | 変更 | Dashboard の通常PJ一覧から AMD 全体PJ (`p00`) を除外し、AMD PJ cockpit の入口をバイタルサイン枠に一本化 | 会社全体PJを通常PJカードとして重複表示しないため | えいみ-worker |
| 2026-06-06 | 2-4 AMD 会社全体 / 4-5 Management Score | 変更 | Dashboard のバイタルサイン枠クリック先を AMD 全体 cockpit (`/project/p00/cockpit`) に変更し、右上の詳細リンクだけ `/management-score` に残す | Dashboard から会社全体PJへ直接戻れる導線にするため | えいみ-worker |
| 2026-06-06 | 2-3 PJ コックピット | 追加 | TODO と経営ハイライトの間に「資料」スペースを追加。ファイル追加時は Google Drive の当該PJ folder配下 `AMD OS 資料` folderへ保存し、OSには `project_documents` のmetadata/linkだけを残す仕様を追記 | PJ cockpitから資料をOS上のリンクとして残し、実ファイルは共有DriveのPJ folder配下に集約するため | えいみ-worker |
| 2026-06-04 | 2-4 AMD 会社全体 / 4-9 研究機関 ERS | 変更 | `/dashboard` の本文表示順を PJ一覧 → 研究機関ERSリスト → Company Content shelf と明記し、Company Content shelf をメンバー / 沿革 / メディア掲載 / photo の4カラム棚として整理 | まさ指示「研究機関リストがcompany contentより下にいっちゃってるから、上にもってきて。PJリストと続きで表示されるようにして」への対応 | えいみ-worker |
| 2026-06-04 | 2-4 AMD 会社全体 / 4-9 研究機関 ERS | 変更 | `/dashboard` の研究機関ERSリストを、PJ一覧と同じ左/mainカラム内の直下に置くと明記 | まさ指示「まだマイページより下にあるよ。PJリストと同じカラムに入れて。」への対応 | えいみ-worker |
| 2026-06-04 | 4-9 研究機関 ERS / 2-4 AMD会社全体 | 変更 | Dashboard の通常PJリストから `project_category='ecosystem'` または `p25` / KUTE名に該当するPJを除外し、KUTE (`inst_kute`) は研究機関ERSカードから `/institutions/inst_kute/cockpit` へ入り、既存KUTE PJ (`p25`) のコックピット内容を同画面で見る仕様へ変更 | KUTE が通常PJリストと研究機関リストに二重表示されていたため。PJ側KUTE cockpit content と研究機関側ERS/制度比較contentを消さず、研究機関側へ導線を寄せるため | えいみ-worker |
| 2026-06-02 | 2-4 AMD 会社全体 | 変更 | `/dashboard` に Company Content shelf を追加し、メンバー / 沿革 / photo を3カラム preview として配置。右カラムのMyPage埋め込みは「今週やったこと」までに留め、月別PJカードは `/mypage` 単体にだけ残す仕様を追記 | まさ指示「ダッシュボードページのPJリストの下に設置」「右カラムのマイページ枠の各PJごとのカードは削除」「メンバーリスト、沿革、photoを３カラムで並べたい」への対応 | えいみ |
| 2026-05-29 | 9-3（新設） | 追加 | 附則（変更履歴）章を新設。manual-chapters.ts に登録 | まさ指示「マニュアルも勝手に消されないように、附則のセクションを末尾につけて変更履歴はすべてそこに残して」 | えいみ |
| 2026-05-29 | 3-2 データと抽出 | 追加 | 「🎨 カレンダー色→PJ判定（恒久仕様・削除禁止）」セクションを追加。CFG_ColorPJHistory + CFG_PJAlias による色優先 PJ 判定の正本化 | #71 の Claude routine 移植時に色判定が無断で削除され `project_name` substring match に簡略化されていた事故（まさ「作ってきた機能が勝手に消されてる」「二度と起きないようにして」2026-05-29）の再発防止。正本に無かったため移植で消えても気づけなかった | えいみ |
| 2026-05-29 | 1-1 AMD OS とは / 9-2 開発者向け | 追加 | `/manual` の検索欄と検索 index の説明を追加 | まさ指示「OSマニュアルに検索機能をつけてほしい」への対応。次回以降も検索 index の対象と更新箇所が分かるようにするため | えいみ |
| 2026-05-29 | 1-1 AMD OS とは / 3-3 通知・つくよみ / 9-2 開発者向け | 追加 | `/manual` 限定の Gemini 版つくよみ Manual Q&A の説明を追加 | まさ指示「gemini使ってつくよみに質問して回答してもらう形式も試してみたい」「このページだけつくよみフロートを復活」への対応。global mascot 復活と混同しないため | えいみ |
| 2026-05-29 | 1-1 AMD OS とは / 3-3 通知・つくよみ / 9-2 開発者向け | 変更 | Manual Q&A の回答 UX を「ここ見たらOK」の参照章リンクつき、敬語なしのつくよみ口調、高校生にも分かる説明へ明記 | まさ指摘「ここ見たらいいよ！って案内するなら、リンクつけてほしい」「つくよみって敬語使わない」「高校生とかに教えるように」への対応 | えいみ |
| 2026-05-29 | 1-1 AMD OS とは / 3-2 データと抽出 / 3-3 通知・つくよみ / 9-2 開発者向け | 変更 | 共通用語表、3-2 用語表、3-3 前提語、マニュアル本文品質ルールを追加。1-1 の M/W/D/H L2一覧を 3-2 正本に合わせて修正 | まさ指摘「なんの説明もなしに生データとか言われるし、L2については一切説明もない。これじゃマニュアルと呼べない。3-2に限った話じゃなく全体」への対応 | えいみ |
| 2026-05-29 | 2-1 はじめて使う人向け / 3-1 全体設計 / 4-1 判断エンジン | 変更 | 2-1 の L2 候補に短い説明を追加、3-1 冒頭に共通用語リンクを追加、4-1 の見出し typo を修正 | 全体を通して初出用語が説明不足にならないようにするため | えいみ |
| 2026-05-29 | 2-5 探索系アセット | 変更 | 見出し typo `つの役割` を `4 つの役割` に修正 | マニュアルとして読み始めた時の雑さを減らすため | えいみ |
| 2026-05-29 | 3-2 データと抽出 / 4-8 月次報告書 / 6-1 Operations / 8-3 L2 routines | 変更 | M-1 `monthly_reports` の「対象外 = R313 のまま」を、Claude routine 移管対象外と明記。R313 は未生成/差分あり時に Claude API を呼びうるため課金対象外ではないこと、実画面確認時点で `run_monthlyReportCron` / `run_L2CronDaily` trigger が存在しないことを追記 | まさ指摘「L2-1が対象外 R313のままってどういう意味？まさかトークン課金続いてないよね？」への回答と再発防止 | えいみ |
| 2026-05-29 | 3-2 データと抽出 / 4-8 月次報告書 / 6-1 Operations | 変更 | M-1は生成されるべき必須データであり、止める対象は意図しない有料API定期実行だけだと明記。現行 route を PWA manual/backfill、月次報告モーダル、Codex automation outbox として整理 | まさ指摘「生成されてたら困るみたいな言い方なのが気になる」への対応。生成そのものと自動課金リスクを混同しないため | えいみ |
| 2026-05-29 | 3-2 データと抽出 / 4-8 月次報告書 / 6-1 Operations / 8-3 L2 routines / 9-1 判断履歴 / L2_DATA / scheduled-tasks | 変更 | M-1 `monthly_reports` を定額 subscription automation の正式対象に訂正。Codex automation `AMD OS M-1 月次報告抽出` と `pwa/scheduled-tasks/amd-os-l1-monthly-report-extract/SKILL.md` を正本化し、R313 は旧有料API経路で trigger 復活しない扱いに変更 | まさ指摘「主役とか主役じゃないとか決めたことはない」「L2の全データが定額課金automationの中で全部安定して抽出できてることを求めた」「マニュアルがすべての正本」への訂正対応 | えいみ |
| 2026-05-29 | 3-2 データと抽出 / 4-8 月次報告書 / 6-1 Operations / 8-3 L2 routines / 9-1 判断履歴 | 変更 | D-2 `milestone_monthly_progress` の定期抽出 writer を **MMOマシン automation `amd-os-l3-ms-progress-extract`** と明記。旧 PWA `/api/cron/hourly-estimate` + GAS 154 は停止済み fallback として整理 | まさ指摘「`amd-os-l3-ms-progress-extract` が何かがよくわかってない。MMOマシン automation ならマニュアルにもそう書いてほしい」への対応 | えいみ |
| 2026-05-29 | 3-2 データと抽出 | 変更 | 冒頭の画面内に「先にここだけ読む: D-2 MS 進捗の抽出ルート」を追加し、実行場所・処理名・課金ルート・復旧時に見る場所・書き込み先を箇条書きで明記 | まさ指摘「人間に一発でわかるようにしてほしい。あの画面範囲でそう言えるの？」への対応。表セル内の `MMOマシン automation` だけでは意図が伝わらないため | えいみ |
| 2026-05-29 | 3-2 データと抽出 | 変更 | 冒頭の MS 進捗専用ブロックを M-1〜D-6 全体の抽出ルート早見表へ拡張。各 L2 について実行場所・現行処理・課金ルート・止まった時に見る場所を同じ粒度で明記し、下の writer 表も `Mac 登録済` などの古い/曖昧な表現から現行運用表記へ更新 | まさ指摘「D-2だけやったから、人間はこの画面範囲全部理解できる状態になったといえるの？」「D-2以外はどうなの？」への対応。1行だけ直すのではなく、同じ画面範囲の同種問題を横展開して潰すため | えいみ |
| 2026-05-29 | 8-3 L2 Extraction Routines（章メタデータ） | 変更 | `manual-chapters.ts` の 8-3 summary / topic description に残っていた `Claude routine` 中心の古い表記を、M-1〜D-6の現行 writer / MMOマシン automation / outbox-applier / 課金ルート / 復旧場所の説明へ変更 | マニュアル本文だけでなく検索・章カード・トピック説明にも古い current-looking 表記が残ると、人間にもつくよみにも誤解されるため | えいみ |
| 2026-05-31 03:20 JST | 3-2 データと抽出 / 8-3 L2 routines | 変更 | D-7 Textbook Insights を追加し、候補DB・通知承認・approved後の local BZM applier 経路、Vercel runtime から git file を直接編集しない境界を追記 | Before Zero 知見抽出を運用者が復旧できる粒度で把握できるようにするため | えいみ |
| 2026-05-30 | 4-9 研究機関 ERS（新設）| 追加 | section 4 に「4-9 研究機関 ERS (機関エコシステム整備度)」章を新設。苗床レイヤー指標 (8 軸 × サブ軸 Lv1-5 の加重和・充足率)、AMD Score (個体) とは別ロジックで σ_SU 経由で概念連動し計算式に二重計上しない点、`/institutions`・`/institutions/{id}`・ダッシュボード下段の画面、4 テーブルのデータモデルを記載。`manual-chapters.ts` (MANUAL_SECTIONS decision / MANUAL_CHAPTERS / decision・discovery topic nodes) と `os_manual.md` section 4 リストも同時更新 | まさ指示「handoff でちゃんとマニュアルに追記するところまでやらないとだめ。少し大きいけどマニュアルに書いておいてほしい」への対応。ERS は新規ユーザー向け画面・概念なので OS マニュアル正本に章として残す | えいみ (cowork) |
| 2026-05-31 01:21 JST | 9-1 過去判断と経緯 / 9-2 開発者向け / 9-3 附則 | 移植・変更 | 9-1 / 9-2 に残っていた開発・deploy・automation・事故ログの詳細を `/spec/5-1`〜`/spec/5-4` へ移植し、manual 側は読者向け入口に変更。manual 変更履歴へ本行を追記 | まさ指摘「9章とか、全部移植すべきでは？」と「manual / spec / textbook 全部に附則」を受け、manual から確定実装仕様を分離するため | えいみ |
| 2026-05-31 | 2-3 PJ コックピット | 変更 | SU 系 PJ cockpit の Hero 下に `進捗管理` / `スコア詳細` タブを置き、AMD Score + XRL グラフはタブ外で常時表示する構成を追記 | まさ依頼「コックピットにスコア詳細ページを統合したい。トップの AMDスコアグラフ + XRLグラフ は常時表示」への対応 | えいみ |
| 2026-05-31 | 3-2 データと抽出 / 8-3 L2 routines | 追加 | H-1の開催済みソース guard を追加。Calendar添付Geminiメモ、Notion eventId空 fallback、report_emails空PJのGmail fallback、upcoming rowとの `prep_source_meeting_id` 紐付け、`npm run test:l6-held-source-guard` を明記 | 2026-05-26 東京理科大学 飯野さんMTGが実施後ソースありなのに upcoming 準備カードだけで止まった事故の再発防止 | えいみ |
| 2026-05-31 | 4-9 研究機関 ERS | 変更 | NIMSカードから `/institutions/inst_nims/cockpit` へ入り、既存CX (`p20`) コックピットでMS進捗・月次・MTG履歴を追う導線を追記 | まさ依頼「NIMSの箱はもうある。新しいPJを作るより、そのカードをコックピット化する」への対応。重複PJを作らず既存カードと既存PJ導線を活かすため | えいみ |
| 2026-05-31 | 2-3 PJ コックピット | 変更 | コックピット2タブを横幅いっぱいの左右2分割にし、スコア詳細は裏読み込みと短期再利用で2回目以降の読み込み待ちを減らす仕様を追記 | まさ指摘「スコア詳細タブのローディングに時間がかかる」「2タブしかないので横幅いっぱいを2分割」への対応 | えいみ |
| 2026-05-31 | 8-3 L2 routines | 追加 | 先手力 heartbeat を L2 ではなく control layer として追記し、10:15-20:15 JST 毎時15分に `proactive_outbox` queued/blocked をPJ司令塔へ通知して `mark-sent` 記録する運用と SKILL 正本へのリンクを追加 | 先手力 outbox が積まれても司令塔へ届かない未完を解消し、復旧時に見る場所を人間にも分かるようにするため | えいみ |
| 2026-05-31 | 8-3 L2 routines | 変更 | D-7 Textbook Insights の target routing を第8部新章に同期。`decision_branch` は8-2、`failure_learning` は8-3、`relationship_playbook` は8-4、`reusable_question` / `field_transition` は8-5、`cross_project_pattern` は8-1 default、`theory_case` は6-1 + BZM review 前提にする運用を追記 | Before Zero実践テキスト化に合わせ、承認候補が旧default `8-1` に集中しないよう人間向けマニュアルにも残すため | えいみ |
| 2026-05-31 | 2-3 PJ コックピット / 2-4 AMD会社全体 | 変更 | Dashboard / PJ cockpit の先手力表示名を TODO に統一。Dashboard は未送信/要対応を最大3件、PJ cockpit はPJ別TODOを表示し、行クリックで経緯・資料リンク・次アクションのモーダルを開く仕様へ変更 | 「今日打つべき一手」と「先手キュー」の名前揺れ、重複表示、件数増加、カードクリック時に状況が分からない問題を解消するため | えいみ |
| 2026-05-31 | 4-9 研究機関 ERS | 変更 | NIMS研究機関コックピットの構成を、ERS概要・readiness snapshotを先頭にし、`進捗管理` / `スコア詳細` の2タブへ整理。MTGツリーは進捗管理下部へ移し、スコア詳細はERS 8軸・評価項目・Lv/根拠メモを見る場所として明記 | まさ依頼「一番上にMTGツリーが出ているのを他PJコックピットと同じような構成へ直したい」への対応。研究機関でも基本構成をPJ cockpitに寄せつつ、SUのAMD ScoreとERSを混同しないため | えいみ |
| 2026-06-01 | 4-5 Management Score | 変更 | `/management-score` に各軸の変動理由UIを追加し、まさえいMTG確定シグナル帯をchip羅列から読めるカード一覧へ変更する仕様を追記 | まさ依頼「新規 推移が急落した理由を画面で追いたい」「まさえいMTGで確定したシグナルが何なのか・どう使うのか分からない」「文字が入り切らない」への対応 | えいみ |
| 2026-06-01 | 4-5 Management Score | 変更 | pre-month snapshot を最新の経営バイタル扱いから外す鮮度 guard、ヘッダーの材料時点表示、確定シグナル帯を対象月・score対象type・axisありに絞る仕様を追記 | 6月snapshotが5/26材料で作られ、5/28-29の香川大活動前なのに「今月の新規急落」に見えた問題と、2023年/食品排液の古い・薄いsignalがスコア欄に混ざった問題の再発防止 | えいみ |
| 2026-06-01 | 4-5 Management Score | 変更 | Management Score が `project_strategy_signals` から読む対象を `project_id='p00'` の会社スコープに限定し、高確度 pipeline candidate の暫定評価ルールと、根本修正で必要な `signal_scope` / `applies_to_company_score` / pipeline 確度・金額・時期の設計案を追記 | LST/p07 など個別PJの技術・設立・事業論点が AMD会社全体の経営バイタルへ混入する入口を止め、香川大のような契約前高確度パイプラインはMTG後追いでなく事前評価できるようにするため | えいみ |
| 2026-06-01 | 4-5 Management Score | 変更 | `/management-score` のGAS月次試算表に freee PL実績・入金確認済み・支払通知書送付済み行を追加し、キャッシュ判断パネルで当月着地見込み、先3か月Cash、未入金予定、支払済み未反映アラートを表示する仕様を追記 | まさ依頼「予算だけでなく実績も見える表」「5月に実際いくら入金され、いくら支払い、予算との差がどこで出たか」「先3か月の資金繰りを見たい」への対応 | えいみ |
| 2026-06-01 | 4-5 Management Score / 8-2 Notifications | 変更 | 会社バイタル分類列、raw置換ルール、契約前高確度pipelineの採用条件、香川大/KUTE/NIMS initial backfill、202605/202606 再計算結果を追記 | 個別PJ除外で必要な会社level案件まで落ちる過小評価を防ぎ、継続/新規/方向が低い理由を inputs/evidence から説明できるようにするため | えいみ |
| 2026-06-01 | 3-2 データと抽出 / 8-3 L2 routines | 追加 | H-1 MTGサマリ / 予定MTGカードの Notion文字起こし導線を追記。`notion_url` があればNotionを開き、無い予定MTGはCalendarへ逃がし、PWAから録音開始やDB DDLをしない境界を明記 | まさ依頼「MTGカードからすぐNotion文字起こしを始める導線がない」への対応。L6はNotionメモを読むが、開始操作はNotion側で行う前提を残すため | えいみ |
| 2026-06-01 | 4-9 研究機関 ERS | 変更 | 制度比較マトリクス実データ96件 (3機関 × 32項目) の入力記録 `pwa/design/institution_policy_matrix_inputs_2026-05-31.md` と再構築用seed `pwa/scripts/migrations/120_institution_policy_assessments_seed.sql` を追記 | ERS制度比較マトリクスのworker成果を最新mainへ取り込める形にし、既存116/117/118/119 migrationと衝突しないよう120へ採番するため | えいみ-worker |
| 2026-06-01 17:43 JST | 4-3 AMD Score 詳細仕様 | 変更 | `/venture-map/amd-score/retrofit` の PRS候補比較試算を追記。P/R_netは正式DB列ではなく保存しない仮入力、未設定時は `not enough data`、正式採用はBZM review requiredと明記 | 現行7軸AMD Scoreと新モデル候補を混同せず、運用者が比較レイヤーとして読めるようにするため | えいみ |
| 2026-06-06 19:54 JST | 4-3 AMD Score 詳細仕様 | 変更 | PRS を primary、legacy AMD を comparison として説明を更新。`amd_score_inputs.prs_potential/prs_r_net` の nullable 保存と review queue を追記 | 画面の主表示と文書の説明を一致させ、missing 時に silent fallback しない current truth を残すため | えいみ |
| 2026-06-01 | 4-5 Management Score | 変更 | `/management-score` 下部のGAS月次試算表を、項目ごとの `予算` / `実績` / `差額` subrowで読める予実管理表へ修正する仕様を追記 | 前回のキャッシュ判断パネル追加だけでは、まさが期待した「添付の月次試算表そのものの予実比較」になっていなかったため | えいみ |
| 2026-06-01 | 2-6 adminオペ | 追加 | `/admin/private-wiki` 裏wikiを追加し、人物単位の関係性メモ、source/evidence、admin-only境界、archive運用を追記 | センシティブな人物メモを通常cockpitや外部workspaceへ出さず、admin専用で保存・編集できるようにするため | えいみ-worker |
| 2026-06-01 | 2-3 PJ コックピット / 2-4 AMD会社全体 | 変更 | TODO UIの表示順、`outbox_id` 重複排除、先頭優先TODOと一覧の分離、`proactive_loop_events` 履歴つき詳細モーダル、HUD側旧TODOかんばん非表示を追記 | Dashboard上部がTODOで埋まる問題、注目カードと一覧の重複、名称揺れ、クリック後に状況が分からない問題をまとめて解消するため | えいみ-worker |
| 2026-06-01 | 4-5 Management Score | 変更 | `/management-score` の月次試算表を、各月の中に `予算` / `実績` / `差分` の3列を持つ横並び予実表へ変更し、予算=グレー、実績=黒、差分プラス=水色、差分マイナス=ピンクで表示する仕様を追記 | subrow縦積みでは月別の比較が読みにくかったため、1か月内で予算・実績・差分を横に並べて判断しやすくする | えいみ-worker |
| 2026-06-01 23:45 JST | 8-1 Knowledge Admin / Tsukuyomi | 変更 | `/admin/protocols` に `protocol_result_observations` の read-only outcome ledger を表示する運用を追記。表示範囲は horizon / valence / confidence / summary / PJ ID / evidence category / 短い reference id に限定し、同一horizonの異valenceだけ矛盾chipを出す | 特許retrofit P0で、秘密情報を出さずに結果観測のappend-only性をadmin画面から確認できるようにするため | えいみ-worker |
| 2026-06-04 | 9-2 開発者向け | 変更 | 当時の暫定 deploy gate を追記 (2026-06-19 に廃止済み。現行は main push = Vercel Git 自動 production deploy、deploy bundle は事後報告、`AMD_OS_VERCEL_DEPLOY_APPROVED=1` は誤実行防止スイッチ) | 2026-06-03にTextbook系の小刻みpush/deployでVercel daily deploy quotaを消費し開発が止まったため。当時の判断記録であり、現行ルールではない | えいみ |
| 2026-06-19 | 4-3 AMD Score 詳細仕様 / 4-4 FRL・HRL | 変更 | AMD Score manual を PRS primary 中心に再整理し、全主要式を LaTeX 化。`P` / `R` / `S` / `K_PRS`、各パラメータ、算出順、ベースデータ、入力解決順、legacy M-X-F appendix を追記。FRL / HRL 章も PRS の S/R evidence として読めるよう更新 | manual だけで AMD Score モデル全体のロジック、各パラメータの意味、算出方法、元データが分かるようにするため | えいみ-worker |
| 2026-06-09 | 2-3 PJ コックピット / 4-1 判断エンジン / 4-3 AMD Score 詳細仕様 / 4-9 ERS | 変更 | AMD Score の読み方を PRS primary へ更新し、legacy M-X-F / 7軸 Cobb-Douglas を comparison / evidence / Appendix 扱いに整理。P/R_net missing は review pending とし、legacy を主表示へ戻さない運用を追記 | v0.16.17 ではマニュアル上の主語がまだ MXF 中心に残っていたため、運用者が現行 primary を誤読しないようにするため | えいみ-worker |
| 2026-06-09 | 4-3 AMD Score 詳細仕様 | 変更 | PRS / legacy AMD の数式を LaTeX 表記へ変更し、スコア詳細ページの PRS Primary、P/R/S、R_net、PRS history、legacy M/X/F、Triple Helix、FRL、XRLチェックリスト、律速の読み方と算出元を追加 | 画面に出ているパラメータの説明が足りず、マニュアルだけではスコア詳細ページを読めない状態だったため | えいみ-worker |
| 2026-06-09 | 4-3 AMD Score 詳細仕様 | 変更 | PRS の `K_PRS` / `P` / `R` / `S` の意味と、足し算ではなく積を取る理由を追加。スコア詳細ページのFormulaPanelにも同じ直感説明を表示 | K/P/R/Sの概念がないと、式は読めてもPRSモデルの経営的な意味が伝わらないため | えいみ-worker |
| 2026-06-09 | 9-2 開発者向け | 変更 | deploy dry-run と本番 `/api/build-info` build stamp の確認先を追記 | 古いcheckout deployによる巻き戻りを人間がdeploy前後に確認できるようにするため | えいみ-worker |
| 2026-06-09 | 9-2 開発者向け | 変更 | deploy script が `.vercel/project.json` の `amd-os-pwa` / project id を検査し、missing / wrong project ならVercel CLI起動前に停止する運用を追記 | worker worktreeで `.vercel` が欠けたままdeployし、新規Vercel projectを作る事故を防ぐため | えいみ-worker |
| 2026-06-12 | 9-2 開発者向け | 変更 | 本番反映 = main push (Vercel Git 自動 deploy) へ変更。CLI 直接 deploy 廃止、ブランチ作成全面禁止、`AMD_OS_VERCEL_DEPLOY_APPROVED=1 bash pwa/scripts/deploy.sh` で検査・push・build監視まで行う運用に更新 (2026-06-19 補正: env は承認フラグではなく誤実行防止スイッチ) | 未 push ブランチへの正本幽閉事故 (2026-06-12) の恒久対策として deploy = push に一本化したため | えいみ |
| 2026-06-09 | 8-3 L2抽出routine | 追加 | MTGカード由来Calendar補完候補とTODO由来`+<PJ>`作業枠候補を作る dry-run plannerを追記。PWA route はCalendarを書かず、候補payloadだけ返す | stale root dirtyに残っていたCalendar一次防御設計をcurrent lineへ救出し、実writeなしで運用検証できるようにするため | えいみ-worker |
| 2026-06-12 | 2-6 adminオペ | 追加 | `/admin/weekly` 週次活動マトリクスに月次報酬を可視化: 各セルにメンバー×PJの月次報酬バッジ (billing_cycles.reward_summary_json の totalPay)、右端列=メンバー別月合計、最下行=PJ別月合計、サマリーバーに今月支払総合計。報酬がある PJ/メンバーは活動が無くても行列に表示 | まさ依頼「活動でいくら報酬が発生しているか同じ表で可視化、今月の支払合計も」。報酬のみで活動ゼロのセルは抽出漏れの発見にも使えるため | えいみ |
| 2026-06-12 | 2-6 adminオペ | 変更 | `/admin/weekly` の報酬表示で役員 (`members.is_officer=true`) を 0 円扱いに変更。セルバッジ・メンバー合計・PJ合計・総合計のすべてから除外 | まさ確定「役員メンバーは金額表示なしにして、金額自体も0円にしておいて」(2026-06-12) | えいみ |
| 2026-06-12 | 8-3 L2抽出routine | 変更 | D-10 Member Weekly を全メンバー抽出対応: `member_google_oauth_tokens` の per-member refresh_token で各メンバー本人として Gmail (sent/drafts) / Calendar (primary) を読む方式へ変更。token は PWA ログインで自動保存、token 無し connected メンバーはまさ代理 calendarList fallback。レスポンスに memberTokenAuths / tokenMissingMemberIds / tokenErrors を追加 | まさ依頼「おれ以外もAMDメンバー全員のデータを抽出できるようにしてほしい」(2026-06-12)。従来はまさ代理 OAuth のみで抽出がまさ偏重 (直近30日: まさ28件 vs 他1〜3件) だったため | えいみ |
| 2026-06-12 | 8-3 L2抽出routine | 変更 | D-10 Member Weekly を 3系統並走 writer 体制へ: 1 Claude cloud routine (08:00) 2 Mac Codex automation `amd-os-l2-2` (18:30) 3 MMO Task Scheduler launcher (19:30、run-d10.ps1)。全系統が同じ PWA route を叩き、窓単位 delete-then-upsert + UNIQUE 制約で重複行は構造的にできない。1系統でも生きていれば抽出は漏れない | まさ依頼「claude routines / Mac codex / MMO codex の3つが動く状態に。重複データは抽出しないロジックで」(2026-06-12)。D-10 抽出漏れ実害への冗長化対策 | えいみ |
| 2026-06-16 | 6-1 adminオペ / 8-3 L2抽出routine | 変更 | OS 上で current truth が分かるよう、`/admin/settings` のデータリスト説明を更新。M-1 / D-6 / D-8 / W-1 の Codex automation を active writer として明記し、D-10 は `AMD OS D-10 メンバー活動根拠抽出 (Mac)` に名称統一したうえで「Codex 側 writerだが内部 route は Anthropic API の例外」と書き分けた | まさ依頼「現状のL1-3データリストを見ても、そういう状況が全然見えない。もっと平易な日本語で分かりやすく。automation 名もどのデータを抽出するか明確に」に対応し、OS画面と manual の見え方をそろえるため | えいみ |
| 2026-06-16 | 2-6 adminオペ / 3-1 L2データ抽出 / 5-3 automation責務 | 変更 | `Claude routine / route が LLM 抽出` と読める古い current-looking 文言を current truth に修正。admin/weekly の D-10 導線は Mac / MMO の Codex writer を主語にし、spec 3-1 / 5-3 の D/M/W/H writer 表も Codex / MMO / PWA non-LLM / collector planned へ置換 | v0.22.13 確認時点でも D系データの抽出元説明が古く、現行 writer と health 状態が噛み合って見えなかったため | えいみ |
| 2026-06-13 | 8-3 L2抽出routine | 変更 | daily consolidated routine の Phase K で D-11 Media Mentions と D-13 Contract Signals が見出し衝突していたのを **Phase K-A (D-11) / Phase K-B (D-13)** に分離。D-13 Contract Signals を独立ブロックとして手順明記 (`POST /api/contracts/extract-l2` 経由、契約予兆 signal 分類、raw本文非保存)。8-3 の L2 一覧表に D-13 行を追加し、参照 md (spec 3-0/3-1/5-3、scheduled-tasks README、L2_DATA.md) の `Phase K` 表記を K-A/K-B へ統一 | 同名 Phase で 2 つの L2 が同居して曖昧だったため。読み手 (次セッション) がどの手順を指すか一意に分かるようにする整合のみで機能差分なし | えいみ |
| 2026-06-15 | 新章 株主・ガバナンス/要対応 | 追加 | JOYCLE臨時株主総会招集通知のOS取りこぼしを起点に、要対応(期日つきinbound義務)抽出+株主/総会・決議/AMD保有株式・バリュエーションを新設。PJ cockpitに「🏛株主・ガバナンス」欄(終了PJでも表示)、/dashboard・/notificationsに「要対応(期日順)」面、/admin/governanceに手動記録CRUD。DB=migration137(action_items/project_shareholders/project_valuation_rounds/project_shareholder_meetings)。設計=design/governance_action_items.md | まさ確定「要対応案件の抽出全般+各案件の株主情報・総会開催履歴・決議内容+保有株式/バリュエーションの記録場所が必要、フル実装」(2026-06-15) | えいみ |
| 2026-06-15 | 2-3 PJコックピット | 追加 | cockpit「🏛株主・ガバナンス」欄(終了PJでも表示)+/dashboard・/notifications「要対応(期日順)」面+/admin/governance編集の使い方を2-3章に追記。詳細はdesign/governance_action_items.md | JOYCLE招集通知取りこぼし起点の新機能(v0.20.12)をマニュアル正本に反映 | えいみ |
| 2026-06-16 | 2-3 PJコックピット | 更新 | 「🏛株主・ガバナンス」欄を直近総会表示から総会・取締役会履歴一覧へ拡張。メール由来の総会/取締役会/書面決議候補は `/api/governance/extract` に入り、未確認は review candidate、確認済みは `project_shareholder_meetings` に反映される | LST の取締役書面決議メールを OS に抽出できるようにする依頼 (v0.23.0) | えいみ |
| 2026-06-16 | 2-3 PJコックピット | 更新 | 総会・取締役会履歴の添付資料を Drive link としてクリック可能にする仕様を追加。抽出時に添付本文が渡された資料は当該PJ folder直下の `YYMMDD_会議名` folder に保存し、cockpit/admin には `webViewLink` を表示する | LST 添付資料をクリックできるようにし、保存先をPJフォルダ直下へ統一する依頼 (v0.23.3) | えいみ |
| 2026-06-16 | 2-3 PJコックピット / 6-2 Admin Projects / 8-3 L2抽出routine | 追加 | `/admin/projects` に「総会」「役会」checkboxを追加し、ONのPJだけ `report_emails` × ガバナンスkeywordでGmail検索する D-14G `/api/cron/governance-email-sweep` を追加。既定は `/api/governance/extract` candidate、`apply=1` で履歴反映 + Drive添付保存 | LST/CLG/AMD など監視対象PJだけを狭く検索し、総会・役会情報を各PJ cockpitへ自動格納するため (v0.24.0) | えいみ |
| 2026-06-16 | 4-5 収支シミュレーション | 追加 | **別財布（別契約）売上**を一級市民化。`billing_cycles.extra_revenue_json` (migration 142) に本契約とは別枠の単発受託売上を構造化保存し、live builder が全PJから読んで `projectRevenues[].extraRevenue` 注入、エンジン (`monthly-pl-simulation.ts`) が売上計・粗利・消費税・CF・残高に加算 (原価率は通さない=cap_extra側で計上済みのため二重計上防止)。請求日ベース同月キャッシュ、単発セマンティクス (翌月持続しない)。収支表はPJ名に `🔵別財布込`。初回投入: OkuDoorシステム開発 ¥2,000,000 (p19/202603, INV-0000000305) | ZMP(p19)で別契約OkuDoor開発の原価(cap_extra)だけが乗り売上が無く、粗利を食い潰して収支ゼロ〜赤字に見えていた問題の根本対策。今後の複数財布PJ汎用管理基盤 (まさ確定 2026-06-16, B案+C案①) | えいみ |
| 2026-06-17 | 4-5 収支シミュレーション | 変更 | 別財布（別契約）売上の開発期間按分を `/management-score` だけでなく `/admin/payouts`「先12か月 PJ収支」表にも反映。按分ロジックを共通 lib `src/lib/finance/extra-revenue.ts` (`expandExtraRevenue`) に集約し両系統が同一関数を呼ぶ構造へ一本化。route が `extra_revenue_json IS NOT NULL` の全行を返し (按分元 ym が表示窓より前でも取りこぼさない)、表は各月セルに `別財布 ¥…` を加算表示 (v0.25.2) | v0.25.1 で (A) live builder だけに按分を実装し (B) PJ収支表を verify 時に見落として横ばい表示になったため (BUGS.md 2026-06-17)。収支系コンポーネントが複数あっても按分を取りこぼさないよう、ロジックを1箇所に集約した | えいみ |
| 2026-06-17 | 4-5 収支シミュレーション / 7-1 報酬計算 | 変更 | `/admin/payouts`「先12か月 PJ収支」表の将来原価を、実績メンバー無し将来月で `cycle.budget_yen` 決め打ちしていたのを uncapped 投影 (`computeForwardUncappedMemberCosts`) へ統一。route が active PJ ごとに forward uncapped 原価を計算して `forecastUncapped` で返却、client が `forecastPayoutYen` を uncapped 優先に置換 (plan 期間外のみ budgetYen フォールバック)。「将来原価 = uncapped」が (A) /management-score と (B) /admin/payouts の両系統で正本になった (v0.25.3) | plan 期間内の月でも原価が予算 (¥195,000) と同額に張り付き「予算=原価」で収支ゼロに見える嘘が出ていたため (まさ指摘・BUGS.md 2026-06-17)。p19 202607 の真の uncapped 原価 = ¥393,705 (まさの稼働分含む)。(A) だけ uncapped 投影で (B) が budgetYen 決め打ちの非対称を解消 | えいみ |
| 2026-06-17 | 4-5 収支シミュレーション / 7-1 報酬計算 / BUGS | 修正 | **v0.25.3 を訂正**。(B) `/admin/payouts`「先12か月 PJ収支」表の将来「**支払予定**」は原価 (uncapped) ではなく **capped (月次キャップ + 繰越平準化) + 役員/支払対象外の除外**が正本。`reward-summary.ts` に `computeForwardCappedMemberCosts` を新設 (各月 `buildRewardSummary` で capped 投影、`is_officer`/`exclude_from_payout_notice` を単に落とす=再配分しない i 案)。route が `forecastCapped` を返し、client は capped が計算済みなら値 0 でも使う (役員のみ PJ の支払予定ゼロを budget フォールバックに落とさない)。build v0.25.4 | v0.25.3 で (B) の支払予定列に uncapped を入れたため、pt 消化が厚い月に budget を超えて跳ね、まさのスクショで ① マイナス月 ② ZMP 巨額 ③ 役員のみ PJ (KUTE) で巨額の支払予定、が露見 (BUGS.md 2026-06-17)。原価 (uncapped) と支払予定 (capped) は列の意味が違う。役員除外がコア計算に無く各画面で後付けしている穴も是正。実測: KUTE capped 支払予定 ¥0、ZMP 202609 ¥777,465→¥215,169 に平準化、マイナス月消滅 | えいみ |
| 2026-06-18 | 4-5 収支シミュレーション / 7-1 報酬計算 | 変更 | `/management-score` 下部「PJ別 先12か月収支」表に `MS月割 +{pt}pt / {N}MS` を追加。`value_milestones.period_start_ym`〜`target_ym` と `anchoredExpectedCumPctForYm` でPJ×月の単月MS消化ptを表示し、PM locked 行は `PM確定`、期間不足は `期間未設定` として見える化 | 12か月予測で全PJのMS進捗がプロラタに散っているか確認できず、支払予定だけ見ても「どのMSがどの月に進む前提か」が分からなかったため | えいみ |
| 2026-06-18 | 3-10 MS進捗 / 4-5 収支シミュレーション | 修正 | `anchoredExpectedCumPctForYm` で、PM locked アンカーが `period_start_ym` より前にある場合は起点を `period_start_ym` の直前月へ丸めるよう修正。開始前 0% 確認が開始月 100% 完了に化ける境界バグを防ぐ。回帰チェック `scripts/check_ms_schedule_anchor_boundary.mts` を追加 | ZMP(p19) の `OkuDoor現地運用・オープン検証` (202609〜202612, 20pt) に 202604 の 0% lock があり、旧式では 202609 に20pt全額消化され支払予定が不自然に跳ねたため。local修正後は 202609〜202612 各5ptへ戻る | えいみ |
| 2026-06-20 | 7-1 報酬計算 / 6-5 Admin Payouts / FEATURE_REGISTRY / design season_budget_actual | 追加・変更 | **別財布 (cap_extra) プールを汎用化** (v0.29.2)。`billing_cycles.extra_budget_yen` (migration 149) を別プールの月次cap (NULL=未設定/0=全額繰越/N=上限) とし、extra pt単価を `Σextra_budget_yen ÷ Σcap_extra pt` で本契約から独立化。完了月だけ満額capで「完了時一括支払」。予実表 `computeSeasonPl` も regular/extra pt単価分離に改修 (member の予算取り分を `regularEarnedPt×regular単価 + extraEarnedPt×extra単価` で計算、検算④を regular 分母で突合)。7-1 に「別財布 (cap_extra) プール」章、6-5 の別財布手順、design §5.2 に汎用プレイブック (3ステップ) を追加。**ZMP/OkuDoor で実証**: total_points 187→177 / OkuDoor share まさ0.6923系 / extra_budget_yen 202610=130万。うめ/あび各20万・OkuDoor総消化130万に収束、regular pt単価 21,273 で汚染解消 | 予実表が検出した ZMP の「原資≠Σcap・役員stock非収束」が別財布OkuDoorの本契約汚染 (即払いでΣcap膨張・別財布pt混入でpt単価薄まり) だったため。別財布も特殊計算せず全PJ共通ルール (65%/pt単価/cap/繰越) で処理する設計 (まさ確定 2026-06-20) | えいみ |
| 2026-06-22 | 2-3 PJ コックピット | 追加 | 月次モーダル レポートタブの `📄 印刷 / PDF` ボタンを実装 (v0.31.0)。クリックすると新規タブで A4 縦の印刷ビューが開く。Team ARMADA ブランドで表紙→当月サマリ→MS進捗→主要MTG→経営シグナル→担当メンバーの順に整形され、Cmd+P → 「PDFとして保存」でクライアント提出用 PDF が出力できる。外販クライアント (愛大 / NIMS) への月次提出フォーマットもこれを使う。手順は 2-3 章末尾「クライアント提出用 月次レポート印刷」節を参照、仕様正本は `pwa/spec/3-2-monthly-reports-current-spec.md` §クライアント提出用 印刷出力 | OS 内で蓄積した月次データを各PJのクライアントにそのまま提出できる品質で出力したい依頼 (まさ 2026-06-22) | えいみ |
| 2026-06-22 | 2-3 PJ コックピット | 拡張 | 月次レポート印刷ビューを **国プロ網羅型** へ全面拡張 (v0.32.0)。大学・研究機関クライアント (CX=NIMS / SX=愛媛大 / KUTE=工学院大学) への月次提出を一次想定。表紙→Exec Summary (業務遂行レポート見出し文・RAG 3軸・XRL前月比) →進捗 (本文+MS表) →**Gantt** (SVG手書き、計画期間バー+進捗fill+当月マーカー) →成果 (大進捗シグナル+公募採択+メディア掲載) →主要会議 (narrative+Decided+NextActions+Risks) →体制 (担当+MS別share+関連キーパーソン) →課題 (Risk Register+Action Items+ボトルネック) →財務 (契約金額/予算vs計上/立替/補助金) →次月計画→添付資料 の 10 章。共通ヘッダ (機関名+期間) / フッタ (©+Page X/Y) は `@page` CSS で全ページ自動付与。RAG軸はスケジュール・コスト・リスクの 3 軸のみ (まさ確定)。表紙には契約番号・入札番号・見積書番号・AMD契約番号があるものを併記表示 (列が無い AMD番号・入札番号は別タスクで列追加してから埋まる) | 既存印刷ビュー (v0.31.0) では情報量が薄く「そのまま印刷して提出できる」レベルになっていなかったため、国プロ (NEDO/JST/AMED/SIP) と民間コンサル両方の標準を実調査して網羅型へ作り直し (まさ 2026-06-22) | えいみ |
| 2026-06-22 | 2-3 PJ コックピット | 変更 | 月次レポートを「正式な対外報告書」品質に近づける整理 (v0.33.0)。**(1) 月次モーダルの「レポート」タブを廃止**: 進捗確認と印刷ビューが二重に存在する状態を解消し、進捗確認のみ常時表示の単一画面に。レポート本文の生成・編集・FIX (確定) は折りたたみ「📝 レポート本文を編集」アコーディオンで提供。「📄 印刷 / PDF」リンクをモーダルヘッダへ移動し、印刷ビューを唯一の提出フォーマットに統一。**(2) §05 主要会議セクションを削除**: 会議の議事録・narrative は提出物の主役ではないため除外。**(3) §03 当月の成果を厚く**: 会議由来の「議論で固まった事項」(Decided + 会議名 + 日付) と会議外の経営シグナルをナラティブで統合表示。**(4) §07 添付資料から会議資料 (meeting_assets) を削除**: 議事録添付を行わない方針。**(5) §07 財務サマリを削除**: 大学・研究機関提出には不要 (契約金額・予算は表紙メタで足りる)。**(6) 本名表示への切り替え**: 正式報告書のため `members.code_name` (「まさ」「かる」「ちこ」等の内輪呼称) ではなく `members.member_name` (本名: 山地 正洋・輕部 琢真・遠藤 千穂 等) を優先表示。フォールバックは code_name。Team セクションのヘッダーも「氏名 (コードネーム)」→「氏名」へ、Closer タグも削除。章番号は §01〜07 に再付番 | 印刷ビュー (v0.32.0) を実画面で見たまさからの整理依頼: 「レポートタブを廃止して印刷ボタンだけに」「議事録は削除」「成果に会議由来も統合」「財務は削除」「コードネームは使わない」(まさ 2026-06-22 同日 2nd iteration) | えいみ |
| 2026-06-19 | 6-5 Admin Payouts / 支払通知書 | 追加 | **シーズン予実表 `/admin/season-pl` を新設** (v0.29.0)。`/admin/payouts` が支払月単位なのに対し、plan cycle 単位で①収入 (請求額/入金確認) ②配分 (バッファ内訳/メンバー原資 (請求−バッファ)×65%/AMDマージン35%/閉じ検算) ③メンバー別 pt 比予実 を集約し、`閉じ検算` / `未割当pt` / `原資=Σ月cap` / `pt単価` / `役員stock収束` を全PJで検算する監査ビュー。集計は `src/lib/season-pl.ts` `computeSeasonPl` (= `reward-summary.ts` の月次集計を cycle 集約)。バッファ内訳は `value_plan_cycles.buffer_breakdown_json` (migration 148, 表示+検算専用)。未割当pt は `total_points − Σ(MS points)` で判定 (期中の earnedPt 不足は誤検知しない) | pt単価是正・役員stock繰越是正の後に「入金と配分が閉じているか」を全PJで常時見える化する安全網が必要だったため (設計正本 design/season_budget_actual.md)。実データで SX 1pt 穴 / ZMP cap-原資不整合 / KUTE 非閉じ を検出 | えいみ |
| 2026-06-26 | 8-3 L2抽出routine | 変更 | H-1 の Notion connector 認証切れ時に `npm run h1:local-notion-fallback` で Notion Desktop local cache を自動探索する運用と、直近24時間の `議事録なし` marker を再探索する recovery を追加 | 三浦工業MTGのように議事録が存在するのに connector auth と時間窓で H-1 が `none` のまま止まる事故を、手動DB修復ではなく自動抽出レーンで解消するため | えいみ |
| 2026-06-26 | 3-3 通知・つくよみ | 変更 | `/notifications` の通知を「緊急性の高い通知」と「通常通知」に分離。通常は新データ/候補/通常レビュー、緊急は再認証・契約/法務/SHA/総会/役会・重要automation blockerとして運用者向けに説明を追加 | 見落とすと事故る通知を通常レビュー候補に埋もれさせないため | えいみ-worker |
| 2026-06-26 | 3-3 通知・つくよみ | 変更 | `guardrail_match` を経営ガードレール通知として追記し、「はい」で確認済み、 「いいえ」で見送りになる運用を追加 | 設立前SHAや大企業入口設計など、見落とすと後戻りが大きい注意点を通常の候補通知と区別して扱うため | えいみ |
| 2026-06-27 | 3-3 通知・つくよみ / 2-7 タスク管理 | 変更 | task 作成ログと MTG action 作成ログを OS通知から除外。通知は読後に採否・復旧・確認・再試行などの具体アクションを取れるものだけに限定する | 読んでも何もできないログ通知が未読に積まれ、まさの注意を浪費していたため | えいみ |
| 2026-06-26 | 2-3 PJコックピット / 3-2 データと抽出 / 8-3 L2抽出routine / spec 3-3 meeting flow | 変更 | H-1 Phase P の prep worker が、会議前に Notion 側で自動生成された AI Meeting Notes page を search/fetch し、PJ固有名詞・略称・拾うべき論点を `amd-os:notion-ai-context` marker 付き block として insert-only で入れてよい仕様を追加。PWA はNotion録音開始/API作成をせず、summary / transcript / 開催後議事録本文は上書きしない。pageが見つからない/書けない場合もworker failureにはしない | Notion文字起こし後の補正ではなく、Notion側の会議前メモ欄に文脈を渡して固有名詞書き起こし精度を上げるため。AI Meeting Notes の `日付` property は空のことがあるため、title / updated time / fetched body で照合する fallback も正本化 | えいみ |
| 2026-06-27 | 8-3 L2抽出routine / spec 3-3 meeting flow | 変更 | H-1 が KUTE (`project_id=p25`) の開催済みMTG議事録 `narrative_md` を新規保存または本文更新したら、同じ H-1 run で KUTE外部共有クローズアウトworkerを起動する仕様を追加。H-1は会議ID・日付・タイトル・`source_kinds`・保存済み `narrative_md` だけを一時payloadで渡し、workerは `output/mtg_YYYYMMDD/share/` に安全版、PDF、manifest、メール下書きを作る。Gmail下書きはworkerまたはH-1本体が作成し、本送信は禁止 | まさの期待は「共有したい」と言われてからではなく、MTG後に議事録が生成されたタイミングで外部共有チェックから下書きまで自動で走ることだったため。KUTE側プレイブックだけでは実行主体に繋がらない事故を防ぐ | えいみ |
| 2026-06-27 | 4-5 Management Score | 修正 | 先手力の不可逆定義を「90未満は危機状態」に固定し、70段階・非合意の55/70/85点capを禁止。起点不明や分類不足は点数低下ではなく判定信頼度低下として扱い、分類不足時は総合点への不可逆ペナルティを発動しない仕様へ訂正 | 起点不明124/124件を55点にcapし、総合点へ×0.3を掛ける設計バグが出たため。まさ定義「先手力90未満は危機、70ではもう遅い」を正本mdに残し、見えないところで閾値が作られる事故を防ぐ | えいみ |
| 2026-06-27 | 2-6 admin ops (新節「/proactive 先手 TODO リスト」) / dashboard 上段 / 旧 /loop 全廃 | 追加・削除 | 旧 `/loop` 5段ループ盤面 (観測→評価→判断→実行→学習) と dashboard 上段の `LoopKernelBoard` 埋め込みを **全面廃止**。代わりに `/proactive` (admin、1画面・期限順・3ボタン完了UI = ✅完了 / ⏸ブロック / 🗑関係ない) と dashboard 上段の `ProactiveTodoBadge` を実装。検知は毎時 cron `/api/cron/proactive-todo-extract` が `project_meeting_summaries.next_actions` (過去14日) と 3営業日以内の予定MTGから自動投入、LLM 不使用で AMD ボール判定は文字列ヒューリスティック | まさ判断 2026-06-27: 旧 5段盤面は (a) 5段中4段が先手力と無関係のテーブルダンプ、(b) 実行段に完了UIが無く超過666hのseedが叫び続け、(c) heartbeat の受け側 (司令塔セッション) が運用上消滅した。「先手 TODO リスト 1 画面」に作り直し、MTG 起点で 80% カバーできる仮説に賭けた | えいみ |
| 2026-07-01 | 2-5 探索・知識アセット | 追加 | `/knowledge-map` を追加。OS 内の L2 / manual / spec / BZM 候補を束ねる AMD Knowledge Map として、NotebookLM へ渡す Knowledge Pack の OS 側プレビューを説明 | NotebookLM に知識コピーを入れる前に、OS 内にも同じノウハウ地図を持ち、正本への入口と raw 本文非保存境界を固定するため | えいみ |
| 2026-07-02 | 6-8 Admin / MS Overview | 変更 | `/admin/ms-overview` 編集モードの上段を **全MS 編集テーブル** に拡張。MS名 / tag / 期間 / pt / pt配分スライダー / 設計額 / メンバーのエフォート / 無効化を同じ行で扱い、メンバー別 pt配分にも設計額を併記。支払確定額ではなく `effectivePoints × plannedShare × designUnitYen` の設計額だけを出す境界を明記 | MS編集をこの画面だけで完結させ、シーズン途中編集時にメンバーへの過不足を事前に見つけやすくするため。支払確定額は reward cache / season-pl / payouts を正本に残す | えいみ |
| 2026-07-09 | 6-3 請求書発行 / 6-2 Admin Projects | 変更 | `/admin/invoices` の発行前チェックを `freee取引先 / 請求額` に限定し、報告書FIX・立替精算を請求書発行 blocker から除外。設定不足は freee 取引先候補をプルダウン検索して保存する導線に変更し、`/admin/projects` の freee 欄も同じ選択 UI に統一 | きよの実務では請求書発行を止める条件は取引先設定と金額だけでよく、月次ルーティン由来の報告書・立替 gate が残ると発行作業を誤って止めるため | えいみ |
| 2026-07-09 | 2-5 探索系アセット / 5-1 Research Assets / PoC Matching | 変更 | `/poc` を全面マトリクスから、タグ付き `PoC先候補リスト` とシーズごとの `案件化キュー` へ変更。PoC先の業界タグ・地域・規模感・状態で候補を絞り、上位候補の `案件化` から `poc_matches` を作る運用にした | 100シーズ x 500PoC先のような巨大マトリクスでは空白セルが増えすぎ、PoC開拓の実務で使いづらいため。PoC先候補リストを先に整備し、タグで各PJ/シーズに合う候補を選ぶ形へ寄せる | えいみ |
| 2026-07-10 | 2-5 探索系アセット / PoC Matching | 変更 | `/poc` の `PoC先候補リスト` をカード表示から比較表へ変更。PoC先名、タグ、規模/地域、状態、PoC相性、謝礼・履歴、案件数、担当/次アクションを横並びで比較できるようにした | 候補先は複数条件で比較して選ぶ必要があり、カード表示では候補同士の差分が読みづらかったため | えいみ |
| 2026-07-10 | 2-6 admin ops / 先手 TODO | 修正 | `/proactive` の開催済みMTG next_action TODO は、本文内に日付つき期限があればそれを優先し、読めない場合だけ前回MTG日 + 7 日を仮期限にする仕様へ変更 | 「2026-08-04 次回MTGまでに提示資料を作成する」TODO が 7/1 期限で赤化し、実期限と仮SLAが混ざっていたため | えいみ |
| 2026-07-14 | 8-3 L2抽出routine | 変更 | W-Prep は Calendar直読み時に `CFG_ColorPJHistory` を先に使い、`2025-06-01` 以降の `colorId=4` と `SolvioraX` alias を SX/p21 として扱う運用を追記。DB upcoming だけで完了扱いにせず、`SolvioraX経営会議` を unmapped skip しない | 2026-07-14 の SX経営会議 prep 起動漏れを受け、既存スプシ正本を W-Prep が必ず使う契約に固定するため | えいみ |
