# シーズ一次選別（スクリーニング帯）の設計

> **状態**：`proposal v0.4 / dual-audited(v0.1) / partially-decided`
>
> **v0.4の未監査追加**：2章9項（状態仮説駆動の補完）と2章8項の改訂（単一スコアソートの採用）は2026-08-15のまさ判断・提案による追加であり、二役監査は正本確定前に再実施する。
>
> **用語**：本文書で層に使ってきた作業名の日本語名を定める。**一次選別**（作業名Tier 0）は、情報の薄いシーズを帯でふるい分ける層。**本測定**（作業名Tier 1）は、SXとLSTで確立した事前登録つきの測定（工程骨格、資金の崖、$q$）。以下、日本語名を主にする。
>
> **作成日**：2026-08-15（v0.1に二役監査を反映=v0.2、同日まさの判断4件を反映=v0.3。残る未確定は§6の2件）
>
> **依頼**：まさ 2026-08-15。「PJ化したもの以外のシーズリストに載ってる案件をスコアリングしていこうよ。でもそれらは情報が圧倒的に不足しているから、そういうものに対してどうデータを補完するかもモデルとして大事な要素になると思う」
>
> **関連正本**：[到達見込みモデル](./sps-2-0-reachability-model.md)（5.1節：確率入力の地位）、[測定可能性ゲート](./BZM_2_0_MEASURABILITY_GATE.md)、`pwa/design/seeds.md`、`pwa/design/institution_seed_project_model.md`

---

## 1. 対象と現状（2026-08-15照会のスナップショット）

OSの`seeds`テーブルには176件のシーズがあり、`seed_projects`へ紐づかない未PJ化案件は156件である（investigating 131、candidate 24、discussing 1）。
状態別の総数で数えると investigating 132、discussing 5 となり、PJ化済みの行が混ざるため、本設計の母集団は「`seed_projects`に紐づかない行」で定義し、照会日を必ず添える。

情報密度は薄い。
131件のinvestigatingは要約文だけを持ち、TRLは0件、AMD評点は2件しか付いていない。
SPS評価（`seed_sps_assessments`）が付いているのは全体で7件である。
SXとLSTで確立した事前登録測定（工程骨格、出所タグ、資金の崖）は、この情報密度では実行できない。
情報が無いから測れない、で止めるか、測れる範囲を定義するかの分岐であり、本設計は後者を取る。

## 2. 設計原則

1. **二層の分離**。シーズ層の出力を$q$と呼ばない。事前登録測定（SX／LST級）をTier 1、シーズスクリーニングをTier 0とし、Tier 0の出力名は**スクリーニング帯**とする。Tier 0からTier 1への昇格条件（接触開始、工程骨格が文書で構成可能になった時点）を、結果を見る前に決める。
2. **点を出さず幅を出す。位置と幅の分業**。疎情報下の各軸は点推定ではなく下限と上限の帯で持ち、帯の広さが「知らなさ」の表示になる。補完値（後述）は帯の**幅**にのみ寄与させ、帯の**位置**（層別の順位）はシーズ固有のdocument由来入力だけで動かす。全案件へ同じ中立点を置くこと（撤回済み`P measurement v0.1`と同型）はしない。
3. **補完は規則で行い、出所タグを付ける**。欠測の埋め方は三種に限る。
   - `imputed_from_domain`：分野レーン（`domain_lane`）別の参照クラス。
   - `imputed_from_institution`：機関環境（ECR、公的採択実績）からの事前分布。**採用で確定**（2026-08-15 まさ「機関環境は少なからず影響する」）。条件は三つ: (i) シーズ固有文書が入った軸では機関補完を退役させる、(ii) 機関補完だけで構成された帯から「対象外」への層別を行わない（保留まで）、(iii) 帯の表示に機関補完の寄与率を併記する。ECRとSPSの境界規定は合成単一スコアの禁止であり、幅への補完利用はその適用外であることを`institution_seed_project_model.md` 2章へ同日付で明記した（同境界の2026-08-01導入はOS実装セッションによるもので、まさの確定文書ではなかったことも記録済み）。機関環境を補完源に使うため、機関環境を独立軸として重ねて数えない（同一因子の二重計上禁止。2.2影試算の教訓）。
   - `document`：シーズ固有の文書（researchmap、公的採択DB、論文、プレス）。
   その場の感覚で数値を置くことは、Tier 0でも禁止する（測定可能性ゲート問6と同じ規律）。
4. **定数は情報を運ばない**。全シーズへ同じ値が入る軸は順位に寄与させず、帯を最大幅で表示する（2.2影試算で全PJ共通の事前値0.45が$Q$の一因子になった教訓の反映）。
5. **主出力は順位ではなく、層別と次の一手**。Tier 0の主出力は、(a) スクリーニング帯による粗い層別（上位候補、保留、対象外）と、(b) 各シーズについて**期待帯縮小量**が最大の未取得情報、すなわち次に調べるべきこと、の二つである。期待帯縮小量は決定理論のVOI（情報の価値）ではない。VOIは層別判定を覆す確率と覆すことの価値の積として別に定義でき、判定を変えない帯縮小はVOIゼロである。この区別を保ち、期待帯縮小量の「期待」が補完の事前分布に依存する（補完バイアスを相続する）ことを表示に明記する。
6. **評価者入力は軽く、盲検で**。確信度が要る軸は、確信度形式の軽量版（数値アンカー付きの固定語彙、1件あたり数分以内）で取り、評価者ID、日時、様式を保存する。評価者は当面まさ1人（2026-08-15確定）。1人の間も、入力前に過去の自分の評点と旧モデルのスコアを見ない（アンカリングの防止。旧`amd_rating`は5章の通り凍結するため表示されない）。評価者を増やす段になったら、他評価者の値を見ない情報遮断と担当交差の盲検再評価を適用する。
7. **用途の限定と無作為探索枠**。スクリーニング帯は接触と調査の優先順位づけにだけ使い、投資判断、PJ化の自動判定、対外表示には使わない。ただし用途を限定しても、帯に従う接触配分は下位層の情報を永久に増やさない（探索の枯渇）。これを防ぐため、**接触キャパシティの2割（2026-08-15 まさ確定。事前登録で固定）は層別と無関係の無作為抽出で配分する**。この無作為枠は探索予算であると同時に、3章の検証で配分の交絡を除く唯一の同定手段である。
8. **保存と表示**。帯は`seed_sps_assessments`とは別の新テーブルへ保存し、外部ワークスペースDTOの許可列に含めないことをテストで強制する。一覧は単一スコア（SPS帯）によるソートを提供する（2026-08-15 まさ確定「何かしらの単一スコアでソーティングできることは必須要件」。v0.2が置いた中点ソート禁止は監査人の提案であり、まさの判断で撤回）。ソートキーは帯の下限、中点、上限のどれで並べているかを明示ラベルで選択でき、行には帯の幅を常時併記する（監査人の懸念だった「点数だけが独り歩きする」への手当てはこの二点で行う）。詳細画面は9軸すべてを帯のまま等格で表示し、軸の選抜表示をしない（2026-08-15 まさ確定「全部を主役として画面に出した方がいい」）。
9. **状態仮説駆動の補完**（2026-08-15 まさ提案。本設計の主機構）。パラメータを個別に補完せず、先にそのシーズの現在段階（例: オフサイトPoCまで完了しているだろう）を幅つきで仮置きし、段階に相関する各軸の帯を、版管理された段階→軸帯の対応表から機械的に導く。段階という一つの仮説が全軸の整合性を担保し、入力の手間を1シーズ1判断へ圧縮する。規律は三つ: (i) 段階仮説は1点でなく幅（±1段階の包絡）で置く、(ii) 対応表は全シーズ共通の雛形として凍結し、シーズごとに手で数値を置かない、(iii) 段階仮説に根拠タグ（文書引用可／推定／材料なし）を付け、根拠を引用できる場合に限り帯の位置へ寄与させ、できない場合は帯最大幅のまま残す。接触時に段階仮説の当否が即座に判明するため、段階仮説の的中記録は本体系で最速の前向き較正データになる。対応表の雛形v0.1は2026-08-15にcandidate 24件へ試適用中。

## 3. 検証設計

接触成立、協議化、PJ化はいずれもAMD自身の資源配分を経由する結果であり、配分が帯に従う以上、帯上位と下位の遷移率差だけでは判別力と自己成就を分離できない。
したがって検証は次の三本立てとする。

1. **主検証**：無作為探索枠（2章7項）の内側で、帯上位と下位の事後の遷移差を事前登録して追跡する。非無作為枠の遷移差は妥当性の証拠に使わない。
2. **副検証**：AMDの行動を経由しない事象（外部資金の採択、特許出願、他機関や他VCの接触、研究者の異動）を追跡する。
3. **偽陰性の推定**：「対象外」とした シーズの外部での事後結末（他所での事業化、調達、ライセンス）を年次で追跡する。

群間比較は追跡開始からの経過時間を揃えて行い（帯上位の早期接触による観察時間差を補正する）、分析開始の最小イベント数を事前登録する。
歴史的な基礎率（spun_off 15件）から見て、判別性能の区間は当面広い。
シーズ層はn=156で状態遷移が数か月単位で観測できるため、BZM体系の中で統計的な前向き検証が最も早く回る層であることは変わらないが、論文のデータ源にできるのは上記1と2を満たした部分に限る。

## 4. 前提インフラ（帯の凍結より前に閉じる）

1. `seeds.status`の語彙確定とCHECK制約。設計上の語彙（candidate／investigating／contacted／discussing／spun_off／declined）のうち`contacted`と`declined`は現DBに1件も存在せず、制約も無い。
2. **状態遷移履歴テーブルの新設**（旧状態、新状態、時刻、変更者）。現行スキーマは`updated_at`しか持たず、遷移時刻を観測できない。履歴が無ければ3章の遷移率は測れない。
3. 帯の保存先テーブル（2章8項）。

## 5. 実施計画

1. 前提インフラ（4章）を先に閉じる。
2. candidate 24件で入力票のパイロットを行う（工学院大の7件は評点済みで相対的に情報が厚い）。
3. 文書自動補完のパイプライン（researchmap、公的採択DB）は、**層別と無関係に全件へ走らせる**（下位層の情報枯渇の緩和）。investigating 131件はこのパイプラインが通ってから流す。
4. 旧`amd_rating`（1〜5点）を凍結する：新規入力と一次選別での利用を停止する（2026-08-15 まさ「過去のモデルのスコアは不要」）。既存データは削除せず履歴として残す。
5. 一次選別自身の事前登録を行う：層別の凍結、無作為枠の比率（2割）、最小イベント数、追跡の開始日。

## 6. 判断の記録と残る未確定事項

2026-08-15にまさが確定した判断:

1. 機関由来補完は採用する（「機関環境は少なからず影響する」）。条件は2章3項の三つ。機関環境は補完源とし、独立軸には置かない。
2. 評価者は当面まさ1人。
3. 無作為探索枠は2割。
4. 旧`amd_rating`は凍結する（新規入力と一次選別での利用を停止。データは削除しない）。「モデルはどんどんアップデートしていくから、過去のモデルのスコアは不要」。

5. 帯の対象は既存の運用SPS 9軸（σ_SU、P、TRL、BRL、GRL、SRL、HRL、FRL、R_net）そのものとし、新しい軸は作らない。詳細画面は9軸を全部等格で表示し、一覧は単一スコアソートを提供する（2026-08-15確定。2章8項）。
6. 補完の主機構は状態仮説駆動とする（2026-08-15 まさ提案。2章9項）。

（2026-08-15 同日の経緯記録）この位置に一度「運用SPS（9軸）は2.xで廃止されていない」という説明を書いたが、これは当時の正本の字面（2.1／2.2の「別に残す」記述）に依拠した誤りだった。まさの確定により、9軸診断指数は旧版として退役し、SPSの現行版は$\sum_o q_oP_o$である。2.1／2.2の該当記述も同日付で訂正済み。まさの「2.xで廃止になったと思ってた」が正しかった。

7. 段階の刻みは6段（構想・原理提示／ラボ実証／オフサイトPoC／オンサイトPoC／有償実証／事業化準備）で確定（2026-08-15 まさ「6段階の刻みはあってる」）。
9. **スコア集約式の訂正**（2026-08-15 同日）：帯を通す集約式は9軸Cobb-Douglas（旧版）ではなく、**現行SPS＝$\sum_o q_{o}P_{o}$（経路確率×経路価値の円建て帯）**とする（まさ確定「それ以外のSPSの式は古いバージョンで抹消されるべき情報」。§6の5に先立つ「帯の対象は9軸」の記載は、9軸を旧版と認識する前の確定であり本項が上書きする）。TRL等の指標は段階仮説と状態の記述子として入力側に残すが、スコアの集約に使わない。段階→(q帯, P帯)の対応表をv0.3で作り、24件の円建て帯を再計算する。台帳の既存スコア帯は旧式による暫定値と注記する。
8. 一覧のスコア表示は、9軸帯をSPS式へ通した**スコアの最小値と最大値**を必ず出す（2026-08-15 まさ「帯っていうからには、スコアがいくつからいくつ、っていう最小と最大の値が出てないとダメ」。式が単調のため全軸下端＝厳密な最小、全軸上端＝厳密な最大）。

10. **q帯の評価者はえいみ**（2026-08-15 同日）。段階ごとのデフォルトq案（えいみ）はまさが棄却（「シーズによってめちゃくちゃ変わる」）。続いてまさが評価者を変更（「その評価はおれよりもえいみがやった方が正確。見るべきポイントを先におれと合意しておけばできる」）。見るべきポイントの合意物が[SEED_Q_RUBRIC_2026-08-15.md](./SEED_Q_RUBRIC_2026-08-15.md)（v1凍結）。したがって2章9項の状態仮説駆動の補完は、qには適用せず（ルーブリック評価者入力）、P・状態記述子側の機構として残る。まさの較正経路は抜き打ち独立評価（様子を見ながら）。

（実施記録 2026-08-15）ルーブリックv1によるcandidate 24件のq帯評価を同日実施・凍結（[SEED_Q_EVAL_LEDGER_2026-08-15.md](./SEED_Q_EVAL_LEDGER_2026-08-15.md)。評価者えいみ、盲検、11要因の根拠引用つき）。P帯は円建て類型ルックアップ（対応表v0.3 §4.2、アンカー出典つき叩き台）で構成し、円建てSPS帯一覧v0.3を[パイロット台帳](./SEED_SCREENING_PILOT_LEDGER_2026-08-15.md)末尾に追加した。

残る未確定（議論して決める）:

1. 一次選別から本測定へ移す条件（接触が始まり、工程骨格を文書で組める密度に達した時点、が現在の案）。
2. P帯ルックアップ表の値のまさレビュー（0〜9帯=雛形§4.1と、**円建て帯=雛形§4.2の叩き台**の両方）。

## 7. 変更規律と反証条件

本設計の変更は旧版を残して改訂を追加し、[`9-5-appendix-changelog.md`](./9-5-appendix-changelog.md)へ日時つきで追記する。
次のどれかが観測されたら、該当箇所を改訂または棄却する。

1. 盲検入力で自レーンへの系統的な上方バイアスが検出される（担当交差評価へ改訂）。
2. 文書パイプラインの走行後も下位層の帯だけが縮まない（無作為枠の比率を引き上げる）。
3. 無作為枠内で、帯上位と下位のAMD非経由アウトカム差が、事前登録した最小イベント数の到達時点で出ない（補完主導の帯を棄却する）。
4. 補完寄与率と層別順位の順位相関が、文書の増加後も高止まりする（層別が機関序列の写像になっている証拠として、補完規則を棄却する）。
5. 層別と期待帯縮小量が接触判断の記録に引用されない状態が続く（評価装置が正当性の演出として消費されている証拠として、既存評価レイヤーとの統廃合を検討する）。
6. 1件あたりの入力負担が数分を超え、回答拒否が増える（軽量様式を再設計する）。

## 8. 監査記録（2026-08-15）

v0.1に対し、経済学者役と経営学者役の独立監査を実施した（AI模擬監査。実在専門家の署名ではない）。
両役が一致して差し戻した点は、(a) 遷移率検証の自己成就（接触配分が帯に従う限り判別力と分離できない）、(b) 機関由来補完の二重計上経路とECR境界規定との衝突、の二つで、それぞれ2章7項の無作為枠と3章、2章2項・3項の位置と幅の分業へ反映した。
経済学側は追加で、VOIの用語誤用（期待帯縮小量への改名で反映）、状態遷移履歴の不在（4章へ反映）、曝露時間の差（3章へ反映）を指摘した。
経営学側は追加で、探索の枯渇はMarch（1991）の活用過剰適応として用途限定だけでは防げないこと（無作為枠で反映）、既存評価レイヤーの低利用率（`amd_rating` 2件、SPS 7件）から新レイヤーも使われない可能性（7章5項の反証条件で監視）、帯中点ソートの禁止（2章8項で反映）を指摘した。
機関由来補完の採否は両役とも設計側で決めきれない判断として保留し、2026-08-15にまさが採用で確定した（6章）。
