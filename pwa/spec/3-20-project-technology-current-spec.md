# 3-20 PJ技術台帳 (技術タブ) — current spec

> 正本。PJコックピットの「技術」タブと `project_tech_*` 2テーブルの現行仕様。
> 使い方は [`manual/2-3-pj-cockpit.md`](/manual/2-3-pj-cockpit) の「技術タブ」節。
> 追加: 2026-08-29 / migration `scripts/migrations/339_project_tech_ledger.sql`、初期データ `340_seed_project_tech_sx_cx.sql`

## 1. 目的とスコープ

技術に関する事実の置き場所。まさの依頼 (2026-08-29):

> SXだったら、シアノが何度から何度の温度帯で使えるのか、どの元素は取り込めるのか、pHはいくつからいくつまで使えるのか。
> CXだったら、磁気冷凍と気体冷凍の違いの説明とか、現状何度まで冷やせるのかとか、kiutraとかの競合との差が分かるような星取り表。

作る前の状態はこうだった。技術の事実は `project_knowledge` (category = `tech` / `term` / `competitor`) に
自由文の断片として毎朝貯まっていたが、**PJコックピットからは1件も読めず**、
ナレッジマップと通知にしか出ていなかった。数値の比較にも使えない。ここを構造化して置き場所にする。

`project_management_technical_tests` (週次管制のマイルストーン判定に使う試験) とは目的が違う。
あちらは「この試験をやって目標を満たしたか」、こちらは「この技術は今どういう性質を持つか」。
同じ実測値を二重の正本にしないため、試験の結果を技術タブへ載せるときは
`source_kind='measurement'` + `source_ref` に試験名を書き、値の正本は試験側に残す。

### 1.1 技術タブに置かないもの (まさ確定 2026-09-04)

**訪問日程・次の約束・相手ごとの調整状況は技術ではない。** 進捗管理の関係先
(`project_management_partners`) が正本で、技術タブには置かない。

> 「日程とかは技術と無関係なので技術タブには入れないで、進捗タブ側に入れて」

排液を出す会社そのものは技術タブに載ってよい。ただし載せるのは**性状**
(成分・pH・COD・どの処理工程の液か・評価方法の制約) だけで、いつ訪問するか・誰が何を約束したかは関係先へ書く。
2026-09-04 に、技術タブへ入れてしまった「調整中の入手先」1行 (`pte_sx_ef06`) を削除して関係先へ移した。
同じ相手が両方に出ることは問題ない。**同じ事実を2か所の正本にしないことだけを守る。**

## 2. PJごとの違いをテーブルではなくデータで吸収する

**PJごとに必要なフォーマットは違うが、形は4種類しかない。** これが設計の中心。

| block_kind | 表示 | 何を置くか | SXの例 | CXの例 |
|---|---|---|---|---|
| `condition` | 成立条件の表 | 使える範囲。項目 × 下限/上限/単位/条件/確度/出典 | 水温・pH・光量・滞留時間 | REBCO細線の磁場性能・臨界温度 |
| `article` | 解説 (Markdown) | 原理・用語の説明。`topics.body_md` が本体 | 高熱性シアノで排水を処理するとは | 磁気冷凍と希釈冷凍機の違い |
| `matrix` | 星取り表 | 比較軸 × 相手。◎○△× + 実数値 + 根拠 | 既存の物理化学処理・生物処理との比較 | kiutra / LEMON / Bluefors との比較 |
| `record` | 到達実績の表 | 今どこまで行っているか。同じ項目を並べると推移 | 47L培養装置・TRL・排水サンプル | TESエッチング内製化・TRL |

PJごとに変わるのは「並べるトピックと項目名」だけで、テーブルもコンポーネントも共通。

**PJの顔になるまとめトピックは `condition` で作り、`sort_order` を一桁にして最上段へ置く。**
技術区分の並びは区分内の最小 `sort_order` で決まるので、専用の `tech_domain` (SXは `性能まとめ`) を与えると
そのPJで最初に読ませたい1枚が必ず先頭に来る。行頭を `【生きられる環境】` のように括ると、
1つの表のまま分類が読める (`condition` は行のグループ表示を持たないため)。
まとめは**再掲**であり、値の正本は各トピックとコスト試算タブに残す。行ごとに元の `pte_*` を `source_ref` に書いて往復できるようにする。
**PJ専用のコンポーネントを作らないこと。** p25 専用の規程・内規タブと同じ形にすると、PJが増えるたびに実装が増える。

## 3. データモデル

列の正本は [`design/db_schema.md`](../design/db_schema.md)。ここは役割だけ書く。

| table | PK | 役割 |
|---|---|---|
| `project_tech_topics` | `tech_topic_id` (`ptt_*`) | トピック1件。形式・タイトル・1行説明・本文・技術区分・並び順・社外開示可否・出典・星取り表の見せ方 (`presentation`、§5.4) |
| `project_tech_entries` | `tech_entry_id` (`pte_*`) | トピックの中身1行。成立条件の1項目、星取り表の1マス、到達実績の1測定を同じ形で持つ |

`project_tech_entries` の使い分け:

| block_kind | row_label | col_label | 値 |
|---|---|---|---|
| `condition` | 項目名 (水温) | null | `value_min` / `value_max` / `unit`、列挙は `value_text` |
| `matrix` | 比較軸 (到達温度) | 比較相手 (kiutra) | `rating` (◎○△×) + `value_text` / 数値 |
| `record` | 測る対象 | null | 値 + `observed_on` |

- `tech_domain` はトピックを束ねる見出し。PJごとに自由な語彙を使う (培養 / 排水処理 / 磁気冷凍)。
  **区分の並びは五十音ではなく、その区分に入るトピックの `sort_order` の最小値順**。読ませたい順をデータで決めるため。
- `confidentiality` は `public` / `internal` / `confidential`。SXは株・培養条件・リアクター内部条件・光・二酸化炭素・pH・温度・滞留時間を
  初回面談では出さない方針 (2026-06-26 三浦工業MTG) なので、培養条件のトピックは `confidential`。
- **出典 (`source_kind` + `source_ref` + `source_url`) は全行に持たせる**。根拠のない数値は後から見た本人に消される。
  `source_kind` は `manual` / `l2_extraction` / `meeting` / `literature` / `vendor_spec` / `measurement` / `estimate`。
- `confidence` は `high` / `medium` / `low` / `unverified`。**空欄で放置せず「未測定」+ `unverified` を入れる**。
  何を調べていないかが表に出ることが目的で、空白のままだと調べたのか調べていないのかが読めない。
- 星取り表の列順は `entries` の登場順 (`sort_order` → `row_label`)。自社を左に置きたいので並べ替えない。
  **`sort_order` は行×列で一意に振る** (行10番台・20番台…、列は下1桁)。同値だと列順が不定になる。

RLS は3ポリシー規約どおり (`_member_read` = `amd_os_is_member()` / `_admin_all` = `is_admin()` / `_service_role`)。

## 3.4 食い違いは消さず、両方残して要確認にする

まさ確定 (2026-08-29):「そういう食い違いも、両方の情報とも記しておいたうえで、要確認って書いておいてほしい」

資料によって値が違うとき、**新しいほうを採って古いほうを消さない**。両方を出典つきで別の行として残し、
`needs_check` を立てて `check_reason` に「何を、誰に、いつ確かめるか」を書く。

`project_tech_entries.needs_check` / `check_reason` (migration 343) と、
`project_tech_topics` の同名の列。要確認にするのは次の3つ。

| 立てる場面 | check_reason に書くこと |
|---|---|
| 資料間で値が食い違う | 両方の値と出典、どちらを採るかで何が変わるか、いつ決めるか |
| 実測が無い (置き値・見込み) | 何を測れば確定するか |
| 訴求と外部の見方がずれている | 誰がどう見ているか (例: 自社は無振動を優位性に置くが、産総研G-QuATは振動を課題と見ていない) |

画面では、要確認の行は薄い琥珀の背景と行頭の `⚠`、値の下に赤字で理由が出る。
トピック見出しと画面上部に件数バッジが出るので、本文を読まずに「何件が未確定か」が分かる。
`confidence = 'unverified'` の行は migration 344 で機械的に `needs_check` を立てた。

## 3.5 値をどこから取るか (これを間違えた)

**`project_knowledge` だけを見て埋めない。** 初版 (migration 340) はこれをやって、
SXの培養条件を9項目すべて「未測定」、処理単価を2,500〜3,000円/m³ (2026-03-30の古い事業計画値) として入れた。
まさの指摘 (2026-08-29) —「これまでのMTGで何度も杉浦先生言ってるよ。だからnotionの文字起こしを見てと言ったの」
「SXの処理単価、めちゃくちゃ高くない? こないだちこちゃんが作ったコスト試算表見てないの?」

実測値は生データ側にある。技術タブを埋めるときは次の順で当たる。

| 見る順 | 場所 | 何が取れるか |
|---|---|---|
| 1 | `source_cache` の `gmeet_minutes` | MTGの文字起こし全文。研究者本人が数値を言っている一次情報。最長で3万字を超える |
| 2 | `source_cache` の `drive` | 試算表・報告書・提案書のテキスト。数値の一覧が表で入っている |
| 3 | `project_cost_*` (コスト試算タブ) | 単価・原価・前提。**金額は必ずここを正本にする** |
| 4 | `project_meeting_summaries.narrative_md` | 要約。数値は落ちていることが多い |
| 5 | `project_knowledge` | 断片。入口としては使えるが、これだけで表を埋めない |

`source_cache` は本文検索が効く。例:

```sql
select s.item_date::date, left(s.title,40), m[1]
from source_cache s, lateral regexp_matches(s.content_text, '(.{0,200}℃.{0,200})', 'g') m
where s.project_id = 'p21' and s.source in ('gmeet_minutes','drive');
```

**金額と単価はコスト試算タブの前提 (`project_cost_assumptions`) と食い違わせない。**
古いMTGの数字を技術タブへ書くと、同じPJの2つのタブが違う単価を主張することになる。
時系列で単価が動いた経緯を残したい場合は、`record` のトピックで「いつ何を根拠に動いたか」として並べる (SXの実例)。

## 3.6 単位の書き方

単位は記号で書く。**「立方メートル」と綴らない** (まさ確定 2026-08-29)。

| 使う | 使わない |
|---|---|
| `m³` `円/m³` `m³/h` | 立方メートル、立方m、m3 |
| `℃` `K` `mK` | 摂氏、度C |
| `mg/g-DCW` `g-DCW/L` | ミリグラム毎グラム |

上付きの3は `³` (U+00B3) をそのまま入れる。

## 4. API — `/api/project-tech`

`runtime = "nodejs"`。

| method | 認可 | 動作 |
|---|---|---|
| `GET ?projectId=` | `requireAuth()` | `{ ok, canEdit, topics, entries, fragments }`。3クエリを並列で1往復。`Cache-Control: private, max-age=60, stale-while-revalidate=300` |
| `POST { entity, row }` | `requireAdmin()` + `createAdminClient()` | PKを `<prefix>_<uuid12>` で自動採番。`project_id` 必須、topic は `title`、entry は `tech_topic_id` と `row_label` が必須 |
| `PATCH { entity, id, patch }` | 同上 | PKはpatchから除去。`updated_at` / `updated_by` を更新 |
| `DELETE ?entity=&id=` | 同上 | 物理削除。トピックを消すと中身も cascade で消える |

`entity` は `topic` / `entry`。

`fragments` は `project_knowledge` の `tech` / `term` / `competitor` を更新順に最大300件。
技術タブの「未整理の断片」タブ (「まだ整理していない技術の断片」) に出し、そこから区分のトピックへ写して構造化する導線にする。

## 5. UI

- 実体: [`src/components/cockpit/CockpitTechnology.tsx`](../src/components/cockpit/CockpitTechnology.tsx) / 型とラベルは [`src/lib/project-tech.ts`](../src/lib/project-tech.ts)
- タブ配線: `CockpitView.tsx` の `CockpitTab` union と `tabs` 配列 (= 正本)、URL同期は `cockpit/page.tsx` の `NON_DEFAULT_TABS`。URLは `?tab=technology`
- 構成: 説明帯 (形式別の件数 + adminの「＋トピック追加」) の下端に**区分のタブ** (全体像 / 技術区分ごと / 未整理の断片)。区分のタブの中はトピックを1つずつ開く ((xl以上) 左にその区分のトピック一覧 ／ (xl未満) 上に横並び、下に前後のトピック)。開いている表示は `?tech=`。詳細は §5.1
- **参照系**。クライアントは [`src/lib/project-tech-client.ts`](../src/lib/project-tech-client.ts) のキャッシュ層だけを通し、画面から素の fetch をしない
  (guard: `scripts/check_reference_data_cache_contract.mjs` の `REFERENCE_DATA_ENDPOINTS` に登録済み)。
  タブ見出しの hover で `prefetchProjectTech()` を呼び、押した瞬間に表が出ている状態にする
- 値の表示は `formatTechValue()` が唯一の実装。`min` と `max` が両方あれば範囲、片方だけなら「以上」「以下」、
  **単一値は `min` = `max` を入れる** (片側だけだと「10T 以上」と誤って読める)
- 編集は `canEdit` (= `members.is_admin`) のときだけ。行の編集ボタンは既定で畳み、「行を編集 (N)」を押したときだけ出す
  (星取り表は24マスになるので、常時出すと表より場所を取る)
- 2026-09-01以降、技術は上段の`事業計画`グループに属する子タブ。子タブ列は`overflow-x-auto`と`whitespace-nowrap`で、狭幅でもラベルを途中で折らない。
  旧フラット列で技術を足して15枚になった時点の折返し問題は、二階層化により解消した
- 2026-09-14以降、区分「競合比較」のトピックは技術タブに出さず、事業計画グループの「競合比較」タブ (`?tab=competition`) に出す。§5.3
- 同日から、区分「ビジネスモデル」のトピックも技術タブに出さず、事業計画グループの「ビジネスモデル」タブ (`?tab=business-model`) に出す。§5.5
- 2026-09-17以降、本文のコードブロックの言語が `pictogram` のものは、ビジネスモデルのピクト図として描く (`PictogramDiagram`)。§5.6

### 5.1 全体像と区分のタブ (2026-09-14)

まさの依頼 (2026-09-14):

1. 「技術タブのコンテンツが増えてきて、何がどこにあるのか分からなくなってきてるから、全体感も見えるようにしてほしいし、それぞれのコンテンツへのアクセスもしやすくしてほしい」。SXのトピックが20件・行が200を超えた時点で起きた。最初は全体像と目次を置き、押すとその位置へスクロールする形にした (commit `239e42c3`)。
2. 同日「それぞれの項目をクリックするとただそこにスクロールしていくだけになってるけど、タブ分けした方が見やすいよ」。区分をタブにし、トピックを1つずつ開く形へ変えた。目次・現在地バー・位置へのスクロール移動・区分の絞り込みチップはこのとき廃止した。

| 部品 | 置き場所 | 中身 |
|---|---|---|
| 区分のタブ (`data-testid="tech-domain-tabs"`) | 説明帯の下端 | 全体像 → 技術区分 (トピックの sort_order 順、名前の横にトピック数) → 未整理の断片 (件数)。狭い画面は横にスクロールし、選んだタブが見える位置まで横にだけ寄せる |
| 全体像 (`data-testid="tech-overview"`) | 全体像のタブ | 区分ごとのカード。各トピックの形式・タイトル・行数・要確認件数。区分の名前を押すとその区分のタブ、タイトルを押すとそのトピックが開く。未整理の断片へのカードもここ |
| トピック一覧 (`data-testid="tech-topic-list"`) | xl (1280px) 以上、区分のタブの左の列に固定 | 開いている区分のトピックを番号つきで並べ (「 — 」より後ろの補足は落とす)、開いているものを `aria-current="page"` で光らせる |
| トピックの並び (`data-testid="tech-topic-chips"`) | xl 未満、トピックの上 | 同じ一覧を横並びの丸いボタンで出す。スマホは横にスクロール、sm 以上は折り返す。トピックが1件だけの区分では出さない |
| 前後のトピック (`data-testid="tech-topic-pager"`) | トピックの下 | 「← 前のトピック」「次のトピック →」。区分の境目では「次の区分「排水処理」」のように区分をまたぐ |

決めごと:

- **1回に開くのは1トピック。** 選んでいないトピックは描画しない (mermaid 図は開いたときに描く)。全区分・全トピックを1枚で見るのは全体像のタブ。
- 開いている表示は URL の `?tech=` に持つ。トピックは `tech_topic_id`、未整理の断片は `fragments`、全体像は付けない。書き戻しは `history.replaceState` (`router.replace` だと押すたびにサーバへ取りに行く。履歴も積まない。Next.js が `useSearchParams` に反映する)。再読み込み・共有したリンク・ほかのコックピットタブから戻ったときに同じトピックが開く (コックピットのタブ切り替えは `?tech=` を残す)。存在しないトピックを指していたら全体像を出す。
- 区分のタブを押すと、その区分で最後に開いていたトピック、無ければ先頭のトピックが開く (トピックから離れるときに覚える。画面を開き直すと忘れる)。
- 表示を差し替えたとき、差し替えた場所の頭が画面より上に隠れていれば、区分のタブが見える位置まで一度に戻す (`scroll-margin-top` 64px、滑らせない)。見えているときは動かさない。**位置へスクロールして移動する形には戻さない。**
- ＋トピック追加の直後は、足したトピックを開く (追加 API は id を返さないので、読み直した中から題名と区分が同じ最新のもの)。
- effect の中で state を同期的に変えない (`react-hooks/set-state-in-effect`)。URL の外からの変化は前回値を state に持って描画中に合わせる。
- 画面設計規約 (2-7) の「タブやfilterで一方を隠さず、同じ画面へ同時表示する」は、意味の異なる運用群を並べる一覧についての決まり。技術タブは全体像のタブで全区分・全トピックを1枚に並べたうえで、読むときはタブで1つずつ開く (まさ判断 2026-09-14)。
- スマホの主要操作は44px以上 (区分のタブ・トピックの並び・全体像の行・前後のトピック)。
- 未整理の断片の区分の選択と語の入力欄は折り返す (幅375px未満のスマホで右へはみ出していた)。
- 星取り表で `rating` が null のセルは記号を出さず、値だけを左寄せで出す (条文の要件を並べる表など)。「調べていない」は `rating = 'unknown'` を明示して入れる。null を「?」扱いしない。
- 星取り表の最小幅は「比較軸の列 128px ＋ 相手の列の数 × 96px」(最小 520px、`MATRIX_AXIS_COL_PX` / `MATRIX_COL_PX`)。それより狭い枠では表の中で横にスクロールする (比較軸の列は固定のまま)。2026-09-14 に SX の競合比較 (相手8列) を入れたとき、固定の最小 520px のままだとスマホ幅で1列が 45〜77px に押しつぶされ、1行が最大 437px まで伸びた。1440px 幅では 8 列でも枠に収まる。

### 5.2 コスト試算（燃料）は技術タブに置かない (2026-09-14)

- 2026-09-14 に「OSの技術ページに、新たに『コスト試算（燃料）』を追加してほしい」で技術タブの中のタブとして置いたが、同日にまさ「事業計画グループ内に置いてほしかった。元々ある『コスト試算』は『コスト試算（廃液）』に変えて、それの右に並べて」で、事業計画グループのタブ（`?tab=cost-fuel`）へ移した。仕様の正本は [5-16](5-16-project-fuel-cost-model-current-spec.md)
- 技術タブには、燃料のタブ・全体像のカード・`?tech=cost-fuel` を置かない。`?tech=cost-fuel` の古いリンクは、存在しない表示として全体像を出す
- 燃料の試算の数字 (売価・原価・収率など) は技術台帳のトピックに書き写さない。金額は試算が正本 (§3.5)

### 5.3 競合比較は事業計画グループのタブ (2026-09-14)

まさ (2026-09-14)「この競合比較は、技術タブの中じゃなくて事業計画グループの直下に置いてほしい」。同日に 414 で技術タブの区分として置いたものを移した。

- タブ: `cockpit-tabs.ts` の `competition`。事業計画グループの並びは スコア詳細 → 技術 → **競合比較** → ビジネスモデル (§5.5) → 事業計画 → コスト試算（廃液）→ コスト試算（燃料）→ 知財 → 資本政策。URL は `?tab=competition`
- **データは技術台帳のまま。** 区分 (`tech_domain`) が `競合比較` (`COMPETITION_TECH_DOMAIN`) のトピックだけを出す。別テーブル・別 API は作らない。判定は `isCompetitionTopic()` (`project-tech.ts`) が唯一の実装。どのタブに出すかは `techLedgerTabOf()` にまとめ、画面は `techLedgerTabOf(t) === mode` で絞る (§5.5)
- 画面は `CockpitTechnology` を `mode="competition"` で使う。区分のタブ・全体像・未整理の断片は出さず、開くと先頭のトピック (社外に出す星取り表) が開く。トピックの一覧・並び・前後のトピックは技術タブと同じ。開いているトピックは `?competition=` に持つ (技術タブの `?tech=` と分け、タブを行き来しても互いの開いている表示を壊さない)。「＋ トピック追加」の区分の初期値は `競合比較`
- 技術タブ (`mode="technology"`) は区分「競合比較」「ビジネスモデル」のトピックと行を外して数える (形式別の件数・要確認・全体像・区分のタブ)
- **表示条件**: 区分「競合比較」のトピックを持つPJだけ。`CockpitView` が参照系のキャッシュ越しに技術台帳を読んで決める (`loadProjectTech` / `peekProjectTech`。技術タブと同じ束なので、ここで読むと技術タブも待たずに開く)。研究機関PJは読まない。読み込み中に `?tab=competition` で開いたときは、同じグループの先頭へ落とさずに待つ。見出しの hover で `prefetchProjectTech()`
- PJワークスペース: `事業計画` の技術の右隣に「競合比較」(`#competition`)。コックピットと異なり、未登録でも空状態の入口を残す。当該PJのワークスペースメンバーへ読み取り表示する
- 回帰防止: `check_pwa_critical_ui.cjs` (事業計画グループの並び、`techLedgerTabOf(t) === mode`、`mode="competition"`、表示条件、ワークスペースの並び) と、`check_project_fuel_cost_model.mts` の事業計画グループの並び


### 5.4 社外に出す形の星取り表 (`presentation`、migration 425 / 2026-09-14)

まさ (2026-09-14、VC 提出用の PDF を見て):

> PDFの比較表めっちゃよく出来てるから、この３つそのままOSにも入れておいてほしい。

「この３つ」は PDF の比較表 (競合の会社との比較・既存の方式との比較・燃料の比較)。3枚の中身はもともと技術台帳にあり、違っていたのは見せ方 (表の上の一文、自社の列の色、強調する行、表の下の注記) だったので、見せ方を DB に持たせて画面と PDF で同じものを使う形にした。

- **列**: `project_tech_topics.presentation` (jsonb、null 可、`jsonb_typeof = 'object'` の制約 `project_tech_topics_presentation_object`)。中身は `heading` (見出し。PDF のページの題、番号は付けない) / `eyecatch` (表の上に太字で出す一文) / `lead` (その下の説明) / `note` (表の下の注記。PDF のページ番号の参照は書かない) / `self_col` (自社として色を付ける列の名前) / `highlight_rows` (強調する行の名前の配列)
- **読み方**: 画面では必ず `readTechPresentation()` (`project-tech.ts`) を通す。文字でない値・空の値は捨て、何も残らなければ null (= 通常の星取り表)。列の名前・行の名前が表に無ければ色を付けない
- **画面**: `block_kind = 'matrix'` で presentation があるトピックは、`TopicCard` が通常の `MatrixBlock` の代わりに `MatrixSheet` (`data-testid="tech-matrix-sheet"`) を出す。並びは PDF と同じ 見出し → 一文 → 説明 → 表 → 注記 で、本文 (比べる相手・記号の付け方・時点) は表の下に「表の補足」として回す。presentation の無いトピック (CX の星取り表、SX の閉鎖系の表、社内限定のページ) の見た目は変えない
- **色**: 画面は OS の色の決まり (spec 2-7) に合わせる。自社の列の見出しは sky (`#027FDC`) に白字、列の中は薄い sky、強調する行は `#e8f3fc` と左の sky の線。記号は ◎ `#0267b2` / ○ `#027FDC` / △ `#d97706` / × `#b71c1c` / —と? は灰。**PJ のブランドの色 (SolvioraX の紺・水色・オレンジ) は画面に持ち込まない**。PDF だけが SolvioraX の事業概要に合わせた色を使う
- 表の幅: PDF と同じく相手の列を一度に見せるため、通常の星取り表より細い「比較軸の列 104px ＋ 相手の列の数 × 68px」(最小 520px、`SHEET_AXIS_COL_PX` / `SHEET_COL_PX`)。トピック一覧が左にある xl 以上なら、窓の幅 1300px 前後でも 9列 (自社＋8社) が横スクロールなしに収まり、広い画面では列が等分に広がる。列の幅はそろえ (`table-fixed`)、**表の中で縦にスクロールさせない** (全行を1枚で見せる。v3.130.0 は通常の星取り表と同じ 128px ＋ 96px と表の中の縦スクロールで出し、本番で右の2列が隠れ、表の中だけが縦にスクロールしたので v3.130.1 で直した。v3.130.1 の 112px ＋ 84px は窓の幅 1309px でまだ右が隠れたので、v3.130.2 で 104px ＋ 68px にした)。狭い画面だけ表の中で横にスクロールし、比較軸の列は左に固定する
- **編集**: admin のトピック編集フォームで、形式が星取り表のときだけ「社外に出す資料と同じ形で見せる (任意)」の欄 (`data-testid="tech-sheet-fields"`) が出る (見出し・自社として色を付ける列・表の上の一文・一文の下の説明・表の下の注記・強調する行を1行に1つ)。全部空で保存すると null に戻る。星取り表以外の形式では presentation を送らない
- **PDF**: VC 提出用の PDF の星取り表のページは、見出し・一文・説明・注記・自社の列・強調する行をこの列から読む (scratchpad の生成スクリプト。ページの番号と「記号の付け方は NN ページ。」は PDF を作るときに足す)。presentation の無い星取り表を PDF のページにしようとすると止まる。425 を当てたあとの PDF の HTML は、当てる前 (pages.json に文を持っていた版) と画像以外が1文字も違わないことを確かめた
- SOL の3枚の中身: 競合の会社との比較 (強調する行「生きた細胞が金属を取り込む」)、既存の方式との比較 (同)、燃料の比較 (強調する行なし)。自社の列はどれも SolvioraX
- 回帰防止: `check_pwa_critical_ui.cjs` が `readTechPresentation(`、`data-testid="tech-matrix-sheet"`、`data-testid="tech-sheet-fields"`、`TopicCard` の presentation の読み方と `<MatrixSheet …>` を検査する

### 5.5 ビジネスモデルは事業計画グループのタブ (2026-09-14)

まさ (2026-09-14、オフサイト廃液処理の検証を Drive の資料と画面で受け取ったあと):

> そもそも本来はOSに置くべき資料だと思う。とはいえ、ちょうどいいページが見つからない。例えば事業計画グループの中に「ビジネスモデル」っていうタブを新たに追加して、その中に入れておくのはどう？

誰に何を売り、どこで稼ぐかと、その事業の形が成り立つかの検証を置くタブ。競合比較 (§5.3) と同じく、技術台帳の区分で出し分ける。

- タブ: `cockpit-tabs.ts` の `business-model`。事業計画グループの並びは スコア詳細 → 技術 → 競合比較 → **ビジネスモデル** → 事業計画 → コスト試算（廃液）→ コスト試算（燃料）→ 知財 → 資本政策。URL は `?tab=business-model`
- **データは技術台帳のまま。** 区分 (`tech_domain`) が `ビジネスモデル` (`BUSINESS_MODEL_TECH_DOMAIN`) のトピックだけを出す。判定は `isBusinessModelTopic()`。どのタブに出すかは `techLedgerTabOf()` (`technology` / `competition` / `business-model`) が唯一の実装で、`CockpitTechnology` は `techLedgerTabOf(t) === mode` で絞る (同じトピックを2つのタブに出さない)
- 画面は `CockpitTechnology` を `mode="business-model"` で使う (`data-testid="cockpit-business-model-tab"`)。競合比較と同じく、区分のタブ・全体像・未整理の断片は出さず、開くと先頭のトピックを開く。開いているトピックは `?business-model=` (`BUSINESS_MODEL_VIEW_PARAM`)。「＋ トピック追加」の区分の初期値は `ビジネスモデル`。説明帯は「誰に何を売り、どこで稼ぐかと、その事業の形が成り立つかを検証した結果を置く場所。原価と売価の数字はコスト試算のタブが正本で、ここには検証した時点の数字と出典を書く」
- **表示条件**: 区分「ビジネスモデル」のトピックを持つPJだけ。競合比較と同じ読み方で、`CockpitView` とワークスペースは `ledgerTabsPresent()` (`{ competition, businessModel }`) で両方の有無を1回の読み込みで決める。研究機関PJは読まない。読み込み中に `?tab=business-model` で開いたときは待つ。見出しの hover で `prefetchProjectTech()`
- PJワークスペース: `事業計画` の競合比較の右隣に「ビジネスモデル」(`#business-model`)。コックピットと異なり、未登録でも空状態の入口を残す。当該PJのワークスペースメンバーへ読み取り表示する
- **金額は書き写さない (§3.5)。** コスト試算の総コスト・利益率を本文に載せるときは時点を書き、正本がコスト試算であることを添える
- 回帰防止: `check_pwa_critical_ui.cjs` (事業計画グループの並び、`techLedgerTabOf(t) === mode`、`BUSINESS_MODEL_VIEW_PARAM`、`isBusinessModelTopic` / `techLedgerTabOf` / `ledgerTabsPresent` の export、`mode="business-model"`、表示条件と hover の先読み、ワークスペースの並びと `#business-model`) と、`check_project_fuel_cost_model.mts` の事業計画グループの並び

### 5.6 ビジネスモデルのピクト図 (本文の `pictogram` ブロック、2026-09-17)

まさ (2026-09-17、429 の mermaid の図を見て):

> ビジネスモデルは、ちゃんとピクト図にしてほしい。カネとモノの流れを示す矢印はそれぞれ別の色にして、カネのところはお金っぽいアイコンを添えて。

「ビジネスモデル・ピクト図解」の形 (3×3 の枠にヒトを置き、モノとお金の流れを矢印で結ぶ) で、事業全体のお金とモノの流れを描く。

- 本文 (Markdown) のコードブロックの言語を `pictogram` にして JSON の定義を書くと、`MarkdownView` が [`PictogramDiagram`](../src/components/cockpit/PictogramDiagram.tsx) で図にする (`mermaid` と同じく `<pre>` の枠を外す。判定は `containsDiagram()`)。**技術台帳の形式 (condition / article / matrix / record) は増やさない。** 解説の本文の中に置き、本文の編集欄からそのまま直せる
- 定義 (検査は [`src/lib/pictogram.ts`](../src/lib/pictogram.ts) の `parsePictogram()`):

  ```json
  {
    "nodes": [{ "id": "sol", "cell": "c", "icon": "self", "label": "SolvioraX", "note": "…", "planned": false }],
    "flows": [{ "from": "sol", "to": "plant", "kind": "goods", "label": "装置・菌体・保守", "planned": false }]
  }
  ```

  - `cell`: 3×3 の枠の位置 `tl / t / tr / l / c / r / bl / b / br`。1マスに1者。真ん中 (`c`) に事業の主体を置く
  - `icon`: `self` (事業の主体。sky の枠と塗り) / `company` / `factory` / `university` / `person` / `shop`。絵は lucide の `Building2` / `Factory` / `GraduationCap` / `User` / `Store`
  - `label` は10字まで、`note` は22字まで (枠の中で名前1行・補足2行に収まる長さ)
  - `kind`: `goods` (モノ・サービス。青 `#2a78d6`、箱の印) / `money` (お金。橙 `#eb6834`、¥ の硬貨の印 `JapaneseYen`)。色はコスト試算タブの区分の色 (白地で見分けを検査済み) の青と橙
  - `planned: true` のヒトは破線の枠と「計画中」の札。そのヒトにつながる流れは書かなくても破線になる。流れだけを `planned` にもできる
  - 流れの `label` は24字まで。同じ2者のあいだの流れは3本まで。**先に書いた流れほど上 (縦並びは左、斜めは上側) の矢印**になる (id の並びに左右されない)
  - 読めない定義 (JSON でない、マスの重複、いないヒトへの流れ、4本目の流れなど) は、理由 (「ピクト図の定義を読めない：…」) と元の文をそのまま出す
- 配置は純関数 `layoutPictogram()`。図は **1000×680px** で組み、枠 (148×108px、事業の主体は 168px) は列と行の中心に置く。同じ2者の流れは 34px (3本なら 40px) 間隔の平行な矢印にし、矢印の端は枠から 6px 離す
- **大きさ**: 図の枠の幅を `ResizeObserver` で測り、1000px より狭ければ `pictogramFitScale()` の縮尺 (小数第3位で切り捨て、下限 0.7) で図全体を縮めて見せる。ノートPC (1440×900 の窓、内側 約1,309px) ではトピック一覧と並んで枠が 約790px になり、実寸のままだと右の列 (顧客) が枠の外に出た (2026-09-17 本番確認)。凡例の右端の「実寸で表示」/「枠に合わせて表示」で切り替えられる。枠が 640px 未満 (スマホ) は縮めずに枠の中で横に動かす。縮めた図が枠に収まらない分も枠の中で横に動き、親の幅は押し広げない (`contain: inline-size`)
- **矢印の添え書き**は、真ん中の矢印は線の上 (両脇に 20px ずつ矢印が見える幅)、ほかは矢印の外側 5px に置く。位置は矢印の 14〜86% (3%刻み) の候補から、ヒトの枠と図の外を避け、ほかの矢印・置いた添え書きから 20px まで離れているほどよい位置を選ぶ (自分の矢印よりはっきり遠くないと、どの矢印の添え書きか読み違えるため)。3px より近いものは `collisions`、矢印がほかのヒトの枠を横切った数は `laneCrossings` に数える。文字の幅は全角 11px・半角 6.8px で見積もり、折り返しは行頭禁則の1字分を見込む
- 画面: 図の上に凡例 (モノ・サービスの流れ / お金の流れ / 破線は計画中・形が未定)。**ヒトにカーソルを合わせると、そのヒトの流れと相手だけを濃く出す**。画面または図の枠が 640px 未満では図の下に流れの一覧 (「A → B：何が流れるか」) を出し、それ以上の幅では読み上げ用にだけ持つ (図そのものは `aria-hidden`)
- 回帰防止: [`scripts/check_pictogram.mts`](../scripts/check_pictogram.mts) (`npm run test:critical-ui` から実行)。定義の検査、矢印の並びと向き、添え書きの箱の見積もり、枠の幅に合わせた縮尺、SOL の本番の図 (migration 443 の本文から取り出す) で重なり・はみ出し・横切りが0件で本文に mermaid と「菌の工程」が残らないこと、`MarkdownView` の配線と `PictogramDiagram` の色・印・破線。あわせて `check_pwa_critical_ui.cjs` が配線の文字列を見る

## 6. 投入済みデータ (2026-09-04 時点)

| PJ | トピック | 行数 | 主な出所 |
|---|---|---|---|
| p21 (SX) | 性能一覧 / 解説 / 培養の成立条件 / リアクターの運転条件 / 対象にできる物質 / 選好 / 限界 / 実排液の入手元 / 有価物 / 単価と原価の推移 / バイオ燃料 / 到達実績 / 閉鎖系（強化株）7件 / 競合比較 13件 (事業計画グループの競合比較タブに出す、§5.3) / ビジネスモデル 11件 (事業計画グループのビジネスモデルタブに出す、§5.5) | 521 (7区分・43トピック、2026-09-14 時点) | 2025-05-08・2026-02-26・05-20・06-03 Plaud議事録、2025-11-05 杉浦先生の文字起こし、SX定例MTG、★SolvioraX_m³換算版、SX_コスト試算 各版、260819 バイオ燃料アップサイド検討資料、SX 定例報告会_260902 (杉浦先生 配布資料)、2026-09-14 オフサイト廃液処理の検証 (環境省・警察庁・岡山県の公的資料、決算短信) |
| p20 (CX) | 性能一覧 / 解説・要素技術の地図・技術の系譜 / 要素技術ごとの成熟度 / 材料 / 冷却パワーの打ち手 / TES / ADR実証機の構成 / 用途ごとの要求条件 / 到達温度の目標と限界 / 使えなくなる条件と弱点 / 星取り表 / 競合製品 / 業界の実用化状況 / 到達実績 / 見立ての推移 / TRL4へ上がる条件 / 関連特許 | 203 (7区分・19トピック、2026-09-14 時点) | 面着打合せメモ、キックオフ会議メモ、Slack (LEMON調査・G-QuAT面会報告)、kiutra MTGアップデート、OIST審査用ピッチ資料 (2026-08-27)、2026-09-04 BUILD鮫島さん訪問の文字起こし、CryoXプロジェクト_TRL現状_20260727.xlsx、NEDOフロンティア育成事業 情報提供書 (2026-07)、2010年の連続ADR研究会資料 |
| p07 (LST) | 電解膜の運転条件と限界 / 原料ごとの前処理の成立条件 / 装置の到達実績 | 15 | 2026-09-02 LiSTie経営会議 (Notionローカル取り込み)、2026-08-26 LiSTie取締役会、2026-07-15 経営会議 |

初版 (340) との違いは §3.5 のとおり。SXの培養条件は「9項目すべて未測定」ではなく、
**培養温度 45〜70℃ / pH 7.5程度 / 二酸化炭素 0.03〜10% / 赤650nm・青440〜457nm** が
杉浦先生の指定値として 2025-11-05 の文字起こしに残っていた。

CXの到達温度も「未確認」ではなく、**NMR/MRI向け 100mK を FY2025 内に達成見込み、
量子コンピュータ向け 20mK はその次、方式の原理的限界はおおよそ 10mK** が一次情報にあった。

星取り表には自社に不利な事実も入れる。CXでは「産総研G-QuATでは希釈冷凍機の振動・熱の染み出しは
大きな課題として認識されていない」「大規模化の主流は1台への量子ビット集約」を注記として置いた。
訴求軸への反証を消すと、星取り表が営業資料になって判断に使えなくなる。

### CX 追記 (migration 352 / 2026-08-29)

OIST審査用ピッチ資料 (2026-08-27・全31枚、資料室 > ピッチ資料) の技術情報を入れた。
トピックを3つ足し (`ptt_cx_industry` / `ptt_cx_power` / `ptt_cx_rivalspec`)、星取り表へ **冷却パワー** の軸を加えた。

冷却パワーは資料の技術ストーリーの中心なので軸として独立させた。入れてみて分かるのは、
100mK での冷却パワーが **kiutra ≦1µW に対し Bluefors は 450〜3,000µW** と桁で違うこと、
そして **自社の冷却パワーには実測値がない** こと。磁気冷凍がどこで負けているかが表に出る。

§3.4 のとおり食い違いは両方残した。CXで立てたのは次の2つ。

| 食い違い | 残し方 |
|---|---|
| 連続運転の到達温度が **126mK** (2026-06-16 NIMS桜MTG) と **120mK** (資料 p14) | 別の行として両方置き、双方 `needs_check`。資料 p14 の出典が2021年 MT27 の概念設計発表で実証時期と結びついていない点も `check_reason` に書いた |
| ヘリウム3が **完全に不要** (既存の星取り表) と **使用量 1/1000以下** (資料 p12、＝ゼロではない) | 星取り表のセルは値を残したまま `needs_check` を立て、資料側の数値は `ptt_cx_power` の行として別に置いた |

金額は §3.5 のとおりコスト試算タブが正本なので、資料 p30 の機種別価格はここへ持ち込んでいない
(資料内の kiutra 価格は社内ヒアリング由来の非公開情報でもある)。

### LST 追記 (2026-09-02)

まさの依頼「どういうハードルがあって、どれを超えると事業が成立するのかを格納できるようにしてほしい」に対して、
**技術タブと `project_management_technical_tests` の両方へ入れた**。§1 の使い分けどおり、
性質は技術タブ、判定は試験側、値の正本は試験側に置いて技術タブから `source_ref` で指している。

中心は**電解膜の通電電流**である。連続通電では 10A で抵抗増加が不可避で、間欠運転 (4秒印加・2秒休止) にすると
5A (実効 3.3A) まで通り、抵抗値の上昇が観察されない。**実効 3.3A で事業が成立するかを次の技術検討会 (約2か月後) までに
精査する**ことが決まっており、これが分岐点になる。判定が出た時点で BZM 3.0 の証拠水準と変換能力を測り直す
(`model/cases/p07_lst.md` §6.3)。

出所は Notion の議事録で、まだ OS へ取り込まれる前の書き起こしを `scripts/h1_local_notion_fallback.mjs` で
ローカルの Notion データベースから読んだ。**Notion アプリが開いていると SQLite がロックされる**ので、
`--db` にコピーを渡して読む。

### CX 追記 (migration 368 / 2026-09-04)

2026-09-04 の BUILD 鮫島さん訪問 (Notion AI ミーティングノートの文字起こし全文) から 9 行を足した。
トピックは増やさず、`ptt_cx_record` / `ptt_cx_power` / `ptt_cx_temp` / `ptt_cx_industry` へ入れている。

中心は**超伝導線材の細線化**である。高温超伝導体は工場から幅 4mm のテープで供給され、
機械加工では傷が入り、レーザーでは周囲まで溶けるため細線化できなかった。
半導体の微細加工技術を応用してこれを解いたのが今回の差分で、冷却パワーは 2〜3 倍になる見込みとされた。

§3.4 のとおり食い違いは両方残した。今回立てたのは次の3つ。

| 食い違い | 残し方 |
|---|---|
| 細線化の到達幅が **10µm 加工に成功** (資料 p14/p28) と **40µm が先週形になった / 10µm はオーダーとしての見込み** (当日発言) | 40µm を新しい行として置き、既存の `pte_cx_p_pw30` / `pte_cx_p_r80` にも要確認を足して相互参照させた |
| 冷却パワー向上が **10倍** (資料の理論値) と **2〜3倍** (当日発言) | 2〜3倍を別行として置き、何に対する比かを確認事項にした |
| 水素液化の温度が **20mK** (既存行の行名) と **20K** (当日発言) | 20K を新しい行として置き、既存の `pte_cx_t03` に要確認を足した。20mK は超伝導方式の量子コンピュータ側の値で、単位の違う2用途が1行に同居していた |

到達温度については、**120mK のデータは約20年前に NASA と共同で出したもので、ニオブチタン (低温超伝導) コイルによる実績**
であることが分かった。内製化した現行機ではまだそこに達していない。
これは 352 で立てた 126mK / 120mK の食い違いの背景そのものなので、`pte_cx_p_r65` / `pte_cx_p_r70` の
`check_reason` を、当日わかったことを含む形へ書き直した。

文字起こしは自動生成で聞き取りが不明瞭な箇所がある。ヘリウムの「過去20年で30〜60倍」は
需要か価格かが判別できないため `confidence='low'` + 要確認で置いた。
市場規模と金額はここへ持ち込まず、コスト試算タブと事業計画側を正本のままにしている。

### CX を SX と同じ構成へ (migration 408 / 2026-09-14)

まさの依頼「SXの技術タブがかなり充実してきた。同じ仕様でCXも技術タブを充実させてほしい」。
それまでCXは区分「磁気冷凍」1つに8トピック・86行で、SXにある**性能まとめ・使えなくなる条件・用途ごとの条件・見立ての推移**が無かった。

区分を7つに分け、トピックを11足した。既存トピックは消さず、区分と並び順だけ変えた。

| 区分 (sort_order) | トピック |
|---|---|
| 性能まとめ (5) | `ptt_cx_spec_sheet` 性能一覧 20行。【冷やせる温度】【冷却パワー】【連続運転と保持】【ヘリウム・振動・保守】【部材の性能】【検出器 (TES)】【まだ届いていないこと】 |
| 原理と方式 (10〜14) | 既存の解説 / `ptt_cx_components` 要素技術の地図 / `ptt_cx_lineage` 技術の系譜 |
| 要素技術 (20〜39) | `ptt_cx_elements` 11要素のTRL / 既存の材料・冷却パワー / `ptt_cx_tes` / `ptt_cx_adr_build` |
| 用途と限界 (40〜45) | 既存の到達温度 / `ptt_cx_applications` / `ptt_cx_limits` |
| 競合と業界 (50〜55) | 既存の星取り表 (熱スイッチの軸を追加)・競合製品 (Maybell Quantum・上海技術物理研究所・NASAゴダードを追加)・業界 |
| 到達実績と計画 (60〜67) | 既存の到達実績 (2010年の旧機の実証を追加) / `ptt_cx_roadmap` / `ptt_cx_trl_gate` |
| 知財 (70) | `ptt_cx_patents` 10行 |

§3.5 のとおり、値は `project_knowledge` ではなく生データから取った。今回いちばん効いたのは Drive の
**CryoXプロジェクト_TRL現状_20260727.xlsx** で、神谷さん本人の記入欄に要素ごとのTRL・課題・特許の状態がある。
次が NEDO 情報提供書と OIST ピッチの**スライドのノート** (本文に出ない出典と数値がノートにある)。

中心になった事実は2つ。

1. **対外資料の「120mK連続運転」は2008〜2015年の旧機 (ニオブチタンのコイル) の実績**で、内製の現行機はまだ届いていない。
   2010年の研究会資料に、105〜120mKの制御とTES型X線カロリメータの動作確認の記録があった。星取り表の自社の到達温度 (`pte_cx_c11`) は2025-12の「FY2025内100mK見込み」のままだったので現状へ直し、見込みは到達実績 `pte_cx_r02` に残した。
2. **自社の冷却パワーは未実測**。既存の磁気冷凍方式は100mKで約1µW級で、希釈冷凍機の3桁下 (NEDO情報提供書)。

§3.4 のとおり食い違いは両方残した。

| 食い違い | 残し方 |
|---|---|
| 超伝導線材のフィラメント幅が **6µm** (技術要素マップ) / **10µm** (対外資料) / **40µm** (9/4口頭) | `pte_cx_x_pw100` を足し、`pte_cx_p_pw30` に要確認を書き足した |
| 超伝導線材の特許が **出願済み** (対外資料) / **出願準備中** (神谷さん記入) | `pte_cx_ip070` と `pte_cx_p_r80` の双方に要確認 |
| ロードマップが **2038年に100mKでmW級** (NEDO) / **2035年に量子コンピュータ冷却へ参入** (OISTピッチ) | `pte_cx_rm060` / `pte_cx_rm080` を別行、トピックにも要確認 |
| 共有特許の持分が **NIMS 7 : KEK 3** / **技術ごとに70/30・50/50・100** | `pte_cx_ip100` を足し、`pte_cx_r03` に要確認 |
| 解説の **ヘリウムを完全に使わない** / **使用量1/1000以下** | `ptt_cx_principle` に要確認 |

入れなかったもの: 金額 (コスト試算タブ)、事業モデル・資金調達・設立時期 (技術ではない)、プランBの中身 (特許出願前)。
特許の状態は、知財台帳 (`project_ip_assets`) に p20 が1件も無いため当面技術タブに置いた。登録したら状態の正本を移す。
TRL4 の5項目は `project_management_technical_tests` にも p20 が0件なので、試験として起票するのは次の一手 (§7)。

### SX バイオ燃料追記 (migration 362 / 2026-09-03)

`ptt_sx_biofuel` に、ラボスケールで燃料を作った経験、既存燃料へ数%を混合する段階導入、
2,000 L級設備の年間48〜130 kg（55〜148 L）という内部試算、量産時の必要設備数、
重金属回収と燃料生産を別工程にする必要性をまとめた。

ラボ経験と開発経路は2026-04-28のSX定例、数量は260819内部検討資料を出所とする。
数量は実測ではなく `estimate`、ラボ経験も定量記録が未確認のため `needs_check=true`。
販売価格と原価の成立ラインは技術タブへ重複させず、コスト試算タブの `project_cost_notes` を正本にする。

### SX 性能一覧・実排液・9/2定例 (migration 363 / 364 / 365 / 2026-09-04)

杉浦先生から共有された「SX 定例報告会_260902」(配布資料) の反映と、まさの依頼による最上段のまとめ。
**この番号は他セッションと重複している** (`364_seed_project_tech_kenq.sql` / `365_kenq_xrl_evidence_and_score_2026-09-04.sql` が同番号)。
適用は手動なので実害は無いが、次に採番するときは `ls` で実在番号を見てから決める。

| migration | 入れたもの |
|---|---|
| 363 | 新トピック `ptt_sx_effluent_sources` (実排液の入手元と性状 7行)、`ptt_sx_reactor` へ鉄取り込み試験の液組成と元の測定値、既存の9/2由来行への出典追記と `check_reason` の具体化 |
| 364 | 新トピック `ptt_sx_spec_sheet` (性能一覧 28行)、`pte_sx_ef06` (訪問調整中の一覧) の削除 |
| 365 | 9/2資料の訪問日程・排液状況を `project_management_partners` へ (§1.1 の境界) |

9/2定例の内容は Notion 文字起こし経由で既に大半が入っていた (`pte_sx_r19`〜`r23` / `d11` / `d14`〜`d16` / `l13` / `l14` / `e03`)。
**配布資料が足したのはこの4点**で、文字起こしだけでは落ちていた。

1. **試験に使った液の限定条件** — 鉄模擬排液は培地成分を含まず、水と Fe(NO₃)₃ のみ (`pte_sx_r24`)。
   pH2.6→6.2 の上昇を「鉄を取り込んだから」と読める根拠であると同時に、実排水では成立しない可能性を示す条件。これが抜けると「酸性排水もいける」と読み違える
2. **元の測定値** — 2.5 mg-Fe / 47 mg-DCW (`pte_sx_r25`)。5.3%という割合だけでは菌体量からの試算に使えない
3. **評価方法の制約** — 有機物が多い食品系排液では炭素が多くICP-MSが使えず、CODで評価する (`pte_sx_ef07`)
4. **社名** — 「食品系排液A / B」がオカベ / 日本食研であること

要確認の理由は、資料に書かれた次の実験 (再現性実験 / Fe消費の半減期による野生型と強化株1の比較 /
pHを中性付近へ調整した条件での取り込み / 強化株2 (TmtA遺伝子3つ) での同一試験) へ書き直した。

`ptt_sx_spec_sheet` は §2 のまとめトピックの実例。28行を
【生きられる環境】8 / 【力を発揮する条件】5 / 【取り込めるもの】4 / 【生み出せるもの】6 / 【株による違い】5 に分けている。
オカベ排液については、7月の「糖分ばかりで分解対象となる成分がない」という見立てと
9月の「対照より速く増えた」という結果が逆になったので、§3.4 のとおり**両方を残して**関係先の `effluent_note` にも併記した。

オカベ排液の4行（`pte_sx_d14` / `pte_sx_ef01` / `pte_sx_l02` / `pte_sx_ss13`）は、観測（対照より速く増えた、43時間で pH 5→約8）と、
**炭素源は CO2 のみ**（先生の資料 SX_事業概要 v1.0 の「栄養要求性（C源）: 本技術 CO2のみ」。ユーグレナなど「糖など + CO2」との違いとして挙げている）、
増えた分の栄養は排液の窒素・リン・硫黄（2026-09-02「硝酸・亜硝酸を短時間で取り込む」、2026-09-15 BNV定例「排液の窒素酸化物・硫黄酸化物を栄養にしている」）を書いている。
処理（リアクター）ではこの増え方を当てにしない（まさ 2026-09-17）。要確認は処理前後の COD 値の実測（migration 436・447）。

### SX Plaud技術開発会議4件 (migration 377 / 2026-09-07)

2025-05-08、2026-02-26、2026-05-20、2026-06-03のPlaud共有議事録を、
開催済みMTGカード4件、技術台帳10行、技術試験5件へ反映した。

技術台帳へ入れたのは、後続資料では拾えなかった次の情報だけである。

- 2025-05-08: pH6.5〜9という初期発言、高濃度二酸化炭素10〜90%の応答仮説、菌体約1×10µmという回収設計の入力
- 2026-02-26: 30Lラボ培養で1日2gという燃料関連生成量の発言
- 2026-05-20: 試作機のエア1.5〜2L/min、二酸化炭素3%時50〜100mL/min、10Lタンクが30L/minで約20秒で空になる観測、光量不足仮説
- 2026-06-03: サージタンク底部の緑色残液15〜50mL、二酸化炭素濃度分布の未測定状態

Plaudの要約は、微生物名、化学種、単位、話者の誤変換を含む可能性がある。
そのため10行はすべて `needs_check=true` とし、2025-11-05や2026-09-02の後続値を上書きしていない。
とくに2026-02-26は「30Lで2g/日」「5倍」「10g/L」が同じ要約内で整合しないため、
30L・2g/日を発言記録として残すだけにし、実験原簿で生成物と単位を確定するまで実測値として使わない。

技術試験は、循環死角、装置内二酸化炭素分布、ポンプ方式と細胞損傷、照射位置とPPFD、
菌体の沈殿・フィルター回収の5件。2026-05-20と06-03に出た過去の発注日・工事日・貸出予定は、
完了を確認できないため現在タスクへ再起票せず、MTGカードの当時の段取りにだけ残した。

### SX 閉鎖系（強化株）追記 (migration 401 / 2026-09-14)

まさの依頼「閉鎖系のレポートを技術タブに入れておいて」。Drive `p21_sx/260911_閉鎖系要件と論点整理` のレポート (公開URL `https://sx-heisakei-ronten.vercel.app`) を、**レポートの問いの順のまま**区分「閉鎖系（強化株）」に7トピックで置いた。区分の `sort_order` は 22〜28 で、培養 (10) と排水処理 (30) の間に並ぶ。

| トピック | 形式 | 秘匿 | 中身 |
|---|---|---|---|
| `ptt_sx_closed_overview` 全体像 | 解説 | 要秘匿 | 結論2点、問いの系譜 (mermaid)、合流点 (処理槽で細胞を増やすか)、タブ内の置き場所 |
| `ptt_sx_closed_law` 閉鎖系とは何か | 解説 | 社内限定 | 第二種使用等の定義、使用等に保管・運搬・廃棄が入ること、審査期間、所管 (未確認)、大学の20L |
| `ptt_sx_closed_categories` 区分ごとの手続きと措置 | 星取り表 | 社内限定 | GILSP / カテゴリー1 / その他 × 大臣確認・審査期間・処理水・設備と運用。条文の要件なので記号なし |
| `ptt_sx_closed_gilsp` GILSPの判定条件 | 成立条件 | 要秘匿 | 基準7項目とSXの強化株で分かっていること。未確認5件に確かめ先 (好熱性の下限温度など) |
| `ptt_sx_closed_reactor` リアクターの設備と運用 | 成立条件 | 社内限定 | 7/10封じ込め設計で足りている4項目・足りない4項目・記録保存・大学の20L。未確認5件 |
| `ptt_sx_closed_mechanism` 金属を取る仕組みと光 | 解説 | 要秘匿 | 吸着と取込み、光の3段階、必要な菌体量、死菌案を外した理由 |
| `ptt_sx_closed_precedent` 世界の先行事例 | 解説 | 社内限定 | 活性汚泥・菌の添加・金属吸着材、実用化されていない理由、SXにとっての意味 |

§3.5 のとおり**金額は入れていない** (不活化の費用などはコスト試算タブが正本)。菌体量などの物量は入れている。
誰に何を確かめるかと進み具合の正本はゴールツリー「強化株を閉鎖系で使えるか」で、ここは性質の置き場所に徹する。

### SX 競合比較 (migration 414 / 415 / 2026-09-14)

まさの依頼 (2026-09-14、JMTC 面談で「微生物の金属回収はガルデリアを思い浮かべるが進まない印象、どう突破する？」「従来技術と比べたコストは？」と聞かれて答えに困ったあと):

> 技術ページの中に競合比較のタブを新たに作って、そこにガルデリアをはじめとして、他の技術との比較を作ってほしい。星取り表みたいなものをトップにおいて、それぞれの観点での詳細な比較表をその下に作るイメージだけど、もっといい構成があれば提案してほしい。

区分「競合比較」を `sort_order` 80〜95 で置いた (バイオ燃料 65 の後ろ)。**コードは足さず、4つの形のトピックだけで作った** (§2)。

| sort | トピック | 形式 | 中身 |
|---|---|---|---|
| 80 | `ptt_sx_compare` 排水処理と金属回収の星取り表 | 星取り表 | SX / ガルデリア / 凝集沈殿 / イオン交換・キレート樹脂 / オゾン・フェントン酸化 / 活性炭 / 活性汚泥 / 外部委託（産廃）× 10観点。記号の付け方は本文の表 |
| 81 | `ptt_sx_comp_galdieria` ガルデリアとの違い | 解説 | 分かっていること、同じところ・違うところ、答え方の例、言わないほうがいいこと |
| 82 | `ptt_sx_comp_qa` よく聞かれる問いと答え方 | 解説 | 7問。答え方の例・根拠のトピック・言わないほうがいいこと |
| 83〜88 | 仕組みと取れるもの / 使える液の条件 / 処理のあとに残るもの / コスト / 実績と成熟度 / 規制と手続き | 成立条件 | 行＝相手、行頭の【】で束ねる。値と条件と時点と出典を1行ずつ |
| 90 | `ptt_sx_comp_fuel` 燃料の星取り表 | 星取り表 | SX / ユーグレナ / IHI / ちとせ / 海外の撤退・転換 / 廃食用油の燃料 × 7観点。記号は規模・コスト・いまの状態の3行だけ |
| 92 | `ptt_sx_comp_weak` SXが負けるところと、これから示すこと | 成立条件 | 12項目 |
| 95 | `ptt_sx_comp_players` 調べた会社・方式の一覧 | 成立条件 | 15社・方式、発表日つき |

まさの案 (星取り表 → 観点ごとの詳細) から変えたのは3つで、作る前に会話で示した。

1. **答え方のページを足した。** 面談で困ったのは表がないことより、その場で言える一言がなかったこと
2. **SXが負けるところを1ページにした。** ◎ばかりの表は営業資料になり、投資前の調査でかえって信用を落とす (§6 の CX と同じ考え方)
3. **相手ごとに確認日 (発表日) を持たせた。** ガルデリアは2026年6月に商用導入したばかりで、情報がすぐ古くなる

決めごと:

- **SX の原価と売価は書かない** (§3.5)。星取り表の運転費の SX のマスは「試算中（コスト試算タブ）」、燃料は「売価200円/Lを大きく上回る」とだけ書く。既存方式の費用は、社内の市場ヒアリング (2026-07-29 の整理 310〜550円/m³、07-08 の専門家) と公開資料 (畜産排水の脱色の実証、産業廃棄物の業者相場) から入れ、条件を必ず書く。2つのヒアリングで減価償却の割合が違う (30〜50% / 10〜25%) ので、§3.4 のとおり両方に要確認を立てた
- **取り込みは細胞の中** (まさ確認 2026-09-14「確認するまでもなく、最初から細胞の中」)。ガルデリアの吸着剤は乾かした細胞の表面に付く (会社サイト・2021年の XAFS の論文)。この違いを答え方の軸にした。あわせて `ptt_sx_principle` の「取り込むか表面に吸着させる」、`ptt_sx_closed_mechanism` の「吸着と取込みが分けて測られていない」、`ptt_sx_closed_overview` の図の「吸着か取込みか（未確認）」を、細胞の中への取込みに書き直した (414 の本文置換。置換元が無ければ止まる)
- **SX に不利な事実も入れた**: 煮豆の排液では色が消えなかった (2026-07-01)、金属が混ざると処理時間が単体30分→24時間、鉄の試験は培地成分のない液、染色・食品工場の排水温度の実測がない、「高温で雑菌が増えない」はガルデリア・ユーグレナ・IHI も同じ方向の強みにしている
- 旧 `ptt_sx_compare` (区分「排水処理」の星取り表 21マス、2026-08-29) は作り直してこの区分の先頭に移した。SX側の原価 467.63円/m³ (2026-08-11) は単価と原価の推移 `ptt_sx_price` に残っている。既存の物理化学処理の費用と汚泥の量はコストと残るもののトピックへ移した。運転に要る人手 (1バッチ5.75/7.5時間) は顧客の運転になったため外した
- 左のトピック一覧は「 — 」より前だけを出すので、星取り表の題は「排水処理と金属回収の星取り表 — …」にした (415。414 の題では「星取り表」とだけ出た)。星取り表のマスの要確認の理由は14字以内にした (415。確かめる相手と中身は観点のトピックと「SXが負けるところ」にある)
- 表の最小幅を列の数から決めるようにした (§5.1。相手8列がスマホ幅で押しつぶされたため)

この版の星取り表と燃料の星取り表は、同日に社外に出す形へ作り直した (下の 417)。

調べ方: 公開情報を5つに分けて調べた (ガルデリア / 金属の既存方式 / 色の既存方式 / 生きものを使う国内外の会社 / 藻類の燃料)。社内の記録は `source_cache` (Slack の専門家ヒアリング、コスト試算の市場ヒアリング)、技術タブの既存行、事業概要 v1.7、7/2 煮豆排液・7/10 Dy の試算メモから取った。生成スクリプトは scratchpad (コミットしない) で、行 id の重複・空のマス・出典のない行・未確認なのに要確認がない行・「吸着か取込みか」の書き方・「PoC」「m3」の表記が残ると止まる。


### SX 競合比較を社外に出す形へ (migration 417 / 2026-09-14)

まさ (2026-09-14、区分を事業計画グループのタブへ移す依頼 (§5.3) と同じ会話):

> この星取り表をVCに提出するので、嘘じゃない範囲でSXをポジティブに書かないといけない。あとSXじゃなくてSolvioraXって書いて。あとバイオ燃料化についても比較してほしい。
> 「菌体を酸で溶かして取り出す」が△で「吸着剤ごと精錬会社へ売っている」が◯ってのが納得いかない。なるべく有利に解釈して。
> (414 の色のマスに対して) すでに様々な着色廃液で透明にできてることは確認できてるし、煮豆は別の色に変わったけど、別の有価物（ケルセチン）が得られただけなので、分解できないって話じゃないよ。
> (414 の金属のマスに対して) Dy、Ndも速報とはいえ実際に確認できてるから、ちゃんと書いて。

| sort | トピック | 秘匿 | 中身 |
|---|---|---|---|
| 80 | `ptt_sx_compare` 排水処理と金属回収の星取り表 | 公開可 | SolvioraX / ガルデリア / 凝集沈殿 / イオン交換・キレート樹脂 / オゾン・フェントン酸化 / 活性炭 / 活性汚泥 × 9観点 (仕組み・色を消す・金属を取る・狙った金属を選んで取る・熱い排水・取った金属を資源に戻す・汚泥・規制・段階)。仕組みと段階は記号なし |
| 81 | `ptt_sx_comp_fuel` 燃料の比較表 | 公開可 | SolvioraX / ユーグレナ / IHI / ちとせグループ / 海外の撤退・転換例 / 廃食用油の燃料 × 6観点。記号なし (90 から移した) |
| 82〜89 | ガルデリアとの違い / よく聞かれる問いと答え方 / 仕組みと取れるもの / 使える液の条件 / 処理のあとに残るもの / コスト / 実績と成熟度 / 規制と手続き | 社内限定 | 414 の中身を SolvioraX 表記にし、色・Dy・Nd・鉛の事実を足した (使える液の条件に3行) |
| 92 | `ptt_sx_comp_weak` SolvioraXが負けるところと、これから示すこと | 社内限定 | 12項目。煮豆を「色が消えなかった」と書いた箇所を外した |
| 95 | `ptt_sx_comp_players` 調べた会社・方式の一覧 | 社内限定 | 15社・方式 |

決めごと:

- **公開可の2枚は社外に出す文書として書く。** 嘘にならない範囲で SolvioraX に有利な読み方をする。マスのメモは画面のマスの中に出るので、社外の人が読んでよい言葉だけで書く (社内の言葉・要確認・未確認を置かない)。判断に使う不利な事実 (薄い液・費用・混ざった液・実排水の温度・他社も同じ強みを言うこと) は社内限定のページに残す。§6 冒頭の「星取り表には自社に不利な事実も入れる」は、社内で判断に使う表の決まり
- **観点と相手の選び方**: 「薄い液（数ppm）から取る」「運転費」の行と「外部委託（産廃）」の列は外した。薄い液は SolvioraX の数字がまだ数十ppm (鉄50ppm) までしかない。費用は液の種類と規模で大きく変わり、SolvioraX の原価と売価はコスト試算が正本 (§3.5)。外部委託は処理の方式ではなく処分の手段。どれも社内の観点ごとの表 (使える液の条件・コスト) には残っている
- **「取った金属を資源に戻す」は SolvioraX とガルデリアをどちらも ○**。◎ は「金属のまま高純度で取り出す」、○ は「資源として取り出す・売る」。SolvioraX は菌体を溶かして金属を取り出す (分離・精製して売る計画)、ガルデリアは吸着剤ごと精錬会社へ売る。商用で売った実績の差は「段階」の行で読ませ、実績を2つの行で数えない (414 は SolvioraX △・ガルデリア ◎ で、実績の差をこの行でも数えていた)
- 「色を消す」の ◎ は「色素を分解して消す」。SolvioraX (様々な着色廃液を透明化、ラボ) とオゾン・フェントン酸化が ◎
- 「熱い排水」のイオン交換・キレート樹脂は、Purolite S930Plus の製品データシートの使用温度の上限 80℃ から ○ にした (414 は「調べていない」)
- 燃料は各社で段階も規模も違い、記号を付けると軸の選び方で結論が決まるので付けない。1Lあたりのコストの行は外した (SolvioraX の燃料の原価はコスト試算（燃料）が正本)
- **色の事実**: 様々な着色廃液で透明化を確認 (まさ 2026-09-14)。煮豆は透明にならず別の色に変わったが、色素を分解できなかったのではなく、カルコンがケルセチンに変わったとみている (杉浦先生 7/1)。ケルセチンを分析で同定した記録は無いので、技術タブのケルセチンの行 (`pte_sx_d12` / `v02` / `ss19`) の要確認は残した
- **Dy・Nd・鉛**: Slack #01_定例 2026-07-10 の杉浦先生の速報 (Nd と Dy の水溶液に細胞を入れ、3時間後には液中のレアアースがほぼ無くなった。Pb も取込み OK)。磁性材料の会社の廃液そのものは、溶媒のイソプロパノールが分析を妨げてまだ定量できていない。初期濃度と取り込み量の数値は記録に無い
- 技術タブの `pte_sx_e13` / `d12` / `v02` / `ss14` / `ss15` / `ss19` / `e07` / `e10` と `ptt_sx_valuables` の本文・要約を、同じ事実へ直した
- **migration 418**: `pte_sx_e13` の数値欄に 2026-02-28 の技術目標 (メチレンブルーの脱色率 90% 以上・30分以内) が残っていて、本番の画面で「90% 以上 (○ 様々な着色廃液で透明化を確認 (ラボ))」と実測のように読めたので、数値欄を空にして目標は条件の文へ移した。**値の文を書き換えるときは、同じ行の数値欄 (`value_min` / `value_max` / `unit`) も見る** (`formatTechValue()` は数値と文をつないで1つに出す)
- ゴールツリー: 未承認の問い「9/2の金属取り込みは細胞表面への吸着か細胞内への取り込みか」を却下した (画面の却下と同じ論理削除)。「処理槽で細胞を増殖させるか」の判断材料から同じ項目を外した。閉鎖系の論点資料 (sx-heisakei-ronten) からも同日に外してある
- 生成スクリプト (scratchpad、コミットしない) が止めるもの: 公開可のページの社内の言葉・要確認・未確認、区分「競合比較」の文の単独の「SX」、「消えなかった」「除去できず」「色が濃くなった」、415 のあとに誰かが区分「競合比較」を書き換えていた場合と、技術タブの行・ゴールツリーの問いが確認した時点から書き換えられていた場合 (何も書かずに止まる)

### SX 競合比較に競合の会社を足す (migration 420 / 421 / 2026-09-14)

まさ (2026-09-14):

> VC提出用のPDFは作ってほしいけど、その前に、ガルデリア以外にも競合はいっぱいあると思うから、それらを星取り表に加えてほしい。

| sort | トピック | 秘匿 | 中身 |
|---|---|---|---|
| 80 | `ptt_sx_compare` 競合の会社との星取り表 | 公開可 | SolvioraX / ガルデリア / Allonnia / Paques / Mint Innovation / ElectraMet / パナソニック環境エンジニアリング / ディーピーエス / Arvia Technology × 狙う液・仕組み・色を消す・金属を取る・狙った金属を選んで取る・熱い排水・取った金属を資源に戻す・汚泥・段階 (狙う液・仕組み・段階は記号なし) |
| 81 | `ptt_sx_compare_methods` 既存の方式との星取り表 (新規) | 公開可 | 417 の星取り表からガルデリアの列を会社の表へ移した残り (SolvioraX / 凝集沈殿 / イオン交換・キレート樹脂 / オゾン・フェントン酸化 / 活性炭 / 活性汚泥 × 9観点) |
| 82 | `ptt_sx_comp_fuel` 燃料の比較表 | 公開可 | 変えていない (並びだけ 81 → 82) |
| 83〜96 | 社内限定の10ページ | 社内限定 | 並びを1つずつずらした。調べた会社・方式の一覧に19社 (星取り表に載せたか・載せなかった理由つき)、問いと答え方に 8「電気で回収する装置との違い」・9「Paques との違い」、負けるところに2行 |

決めごと:

- **会社と既存の方式を2枚に分けた。** 会社8社を1枚に足すと14列になり、画面でも A4 横の PDF でも読めない。記号の付け方は同じ (「狙った金属を選んで取る」の ○ を「条件・株・材料を変えて、狙った金属を取れる」に広げ、△ を「選ぶ力は弱い」にした)。会社の表には規制の行を置かない (各社とも公開資料に手続きの記載がなく、ほぼ「?」になるため。既存の方式の表に残す)
- **会社の選び方**: 公開情報を5つに分けて調べた (微生物で金属を取る / 電気や膜で取る / レアアースの回収 / 色のついた排水の新しい技術 / 日本の会社。sonnet 5体・各20〜30回の取得)。20社超から「液から金属や色を取る」「SolvioraX の顧客と重なる」「投資家が名前を出しそう」で8社を選んだ。固体の磁石くずや鉱石が原料の会社 (Ionic Rare Earths・Phoenix Tailings・Cyclic Materials・信越化学工業・REEgen・Endolith・Nth Cycle) は土俵が違うので載せず、同じ方式の会社は1社で代表させた (BQE Water は Paques、emew は ElectraMet)。載せなかった会社は一覧と PDF の「ほかに調べた会社」に理由つきで残す
- **決め手の事実は一次資料で確かめ直した。** 調査担当の報告のうち、Arvia の事例の数字 (報告は「色度150未満・0.01mg/L・€0.70/m³」、一次資料は「色度1,755→472・滞留約5.4分」) と、ディーピーエスの出資の時期 (報告は 2020-07、一次資料は 2022-03-03) が違ったので一次資料を使った。Paques の温度 (0〜80℃)・銅の量 (年1万t超)、ElectraMet の銅板と汚泥の説明、パナソニック環境エンジニアリングの発売時期、Allonnia の累計調達と RE-Cover の段階、Mint の稼働も各社のページで確かめた
- 各社の欄は各社の公開資料 (会社の説明) による。第三者の検証ではない。「?」は公開資料で確認できないところで、「できない」ではない
- 45℃以上の液をそのまま扱えると公開資料に書いていたのは Paques (0〜80℃) だけ。ほかの会社は温度の記載がない
- **VC 提出用の PDF** (Drive `p21_sol/260914_競合比較_VC提出用/SolvioraX_競合比較_20260914.pdf`、A4 横6ページ: 表紙 / 会社 / 既存の方式 / 燃料 / 記号の付け方とほかに調べた会社 / 出典) は、この3枚の DB の中身から作る (scratchpad `competitor/pdf/`、コミットしない)。見た目は SolvioraX 事業概要 v1.7 にそろえた (右上に SolvioraX のロゴ、右下に team ARMADA のシンボルとロゴタイプ、Noto Sans JP)。作るときに公開可ではないトピックを入れようとすると止まり、出典の一覧に社内の言葉が残ると止まる
- **migration 421**: 既存の方式との表の凝集沈殿の汚泥のマスの出典が社内の資料名 (`SX_コスト試算_260716…`) だったので、「水処理の市場ヒアリングの整理 (2026-07-29、SolvioraX)」にした。公開可のページには社内の資料名を置かない
- 列が9を超える星取り表は、`sort_order` を「行×100＋列」で振る (§3 の「行10番台・列は下1桁」では列が10を超えると重なる)。行の id は `pte_sx_cmpco_<行>_<列>` / `pte_sx_cmpme_<行>_<列>`
- 生成スクリプト (scratchpad `competitor/v420/`) が止めるもの: 417 の決まりに加えて、時点が年月日でない (年月だけの「2023-05」は DB の日付に入らない。分からなければ空)、420 の前に区分「競合比較」・一覧・負けるところの行数が想定と違う

### SOL 競合比較の星取り表を、SolvioraX の違いが出る観点へ組み直す (migration 423 / 2026-09-14)

この節の「色と金属の両方を扱う」の行は、同日に「色素を分解できる」へ変えた (下の 424)。

まさ (2026-09-14):

> さんきゅ。これさ、方式の比較のところ、生きた細胞で事業化しているところは他にないから、そこは思いっきり強調してほしい。あと「金属を取る」っていう項目にすると、差別化しにくくない？「回収できる金属の種類」とかにすれば、SOLだけ◎にできるんじゃないのかな。そういう視点で他の項目も再検討してほしい。

| sort | トピック | 秘匿 | 中身 |
|---|---|---|---|
| 80 | `ptt_sx_compare` 競合の会社との星取り表 | 公開可 | 9列 × 6観点: 生きた細胞が金属を取り込む / 色と金属の両方を扱う / 取れる金属の種類 / 熱い排水 (記号あり。4つとも ◎ は SolvioraX だけ) / 狙う液 / 段階 (記号なし)。本文の先頭に「排水の金属を生きた細胞に取り込ませる方式は、調べた範囲で SolvioraX だけ」と、調べた範囲 (約30社) |
| 81 | `ptt_sx_compare_methods` 既存の方式との星取り表 | 公開可 | 6列 × 6観点: 生きた細胞が金属を取り込む / 色と金属の両方を扱う / 熱い排水 (◎ は SolvioraX だけ) / 取った金属を資源に戻す / 汚泥・廃棄物が少ない / 段階 |
| 83・84・93・96 | ガルデリアとの違い / 問いと答え方 / 負けるところ / 調べた会社の一覧 | 社内限定 | 問い5「生きた細胞で排水を処理する会社はないのか」を直し、10「SolvioraX だけ◎の観点を選んでいるのでは」を足した。負けるところに「星取り表に入れていない観点では他社が先行」。一覧に Veolia (ABMet)。ガルデリアの取るものを発表どおり (パラジウムが商用、金・白金などは展開) に直した |

決めごと:

- **公開可の星取り表の観点は、SolvioraX の違いが出るものを選ぶ。** 他社が先行する観点 (狙った金属だけを選ぶ力・取り出した金属の純度・汚泥を作らないこと) と差の出ない観点 (規制・手続き) は VC に出す表から外し、社内限定の「負けるところ」と「問いと答え方」の10に残す。実績の差は「段階」の行で読ませる (この行は外さない)。外した行: 仕組み・色を消す (新しい2行へまとめた)、金属を取る、狙った金属を選んで取る、取った金属を資源に戻す・汚泥 (会社の表だけ)、規制・手続き (既存の方式の表)
- **「生きた細胞」の強調は「金属を細胞の中へ取り込む」に絞る。** 生きた微生物を使う事業はほかにもある (Paques・BQE Water は微生物がつくる硫化物で沈める、Veolia の ABMet は微生物がセレンなどを還元して沈める、活性汚泥・Gross-Wen Technologies・フレンドマイクローブは有機物・窒素・リン・油)。各社の一次資料で確かめたうえで「排水の金属を生きた細胞の中へ取り込ませる方式は、調べた約30社で SolvioraX だけ」と書く。ガルデリアは死んだ細胞でも同じ効率で、表面に付く (JST 2016 の発表)。「世界で唯一」「生きた細胞で事業化している会社はほかにない」とは書かない
- 「生きた細胞が金属を取り込む」の記号: ◎ 生きた細胞が細胞の中へ取り込む / ○ 生きた微生物がつくる物質で沈める (Paques) / △ 生きものからつくった材料・物質に付ける (ガルデリア・Mint Innovation・Allonnia) / — 生きものを使わない、または金属を取る方式ではない (活性汚泥はここ)
- 「色と金属の両方を扱う」: ◎ 色素を分解し、金属も取る / ○ 両方を取り除く (凝集沈殿・活性炭。汚泥や吸着材に移す) / △ どちらか一方 / × どちらも主な対象ではない (活性汚泥)
- **「取れる金属の種類」は「回収できる」と書かない。** ◎ はレアアースと、銅などのほかの金属の両方。SolvioraX のレアアースは「水溶液から3時間でほぼ全量なくなった」速報までで、細胞の中に入ったか・取り出せるかは測っていない。各社の欄は各社の公開資料に書かれている金属による (ディーピーエスの「レアメタル」はレアアースと書かれていないので ○)
- 本文の「記号の付け方」の表は ◎ ○ △ × — の5列にし、その観点で使わない記号は「（使わない）」と書く。VC 提出用の PDF の記号の付け方のページは、2枚の本文のこの表を読んで作る (同じ観点の書き方が2枚で違うと止まる)
- **VC 提出用の PDF** は A4 横7ページ (表紙 / 位置づけの図 / 会社 / 既存の方式 / 燃料 / 記号の付け方とほかに調べた会社 / 出典)。表紙に要点を1文置いた。位置づけの図は、横が金属の取り方 (細胞の中へ取り込む / 表面に付ける / 沈める・電気で取り出す / 金属は取らない)、縦が色素を分解するか。星取り表の14列を置き、生きた微生物を使うほかの会社 (Veolia・Gross-Wen Technologies・フレンドマイクローブ) を薄い字で足した。図の置き方が星取り表の記号と食い違うと生成が止まる。ほかに調べた会社にも出典を付けた。生成スクリプトは scratchpad (`pdf/`・`v422/`、コミットしない)
- migration の番号: 本番DBへは仮番号 422 で当てたあと、別セッションが 422 (コスト試算の明細) を先に commit したので、ファイルは 423 にした (中身は同じ)
- 生成スクリプトが止めるもの: 420 の決まりに加えて、記号の4行 (会社) と3行 (既存の方式) で ◎ が SolvioraX だけでない、記号の付け方で使わないとした記号をマスに付けた、公開可のページの「回収できる金属」「世界で唯一」、出典名に社内の資料名 (421 の直しを 420 の定義で戻さない)、421 のあとに誰かが区分「競合比較」を書き換えていた場合

### SOL 競合比較: 「色と金属の両方を扱う」を「色素を分解できる」にする (migration 424 / 2026-09-14)

まさ (2026-09-14、423 のあと):

> 「色と金属の両方を扱う」→これは「色素を分解できる」でいいんじゃないの？両方できるところが他にないっていうか、色素を分解できるところがないだけだと思うんだけど。

えいみから、色素を分解できる相手はほかにもある (会社の表の Arvia Technology は電気の酸化で染色排水の色度1,755→472、既存の方式の表のオゾン・フェントン酸化は反応染料を99.5%脱色) ので、行を変えるとその相手も ◎ になる、と伝えて2案 (1. 「色素を分解できる」にする / 2. 両方の行を残して名前だけ変える) を出し、まさ「うん、１にしよう！」。

| sort | トピック | 変えたこと |
|---|---|---|
| 80 | `ptt_sx_compare` 競合の会社との星取り表 | 行20 を「色素を分解できる」に。SolvioraX ◎ (様々な着色廃液を透明化・ラボ) / Arvia Technology ◎ (色度1,755→472) / ほかの7社は対象外 (金属の回収) |
| 81 | `ptt_sx_compare_methods` 既存の方式との星取り表 | 行20 を「色素を分解できる」に。417 の「色を消す」と同じ事実: SolvioraX ◎ / 凝集沈殿 △ (脱色率50〜91%) / イオン交換・キレート樹脂 対象外 / オゾン・フェントン酸化 ◎ (反応染料99.5%) / 活性炭 ○ (吸着して取り除く) / 活性汚泥 × (分解されにくい) |

決めごと:

- **1つの強みを2回数える合わせ技の行は作らない。** 「色と金属の両方」は、金属の強みを「取れる金属の種類」の行と2回数えていた。色素の分解は、分解できる相手 (Arvia・オゾン・フェントン酸化) と ◎ を並べる。色も金属も ◎ なのが SolvioraX だけであることは、2つの行を並べれば読める
- ◎ が SolvioraX だけの行は、会社の表で3つ (生きた細胞が金属を取り込む・取れる金属の種類・熱い排水)、既存の方式の表で2つ (生きた細胞が金属を取り込む・熱い排水)
- 「色素を分解できる」の記号: ◎ 色素を分解して消す / ○ 分解はしないが、吸着などで取り除く / △ 染料の種類や条件で効きにくい / × 色素は分解されにくい / — 色は対象外
- VC 提出用の PDF: 会社の表のページの一言を「色素を分解でき、レアアースとほかの金属の両方を取れるのは、SolvioraX だけである。」にした (423 の「記号を付けた4つの観点で、◎は SolvioraX だけ」は成り立たなくなったため)。位置づけの図の縦 (色素を分解するか) は「色素を分解できる」の ◎ から検査する。同じ Drive のファイルに上書き
- 生成スクリプト (scratchpad `v422/build424.py`) が止めるもの: 423 の決まりに加えて、「色素を分解できる」の ◎ が会社の表で SolvioraX と Arvia Technology、既存の方式の表で SolvioraX とオゾン・フェントン酸化になっていない、「色と金属」が本文や行に残っている、423 のあとに誰かが区分「競合比較」を書き換えていた場合

### SOL ビジネスモデル: オフサイト廃液処理の検証 (migration 428 / 2026-09-14)

まさの依頼 (2026-09-14):

> 特管含む廃液をSOL（SXのことね。さっきPJ名をSOLに変更した。）の保有する産廃処理業者登録のトラックで回収してオフサイト（＝SOLの工場）で処理する事業が成立するかの検証をここで行いたい。

検証の資料は Drive `p21_sol/260914_オフサイト廃液処理の検証` (md・html) に置き、同日のまさ「そもそも本来はOSに置くべき資料だと思う」(§5.5) で、区分「ビジネスモデル」に写した。**コードは足さず、4つの形のトピックだけで作った** (§2)。

| sort | トピック | 形 | 行 | 要確認 |
|---|---|---|---:|---|
| 10 | オフサイトの廃液処理の評価 — 2026-09-14 の検証の結論 | 解説 | — | |
| 20 | オンサイトとオフサイトの比較 — 売価・液・許可・責任 | 星取り表 (12観点 × 2、記号なし) | 24 | 2マス (使用済み菌体の引き取り、カルタヘナ法) |
| 30 | オフサイトの事業の形の選択肢 — 5つの案の比較 | 星取り表 (5観点 × 5案、評価の行だけ ○△×) | 25 | 2マス (C の許可、B の菌の性能) |
| 40 | 対象になる廃液と量 — 特管の範囲と、全国・中国四国の排出量 | 解説 | — | ⚠ 愛媛県の実数 |
| 50 | 引き取りの値段 — 業者の目安とまとまった量の実例 | 解説 | — | ⚠ 公的な料金の統計が無い |
| 60 | 菌で処理できる濃さ — 必要な菌体の量と処分の方法 | 解説 | — | ⚠ 取り込みの上限・処分の方法への当てはまり |
| 70 | 許可の取り方 — 収集運搬業・処分業・施設の設置許可 | 解説 | — | ⚠ 愛媛県への照会 |
| 80 | 廃液処理の競合 — 既存の処理業者と貴金属回収 | 解説 | — | ⚠ 大栄環境の拠点など |
| 90 | 反社会的勢力と既存業者のリスク — 公的資料と防ぎ方 | 解説 | — | |
| 100 | 確認事項 — 誰に何を聞けば決まるか | 解説 | — | |

- すべて `confidentiality = 'internal'`。出典は一次資料 (e-Gov 法令、環境省の統計・通知・報告書、警察庁の報告書、岡山県の実態調査、決算短信) をトピックの本文と星取り表のマスに付けた
- 検証の資料からの変更: 資料の時点では「試算画面のオフサイトに、オンサイトと別の液の濃さを置くか」が要判断だったが、同日まさ「置いて」でコスト試算に置いた (5-13 の migration 427) ので、確認事項から外し、評価に「試算で見たオフサイト（2026-09-14 時点）」の表 (427 の後の総コストと利益率。正本はコスト試算) を足した。菌体の量の表の菌体1kgの原価は、426 で培養の原料を組み直した後の 334円/kg にした
- 生成スクリプト (scratchpad `gen_bm.py`、コミットしない) が止めるもの: PJ が無い、区分「ビジネスモデル」のトピックがすでにある、足す id がすでにある、足した数がトピック10・行49でない

### SOL ビジネスモデル: 事業全体のまとめを先頭に置き、まさの回答を反映する (migration 429 / 2026-09-14)

まさ (2026-09-14、428 の報告への返事):

> １．生の数字は一切ないのでそのままで。２．Cがいいと思う。３．どれか分からん。
> あとビジネスモデルのページのトップは、SOLの事業全体のビジネスモデルを視覚的にわかりやすいような形でまとめておいてほしい。

- **先頭に「SOLのビジネスモデル — 事業全体のお金とモノの流れ」** (`ptt_sol_bm_overview`、解説、sort 1。タブを開くとこれが出る)。**コードは足さず、本文の mermaid の図 (`MarkdownView` → `MermaidDiagram`) で描いた**。図は2枚: 主力の排水処理 (上から 愛媛大学・装置の製造委託先 → SolvioraX → 着色排水の工場・金属を含む排水の工場・取り出した金属の買い手。線のそばに渡すものと「お金：」、点線は使用済み菌体の引き取り、SolvioraX だけ sky の枠) と、主力に上乗せする売上 (オフサイト案C・バイオ燃料)。そのあとに収益の柱の表 (着色排水・金属を含む排水・オフサイト・バイオ燃料 × 顧客・渡すもの・お金の取り方・いまの状態)、成り立つための条件 (ゴールツリー「NewCo設立」の下の開いている問い)、出典
- 図の大きさ: 1440px 前後の窓でトピックの枠 (約 807px) に収まるよう、上から下へ流す形にした (主力の図 742px・上乗せの図 467px)。左から右へ流す形や、上乗せを同じ図に入れた形は 978〜1,085px になり、顧客の側が枠の外に出た。mermaid の `classDef` / `linkStyle` の色は `#hex;` と書くと文字参照と読まれて図が出ないので、行末に `;` を付けない
- 事業の形と収益の取り方は SolvioraX 事業概要 v1.7 (2026-09-06) と月次試算表 v3.2、原価はコスト試算 (廃液・燃料) の 2026-09-14 時点。**金額は書き写さない (§3.5)** ので、試算の数字には時点を添え、最新はコスト試算で見ると書いた。コスト試算の売上に入っているのは定期利用料 (500円/m³) だけで、装置代・保守料・金属の代金は入っていないことも書いた
- まさの回答の反映: 事業の形は案C (既存の処理業者と組む) で試す (評価の「決まったこと」「次にやること」、選択肢の本文・要約と評価の行の C のマス、確認事項)。オフサイトの液の濃さは実測が無いので仮置きのまま。評価の会社設立の時期を、ゴールツリーの「NewCo設立」の期限 (2027-04-01) に直した (428 では「2027年3月予定」と書いていた。事業計画タブの `SX_INCORPORATION_DATE` と資本政策は 2027-02-01 だったが、2026-09-17 まさ「設立は20270401」で migration 445 がそろえた)
- 生成スクリプト (scratchpad `gen_bm2.py`、コミットしない) が止めるもの: 足すトピックがすでにある、書き換える要約・本文・マスが 428 を当てた後に書き換えられている、ビジネスモデルのトピックが11件でない

### SOL ビジネスモデル: 先頭の図をピクト図にし、案C の中身を役割で書く (migration 443 / 2026-09-17)

まさ (2026-09-17、429 の図を見て):

> ビジネスモデルは、ちゃんとピクト図にしてほしい。カネとモノの流れを示す矢印はそれぞれ別の色にして、カネのところはお金っぽいアイコンを添えて。
> 上乗せ売上の「処理業者の工場に菌の工程を入れる」が何を指してるのかが理解できない。廃液回収事業のこと？

- `ptt_sol_bm_overview` の mermaid の図2枚を、ピクト図 (§5.6) 1枚にした。真ん中に SolvioraX、左に愛媛大学 (菌株・培養技術 → / ← ライセンス料)、上に装置の製造委託先 (処理装置・培養設備 → / ← 製造委託費)、右に排水を出す工場 (装置・菌体・保守 → / ← 装置代・定期利用料・保守料 / ← 金属入りの菌体)、右下に金属の買い手 (← 金属の代金 / 取り出した金属 →)、下に既存の廃液処理業者 (菌体・処理装置 → / ← 利用料)、左下に廃液を出す工場 (廃液・処理費 → 処理業者)、右上に燃料の買い手 (バイオ燃料 → / ← 燃料の代金)。オフサイト (案C) の2者と燃料の買い手は計画中 (破線)。図の下に読み方 (色・破線・誰が払い誰が受け取るか・未定のこと) を書いた
- **案C の中身を役割で書いた。** 廃液の回収・運搬・処理は、許可を持つ既存の処理業者 (いまの許可と工場のまま)。SolvioraX は、業者の工場の中和・沈殿の後に置く「菌の処理段階」へ菌体と処理装置を納め、菌が取り込んだ金属を取り出す。概要に「オフサイト（案C）の役割分担」の表 (廃液を出す工場・既存の廃液処理業者・SolvioraX) を足し、同じ言い回し (「菌の工程を入れる」「菌の工程を組み込む」) だった評価の「次にやること」、確認事項の処理業者の行、選択肢の C の中身 (`pte_sol_bm_opt_013`) もそろえた
- 収益の柱の「いまの状態」を、コスト試算の 2026-09-17 時点の値にした: オンサイト・直接投入の総コストは 色素分解 135.0円/m³ (自然株)・139.3円 (強化株)、金属回収 913.2円・749.2円。「排熱利用可能」「排ガス利用可能」「工場の排液を培地に使える」の3つを入れると 32.9円・35.2円 / 209.5円・180.5円。燃料は 基準・委託 7,785.8円/L、3つを入れると 1,163.0円/L。429 の値 (色素分解 54.2円、金属回収 528.3円、燃料 6,033.5円/L など) は、その後の培養の加温・電力 (435)、菌体のマスバランス (437)、排液で増える速さ (438) で変わっていた。**金額は書き写さない (§3.5)** ので、時点と「最新はそれぞれのタブで見る」を添えた
- 成り立つための条件を、ゴールツリーの今の問いにした (「UEが成立するか」に色素分解・金属回収がまとまり、「オンサイトPoCの実現」が加わった。「営利100億いくか？」の下に「バイオディーゼル事業は成立するか」「他のマネタイズポイントの洗い出し」がある)
- 表のマスは「決定」「超過」のように名詞で終える形にした
- 生成スクリプト (scratchpad `gen443.py`、コミットしない) が止めるもの: 書き換える要約・本文・マスが 429 の後に書き換えられている、ビジネスモデルのトピックが11件でない、概要にピクト図が無い・mermaid が残る、区分「ビジネスモデル」の本文に「菌の工程」が残る。ピクト図の定義は `check_pictogram.mts` がこの migration から取り出して並べ、重なりが無いことを確かめる
- 追補 (migration 444、同日): 本番の画面の確認で、選択肢の「C × 要る許可」の要確認の理由 (`pte_sol_bm_opt_023.check_reason`) に「菌の工程を提供する」が残っていたので、同じ言い方 (中和・沈殿の後に菌の処理段階を置き、SOL が菌体と処理装置を納めて金属を取り出す) にそろえた。443 の検査は本文と C の中身だけを見ていた。444 の最後で、SOL の技術台帳のトピックと行の全列に「菌の工程」が残らないことを確かめる

## 7. 未接続 (次の一手)

| 項目 | 状態 |
|---|---|
| 議事録からの自動抽出 | 未接続。いまは `project_knowledge` の断片を人が写す。抽出器が `condition` の項目と数値を候補として直接積むところまで進める |
| 到達実績と `project_management_technical_tests` の接続 | 未接続。試験結果を `source_ref` で参照し、値の二重管理を避ける配線を入れる。CXは試験が0件で、TRL4の5項目 (`ptt_cx_trl_gate`) を試験として起票するところから |
| PJワークスペース (社外向け) への露出 | 出さない。`confidentiality` を持たせてはあるが、外部ワークスペースアカウントへ見せる導線は作っていない |
