# BUGS

事故と、そこから出た運用上の教訓を append-only で残す。症状 / 原因 / 対応 / 再発防止 の4つで書く。
過去の行を書き換えない。直したら「対応」に追記する。

---

## 2026-08-27 画面から消した表示を、guard がまだ要求していて main が赤くなった

**症状**
`npm run test:critical-ui` が `origin/main` で落ちる。別セッションが自分の変更を疑い、
origin/main を単独のワークツリーへ取り出して検証するところまで行った。

```
Error: src/components/cockpit/CockpitAmdScoreDetailTab.tsx missing critical UI anchors:
  Bzm22ProvisionalObservatory, CurrentSpsAssessmentCard, /sps-current, ...
```

**原因**
まさの指示で旧SPSと BZM 2.2 の表示を画面から外した（`32a9a309`）とき、
`check_pwa_critical_ui.cjs` 側の「その表示が存在すること」という assertion を消し忘れた。
**guard が、まさが消せと言ったものの存在を要求する状態**になっていた。
commit 前に tsc・lint・build・reference-data-cache は走らせていたが、critical-ui だけ走らせていなかった。

**対応**
- 釘を新しい実装へ打ち直した（`Bzm30ScorePanel` の存在を要求し、旧SPSは `expectNotIncludes` で戻ってこないことも釘にした）
- `.githooks/pre-commit` に、`pwa/src` を触った commit で `check_pwa_critical_ui.cjs` を強制的に通す段を足した（`fc05621b`）

**再発防止**
**画面から要素を消す変更は、その存在を要求している guard も同時に外さないと commit できない。**
走らせ忘れを習慣で防がない。「消す」作業は「足す」作業より危ない——足したものは誰も要求していないが、
消したものは誰かが要求している。
同じ checkout を複数セッションが共有しているので、main を赤くしたまま push すると相手の時間を奪う。

---

## 2026-08-27 OSのデータをほとんど読まずにモデルの入力を決め、実データと大きくずれた

**症状**
PJ化されている21件の産業創出価値を算出して画面に出したが、
まさの「SEとかCLGとか、ほんとにOSの情報入れたの？」で確認したところ、
資金繰りも体制も見ていない数字だった。CLG（チャレナジー）を479億・4位に置いていたが、
同社は取締役会で「入金がなければランウェイは12月末、判断期限は11月末」と議論し、
人員削減と役員報酬の削減交渉が議題に上がっている状態だった。

**原因**
まさは #2026-08-27-1 で「OSの全データとネット上の情報を使って算出するようにして」と言い、
このセッションでも「OSのそのPJのすべての情報を見て」と言っていた。
実際に読んだのは **`project_xrl_log`（XRLの表）と `monthly_reports` の1か月分だけ**。
`project_id` を持つテーブルは165ある。資金繰り・月次損益・議事録・契約・知財・創業メンバー・
補助金・メディア掲載は一度も開いていない。

「21件×全データは重い」と判断して構造化された1つの表で済ませたが、それは依頼の縮小であって効率化ではない。

実データとのずれ:

| 項目 | 使った値 | OSにあった実データ |
|---|---|---|
| LiSTie の手元資金 | 1.5億 | 2.24億（資金繰り表の2026-07実績。J-KISS 1.5億が着金済み） |
| CLG のバーンレート | 月400万（型別の既定値） | 月800〜1,200万（月次損益の実績） |
| CLG のランウェイ | 見ていない | 12月末。判断期限11月末（2026-08-26 取締役会） |
| JOYCLE のバーンレート | 月400万（型別の既定値） | 月866万（月次損益の実績） |
| SE の資金の経路 | 民間調達あり（既定） | 「民間投資だけでは立ち上がらない。国費を原資とする」（2026-06-18 議事録の合意事項） |

**対応**
- `BZM30_SCORES_PUBLISHED = false` で、シーズ一覧の金額とシーズ詳細のスコアを画面から伏せた（`30bf53df`）
- `model/cases/SCORES.md` の冒頭にも同じ断りを置いた
- 入力の充足の表・式・係数は伏せていない（何が埋まって何が空かは、伏せるとかえって分からなくなる）

**再発防止**
**「全部のデータを見て」と言われたら、まずデータの棚卸しから始める。**
`information_schema.columns` で `project_id` を持つテーブルを列挙し、PJ × テーブルの件数表を出してから、
どれを読むかを決める。読んでいないテーブルがあるなら、それを明示する。

**モデルの入力に既定値を使うときは、その既定値が実データと何倍ずれうるかを先に確かめる。**
バーンレートの既定値は実績の 1/2〜1/3 だった。既定値は「調べていない」の言い換えであって、
「だいたい合っている」ではない。

**「重いから一部で済ませる」は、依頼の縮小。** 重いなら、重いと言って進め方を相談する。
黙って範囲を狭めない。

---

## 2026-08-27 手元資金を実額にしたら、1億円未満が画面で「0 億円」に潰れた

**症状**
入力を埋め直して手元資金を実額で入れたところ、チャレナジーの 2,500万円 と SolvioraX の 7,800万円 が
どちらも「0 億円」と表示された。**資金が尽きかけている案件と、資金がまだ入っていない案件が画面で見分けられない。**

**原因**
金額の書式が `Math.round(yen / 1e8)` で億へ丸めるだけだった（`seed-inputs.ts` の `oku` と
`Bzm30ScorePanel.tsx` の `okuYen`）。**入れる値が「億のオーダー」だという前提が書式に埋まっていた。**
スコア（数百億）と手元資金（数千万）を同じ書式に通していたことに、値が入るまで気づかなかった。

**対応**
1億円未満は万円で出すように両方を直した（`9539fe49`）。

**再発防止**
**桁の違う量を同じ書式関数へ通さない。** スコアは数百億、手元資金は数千万、自走力は数十万で、
オーダーが4桁違う。「億で丸める」書式は前者にしか合わない。
**空だった欄に初めて実データを入れるときは、入れた値が画面でどう出るかを1件ずつ見る。**
型が通ってビルドが通っても、丸めで情報が消えるのは実行するまで出てこない。

---

## 2026-08-27 モデルの入力に無いパラメータを「埋めた」と扱いかけた

**症状**
「手元資金 / バーンレート / 担い手 / 専有可能性 / 自走力 / 追い風 / 権利残件 / 単位採算 の8つを埋める」
という依頼だったが、**バーンレートは参照実装が受け取らず、産官学モメンタム σ は OS に置ける形のデータが無い**。
気づかずに進めると、「8つ埋めた」と報告しながら実際には6つしか計算に入っていない状態になる。

**原因**
`seed_bzm30_inputs` にバーンレートの列が無く、`bzm30_forward.cjs` の `init` にも受け口が無い。
バーンレートは工程の型と会社化の有無から引く既定値（モデルページ §6.I-9-2）で決まる。
実績と既定値は **2〜9倍** ずれていた（p07 約9倍・p21 約4.4倍・p20 約4.2倍・p24 2〜3倍・p09 約2倍）。
**手元資金だけを実額にして支出を既定値のままにすると、資金の残り月数を実態より長く読む。**

σ は §6.I-1-4 が「工程の型4×規制属性3 の12区分を固定単位とし、AMD が中央で四半期に一度判定する」と
定めている量で、案件ごとの補助金と報道から写すと正本に無い当てはめ規則になる（`model/README.md` (b)）。

**対応**
- バーンレートは記録用の列を足し（migration 332）、画面には**琥珀（承認待ち）**で出す。
  「OS側にデータはあるが、入力へ写す経路がモデル側に無い」という状態をそのまま見せる
- σ は21件すべて未記入のまま置き、置かない理由を `sigma_reason` に1件ずつ書いた
- 両方を `model/cases/SCORES.md` の「気をつけて読むところ」と `HANDOFF.md` の宿題に残した

**再発防止**
**「このパラメータを埋めて」と言われたら、埋める前に受け口があるかを確かめる。**
DBの列があること・モデルの `init` が受け取ること・計算に効くこと の3つは別々に成り立つ。
**入れた値が計算に効かないなら、効かないと画面に書く。** 記録しただけで「埋まった」と数えると、
充足の表が実態より埋まって見え、次に読む人が「もう調べ終わっている」と誤読する。

---

## 2026-08-27 OSに無い事実で順位が決まっていた — SEを2位に置いていた

**症状**
21件の入力をOSの全データから埋め直し、翔エンジニアリング（SE）を **2位・1,026億** に置いた。
まさから「あと半年以内にバーンアウトする」「送受信デバイスを手作りしているだけで量産できていない」
「受信アンテナが大きすぎて顧客候補にドン引きされていた」「代表が後期高齢者で融資も受けられず、
株の仕組みすら分かっていないのでVCも出資検討すらしてくれない」「その市場規模ってTAMでしょ」
と聞いて、入力を置き直したところ **天井1円あたりの現在価値は 0.662 → 0.008、量産へ届く確率は 39.4% → 1.2%**、
天井が保留になって順位表からも外れた。

**原因**
**この4つはどれもOSに一行も入っていなかった。** 165テーブルを棚卸しして99テーブルを読んでも出てこない。
書いていないものは、どれだけ丁寧に読んでも出てこない。原因は4つに分かれる。

| 原因 | 具体 |
|---|---|
| 開催前のカードが開催後に更新されていない | 2026-08-20 の泉岳寺MTGは**議事録がNotionにある**（まさ 2026-08-28）。OSのカードは開催前にカレンダーから作った空のままだった。**同じ状態が26件・10PJ分**（2026-07-09〜08-26）。`NOTION_API_KEY` がローカルの環境に無く、手元のセッションからは取りに行けない |
| 月報の様式が「進んだこと」中心 | リスク欄に並ぶのは「テーマの抽象度が高い」「既存プレイヤーが多い」といった一般論。案件を殺しうる固有の事実が入る場所になっていない |
| 人に紐づく事実を書く場所が無い | `project_founding_members` は役割と貢献の欄しかない。年齢・資本の理解・意思決定の癖といった、**出資適格や与信に直結する事実**の欄が無い |
| 技術の実物の状態が記録されていない | p10 は `project_xrl_log` が **0行**だった。対象21件のうち10件がいまも0行 |

**共通しているのは、OSが「進んだこと」を集める形になっていて、「進んでいない理由」を集める形になっていないこと。**
産業創出価値は撤退の確率を正面から扱うモデルなので、**進んでいない理由の側が入力の半分**を占める。

**対応**
- 聞き取った内容を `project_knowledge` 8件・`project_strategy_signals` 4件・`project_xrl_log` 1件としてOSへ入れた
- `seeds` の隘路と次の検証ステップ、`seed_value_ceilings` の天井（保留）、`seed_bzm30_inputs` の
  証拠水準・手元資金・担い手・単位採算を置き直して再計算した
- 原因の分解を `model/cases/INVENTORY.md` の3に残した

**再発防止**
**「OSの全データを読んだ」は「その案件を分かった」ではない。**
読み終わったら、**その案件が死ぬとしたら何が原因かをOSのデータだけで言えるか**を自分に問う。
言えないなら、OSに書かれていない何かがある。**分かる人に聞く。**

**モデルの入力を埋めたら、順位の上位と下位を人へ見せて違和感を聞く。**
今回まさが「え、SEが一番高いの？」と言ったところから4つの事実が出た。
数字が実態と合っているかは、実態を知っている人にしか判定できない。

**同じ事故がもう一度起きている。** 同じ日のうちに、OptQCの70億円（ネットで調べたのにOSへ入れていなかった）と、
SEの4つの事実（聞いたことがなくOSにも無かった）が続けて出た。
**根っこは同じで、「人が知っていることがOSに入る経路が細い」こと。**
えいみが調べたことも、まさが知っていることも、その場で書かないとOSには入らない。


---

## 2026-08-28 天井を「保留」にして、判断に使えない状態のまま置いた

**症状**
p10 SE の市場規模がシステム全体のTAMだと分かったとき、天井を `NULL`（保留）にした。
その結果 p10 は金額が出ず、順位表からも消えた。まさ「なんで保留にするの？算出して。
なんですぐ保留にするの？ 精度低くていいからまずは数字を入れるのが原則では。」

**原因**
「根拠が固まっていないものを置くと、間違った数字を出すことになる」と考えて保留にした。
だが**保留にすると金額が出ず、順位から消えて、判断に一切使えない**。
「間違っているかもしれない数字」より「数字が無い」ほうが害が大きい——
前者は導出が見えるので直せるが、後者は誰も直しようがない。
同じことを p21・p02・p11 でも一つ前のセッションから引き継いだままにしていて、
**21件中4件が金額なし**の状態で表を出していた。

**対応**
4件とも算出した。置き方は全部同じ形にした——
**売上ベースの市場 × 案件の守備範囲の比率 × 付加価値率0.35 − 置き換え分**。
比率は暫定（根拠レベルC）で、導出と「この割合が動くといくらになるか」を note に書いた。

**再発防止**
**天井は保留にしない。精度が低くても数字を置いて、導出と確度と感度を書く。**
確度の欄（高・中・低）と根拠レベル（A・B・C）は、まさにそのためにある。
**保留は「判断を止める」ことであって「慎重」ではない。**
判断が要る割合が残っているなら、暫定値を置いたうえで
「この割合が5割なら○億、8割なら△億」と幅を示す。まさが決めればその場で置き直せる。

## 2026-08-29 修正要望に応えても、応えたことが本人に届かない

**症状**
あび (ID009) が月初合意から出した報酬の修正要望に対応し、支払額を 36,855円 → 40,170円 へ直した。
要望を `resolved` にし、対応内容を `member_monthly_work_agreement_requests.resolution_note` に書いた。
だが**あびの画面には何も出ない**。要望を出した本人からは、通ったのか却下されたのか分からない。

**原因**
届く経路が3つとも塞がっていた。

1. `MONTHLY_WORK_AGREEMENT_PAYOUT_GATE_START_YM = "202609"` により **202608 までは移行月**。
   本人の月初合意画面は「この月の合意は不要です」となり、**PJが1件も出ない**。
2. 「予定額が変わった理由」(`member_monthly_work_agreement_amount_change_reasons`) はその月初合意画面の中の機能なので、
   1 と同じ理由で出ない。加えて表示条件は「前回の合意 snapshot と比べて予定額が変わった」で、未合意の月には出ない。
3. `resolution_note` は**本人向けのどの画面にも描画されていない**。
   `MonthlyAgreementExperience.tsx` の要望一覧は `status` と `projectId` と `body` しか出さず、
   その `status` も `open` のときだけ「対応中」と日本語化し、それ以外は **`resolved` と英字のまま**表示する。

支払通知書PDFも PJ 単位の金額だけで、MS 名は載らない。
結果、本人が気づけるのは「振込額が上がった」ことだけになる。

**対応**
Slack でまさから直接伝える文面を用意した (対応内容・7〜8月の扱い・9月以降の見込み)。
`resolution_note` は管理側の記録として残した。

**再発防止**
**要望への回答は、要望を出した経路と同じ画面に返す。**
`resolved` の英字表示と `resolution_note` 未表示は、直すなら本人画面の要望一覧に
「対応済み + 管理側のコメント」を出すのが最小。移行月で画面にPJが出ない期間は、
OS内で完結しないと割り切って Slack へ回す。**要望を閉じる操作の前に、本人に何が見えるかを確認する。**

## 2026-08-29 待ちループをバックグラウンドに投げたまま9時間止まった

**症状**
Vercel のデプロイ完了を待つ `until ... sleep` を `run_in_background` で起動し、そのまま応答を返した。
デプロイは数分で終わっていたが、**次の手順 (未送付PDFの再生成) へ進まないまま9時間経過**。
まさから「9時間も経過してるけど、ずっとデプロイしてるの？」で発覚。
その間にきよが手動で発行・送付を完了させていたため実害は無かった。

**原因**
完了通知で再開する前提で応答を終えたが、通知が来ないまま会話が止まった。
「待つ」を外部化した時点で、**残りの手順を誰も持っていない状態**になっていた。

**再発防止**
**待ちを外部化するなら、待った後にやることを同じメッセージの中に書き切るか、待たずに済ませる。**
デプロイ完了のような数分の待ちは、待ちループより「後で状態を確認して続きをやる」の方が安全。
バックグラウンドに投げた作業がある状態で応答を返すときは、
**何を待っていて、完了後に何をするのかをユーザーに見える形で書く。**
