# HANDOFF

最終更新: 2026-08-25 JST
対象: モデル層 `/model` — **係数の分野別初期値 v3 がまさの承認待ち**。承認後に active PJ のスコアを算出する

## 今回のセッション（2026-08-25）— 係数の分野別初期値と、前向き計算の参照実装

active PJ のスコアを BZM 3.0 で出すには、先に**係数の分野別初期値**を決める必要があった。3案まで作り直した。

1. **v1**（起案）: 値を表に並べただけ。5属性の監査で**最重要指摘34件**。
   閉包が閉じておらずスコアを1件も計算できない（$\phi_u$・$\alpha_u$・民間調達の過程・$\mu$ の割り付け・応募回数の規則が未定）。
2. **v2**: 監査が揃って「紙の表では確かめられない」と要求したので、**前向き計算の参照実装**
   （`model/tools/bzm30_forward.cjs`。格子上の数値計算で、乱数の試行は用いない）を作って検査した。
   閉包検証（経済学者はコードを読む＋実務横断）で**新規14件**。
   **報告した数値そのものが収束していなかった**（格子を細かくすると $V$ が 44% 動く）、
   申し出の価値づけが §6.A-2 に反する、弾力性の表がラベル違いで結論が逆。
3. **v3 ＋ 補訂1**: 格子をバーンレートに合わせて近似誤差を **1.6〜2.4%** まで下げ**測定値として表示**、
   申し出の価値を承継者の到達確率と残り月数で割り引く、実装に入っていない機構を近似 A1〜A15 として明示。
   第3ラウンド（記述と実装の突き合わせ）で**縮退検査12行・弾力性22行・幅・感度がすべて実装から再現**することを確認し、
   残った15件の誤りを直した。

**計算で分かったこと**

- 較正の最優先は**資金調達（採択率・機会の到来率）**。どの型・規制でも首位（弾力性 +1.3〜+1.5）
- **権利・承認の解決率 $\beta$ の弾力性はほぼゼロ**（−0.00）。産連の実務が最も細かく論じた量は、スコアをほとんど動かさない
- **F1×REG-2（医薬品型）は評価期間240か月内の M4 到達が構造的にゼロ**。価値はライセンスアウトから出る。
  ただし継続価値の比率が 76.9%、$q_{\mathrm{lic}}$ が根拠レベル C なので、**REG-2 は「承認」ではなく「保留」を提案**
- **継続価値の比率が過半の組が5つ**（§6.E-3 の規則で個別レビュー対象）
- **受託だけで資本自立する経路が構造的に生成されない**（要件3 の実装の弱さ。I-13 に記載）
- AMD の台帳の穴: **助成金51件に不採択が0件**（$\phi$ が較正できない）、
  **月次損益が4件・うち2件は将来の計画値**（$\mu$ が実測で置けない）

**まさの判断待ち（3件）**

1. **モデル本体の改訂8点（K1〜K8）の承認**。最大のものは K1 ——
   §6.B-1 がエバンジェリスト機能を「投資家・審査側の判断を動かす」と定義しているのに、
   $\eta_t$ は前進確率にしか乗らず、**その空席が採択率にも申し出到来率にも効いていない**。
   AMD が担い手を供給する価値が、最も効くはずの経路でスコアに乗っていない
2. **根拠の8割が「暫定の置き」の状態で、旧 SPS を置き換えてよいか**（暫定で出す／較正してから／両方）
3. **REG-2（創薬）を保留にしてよいか**

## 次のタスク

まさの判断（上の3件）を受けてから、**active PJ の観測状態と案件パラメータを §6.C の様式で埋め、スコアを算出する**。
判断を待つあいだに進められるもの: 各 PJ の工程の型（F1〜F4）× 規制属性（REG-0〜2）の割り当て。
下見の段階の見立て（**要確認**）: p07 LST・p21 SX は F1×REG-0、p06 CTB は F1×REG-2、
p20 CX は F2×REG-0、p24 CLG は F2×REG-1。p02・p10・p26・p29 は未調査。

## 関連文書

- `model/proposals/2026-08-25_bzm30-coefficients-v3.md` — **提案の本体**（v1 はスタブ化済み。v2 は git 履歴に残る）
- `model/proposals/2026-08-25_coefficients-audit-round1.md` / `-round2.md` / `-round3.md` — 監査3ラウンドの記録と対応表
- `model/proposals/2026-08-25_coefficients-check-results.md` — 参照実装の出力
- `model/tools/bzm30_forward.cjs` — 参照実装（`degen` / `conv` / `calib` / `sens`）

## 前回のセッション（2026-08-24）

運用一式 第一便を採用（#2026-08-24-11。様式4点＋値3つ）／正本の置き場を設計変更（#2026-08-24-12。
BZM 3.0 の全定義を台帳 §5、運用一式を §6 へ統合）／用語と表現を精査（#2026-08-24-13。比喩を全廃）。
詳細は `pwa/design_log/sessions_2026-08.md`。

## Repo 状態

- HEAD は push 済み・origin と同期。未コミットは他セッションの `pwa/scheduled-tasks/.../SKILL.md` のみ（触っていない）。
- 途中で別セッションの push（Tally 同期）と衝突 → merge で統合済み（`model/` とは非重複）。
- ロック12件（台帳・CURRENT.json・`bzm/` の旧正本10件）。検証3本（型検査・critical-ui・式の正本照合）は通過。

## 正本と参照（2026-08-24 設計変更）

- **正本はモデルページ `/model` 本体、ただ一つ**。BZM 3.0 の全定義は §5、運用一式は §6。別ファイルへ切り出してリンクしない。
- **同じ定義を二か所に書かない。** `model/proposals/` は提案中のものだけ。ロックに `model/proposals/` を入れない
  （`model_lock.cjs check` が落とす）。規律は `model/README.md` (a-2)・`pwa/manual/9-2-developer.md`。
- 承認: `model/APPROVALS.md`（#2026-08-24-13 まで）／開発履歴: `pwa/design_log/sessions_2026-08.md`／変更履歴: `bzm/9-5-appendix-changelog.md`
- 経緯の文書（監査報告・改訂1 の記録）は定義を持たないので別ファイルのまま。
- ページ下部の「すべての式・すべての記号」は**旧 BZM 2.2 系列**（画面に札あり）。入れ替えは実装移行と同じ段。

## 第二便の残り（スコア更新の後に続くもの）

1. 735件（active 以外）の判断帯からの移行手順、式の層への 3.0 統合
2. 較正計画の実施順（AMD 接触台帳＋公開情報）
3. 第一便からの申し送り: δ_u の時間形・φ_u の残存期間逓減・技術の核の恒久喪失の消失接続・逓減割引・
   Morgeson 15機能との対応表・組み合わせレバーの検知・AMD 組織外からの牽制
4. 監査の軽微残: P1・P2（`model/proposals/2026-08-24_*audit-round*.md`）
5. 別の理論（要件から除外済み）: AMD 貢献度、ECR、ラウンドテーブル

## 守る運用ルール

- モデルページにはまさが合意した内容だけ。根拠は `[根拠](#evidence "…")` の印（本文に引用を書かない。印の中で `"` を使わない）。
- **比喩・ぼかした表現を使わない。** 記号と期間はその場で定義する。
- 「Aではない」を正本に書かない。「採らない主張」に載せるのはまさが棄却した候補だけ。
- 正本変更: 提案 → 本文で提示 → APPROVALS へ引用つき記録 → 変更 → `node pwa/scripts/model_lock.cjs relock --approval <id>` →
  同じ commit で `bzm/9-5-appendix-changelog.md` に1行。
- 監査は5属性（経営学者・経済学者・DTSU 経営者・VC・大学産連本部長）。学術2体は重いモデル、実務3体は軽いモデル。対応表は正確に書く。
- まさへの説明は平易に。詰め込むと差し戻しになる（たとえ → 役割 → 式との対応、の順）。
- 共有 checkout（常時5〜10並行）: 着手前 `git fetch --all --prune` → log → status。`git add` はパス名指し、stage と commit は1コマンド。
  他セッションの未保存ファイルに触らない（衝突時は rebase でなく merge）。他セッションへメッセージを送らない。

## 検証手順

```
node pwa/scripts/model_lock.cjs check
cd pwa && npx tsc --noEmit && npm run test:critical-ui && npm run test:model-formula-canon
```

本番反映は `main` への push（Vercel Git 自動 deploy）。モデルページは admin 限定。
