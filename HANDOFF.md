# HANDOFF

最終更新: 2026-08-24 JST
対象: モデル層 `/model` — 理論の目的・要件・既存理論の土台・スコアリング模型の構築（理論の議論が主）

## 今回の到達点

まさが決めた順序「1 目的 → 2 要件 → 3 既存理論を論文から持ってくる → 4 足りない部分だけ作る」のうち、
**1・2・3 が閉じ、4 は第3案（5属性×3ラウンドの批判監査済み）まで到達して、まさの採否待ち**。

- **目的3件・要件12件が確定**（モデルページ §1・§2）。要件の定義は「目的を達成するために必ずこれを考慮すること」。
- **手順3（既存理論）を完了**。要件12件それぞれの土台を選定し、二重批判監査を通した（`/model/2026-08-23_step3_giants-shoulders`、付録A〜F）。
  文献の選定はえいみの責任で確定（まさ「既存理論にあるかないかとか聞かれても、見てないから分からんよ」）。
- **手順4 のスコアリング模型を第3案まで構築**（`/model/2026-08-24_step4_scoring-model-v3`、補訂1 済み）。
  監査は経営学者・経済学者・DTSU 経営者・VC・大学産連本部長の5属性×3ラウンド（P0 39→19→10件、全対応）。
- **まさが合意済みの成果**: スコアの最上段の式 $V=\int \mathbb E[\Pi(\omega)\mid\theta]\,dB_0(\theta)$（モデルページ §0）と、
  産業創出効果の数え方＝純増（国内の既存事業の置き換えを差し引く）＋前倒し期間と国内立地差（モデルページ §3）。
- 開発は3件だけ（根拠の印のマウスオーバー表示、式の層のポインタ修正、抜粋ブロックの削除）。詳細は design_log。

## 正本と現在地

- **モデルの正本は OS のモデルページ `/model`**（画面に出ていないものは正本ではない。会話では「モデルページ」と呼ぶ）
- まさの承認記録: `model/APPROVALS.md`（#2026-08-24-9 まで）／運用規約: `model/README.md`
- 提案（未承認・正本ではない）: `model/proposals/`（手順3 は `reference`、手順4 第3案は採否待ち、旧 SPS 改訂案は保留）
- 開発履歴: `pwa/design_log/sessions_2026-08.md`／バグと教訓: `pwa/BUGS.md`／変更履歴: `bzm/9-5-appendix-changelog.md`
- 開発者向けの運用: `pwa/manual/9-2-developer.md`（正本の定義と根拠の印の書き方を記載）

## Repo状態

- branch は `main` のみ、origin と同期済み。HEAD は `a934b1c9`。今セッションで作った branch・worktree は無い。
- 未コミットは他セッションのもの（`pwa/scheduled-tasks/.../SKILL.md`）。触っていない。
- **共有 checkout（常時5〜10セッション並行）**。着手前に `git fetch --all --prune` → `git log --oneline -15` → `git status -sb`。
- `git add` はパスを名指しし、stage と commit を1コマンドにまとめる（staged のまま待つと他セッションの commit に巻き込まれる）。
- 他セッションへメッセージを送らない。調整は repo 内の記録で行う。

## 未解決

1. **手順4 第3案の採否**（最優先）。まさが合意したのは最上段の式と産業創出効果の数え方まで。
   盤面・過程・係数の設計は監査済みだが未承認。
2. **採否が出たあとの次の段**: 標準関門表・機能分解表・登録簿・計画規則テンプレの様式、
   κ（関門の基準速度）・d（空席の遅延）・φ（採択）・ν（申し出到来）・β（承認解決）の分野別初期値、
   社会的割引率の値、継続価値 C の恒久形、資本政策の仮定様式、735件の判断帯からの移行手順、較正計画（第3案 §12）。
3. **軽微な残課題**: 監査の P1 相当31件・P2 相当21件（`model/proposals/2026-08-24_step4_audit-round1〜3` に保持）。
4. **別の理論として構築するもの**（要件から外した）: AMD の貢献度の測定、ECR、ラウンドテーブル。

## 次の最初の行動

**まさへ手順4 第3案の採否を仰ぐ。** 採用なら `model/APPROVALS.md` に引用を記録 → 第3案の状態を承認済みへ →
模型の要約をモデルページのどこまで載せるかを提案 → 次の段（様式・初期値・較正）へ。
差し戻しなら指摘を第4案へ。不採用なら `model/withdrawn/` へ移す。

**やってはいけないこと**: まさが合意していない内容をモデルページへ書く／理論・文献の選定や文書の書き方をまさへ質問する
（聞くのは要件の意味と価値基準だけ。専門語を使わず具体例で書く）／「Aではない」を正本に書く／監査を通していない設計を出す。

## 検証手順

```
node pwa/scripts/model_lock.cjs check
cd pwa && npx tsc --noEmit && npm run test:critical-ui && npm run test:model-formula-canon
```

本番反映は `main` への push（Vercel Git 自動 deploy）。モデルページは admin 限定。
