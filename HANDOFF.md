# HANDOFF

最終更新: 2026-08-23 JST
対象: モデル層 `/model` の新設と、BZM/SPS の目的・要件の再構築（理論の議論が主、実装は器づくり）

## 今回の到達点

まさの「モデルの正本がどこにあるか把握できていない」「勝手にモデルが書き換えられたことに強い不安」から始まり、
**モデルを教科書から分離した層 `/model` を作り、目的と要件をゼロから積み直す**ところまで進んだ。

- **`/model` を新設**（admin 限定）。教科書 `/bzm` は本の原稿、設計書 `/spec` は実装仕様、モデルそのものは `/model`。
  画面は `model/MODEL_VERSION_LEDGER.md` をそのまま描画する（画面側で要約・再構成しない）。
- **承認ロックを3層で実装**。`model/LOCK.json` の正本12件は、まさの承認記録（`model/APPROVALS.md`）なしに
  変更できない。critical-ui guard / `.githooks/pre-commit` / Claude Code の PreToolUse hook。
- **目的を3件、要件を9件まで確定**。まさが一つずつ判断した内容だけを載せている。
- **11要因ルーブリックは「参考情報」へ降格**。まさが正本として合意した記録が無く、735件の評価作業の中で
  使われていただけだった（まさ 2026-08-23「まだ現行と定義しないでほしい。それを正本とした記憶はないので」）。
- SPS 現行正本3本を `sps-current-*` へ改名（旧名の世代番号が「古い版では」という誤解を生んでいた）。
- 忘れられていた要素を3方向から洗い出した（旧9軸 M·P·R·S の全定義 / 教科書の名前つき概念 / 監査と査読の未反映指摘）。
  **自走力（旧 S = 自前の稼ぎ × 集め切る力）が現行から落ちていた理由は「接触前は測れないから」で、
  理論的に不要と判断されたわけではなかった。**

## 正本

- **モデルの正本は OS のモデルページ `/model`**（まさ確定 2026-08-23「正本はmdじゃなくてOSのモデルページと定義しておいて」「おれはUIしか見ないので」）。
  画面が読み込む md は `model/MODEL_VERSION_LEDGER.md`。画面に出ていない内容は正本ではない。会話では「モデルページ」と呼ぶ。
- **まさの承認記録**: `model/APPROVALS.md`（発言の引用つき。relock はこのファイルを検証する）
- **モデル変更の運用規約**: `model/README.md`
- 提案（未承認・正本ではない）: `model/proposals/2026-08-22_sps-propulsion-and-slack.md`
- 開発履歴: `pwa/design_log/sessions_2026-08.md`（2026-08-22〜23節）
- バグと教訓: `pwa/BUGS.md`（2026-08-22〜23節。フックの設計ミス、commit 巻き込み2回、目的の逆算）
- 変更履歴: `bzm/9-5-appendix-changelog.md`

## Repo状態

- canonical checkout: `/Users/masa/projects/AMD/amd-os`、branch は `main` のみ。今セッションで作った branch・worktree は無い。
- 直近の自セッション commit: `3cfc98a7`（要件11・12）、`784a96da`（「何のためのモデルか」ブロックの削除）。push 済み。
- **このリポは常時5〜10セッションが並行する共有 checkout。** 着手前に `git fetch --all --prune` →
  `git log --oneline -15` → `git status -sb` で現在地を再確認する。
- **`git add` は必ずパスを名指しする。** `git add -A` / `git commit -a` を使わない。2026-08-22 に2回、staged のまま
  guard を回している間に他セッションの commit へ巻き込まれた（`68535f38`、`e20f2380`）。
  **stage と commit は1コマンドにまとめ、staged のまま待たない。**
- **他セッションへメッセージを送らない**（まさ明示。届くたびにまさの画面にログが流れる）。調整は repo 内の記録で行う。

## 現在地（2026-08-23 夕方）

まさが決めた順序: 1 目的 → 2 要件 → 3 既存理論を論文から持ってくる → 4 足りない部分だけ作る。
**1 と 2 は閉じた**（目的3件、要件12件、議論中は空）。次は 3。

- 要件の定義: 「目的を達成するために必ずこれを考慮すること」。モデルの中に概念として入れなければ扱えないものは要件に加える。
  個別の確認項目（権利の整理・SHA・ガバナンス）は計算に加える測定項目であって要件の粒度ではない。
- 要件12件: 変換能力／戦略余力／自走力／タイミングの窓／産官学モメンタム／出力は金額／現場の状況をすべて吸い上げる／
  EXIT 形態に依らない／担い手／天井／ユニットエコノミクス（成立しなければ設立すべきでない＝必要条件）／資本集約度。
- まさが触れていない提案2件（えいみの結論。モデルページには書いていない）: 証券クラス別の配分は領域定義 §5 の二階建てで
  BZSF 投資層の担当、順序尺度と比率尺度の混同・後知恵・自己選択・グッドハートは測り方と検証の規律。どちらも要件にしない。
  再提案しないこと。

## 未解決

1. **手順3（既存理論）の提案が承認待ち。** `model/proposals/2026-08-23_step3_giants-shoulders.md`（UI: `/model/2026-08-23_step3_giants-shoulders`）。
   要件12件ごとに土台の既存理論と「手順4で作るもの」を整理し、二重批判監査（経営学／経済学・測定、付録F）を反映した第2版。
   付録A〜D は worker 初稿、付録E は監査で追加・訂正された文献（URL 付き）、付録F は監査報告の全文。
   **まさに決めてもらうこと**（提案 §5）: 要件8「依らない」の意味（物差しが経路を差別しない）、要件7「すべて」の境界（目的1 の情報価値で引く）、
   要件11「必要条件」はまさの設計上の決定として書くこと、領域定義 §7（支援方針条件付きの到達見込み）と要件除外の関係。
   承認されたら APPROVALS に引用 → 手順4 へ。承認されない部分は `model/withdrawn/` へ。
2. **手順4 の出発点**（提案 §4）: 監査で「既存理論にある」と判明した7項目（窓の遅い側、コスト下限、精緻さの水準、桁→崖の回数の関数、
   較正の代替、長期予測の採点、事前の付加価値算定）は既存の枠の移植。新規に作るのは、売上ゼロ・担い手未定で測る手続き、
   変換能力の量の定義、余力の換算則、用途集合と経路集合の関係、担い手の配置を計算に入れる形、要件12 を不足額で効かせる設計、
   評価者が LLM であることの固有課題、時間整合性。手順4 は「この共通の穴（Before Zero 固有の観測単位）を先に定義してから個別の式へ」。
3. **要件ではないが、手順4で扱う論点**（モデルページには書かない。えいみの担当）
   - 天井は固定値ではなく、見えている用途の集合で決まる（教科書 第4章 4.7、機会創造観。Adner & Levinthal 2001、Levinthal 1998）
   - CEO 機能の分解のどこを変数にするか（七分解と Morgeson の15機能の対応表が出発点）
   - 「愛情の総量」は採点変数にせず、実働と判断の記録で見る（改訂要求書 §5.4）
   - 「必須」（エバンジェリスト、ユニットエコノミクス）をゼロにするか強い割引にするか、いつ落とすか
   - ユニットエコノミクスのコスト試算の精緻さ（AACE 18R-97 の見積クラスを根拠 Lv へ写像）
   - 権利の整理・創業者間 SHA・ガバナンスを、変換能力・戦略余力・担い手のどの入力として測るか
   - 誰の割引率か（目的1 の資本コスト／目的3 の社会的割引率）
4. **別の理論として構築するもの**（まさ 2026-08-23 で要件から外した）
   - AMD の貢献度の測定（支援の因果効果、担い手の供給制約）
   - ECR（研究機関のスコアリングシステム）
   - ラウンドテーブル（まだ検討すらできていない）
5. 提案 `model/proposals/2026-08-22_sps-propulsion-and-slack.md` は**未承認のまま保留**。
   式を先に作ったこと自体が、まさの言う順序に反している。手順3 の土台を受けて作り直すか、部品として再利用するかを判断する。
6. 提案をモデルページのサイドバー（CURRENT.json の documents.proposals）に載せるには CURRENT.json がロック対象なので承認が要る。
   いまは直接 URL で読む。

## 次の最初の行動

**手順3 の提案（`/model/2026-08-23_step3_giants-shoulders`）に対するまさの判断を受ける。** 承認されたら APPROVALS に引用を記録し、
提案の「状態」行を承認済みへ更新して（md は `model/proposals/` に置いたままでよい。ロック対象ではない）、手順4 へ進む。
手順4 の最初の作業は「Before Zero 固有の観測単位（売上ゼロ・担い手未定・会社なし）を先に定義する」こと。式はそのあと。

**やってはいけないこと**: 要件に無い要素から式を作ること。作業の都合から要素を逆算すること。
まさが合意していない内容をモデルページへ書くこと。単語や「固定値でない」等の性質を要件として出すこと。
出所を確認していない論文を引用すること。自著（教科書・BZM 2.2）の主張を「文献が示している」と書くこと。
正本が「要件にしない」「採らない」と決めた主張を、別の名前で復活させること（見えない失敗、逆U字の媒介経路）。
