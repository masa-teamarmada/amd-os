# HANDOFF - AMD OS

- Last updated: 2026-05-31 (codex cleanup)
- Canonical root: `/Users/masa/projects/AMD/amd-os`
- PWA root: `/Users/masa/projects/AMD/amd-os/pwa`
- Production URL: `https://amd-os-pwa.vercel.app`
- Current branch: `main`
- Current HEAD: `4195c94 docs(handoff): #101 FRL 2レイヤー化(CES)を design_log 追記 + HANDOFF スリム化`
- Remote state: `main == origin/main` at cleanup start

## Current State

- PWA production is considered Ready from the 2026-05-30 22:33 JST deploy noted by the command thread. No new production deploy was done in this cleanup.
- Main already contains the BZM/ERS/FRL #101 handoff slimming work. The old `feat/bzm-textbook` handoff state is obsolete.
- Local dirty observed during cleanup:
  - `pwa/design/venture_map_demo.md` and spec-related files from another workflow. Left untouched.
  - `pwa/public/bzm/_prxs_9pj.png` untracked generated validation output. Removed locally.
- `git add .` remains prohibited. Stage only known target files.

## Cleanup Decisions

- `pwa/public/bzm/_prxs_9pj.png` is generated by `pwa/scripts/prxs_9pj_inputs.py` as a validation-only, non-distribution figure. Do not formally adopt it unless a future BZM/doc session explicitly promotes it with a non-underscore filename and a page reference.
- Ignore generated underscore BZM PNGs with `pwa/public/bzm/_*.png` so validation runs do not create accidental dirty state.
- PR #2 (`codex/payment-confirm-slack-action`) is still open/draft and should not be merged as-is. It was cut from old base `0d1eb0b`; current main has moved through BZM/ERS/FRL/doc changes.

## PR #2 Safe Handling

Recommended path:

1. Close or supersede PR #2.
2. Recreate from current `main` by cherry-picking/reapplying only the intended 7-file payment-confirm change.
3. Resolve current-main conflicts in `pwa/manual/6-4-finance-payment-confirm-spec.md` and omit/bump `pwa/src/lib/build-info.ts` according to the deploy session.
4. Keep `PAYMENT_CONFIRM_SLACK_INTERACTIVE` off until GAS deploy succeeds.
5. Only after `clasp login` + GAS deploy, enable the Vercel env flag, redeploy PWA, and do Slack end-to-end click verification.

Evidence:

- `gh pr view 2` shows draft/open PR with one commit `dc7027a`.
- PR branch diff from merge-base is 7 files, but `origin/codex/payment-confirm-slack-action..main` appears huge because the branch predates later main work.
- `git apply --check --3way` of the PR diff onto current main applies cleanly for GAS/API/SPEC files, but conflicts in `pwa/manual/6-4-finance-payment-confirm-spec.md` and `pwa/src/lib/build-info.ts`.

## First Read Next Session

1. `AGENTS.md`
2. `CLAUDE.md`
3. `pwa/AGENTS.md`
4. `pwa/CLAUDE.md`
5. `pwa/HANDOFF_pwa_rebuild.md`
6. `pwa/HANDOFF_bzm_textbook.md` if continuing BZM/AMD Score work
7. `pwa/BUGS.md`
8. Relevant `pwa/design_log/sessions_2026-05.md` entries (#97, #100, #101)

## First Next Action

```sh
cd /Users/masa/projects/AMD/amd-os
git fetch --all --prune
git log --branches --not --remotes --oneline
git status -sb
gh pr view 2 --json number,title,state,isDraft,headRefName,baseRefName,mergeStateStatus,statusCheckRollup,url
```

If dirty files remain, identify their owner/workflow before staging or reverting them.
