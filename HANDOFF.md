# HANDOFF

最終更新: 2026-08-26 JST
対象: モデル層 `/model` — **BZM 3.0 での案件ごとの算出。7件が算出済み、3件は天井が保留**

## 今回のセッション（2026-08-26）

まさの承認（[#2026-08-26-1](/model/APPROVALS)）を取り、**正本への反映・係数の統合・参照実装の拡張・p07 の算出**まで進めた。すべて本番に反映済み。

### 決まったこと（M1〜M3）

| # | 内容 |
|---|---|
| M1 | **市場規模は用途ごとに年額の一つの数**で置く。幅を持たせない（幅を持たせると、到達の不確かさと二重に数える） |
| M2 | 旧 SPS の $P^{\mathrm{ind}}$ 帯は**入力に使わない**（$V$ と同じ次元の量なので、割り戻すと出力から入力を逆算する向きになる）。$V$ の上限としての**検算**にだけ使う |
| M3 | 受託の売上は**案件ごとに中身を推定**して置く。型別の一律値は初期値へ降格。上限は億単位まで届く |

あわせて **K1〜K8**（担い手の充足を採択率と申し出到来率にも乗せる、審査の残り月数を状態に追加、ほか）と
**係数の初期値 Q1〜Q27** を承認・反映。係数は §6.I として正本へ統合し、提案ファイルはスタブ化した。

### 作ったもの

- **参照実装に案件ごとの観測状態の入力口**（`model/tools/bzm30_forward.cjs` の `runOne(type, reg, cfg, theta, init)`）。
  `init = { gate, cashMan, rightsOpen, incorporated, underContract }`。省略すれば従来と同じ結果（0.1509 で検算済み）
- **`model/cases/`** — 案件ごとの入力の置き場。[README](model/cases/README.md) に9件の型 × 規制属性の判定

## 算出結果 — [スコア一覧](model/cases/SCORES.md) が正本

| 順位 | PJ | 下限 | 中央 | 上限 | 撤退 | 量産まで | 旧SPS |
|---|---|---|---|---|---|---|---|
| 1 | **p22 OptQC** | 2,561億 | **2,992億** | 3,379億 | 20.3% | 48か月 | 0.5〜70億 |
| 2 | p07 LST | 148億 | **249億** | 332億 | 50.8% | 82か月 | 5.0〜140億 |
| 3 | p10 SE | 100億 | **248億** | 389億 | 78.8% | 119か月 | 0.02〜4.5億 |
| 4 | p24 CLG | 95億 | **190億** | 230億 | 48.5% | 61か月 | 0.2〜15億 |
| 5 | p06 CTB | 17億 | **57億** | 97億 | 67.1% | 181か月 | 1.0〜50億 |
| 6 | p26 VasculaX | 21億 | **51億** | 80億 | 77.1% | 108か月 | 0.03〜2.5億 |
| 7 | p29 KENQ | 9億 | **27億** | 45億 | 81.8% | 127か月 | 0.04〜9.0億 |
| — | p21 SX・p02 r3kt | **天井が保留**（下の判断待ち2） | | | | | |
| — | p20 CX | 天井 706億円／年を置いた（確度は低い）。算出はこれから | | | | | |

**結果の読み方と各案件の理由は [SCORES.md](model/cases/SCORES.md) に書いてある。** 要点だけ:

- **OptQC が突出**（2位の12倍）。市場が9件で最大（年3,952億円）で、2026年8月の70億円調達により**撤退が20%まで落ちる**
- **CTB が5位**なのは制度の長さ。医薬品でゲートが11個並び量産到達まで181か月、評価期間240か月でほぼ届かない
- **現在地が未入力の案件は低く出る**（p10・p26・p29・p02）。入れれば上がる
- **旧SPS と直接は比べられない。** 旧の天井（Lv0）は現在価値、今回は年額で、水準が1〜2桁違う

**天井の一覧と出典は [model/cases/README.md](model/cases/README.md)。** 付加価値率は全件 0.35 の暫定値で、
産業連関表から分野ごとに引き直すのが宿題（医薬品は本来 0.5 前後）。

## まさの判断待ち（2件）

### 1. 反実仮想の控除が効かない

$\alpha_u(t)$ は「評価日から前倒し期間 $L_u$（既定36か月）までは評価日の控除率、以降は立地差だけが残る水準（0.85）」。
ところが**価値が発生するのは M4 到達の後で、それは82〜117か月後**。$L_u$ をとっくに過ぎているので、
**評価日の控除率は一度も使われない**。実際、0.30 → 0.60 → 0.75 と振ってもスコアは1円も動かない（[実測](model/cases/p07_lst.md) §2.5）。

さらに 0.85（貢献として15%残る）は**競合が海外にいる**前提の水準。p07 は競合が国内にいる
（住友金属鉱山が2026年6月にプラント完成・年1万トン処理、JX金属・三菱マテリアルも事業化へ）ので、立地差の貢献も立たない。

**改訂案（まさへ提示済み・返事待ち）**: 立地差水準 $\alpha^{\mathrm{loc}}$ を案件ごとのパラメータにし、
競合が国内にいる案件では 1.0 に近づける。§5.3 の定義文は変えない。§7.1 の Q4 は現在
「立地差水準 0.85、評価日の控除率は案件ごと」としており、**案件ごとにすべき側が逆**。

### 2. 既存産業を置き換える案件の純増をどう数えるか（p21 SX・p02 r3kt）

排水処理場の運転費のうち薬品費は約3割。SX が薬剤を要らなくすると、**工場にとってはコスト削減**だが
**国内の薬剤産業にとっては売上の消滅**でもある。産業連関の考え方では、純増は
「同じ処理をより少ない投入でできるようになった分（生産性の向上）」であって薬剤費の額そのものではない。
**この線で積むと SX と r3kt の純増は大きく下がる。** $\delta_u$ の解釈にあたるので、まさの判断を取る。
記録は [p21_sx.md](model/cases/p21_sx.md) §3.1。

## 型 × 規制属性の割当（9件とも判定済み）

[model/cases/README.md](model/cases/README.md) に一覧と判定の理由。p29 KENQ・p10 SE・p26 VasculaX は仮置きで、
用途が固まると変わりうる（KENQ は航空機用途なら REG-2 まで上がる）。

未算定の active PJ: p19 ZMP・p25 KUTE・p28 NIMS・p30 EHM。

## 次の一手

1. **判断待ち2件**の返事を受けて反映（控除の案件ごと化、置き換え型の純増の数え方）
2. **現在地の入力**を埋める。いま未入力なのは p10 SE・p26 VasculaX・p29 KENQ・p02 r3kt、
   会社化と資金が未確認なのは p06 CTB・p24 CLG・p20 CX。入れればスコアが上がる
3. **付加価値率**を産業連関表から分野ごとに引き直す（いま全件 0.35 の暫定）
4. p20 CX の算出、未算定の active PJ 4件
5. 実装の宿題: **複数用途を1本に畳んでいる**（近似 A16）、$\pi^{*}$ 未実装（A14）

## 正本と参照

- **正本はモデルページ `/model` 本体ただ一つ。** 定義は §5、運用一式は §6（係数は §6.I）、較正の台帳は §7、参考文献は §10
- 承認: `model/APPROVALS.md`（#2026-08-26-1 まで）／規約: `model/README.md`
- 案件ごとの入力: `model/cases/`（[README](model/cases/README.md) に型と天井の一覧、[SCORES.md](model/cases/SCORES.md) にスコア一覧）
- 変更履歴: `bzm/9-5-appendix-changelog.md`／開発履歴: `pwa/design_log/sessions_2026-08.md`

## 守る運用ルール

- モデルページにはまさが合意した内容だけ。提案中の値を、未承認と明示せずに使わない
- 比喩・ぼかした表現を使わない。記号と期間はその場で定義する
- 正本変更: 提案 → 本文で提示 → APPROVALS へ引用つき記録 → 変更 → relock → 同じ commit で changelog。
  **承認エントリの「対象ファイル:」に `model/proposals/` 配下を書かない**（ロックに入って check が落ちる。
  外すときは「削除パス:」節で申告する）
- 共有 checkout（常時5〜10並行）: 着手前 `git fetch --all --prune` → log → status。`git add` はパス名指し。
  他セッションの未保存ファイルに触らない。他セッションへメッセージを送らない

## 検証手順

```
node pwa/scripts/model_lock.cjs check
cd pwa && npx tsc --noEmit && npm run test:critical-ui && npm run test:model-formula-canon
node model/tools/bzm30_forward.cjs degen   # 縮退検査（約20分）
node model/tools/bzm30_forward.cjs conv    # 格子の収束検査
```

本番反映は `main` への push（Vercel Git 自動 deploy）。配信版は `/api/build-info` の `git_sha` で確認する。
