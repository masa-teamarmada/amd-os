# HANDOFF

最終更新: 2026-08-24 JST
対象: モデル層 `/model` — BZM 3.0 の第二便（分野別初期値・735件の移行・較正計画）

## 現在地

まさが決めた順序「1 目的 → 2 要件 → 3 既存理論 → 4 足りない部分だけ作る」は **4件すべて完了**。
さらに**運用一式 第一便（様式4点＋値3つ）も 2026-08-24 に採用済み**——模型を一件の案件に当てられる状態になった。

- 手順4: 第3案を BZM 3.0 として採用（#2026-08-24-10）。ファンドの取り分は全削除（まさ裁定）。
- **第一便**: 標準関門表（T1〜T4×M2〜M4。有償PoC・量産条件・量産契約。関心表明は関門にしない）／機能分解表（八機能。
  充足は直近12か月・2時点以上の実働のみ）／登録簿（24種・1件1行）／計画規則テンプレ（6項目選択式。会社化の既定は
  「需要の証拠×資金の目処（源泉不問）」）＋ 値3つ——**社会的割引率 2.0%（実質・感度1%/4%併記）・地平240か月・
  継続価値は既定規則で120か月延長＋生き残った稼働分に残存の永久価値**（#2026-08-24-11、commit `bbdf2edc`）。
- **本文へ改訂9点を反映済み**: 売上→燃料の経路 y_t／稼働用途の立ち上がり（稼働度12か月線形）と二種の離脱（事象＋陳腐化 λ^obs 年5%帯）／
  受託契約の月額・契約主体（大学経由は使途制限側）・本業との近さ（同源 γ=0・隣接・無関係）／C(x_T,θ)／
  規則の条件づけは観測可能な情報（盤面・地図・契約）のみ／反実仮想の時間形 α_u(t)・前倒し期間 L_u／
  原価下限 c̲_u の体質成分化／「有償評価」→「有償PoC」。うち3点はまさの指摘由来。
- 監査: 第一便は5属性×第1ラウンド（P0 21件）＋閉包検証（新規5件・部分的判定を補訂1 で対応）。報告全文は
  model/proposals/2026-08-24_bzm30-kit-audit-round1〜2.md。
- 実装は未移行: 735件の q は11要因ルーブリックの判断帯のまま。凍結タプル（current-sps-model.ts）も旧 SPS 実装のまま。

## 正本と参照

- モデルの正本は OS のモデルページ `/model`（画面に出ていないものは正本ではない。会話では「モデルページ」と呼ぶ）
- ロック14ファイル: BZM 3.0 本文（改訂1 反映済み）と運用一式 第一便が確定文書入り
- 承認: `model/APPROVALS.md`（#2026-08-24-11 まで）／運用: `model/README.md`／開発履歴: `pwa/design_log/sessions_2026-08.md`

## 次の段 = 第二便（未解決）

1. **分野別初期値**: κ_g（関門の基準速度。工程の型×規制属性別）・d_f（空席の遅延。d_e 最大のみ制約、順位は較正の出力）・
   φ（採択）・ν（申し出到来）・β（承認解決）・λ（消失）・γ の近さ区分値・L_u・λ^obs・M2/M3 の失効期間の較正
2. **移行**: 735件の判断帯 → 盤面・体質の帯への移行手順。式の層（38本の抽出画面）への 3.0 全式の統合もこの段で
3. **較正計画**: AMD 接触台帳＋公開情報での較正の実施順
4. 第一便からの申し送り: δ_u の時間形・φ_u の残存期間逓減・技術の核の恒久喪失の消失接続・逓減割引・
   Morgeson 15機能との対応表・組み合わせレバーの検知・AMD 組織外からの牽制
5. 監査の軽微残: 手順4 の P1 31件・P2 21件（audit-round1〜3）
6. **別の理論**（要件から除外済み）: AMD 貢献度、ECR、ラウンドテーブル

## 次の最初の行動

第二便の作業計画をえいみが設計し、批判監査を通してから、まさへは**完成案の採否だけ**を仰ぐ
（理論・数理・文書の書き方をまさに質問しない。聞くのは要件の意味と価値基準だけ）。
移行（2）は DB の実データ形状（735件の判断帯）を先に確認してから設計する。

## 守る運用ルール

- モデルページにはまさが合意した内容だけ。根拠は `[根拠](#evidence "…")` の印（本文に引用を書かない。印の中で `"` を使わない）
- 「Aではない」を正本に書かない。「採らない主張」に載せるのはまさが棄却した候補だけ
- 正本変更: 提案 → 本文で提示 → APPROVALS へ引用つき記録 → 変更 → `node pwa/scripts/model_lock.cjs relock --approval <id>` →
  同じ commit で `bzm/9-5-appendix-changelog.md` に1行
- 監査は5属性（経営学者・経済学者・DTSU 経営者・VC・大学産連本部長）。学術2体は重いモデル、実務3体は軽いモデル。
  対応表は正確に書く（体裁の誇張は閉包検証で必ず突かれる）。まさへの説明は平易に——詰め込むと「全然理解できない」で差し戻しになる。
  たとえ（ルールブックと道具、名目と実質）→紙の役割→式との対応、の順で出す
- 共有 checkout（常時5〜10並行）: 着手前 `git fetch --all --prune` → log → status。git add はパス名指し、stage と commit は1コマンド。
  他セッションへメッセージを送らない

## 検証手順

```
node pwa/scripts/model_lock.cjs check
cd pwa && npx tsc --noEmit && npm run test:critical-ui && npm run test:model-formula-canon
```

本番反映は `main` への push（Vercel Git 自動 deploy）。モデルページは admin 限定。
