# HANDOFF

最終更新: 2026-08-27 JST
対象: **シーズのスコアを BZM 3.0 へ差し替え。案件ごとの入力を OS のデータから埋め直して再算出し、画面へ戻した**

## 今回のセッション（2026-08-27 夜）

**p10（SE）の知財13件をJ-PlatPatで引き直した。見立ては逆で、7件はSE自身の出願だった。**

台帳は「13件すべてが競合 Space Power Technologies の出願」と読まれていたが、出願番号ごとに照会したところ、
**7件は株式会社翔エンジニアリング（識別番号518195771）自身の出願**だった。台帳側の誤りは4つ。

- 出願人の社名の取り違え（SPT名義は 2018-105943 の1件だけ。それも2018年の出願時は翔エンジニアリング名義で、2020/07/02 に名義変更）
- 2件で**出願番号の欄に公開番号**が入っていた。引くと凸版印刷「紙カップフランジ段差測定器」と三共「仮設足場用足場板」に当たる
- IHIエアロスペースのレクテナ特許を「消滅・審査未請求」としていたが、**3件（5686540・6389114・6414978）は有効**
- 翔エンジニアリング名義の案件が**4件漏れていた**（2025-012236 / 2025-012229 / 2013-225976 / 2012-098183）

台帳は17件へ置き換え、日付・年金・現権利者・発明者・J-PlatPat直リンクも入れた
（`pwa/scripts/seed_project_ip_p10_se.py` / [`pwa/spec/3-19`](pwa/spec/3-19-project-ip-current-spec.md) §6）。
専有可能性 κ_IP を 0.35 → **0.55**（§6.I-9-3「単独出願済み」）へ置き直して再計算し、
**p10 は 808億 → 1,026億**（2位のまま）。`project_knowledge` の特許ポートフォリオ行も書き換えた。

**⚠️ 経営側の確認が要ることが2つ出た。**

1. **2026年7月に、特許7041859（レクテナ装置）と特許6666663（無線電力供給システム）が
   競合のエイターリンク株式会社へ移転している。** 7041859 は2018年にSEが単独で出願した権利で、
   2020年にSPTへ名義変更されたあと譲渡されている。6666663 は京大・玉置電子工業・SE・菊池製作所の
   4者共同だったものが、エイターリンクの単独保有になっている
2. **IHI 特許5686540 の実施許諾契約の現況が未確認。** 権利が生きているので、契約が続いているかを確かめる必要がある
   （棚卸しシートに「いつまで有効？」と書かれたまま）

## 今回のセッション（2026-08-27 午後）

**21件の入力を、`project_id` を持つ165テーブルの棚卸しから埋め直して再計算し、金額を画面へ戻した。**

1. **棚卸し** — 165テーブルのうち、対象21件に行があるのは123。99を読み、66を理由つきで読まないと決めた
   （[model/cases/INVENTORY.md](model/cases/INVENTORY.md)）
2. **8つのパラメータを1件ずつ判定** — 根拠は `seed_bzm30_inputs` の `*_reason` 欄へ。
   シーズ詳細の「入力の充足」の表の5列目にそのまま出る（migration 332 で欄を1件ずつに割った）
3. **21件を再計算**（承認済みの版 40aebfe9 で。休眠は未承認なので使っていない）
4. **`BZM30_SCORES_PUBLISHED = true` に戻した**。`model/cases/SCORES.md` も貼り替えた

実データとの突き合わせで直った主なもの:

| PJ | 午前 | いま | 何が変わったか |
|---|---|---|---|
| p22 OptQC | 3,011億 | 3,016億 | 手元資金70億円の裏づけ（2026-08 シリーズA2）をOSへ登録した。額は変えていない |
| p10 翔エンジニアリング | 685億 | **1,026億** | 段階2→3、受託契約中、担い手0.70。専有可能性は夜の知財照会で0.55へ（午後は0.35を置いていた） |
| p24 チャレナジー | 479億 | **404億** | 手元資金2,500万円（取締役会「入金ゼロならランウェイ12月末」） |
| p26 VasculaX | 85億 | **64億** | 担い手0.35（経営チーム未組成が明示されている） |
| p18 Yellow Duck | 89億 | **62億** | 単位採算 false（XRL に「UE不成立」） |
| p05・p16・p08・p01・p31 | 1〜83億 | 3〜456億 | 現在地が未入力で先頭のゲートから計算していた分が解けた |

## いま止まっているところ

**モデル側の宿題が3つ残っている。どれもこのセッションでは直せない**（モデル本体は別セッションの担当）。

1. **参照実装が案件ごとのバーンレートを受け取らない。** 値は `seed_bzm30_inputs.burn_rate_yen_month` に
   入れてあるが計算に入らない。実績は型別の既定値の**2〜9倍**ずれていて、資金の残り月数を実態より長く読む
   （p07 約9倍・p21 約4.4倍・p20 約4.2倍・p24 2〜3倍・p09 約2倍）
2. **産官学モメンタム σ が21件すべて未記入。** モデルページ §6.I-1-4 は 型4×規制3 の12区分を単位に
   四半期ごとに中央で判定すると定めていて、区分ごとの産業統計はOSに無い。**判定を回す人と手順が要る**
3. **`gateForStage` の規制属性の表記ゆれ。** `REG-0`（ハイフンつき）を渡すと REG-2 の列が返る。
   DB は `REG0` 形式なので実害は出ていないが、呼び出し側が形式を間違えると静かに違う列で計算する

## データ側で見つかった問題（OSの中身の話）

1. ~~p10 の `project_ip_assets` 13件は競合他社の出願~~ → **夜のJ-PlatPat照会で解決**（上の「今回のセッション（夜）」）。
   見立ては逆で、7件はSE自身の出願だった。台帳は17件へ置き換え済み
2. **調べた事実をOSへ落としていないと、後から見た者に消される。** p22 の手元資金70億円は
   2026-08-25 のシリーズA2（NTTリード・計21社・累計91.5億円）の額で正しかったが、
   `project_valuation_rounds` に行が無かったため、この棚卸しで「根拠なし」と誤判定して下げてしまった。
   ラウンドと報道をOSへ登録して復旧済み。**ネットで調べて入力に使った事実は、その場でOSの該当テーブルへ入れる**
3. **p04 の `xrl_feedbacks` に未適用の指摘が3件**（TRL8は過大でTRL6、BRL5、HRL4）。今回は指摘に合わせて段階4にした
4. **`project_ip_deadlines` と `project_important_documents` は21件すべて0行**。権利残件を数える正面の材料が無い

## 未解決（まさの判断待ち）

1. **休眠を入れるか** — 会社化前の資金切れでは死なない、という改訂。別セッションが提案を書いている
   （`model/proposals/2026-08-27_bzm30-dormancy.md`）。**未承認のまま参照実装の既定に入っている**ので、
   いまの算出は承認済みの版（`git show 40aebfe9:model/tools/bzm30_forward.cjs`）で走らせている
2. **旧SPS の DB データ（`seed_screening_bands` 998行）を物理削除するか** — 画面からは全部消した。
   消すと戻せないので確認待ち
3. **置き換え分の割合**（p21 産業排水を7割・p02 動画制作を8割と暫定で置いた）— この割合で純増が2〜4倍動く。
   **天井はもう保留にしていない**（まさ 2026-08-28「精度低くていいからまずは数字を入れるのが原則では」）。
   21件すべてに金額が出ている

## 分担（まさ確定 2026-08-27）

- **別セッション**: BZM 3.0 のモデル本体を作る（参照実装・正本・自動算出の設計 `pwa/spec/5-12`）
- **このセッション**: 現状のモデルでパラメータを埋める

同じ checkout を共有しているので、長い計算の前後で `git log --oneline -5 origin/main` を見る。
計算の入力になる `model/tools/bzm30_forward.cjs` が変わっていたら、計算をやり直す。

## 次にやること（この順）

1. **バーンレートを参照実装が受け取れるようにする**（モデル側。いちばん効く）
2. **σ の12区分の判定を回す**（誰が・どの統計から・四半期のいつ、を決めるところから）
3. ~~p10 の知財13件の帰属を確認する~~ → **完了**。残るのは、エイターリンクへ移った2件の経緯を経営側で確認することと、
   IHI 特許5686540 の実施許諾契約の現況
4. **天井の確度を上げる**（世界市場からの按分をやめ、国内の産業統計から積む）。付加価値率0.35も分野ごとに引き直す

## 追記（2026-08-27 夜・入力の埋め直し側）

**p10 SE は、OSに無い事実で順位が決まっていた。** まさからの聞き取りで4つ出た——
半年以内のバーンアウト、手作りでIC化前・受信アンテナが顧客要求（名刺サイズ）に届かない、
代表の年齢と資本の理解に起因して融資もエクイティも経路が閉じている、市場規模はシステム全体のTAMでSEの守備範囲はデバイスだけ。
**どれもOSに一行も無かった。** 入力を置き直して 天井1円あたり 0.662 → 0.012、量産到達 39.4% → 1.2%、**1,026億 → 6億**（21件中19位）。
聞き取った内容は `project_knowledge` 8件・`project_strategy_signals` 4件・`project_xrl_log` 1件としてOSへ入れてある。

**なぜ抽出できていなかったかの分解は [model/cases/INVENTORY.md](model/cases/INVENTORY.md) の3。**
元データが無い／月報の様式が「進んだこと」中心／人に紐づく事実の欄が無い／XRLが0行、の4つ。
**OSが「進んだこと」を集める形で、「進んでいない理由」を集める形になっていない。**

### ここから先に効くこと（上の宿題より優先度が高い）

1. **開催日を過ぎても中身が空の会議カードを検知して埋める。** p10 の 2026-08-20 泉岳寺MTGは
   **議事録がNotionにある**のに、OSのカードは開催前にカレンダーから作った空のままだった。
   **同じ状態が26件・10PJ分**（2026-07-09〜08-26。LiSTie 8/26経営会議、SolvioraX経営会議、CLG取締役会を含む）。
   なお `NOTION_API_KEY` はローカルの環境に無いので、手元のセッションからは取りに行けない
2. **月報の様式に「この案件が死ぬとしたら何が原因か」を1問置く。** いまのリスク欄には一般論しか集まらない
3. **担い手の欄に「資金調達の主体として成立するか」を持たせる。** 年齢・資本の理解・意思決定の癖は
   出資適格と与信に直結するのに、いまは書く場所が無い
4. **XRLが0行のPJを検知する。** 対象21件のうち10件が0行（p01・p02・p05・p08・p16・p22・p26・p29・p31・熱電）

## 検証（このセッションで実際に走らせたもの）

```
npx tsc --noEmit                     # pwa/ で
npx eslint <触ったファイル>
npm run build                         # exit 0
npm run test:critical-ui              # pre-commit で強制的に走る（2026-08-27〜）
npm run test:reference-data-cache
npm run test:seed-list-display / test:current-sps-only / test:seed-screening-bands
node pwa/scripts/model_lock.cjs relock --approval <id>   # モデル正本を触ったとき
```

本番は `https://amd-os-pwa.vercel.app`。push すると Vercel が自動でデプロイする。
`curl -s https://amd-os-pwa.vercel.app/api/build-info` で git_sha を確認する。
