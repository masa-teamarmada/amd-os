# HANDOFF

最終更新: 2026-08-24 JST
対象: モデル層 `/model` — BZM 3.0 スコアリング模型の次の段（様式・初期値・較正・移行）

## 現在地

まさが決めた順序「1 目的 → 2 要件 → 3 既存理論 → 4 足りない部分だけ作る」は **4件すべて完了**。

- 目的3件・要件12件・確定している構造・要件ごとの土台（手順3）は確定済み（モデルページ §1〜§4）。
- **手順4 は 2026-08-24 に完了。第3案（5属性×3ラウンド監査、補訂2 済み）を BZM 3.0 スコアリング模型として採用**
  （APPROVALS #2026-08-24-10、commit `734f681f`・`ae13b800`）。
  - 補訂2 ＝ ファンドの取り分（V^eq・持分・資本政策）の記述を全削除（まさ「全く無関係なんだから、そもそも入れるべきではない」）。
  - モデルページ §0 に「手順4 の成果 — BZM 3.0」節: 模型の芯5点＋最上段の式 V＋二段目の式 Π（差し引き2点 δ_u・α_u 入り、記号表つき）。
  - 旧 SPS 改訂案（2026-08-22 推進力・余力）は取り下げ（model/withdrawn/）。
- 実装は未移行: 735件の q は11要因ルーブリックの判断帯のまま。凍結タプル（current-sps-model.ts）も旧 SPS 実装のまま。

## 正本と参照

- モデルの正本は OS のモデルページ `/model`（画面に出ていないものは正本ではない。会話では「モデルページ」と呼ぶ）
- BZM 3.0 本文: `/model/2026-08-24_step4_scoring-model-v3`（ロック対象13ファイルの一つ）
- 承認: `model/APPROVALS.md`（#2026-08-24-10 まで）／運用: `model/README.md`／開発履歴: `pwa/design_log/sessions_2026-08.md`

## 次の段（未解決）

1. **様式**: 標準関門表・機能分解表（要件9 の F）・観測→状態の登録簿・計画規則テンプレート（分岐条件の選択式）
2. **分野別初期値**: κ_g（関門の基準速度）・d_f（空席の遅延）・φ（採択）・ν（申し出到来）・β（承認解決）・λ（消失）
3. **値の確定**: 社会的割引率 d、継続価値 C の恒久形、経済評価地平 T
4. **移行**: 735件の判断帯 → 盤面・体質の帯への移行手順。式の層（38本の抽出画面）への 3.0 全式の統合もこの段で
5. **較正計画**: AMD 接触台帳＋公開情報での較正の実施順（BZM 3.0 本文 §12）
6. 監査の軽微残: P1 31件・P2 21件（model/proposals/2026-08-24_step4_audit-round1〜3 に保持）
7. **別の理論**（要件から除外済み）: AMD 貢献度、ECR、ラウンドテーブル

## 次の最初の行動

次の段の作業計画（1〜5 のどれから、どの粒度で）をえいみが設計し、批判監査を通してから、まさへは
**完成案の採否だけ**を仰ぐ（理論・数理・文書の書き方をまさに質問しない。聞くのは要件の意味と価値基準だけ）。

## 守る運用ルール

- モデルページにはまさが合意した内容だけ。根拠は `[根拠](#evidence "…")` の印（本文に引用を書かない。印の中で `"` を使わない）
- 「Aではない」を正本に書かない。「採らない主張」に載せるのはまさが棄却した候補だけ
- 正本変更: 提案 → 本文で提示 → APPROVALS へ引用つき記録 → 変更 → `node pwa/scripts/model_lock.cjs relock --approval <id>` →
  同じ commit で `bzm/9-5-appendix-changelog.md` に1行
- 監査は5属性（経営学者・経済学者・DTSU 経営者・VC・大学産連本部長）。学術2体は重いモデル、実務3体は軽いモデル。
  対応表は正確に書く（体裁の誇張は閉包検証で必ず突かれる）
- 共有 checkout（常時5〜10並行）: 着手前 `git fetch --all --prune` → log → status。git add はパス名指し、stage と commit は1コマンド。
  他セッションへメッセージを送らない

## 検証手順

```
node pwa/scripts/model_lock.cjs check
cd pwa && npx tsc --noEmit && npm run test:critical-ui && npm run test:model-formula-canon
```

本番反映は `main` への push（Vercel Git 自動 deploy）。モデルページは admin 限定。
