# HANDOFF

最終更新: 2026-08-25 JST
対象: モデル層 `/model` — 次は **各案件のSPS算出ができる状態かの最終確認 → 問題なければ現行PJから算出開始**

## 今回のセッション（2026-08-25）

係数の分野別初期値を **v1→v2→v3＋補訂2** まで作り直し、**監査3ラウンド**（5属性 → 閉包検証 → 記述と実装の突き合わせ）を通した。
あわせて**前向き計算の参照実装**（`model/tools/bzm30_forward.cjs`）を作り、モデル正本に**参考文献59件・文献番号44か所**と
**§7 較正パラメータの台帳**を入れた。実装の詳細と教訓は [design_log 2026-08-25 節](pwa/design_log/sessions_2026-08.md)。

- 提案の本体: `model/proposals/2026-08-25_bzm30-coefficients-v3.md`（**未承認**）
- 監査の記録: 同ディレクトリの `-audit-round1/2/3.md`、計算結果は `-check-results.md`
- 正本の変更3件は `model/APPROVALS.md` #2026-08-25-1〜-3 に記録・反映済み

## いま算出できるか — **できない。3つが要る**

| # | 足りないもの | 内容 |
|---|---|---|
| 1 | **承認2件** | 係数の初期値 v3（Q1〜Q27）と、モデル本体の**改訂8点 K1〜K8**。`model/README.md` (b) により、承認まで評価にも本番表示にも使えない |
| 2 | **案件ごとの調査** | 参照実装は**天井を1に正規化**しているので、出力は「天井の年額1円あたりの現在価値」。**円建てのスコアにするには案件ごとに $\bar P_u$（天井）が要る**。同じく $w_u$（支払上限）・$\delta_u$（置き換え分）・$\underline c_u$（原価下限）・$\alpha_u$（評価日の控除率）も案件ごと。**現時点で1件も無い** |
| 3 | **実装** | 参照実装は Tier 0（工程の型と規制属性を選ぶだけ）しか受け取れない。**案件ごとの観測状態**（ゲート位置・資金残高・権利残件・会社化の有無・受託契約）を入れる口が無い。$\pi^{*}$（会社化の早すぎ・遅すぎの診断＝要件4 の測定本体）も未実装 |

**段階的にできること**: 承認さえ下りれば、Tier 0 の型・規制属性だけで「天井1円あたりの倍率」と9区分の分布は出せる。
天井を1件でも調べれば、その案件は円建てで出せる。**旧 SPS（円建ての下限〜上限）を置き換えるには天井の調査が必須。**

## まさの判断待ち（3件）

1. **改訂8点（K1〜K8）の承認。** 最大は K1 ——§6.B-1 がエバンジェリスト機能を「投資家・審査側の判断を動かす」と定義しているのに、
   $\eta_t$ は前進確率にしか乗らず、その空席が採択率にも申し出到来率にも効いていない。**AMD が担い手を供給する価値がスコアに乗っていない。**
   文献の裏づけあり（Shane & Stuart 2002、Beckman ら 2007。#2026-08-25-1 に記録）
2. **係数の初期値 v3 の承認**（根拠の8割が暫定。まさは 2026-08-25「置き換えて。精度が低いのは承知の上」と述べている）
3. **受託だけで自立する経路の扱い**（補訂2 で計算に乗ったが、その産業の大きさ $q_{\mathrm{self}}=0.35$ に案件ごとの裏づけが無い）

## Repo 状態

- HEADはmain / originと同期。Project Share全廃の実装削除は2026-08-26に反映済み。未コミットの
  `pwa/scheduled-tasks/.../SKILL.md` は別セッションの変更なので触らない。
- ロック12件、`model_lock.cjs check` 緑
- `750c7fe3 fix(nav): モデル節一覧を参照キャッシュへ統一` は**別セッション**が、今回入れた左ナビのホバーを
  専用のクライアントキャッシュ（`pwa/src/lib/model-sections-client.ts`）へ寄せ、参照系キャッシュの契約 guard に登録したもの

## 次の一手

1. 上の「いま算出できるか」をまさへ提示し、**判断3件**を取る
2. 承認が下りたら、v3 の内容をモデルページ §6 へ **6.I** として統合し、提案ファイルをスタブ化 →
   `node pwa/scripts/model_lock.cjs relock --approval <id>` → 同じ commit で `bzm/9-5-appendix-changelog.md` に1行
3. 各 PJ の**工程の型 × 規制属性**を確定（下見: p07 LST・p21 SX は F1×REG-0、p06 CTB は F1×REG-2、
   p20 CX は F2×REG-0、p24 CLG は F2×REG-1。**要確認**。p02・p10・p26・p29 は未調査）
4. 天井 $\bar P_u$ の調査の段取りを決める（1件あたりの深さと、誰がやるか）

## 正本と参照

- **正本はモデルページ `/model` 本体ただ一つ。** BZM 3.0 の全定義は §5、運用一式は §6、較正パラメータの台帳は §7、参考文献は §10
- 承認: `model/APPROVALS.md`（#2026-08-25-3 まで）／規約: `model/README.md`／画面仕様: `pwa/spec/5-11-model-canon-page-current-spec.md`
- 開発履歴: `pwa/design_log/sessions_2026-08.md`／変更履歴: `bzm/9-5-appendix-changelog.md`／OS マニュアル: `pwa/manual/9-2-developer.md`

## 守る運用ルール

- モデルページにはまさが合意した内容だけ。**提案中の値を、未承認と明示せずに使わない**（§7 は列で分けている）
- 比喩・ぼかした表現を使わない。記号と期間はその場で定義する
- **画面は情報密度を上げる。** カードのグリッドは高さが揃って空白を生む。項目数がばらつくものは表か密なリストへ。
  提出前に実寸で測る（playwright で `document.body.scrollHeight` と行の高さ）
- 正本変更: 提案 → 本文で提示 → APPROVALS へ引用つき記録 → 変更 → relock → 同じ commit で changelog
- 共有 checkout（常時5〜10並行）: 着手前 `git fetch --all --prune` → log → status。`git add` はパス名指し。
  他セッションの未保存ファイルに触らない（衝突時は rebase でなく merge）。他セッションへメッセージを送らない

## 検証手順

```
node pwa/scripts/model_lock.cjs check
cd pwa && npx tsc --noEmit && npm run test:critical-ui && npm run test:model-formula-canon
node model/tools/bzm30_forward.cjs degen   # 縮退検査（約20分）
node model/tools/bzm30_forward.cjs conv    # 格子の収束検査
```

本番反映は `main` への push（Vercel Git 自動 deploy）。配信版は `/api/build-info` の `git_sha` で確認する。
