# HANDOFF

最終更新: 2026-08-23 JST
対象: モデル層 `/model` の新設と、BZM/SPS の目的・要件の再構築（理論の議論が主、実装は器づくり）

## 今回の到達点

まさの「モデルの正本がどこにあるか把握できていない」「勝手にモデルが書き換えられたことに強い不安」から始まり、
**モデルを教科書から分離した層 `/model` を作り、目的と要件をゼロから積み直す**ところまで進んだ。

- **`/model` を新設**（admin 限定）。教科書 `/bzm` は本の原稿、設計書 `/spec` は実装仕様、モデルそのものは `/model`。
  画面は `model/MODEL_VERSION_LEDGER.md` をそのまま描画する（画面側で要約・再構成しない）。
- **承認ロックを3層で実装**。`model/LOCK.json` の正本12件は、まさの承認記録（`model/APPROVALS.md`）なしに
  変更できない。critical-ui guard / `.githooks/pre-commit` / Claude Code の PreToolUse hook。
- **目的を3件、要件を9件まで確定**。まさが一つずつ判断した内容だけを載せている。
- **11要因ルーブリックは「参考情報」へ降格**。まさが正本として合意した記録が無く、735件の評価作業の中で
  使われていただけだった（まさ 2026-08-23「まだ現行と定義しないでほしい。それを正本とした記憶はないので」）。
- SPS 現行正本3本を `sps-current-*` へ改名（旧名の世代番号が「古い版では」という誤解を生んでいた）。
- 忘れられていた要素を3方向から洗い出した（旧9軸 M·P·R·S の全定義 / 教科書の名前つき概念 / 監査と査読の未反映指摘）。
  **自走力（旧 S = 自前の稼ぎ × 集め切る力）が現行から落ちていた理由は「接触前は測れないから」で、
  理論的に不要と判断されたわけではなかった。**

## 正本

- **モデルの目的と要件**: `/model`（実体は `model/MODEL_VERSION_LEDGER.md`）
- **まさの承認記録**: `model/APPROVALS.md`（発言の引用つき。relock はこのファイルを検証する）
- **モデル変更の運用規約**: `model/README.md`
- 提案（未承認・正本ではない）: `model/proposals/2026-08-22_sps-propulsion-and-slack.md`
- 開発履歴: `pwa/design_log/sessions_2026-08.md`（2026-08-22〜23節）
- バグと教訓: `pwa/BUGS.md`（2026-08-22〜23節。フックの設計ミス、commit 巻き込み2回、目的の逆算）
- 変更履歴: `bzm/9-5-appendix-changelog.md`

## Repo状態

- canonical checkout: `/Users/masa/projects/AMD/amd-os`、branch は `main` のみ。今セッションで作った branch・worktree は無い。
- 直近の自セッション commit: `1aae0483`（要件9の追加と、要件にしない6件の記録）。push 済み。
- **このリポは常時5〜10セッションが並行する共有 checkout。** 着手前に `git fetch --all --prune` →
  `git log --oneline -15` → `git status -sb` で現在地を再確認する。
- **`git add` は必ずパスを名指しする。** `git add -A` / `git commit -a` を使わない。今回2回、staged のまま
  guard を回している間に他セッションの commit へ巻き込まれた（`68535f38`、`e20f2380`）。
  **stage と commit は1コマンドにまとめ、staged のまま待たない。**
- **他セッションへメッセージを送らない**（まさ明示。届くたびにまさの画面にログが流れる）。調整は repo 内の記録で行う。
  実際に一度、実務連絡を受け取った側が「まさから指示が来た」と誤読して `/model` の無承認の大規模作り直しを
  始める事故が起きた（`pwa/BUGS.md` `[process/cross-session-messaging]`）。
- **2026-08-23 時点、他セッションの未コミット差分が残っている**（このHANDOFFを書いたセッションの作業ではない）。
  `pwa/spec/4-7-amd-contributions-current-spec.md`（新規）/ `pwa/src/lib/amd-contributions.ts`（新規）/
  `pwa/src/components/cockpit/CockpitAmdContributions.tsx`（新規）/ `pwa/src/app/api/project/[projectId]/amd-contributions/`（新規）/
  `Bzm22AcquisitionLedger.tsx`・`Bzm22ProvisionalObservatory.tsx`・`CockpitView.tsx`・`check_pwa_critical_ui.cjs`（変更）。
  「AMD の貢献度の測定」は上の「未解決 3」で要件から明示的に外した領域（別の理論として構築）と同じ名前なので、
  次セッションは**この dirty がモデル層の要件と同じものだと早合点しない**こと。中身は未確認。

## 未解決

**要件の議論が途中。** モデルページの「議論中」に2件、洗い出し済みで未判断の候補が複数ある。

1. **議論中の2件**（`/model` の §2 に記載）
   - 用途（正本の言い分は「用途の候補が多いことは決まっていないことと同じ」。まさ「さっぱり分からない」。
     要件1・2 に吸収されるかを含めて未決）
   - 要件9（担い手）の中で、CEO 機能の分解をどこまでモデルの変数にするか
2. **未判断の候補**（洗い出し済み。`pwa/design_log` と下記の調査結果にある）
   - 天井 P を固定値として扱わない（機会創造観。優れた起業家は天井を書き換える）
   - 産学連携の権利台帳（chain of title・FTO・大学承認・COI・輸出管理）
   - 証券クラス別の配分（希薄化・清算優先）— BZSF の投資判定に直結
   - 失敗経路の知識スピルオーバーを 0 と置かない
   - 創業者・大学・投資家の利害分岐（単一主体と仮定しない）
   - 順序尺度と比率尺度の混同、後知恵バイアス、自己選択バイアス、グッドハートの法則
3. **別の理論として構築するもの**（まさ 2026-08-23 で要件から外した）
   - AMD の貢献度の測定（支援の因果効果、担い手の供給制約）
   - ECR（研究機関のスコアリングシステム）
   - ラウンドテーブル（まだ検討すらできていない）
4. 提案 `model/proposals/2026-08-22_sps-propulsion-and-slack.md` は**未承認のまま保留**。
   式を先に作ったこと自体が、まさの言う順序（目的 → 要件 → 既存理論 → 足りない部分だけ作る）に反している。
   要件が固まってから作り直すか、部品として再利用するかを判断する。

## 次の最初の行動

**要件の議論を続ける。** `/model` を開いて現在の目的3件・要件9件・議論中2件を確認し、上の「未判断の候補」から
まさへ順に問う。要件が固まったら、まさの言う順序の Step 3（既存理論を論文から持ってくる）へ進む。

**やってはいけないこと**: 要件が固まる前に式を作ること。作業の都合から要素を逆算すること。
まさが合意していない内容を `/model` へ書くこと。
